---
title: "AutoResearch-RL: Perpetual Self-Evaluating Reinforcement Learning Agents for Autonomous Neural Architecture Discovery"
title_zh: AutoResearch-RL：用于自主神经架构发现的永续自我评估强化学习智能体
authors: "Nilesh Jain, Rohit Yadav, Sagar Kotian, Claude AI"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07300v1"
tags: ["query:self-evolving"]
score: 8.0
evidence: 用于自主研究的永久自我评估强化学习智能体
tldr: AutoResearch-RL 是一个无需人工干预的自动研究框架，利用强化学习（PPO）在固定资源预算下自主探索神经网络架构和超参数。该框架通过将研究过程建模为马尔可夫决策过程，使智能体能够不断修改训练脚本并根据验证性能进行自我演进。实验证明，该系统在单 GPU 预训练任务中，经过约 300 次迭代即可达到或超越人工调优的基准水平，实现了真正意义上的自主神经架构发现。
selection_source: fresh_fetch
motivation: 旨在消除神经架构搜索和超参数优化中对人工干预的依赖，实现完全自主且持续的科研探索。
method: 采用基于 PPO 的强化学习智能体，在保持环境固定的前提下，通过不断修改和执行训练脚本并以验证集性能作为奖励进行策略更新。
result: 在单 GPU 的 nanochat 预训练基准测试中，该框架在约 300 次迭代后发现了性能匹配或优于人工手动调优的配置。
conclusion: AutoResearch-RL 证明了将科研探索过程形式化为强化学习任务的可行性，为实现全自动化的神经网络研究提供了有效路径。
---

## 摘要
我们提出了 AutoResearch-RL，这是一个强化学习智能体在无需人工监督的情况下进行开放式神经架构和超参数研究的框架，该框架会持续运行，直到终止预测器（termination oracle）发出收敛信号或资源耗尽。在每一步中，智能体都会对目标训练脚本提出代码修改建议，在固定的实际运行时间预算内执行该脚本，观察源自验证每字节比特数（val-bpb）的标量奖励，并通过近端策略优化（PPO）更新其策略。关键的设计见解是分离了三个关注点：(i) 冻结的环境（数据流水线、评估协议和常量），确保了跨实验比较的公平性；(ii) 可变的目标文件（train.py），代表智能体的可编辑状态；以及 (iii) 元学习器（强化学习智能体本身），它积累不断增长的实验结果轨迹，并利用这些结果为后续提案提供信息。我们将其形式化为马尔可夫决策过程，在温和的假设下推导出收敛保证，并在单 GPU nanochat 预训练基准上通过实证表明，AutoResearch-RL 在大约 300 次隔夜迭代后，在无需人工干预的情况下，发现了达到或超过手动调优基准的配置。

## Abstract
We present AutoResearch-RL, a framework in which a reinforcement learning agent conducts open-ended neural architecture and hyperparameter research without human supervision, running perpetually until a termination oracle signals convergence or resource exhaustion. At each step the agent proposes a code modification to a target training script, executes it under a fixed wall clock time budget, observes a scalar reward derived from validation bits-per-byte (val-bpb), and updates its policy via Proximal Policy Optimisation (PPO).   The key design insight is the separation of three concerns: (i) a frozen environment (data pipeline, evaluation protocol, and constants) that guarantees fair cross-experiment comparison; (ii) a mutable target file (train.py) that represents the agent's editable state; and (iii) a meta-learner (the RL agent itself) that accumulates a growing trajectory of experiment outcomes and uses them to inform subsequent proposals.   We formalise this as a Markov Decision Process, derive convergence guarantees under mild assumptions, and demonstrate empirically on a single GPU nanochat pretraining benchmark that AutoResearch-RL discovers configurations that match or exceed hand-tuned baselines after approximately 300 overnight iterations, with no human in the loop.

---

## 论文详细总结（自动生成）

这是一份关于论文《AutoResearch-RL: Perpetual Self-Evaluating Reinforcement Learning Agents for Autonomous Neural Architecture Discovery》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：深度学习的进步长期依赖于研究人员的手动“假设-实现-训练-分析”循环，这一过程缓慢且昂贵。现有的自动化机器学习（AutoML）和神经架构搜索（NAS）通常局限于预定义的搜索空间或黑盒评估，无法像人类研究员那样进行开放式的代码修改和算法创新。
*   **核心问题**：如何构建一个能够自主、持续地进行神经架构发现、超参数优化及训练策略改进，且无需人工干预的智能体系统？
*   **整体含义**：论文提出将科研探索过程形式化为一个马尔可夫决策过程（MDP），利用强化学习（RL）训练大语言模型（LLM）作为研究智能体，使其在不断的实验反馈中学习研究启发式策略。

### 2. 核心方法论
*   **MDP 形式化**：
    *   **状态 (State)**：当前源代码、实验历史记录（代码差异与奖励）、系统诊断信息（GPU 内存等）。
    *   **动作 (Action)**：对 `train.py` 脚本进行的结构化代码修改（diff）。
    *   **奖励 (Reward)**：验证集每字节比特数（val-bpb）的改进量，加上计算效率奖励。
*   **关键技术细节**：
    *   **PPO 策略优化**：使用近端策略优化（PPO）对作为智能体的 LLM（Claude-Sonnet-4）进行微调，使其不仅是生成代码，而是学习“研究策略”。
    *   **三方分离设计**：固定环境（数据流、评估协议）、可变目标文件（`train.py`）、元学习器（RL 智能体）。
    *   **自我评估（SE）模块**：通过幂律模型对训练损失曲线进行实时预测，若预测结果不佳则提前终止实验（Early Stop），从而提高实验吞吐量。
    *   **工作记忆**：利用滑动窗口保留最近 32 次实验历史，并记录历史最佳配置，作为智能体决策的上下文。

### 3. 实验设计
*   **数据集与场景**：使用 **nanochat** 预训练基准，数据集为 FineWeb 的子集（10B tokens），固定序列长度为 512。
*   **评估指标**：验证集每字节比特数（val-bpb）。该指标与分词器无关，确保了不同配置间的公平比较。
*   **对比基准 (Benchmarks)**：
    1.  **Human Expert**：手动调优的 GPT-2 small（124M 参数）。
    2.  **Random Search**：在预定义超参数网格上进行均匀随机采样。
    3.  **Greedy LLM (no RL)**：直接使用 GPT-4o 作为研究智能体进行零样本（Zero-shot）尝试，不进行 RL 微调。
    4.  **AutoResearch-RL (Ours)**：本文提出的系统。

### 4. 资源与算力
*   **硬件**：单台 **NVIDIA H100 80GB SXM** GPU。
*   **时间预算**：每个实验固定为 **300 秒**（5 分钟）的实际运行时间，以确保不同架构在相同算力下进行公平对比。
*   **训练时长**：实验涵盖了从隔夜运行（约 8 小时，~100 次实验）到长达一周（168 小时，~2147 次实验）的不同尺度。

### 5. 实验数量与充分性
*   **实验规模**：主实验运行了约 100 次迭代；为了验证持续改进能力，还进行了长达一周的扩展实验（超过 2000 次迭代）。
*   **消融实验**：对比了带有和不带有自我评估（SE）模块的系统吞吐量。
*   **充分性评价**：实验设计较为客观，通过固定时间预算解决了 NAS 中常见的“计算量不均”导致的偏差问题。虽然主要集中在单 GPU 场景，但对于验证“自主研究循环”这一概念非常充分。

### 6. 主要结论与发现
*   **性能超越**：AutoResearch-RL 在 8 小时内达到的 val-bpb（2.681）显著优于人类专家（2.847）和零样本 LLM（2.734）。
*   **自主发现**：智能体自主发现了多种非直观的改进方案，包括：
    1.  Muon 优化器的学习率缩放调整。
    2.  引入 QK-norm（查询-键归一化）以稳定注意力熵。
    3.  设计了动态梯度裁剪计划。
    4.  在 5 分钟预算内通过增加层数（12 层增至 14 层）优化模型深度。
*   **效率提升**：SE 模块使实验吞吐量提升了 1.35 倍，结合策略改进，样本效率最终提升了 2.4 倍。

### 7. 优点与亮点
*   **开放式搜索**：不同于传统 NAS，该系统可以直接修改代码，能够探索包括优化器逻辑、归一化方式在内的全新算法设计。
*   **理论支撑**：论文提供了外层循环收敛性的理论证明，将最佳性能建模为超鞅（Super-martingale）。
*   **自我演进**：通过 RL 微调，智能体内化了研究启发式知识，比单纯依赖上下文学习（In-context learning）的智能体更具方向感。

### 8. 不足与局限
*   **算力范围限制**：目前仅在单 GPU 环境下验证，尚未测试其在多节点、大规模分布式训练中的协调能力。
*   **安全风险**：自主修改并执行代码存在潜在安全隐患（尽管文中提到了隔离措施，如禁止网络访问和限制修改范围）。
*   **搜索空间偏差**：智能体的表现仍受限于初始代码库（`train.py`）的质量和底层 LLM 的基础编程能力。
*   **收敛边际**：虽然理论上可永久运行，但实验显示在运行一周后，性能提升进入边际递减阶段。

（完）
