---
title: "EvoTool: Self-Evolving Tool-Use Policy Optimization in LLM Agents via Blame-Aware Mutation and Diversity-Aware Selection"
title_zh: EvoTool：通过责任感知变异与多样性感知选择实现大语言模型智能体自进化工具使用策略优化
authors: "Shuo Yang, Soyeon Caren Han, Xueqi Ma, Yan Li, Mohammad Reza Ghasemi Madani, Eduard Hovy"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04900v1"
tags: ["query:self-evolving"]
score: 9.0
evidence: 用于优化智能体模块化工具使用策略的自我演化框架
tldr: 针对LLM智能体在复杂任务中工具使用策略优化困难的问题，本文提出EvoTool框架。该框架将策略分解为规划、选择、调用和综合四个模块，通过无梯度的进化范式进行自我演化。其核心贡献在于引入轨迹归因定位故障、反馈引导的针对性变异以及多样性感知的人群选择机制。实验表明，EvoTool在多个基准测试中显著提升了模型性能，并具有良好的效率和迁移性。
selection_source: fresh_fetch
motivation: 现有的LLM工具使用策略优化方法难以处理长轨迹中的信用分配问题，且容易产生行为纠缠或忽略跨模块错误传播。
method: 提出一种模块化自我演化框架，通过故障归因定位、自然语言反馈引导的模块变异以及多样性选择机制来迭代优化工具使用策略。
result: 在GPT-4.1和Qwen3-8B等模型上，EvoTool在四个基准测试中均比强基准线提升了5个百分点以上，并展现出优异的效率。
conclusion: EvoTool通过模块化分解和精准的进化机制，为提升LLM智能体在复杂场景下的工具调用能力提供了一种高效且可迁移的优化方案。
---

## 摘要
基于大语言模型（LLM）的智能体依赖有效的工具使用策略来解决复杂任务，但由于监督延迟以及长程轨迹中信用分配（credit assignment）的困难，优化这些策略仍然具有挑战性。现有的优化方法往往要么是整体式的，容易导致行为纠缠；要么是单方面的，忽略了跨模块的错误传播。为了解决这些局限性，我们提出了 EvoTool，这是一个通过无梯度进化范式优化模块化工具使用策略的自进化框架。EvoTool 将智能体的工具使用策略分解为四个模块，包括规划器（Planner）、选择器（Selector）、调用器（Caller）和综合器（Synthesizer），并通过三种新颖的机制在自改进循环中迭代提升它们。基于轨迹的责任归因（Trajectory-Grounded Blame Attribution）利用诊断追踪将失败定位到特定模块。反馈引导的有针对性变异（Feedback-Guided Targeted Mutation）随后通过自然语言批评仅对该模块进行编辑。多样性感知种群选择（Diversity-Aware Population Selection）保留互补的候选方案以确保解的多样性。在四个基准测试中，EvoTool 在 GPT-4.1 和 Qwen3-8B 上均比强基准模型高出 5 个百分点以上，同时实现了卓越的效率和可迁移性。代码将在论文被接收后发布。

## Abstract
LLM-based agents depend on effective tool-use policies to solve complex tasks, yet optimizing these policies remains challenging due to delayed supervision and the difficulty of credit assignment in long-horizon trajectories. Existing optimization approaches tend to be either monolithic, which are prone to entangling behaviors, or single-aspect, which ignore cross-module error propagation. To address these limitations, we propose EvoTool, a self-evolving framework that optimizes a modular tool-use policy via a gradient-free evolutionary paradigm. EvoTool decomposes agent's tool-use policy into four modules, including Planner, Selector, Caller, and Synthesizer, and iteratively improves them in a self-improving loop through three novel mechanisms. Trajectory-Grounded Blame Attribution uses diagnostic traces to localize failures to a specific module. Feedback-Guided Targeted Mutation then edits only that module via natural-language critique. Diversity-Aware Population Selection preserves complementary candidates to ensure solution diversity. Across four benchmarks, EvoTool outperforms strong baselines by over 5 points on both GPT-4.1 and Qwen3-8B, while achieving superior efficiency and transferability. The code will be released once paper is accepted.

---

## 论文详细总结（自动生成）

以下是对论文《EvoTool: Self-Evolving Tool-Use Policy Optimization in LLM Agents via Blame-Aware Mutation and Diversity-Aware Selection》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：基于大语言模型（LLM）的智能体在处理复杂任务时，其工具使用策略（Tool-use Policy）至关重要。然而，在长程（Long-horizon）任务中，由于监督信号延迟（仅在结束时有反馈）和信用分配（Credit Assignment）困难，优化这些策略极具挑战。
*   **核心问题**：现有的优化方法存在两个极端：
    1.  **整体式优化（Monolithic）**：将整个提示词作为整体优化，容易导致不同行为间的纠缠，修复一个错误可能引发其他功能的退化。
    2.  **单方面优化（Single-aspect）**：仅优化规划或调用等单一环节，忽略了模块间的错误传播。
*   **研究意义**：EvoTool 旨在通过模块化分解和精准的进化机制，实现工具使用策略的自我演化，解决复杂决策链中的错误定位与修复问题。

### 2. 方法论
EvoTool 采用无梯度的进化范式，将工具使用策略分解为四个模块：**规划器（Planner）**、**选择器（Selector）**、**调用器（Caller）**和**综合器（Synthesizer）**。其核心流程包含三个关键机制：
*   **基于轨迹的责任归因（Trajectory-Grounded Blame Attribution）**：
    *   利用“诊断追踪”技术，将执行轨迹中的中间反馈转化为模块级的责任信号。
    *   通过一个 Blamer LLM 对每个模块进行评分（0-1），识别出导致失败的最主要模块。
*   **反馈引导的针对性变异（Feedback-Guided Targeted Mutation）**：
    *   一旦确定目标模块，利用 Mutator LLM 生成基于轨迹证据的自然语言批评。
    *   仅对被指责的模块规范（Prompt）进行编辑，保持其他模块固定，从而实现精准修复。
*   **多样性感知种群选择（Diversity-Aware Population Selection）**：
    *   不单纯依赖全局平均分进行贪婪选择，而是采用“实例级获胜”准则。
    *   保留在特定任务实例上表现最优的候选策略，确保种群能够覆盖异构任务分布，防止策略坍缩。

### 3. 实验设计
*   **数据集/场景**：
    1.  **ToolBench**：测试大规模 API 泛化能力。
    2.  **RestBench**：评估真实 REST API 的顺序调用。
    3.  **$\tau$-Bench**：评估长程、有状态的智能体-用户交互。
    4.  **BFCL (Berkeley Function Calling Leaderboard)**：评估函数调用的准确性。
*   **基座模型（Backbones）**：GPT-4.1 和 Qwen3-8B。
*   **对比方法**：
    *   **手动策略**：ReAct, CoT, Plan-and-Solve。
    *   **整体优化**：OPRO, PromptBreeder, EvoPrompt。
    *   **单方面优化**：AdaPlanner, EASYTOOL, DRAFT, ANYTOOL。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **效率表现**：论文强调了“Token 效率”，通过实验图表展示了 EvoTool 在达到相同性能水平时，其消耗的 Token 成本远低于 EvoPrompt 和 OPRO 等整体优化方法。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准测试的多个子集上进行了全面评估。
*   **消融实验**：非常充分。分别针对责任归因机制、变异引导信号（轨迹 vs 反馈）、以及种群选择策略进行了对比实验。
*   **迁移性验证**：进行了跨数据集（ToolBench $\leftrightarrow$ RestBench）和跨模型（Qwen3 $\leftrightarrow$ GPT-4.1）的策略迁移实验。
*   **客观性**：所有基准线方法均在相同的基座模型和评估预算下运行，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：EvoTool 在所有基准测试中均显著优于现有 SOTA 方法，平均领先幅度超过 5 个百分点。
*   **长程任务优势**：在复杂的 $\tau$-Bench 任务中，EvoTool 表现出极强的鲁棒性，而单方面优化方法在这些场景下往往会失效。
*   **高效演化**：通过针对性变异，EvoTool 能够以更少的迭代次数和 Token 消耗实现更优的策略提升。
*   **强迁移性**：在弱模型（Qwen3）上进化出的策略迁移到强模型（GPT-4.1）时，依然能带来显著的性能增益。

### 7. 优点
*   **精准归因**：解决了 LLM 智能体领域长期存在的“信用分配”难题，使优化不再是盲目搜索。
*   **模块化解耦**：通过解耦 Planner、Selector 等模块，有效避免了复杂提示词优化中的行为纠缠。
*   **多样性保护**：引入实例级获胜机制，使智能体能够学习到应对不同任务类型的“专家级”策略。

### 8. 不足与局限
*   **推理延迟**：进化过程涉及多次 LLM 调用（诊断、变异、评估），在实时性要求极高的场景下可能存在延迟。
*   **模态限制**：目前主要针对文本和 API 调用环境，尚未在多模态工具或具身智能（Embodied AI）场景中验证。
*   **诊断依赖**：系统的有效性高度依赖于 Blamer LLM 的诊断准确性，如果诊断模型产生幻觉，可能会引导错误的优化方向。

（完）
