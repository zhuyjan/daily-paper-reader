---
title: "EvoSkill: Automated Skill Discovery for Multi-Agent Systems"
title_zh: EvoSkill：多智能体系统的自动化技能发现
authors: "Salaheddin Alzubi, Noah Provenzano, Jaydon Bingham, Weiyuan Chen, Tu Vu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02766v1"
tags: ["query:self-evolving"]
score: 9.0
evidence: 用于自动技能发现和改进的自我演化框架
tldr: EvoSkill是一个自动发现和优化多智能体系统技能的自我进化框架。针对现有智能体技能多为手工编写且难以跨模型迁移的问题，该框架通过迭代分析执行失败案例，自动生成并精炼结构化的可重用技能库。实验表明，EvoSkill在OfficeQA和SealQA基准测试中显著提升了准确率，并展现出优异的零样本跨任务迁移能力。
selection_source: fresh_fetch
motivation: 现有的智能体技能大多依赖人工设计，且现有的进化方法往往与特定模型或任务紧密耦合，缺乏领域专业知识和通用性。
method: 提出EvoSkill框架，通过迭代分析失败案例来提炼和编辑结构化技能，并利用帕累托前沿筛选能提升验证性能的技能。
result: "在OfficeQA和SealQA上分别实现了7.3%和12.1%的准确率提升，且进化的技能在跨任务迁移测试中表现出显著的性能增益。"
conclusion: 技能层面的自动化优化不仅能显著增强智能体在特定任务上的表现，还能产生具有跨任务迁移能力的通用领域能力。
---

## 摘要
编程智能体越来越多地被用作通用问题求解器，但其灵活性本身并不能赋予其处理专门任务所需的领域专业知识。最近的研究通过“智能体技能”（agent skills）来解决这一问题：即可重用的工作流和代码，用于增强智能体的领域特定能力。目前大多数技能都是手工构建的，而现有的进化方法主要优化低级产物（如提示词和代码），这些产物与特定模型和任务紧密耦合。我们介绍了 EvoSkill，这是一个通过迭代失败分析自动发现和改进智能体技能的自我进化框架。EvoSkill 分析执行失败，提出新技能或对现有技能进行编辑，并将其具体化为结构化、可重用的技能文件夹。智能体程序的帕累托前沿（Pareto frontier）控制着选择过程，在底层模型保持冻结的情况下，仅保留能提高留出验证集性能的技能。我们在两个基准测试上评估了 EvoSkill：OfficeQA，一个基于美国财政部数据的落地推理基准测试，其精确匹配准确率提高了 7.3%（从 60.6% 提升至 67.9%）；以及 SealQA，一个具有噪声检索的搜索增强问答基准测试，其准确率提升了 12.1%（从 26.6% 提升至 38.7%）。我们还研究了在一个任务上进化的技能向另一个任务的零样本迁移能力；特别是，从 SealQA 进化的技能可以零样本迁移到 BrowseComp，在无需修改的情况下将准确率提高了 5.3%，这表明技能级优化产生了超越训练任务的可迁移能力。

## Abstract
Coding agents are increasingly used as general-purpose problem solvers, but their flexibility does not by itself confer the domain expertise needed for specialized tasks. Recent work addresses this through \textit{agent skills}: reusable workflows, and code, that augment agents with domain-specific capabilities. Most skills today are hand-crafted, and existing evolutionary approaches optimize low-level artifacts (e.g. prompts \& code) that are tightly coupled to specific models and tasks. We introduce \textbf{EvoSkill}, a self-evolving framework that automatically discovers and refines agent skills through iterative failure analysis. EvoSkill analyzes execution failures, proposes new skills or edits to existing ones, and materializes them into structured, reusable skill folders. A Pareto frontier of agent programs governs selection, retaining only skills that improve held-out validation performance while the underlying model remains frozen. We evaluate EvoSkill on two benchmarks: OfficeQA, a grounded reasoning benchmark over U.S.\ Treasury data, where it improves exact-match accuracy by \textbf{7.3\%} (60.6\% $\to$ 67.9\%); and SealQA, a search-augmented QA benchmark with noisy retrieval, where it yields a \textbf{12.1\%} gain (26.6\% $\to$ 38.7\%). We also investigate the zero-shot transfer capabilties of skills evolved on one task to the other; in particular: skills evolved from SealQA transfers zero-shot to BrowseComp, improving accuracy by \textbf{5.3\%} without modification demonstrating that skill-level optimization produces transferable capabilities beyond the training task.

---

## 论文详细总结（自动生成）

以下是对论文《EvoSkill: Automated Skill Discovery for Multi-Agent Systems》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管编程智能体（如 Claude Code）在通用任务上表现出色，但它们往往缺乏处理特定领域复杂任务所需的专业知识。
*   **研究动机**：
    *   **现有方案局限**：目前的“智能体技能”大多依靠人工编写，耗时耗力且难以扩展。
    *   **进化方法缺陷**：现有的自动化进化方法（如 AlphaEvolve）通常在提示词（Prompt）或代码片段等低级产物上进行优化，这些产物与特定模型和任务紧密耦合，缺乏可重用性和跨任务的迁移能力。
    *   **目标**：开发一种能够自动发现、精炼并以结构化形式存储“领域技能”的框架，使智能体能够像人类专家一样积累可迁移的经验。

### 2. 方法论：EvoSkill 框架
EvoSkill 的核心思想是通过**基于文本反馈的迭代进化**来发现技能，其操作对象是高层级的“技能文件夹”而非低级代码。
*   **核心组件（三智能体协作）**：
    1.  **执行者（Executor Agent）**：运行任务并产生执行轨迹。
    2.  **提议者（Proposer Agent）**：分析执行失败的案例，诊断根因，并提出创建新技能或修改现有技能的文本建议。它维护一个反馈历史记录（H），以避免重复错误。
    3.  **技能构建者（Skill-Builder Agent）**：将提议者的建议具体化为结构化的技能文件夹，包含 `SKILL.md`（指令）、触发元数据和辅助脚本（Python/TS）。
*   **算法流程（EvoSkill Loop）**：
    *   **初始化**：从基础智能体程序开始。
    *   **迭代优化**：采用轮询方式从帕累托前沿（Pareto Frontier）选择父程序，在训练集上收集失败案例。
    *   **技能变异**：提议者生成技能修改建议，构建者生成候选程序。
    *   **前沿更新**：在留出的验证集上评估候选程序。如果性能优于前沿中的最弱成员，则替换之。
    *   **最终输出**：返回验证集得分最高的程序。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **OfficeQA**：基于美国财政部公告的落地推理基准，涉及复杂的表格解析和定量推理。
    *   **SealQA**：搜索增强问答基准，包含大量具有误导性或噪声的检索结果。
    *   **BrowseComp**：用于测试零样本迁移能力的浏览器智能体基准。
*   **对比设置**：
    *   **基准线**：使用 Claude Code 配合 Opus 4.5 模型的原始表现。
    *   **消融/变体**：测试了不同规模的训练集（5%、10%、15%）以及“技能合并（Skill-Merge）”配置（合并多个独立运行产生的独特技能）。
*   **底层模型**：所有实验均使用 **Claude Opus 4.5**，且在进化过程中模型参数保持冻结。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本提示**：作者提到由于使用 Opus 4.5 模型的计算成本极高，每个配置仅进行了一次完整的进化运行，这暗示了该框架对高性能 API 或算力有较高依赖。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 OfficeQA 上进行了多组训练比例实验（5% 到 15% 数据量）。
    *   在 SealQA 上进行了 10% 数据的进化实验。
    *   进行了跨任务（SealQA -> BrowseComp）的迁移实验。
*   **充分性评价**：
    *   **优点**：实验覆盖了文档推理和网络搜索两个截然不同的领域，且包含了迁移学习验证，证明了技能的通用性。
    *   **不足**：由于成本限制，缺乏多随机种子的方差分析（Variance Analysis），且实验主要集中在单一的高性能模型（Opus 4.5）上。

### 6. 主要结论与发现
*   **显著提升**：EvoSkill 在 OfficeQA 上将精确匹配准确率提升了 **7.3%**（达到 67.9%），在 SealQA 上提升了 **12.1%**（达到 38.7%）。
*   **技能互补性**：通过合并不同运行产生的技能，性能得到了进一步增强，说明 EvoSkill 能从不同失败案例中提取互补的能力。
*   **零样本迁移**：在 SealQA 上进化出的“搜索持久化协议”技能，在无需修改的情况下使 BrowseComp 的准确率提升了 **5.3%**，证明了技能级优化比提示词优化更具迁移价值。
*   **可解释性**：生成的技能（如数据提取验证协议）是人类可读的结构化文档，具有极高的透明度。

### 7. 优点
*   **高层抽象**：跳出了低级提示词优化的窠臼，通过“技能”这一抽象层实现了能力的模块化。
*   **自我演化**：无需人工干预即可通过失败分析自动闭环学习。
*   **强迁移性**：证明了在 A 任务上学到的策略可以有效辅助 B 任务，这是迈向通用智能体的重要一步。
*   **结构化产出**：生成的技能文件夹符合工业界标准（如 Agent Skills 规范），易于集成到现有系统中。

### 8. 不足与局限
*   **计算成本**：依赖于强大的底层模型（如 Opus 4.5）进行失败分析和技能构建，运行成本较高。
*   **评估深度**：尚未在多模态任务或更广泛的智能体框架（如视觉智能体）上进行验证。
*   **过拟合风险**：在 OfficeQA 实验中，当训练数据增加到 15% 时性能出现轻微下降，提示在大规模数据上可能存在过拟合或技能冗余问题。
*   **模型依赖**：虽然技能是文本形式，但其有效性可能仍受限于执行者模型的指令遵循能力。

（完）
