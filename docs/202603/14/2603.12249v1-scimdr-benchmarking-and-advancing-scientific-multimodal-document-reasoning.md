---
title: "SciMDR: Benchmarking and Advancing Scientific Multimodal Document Reasoning"
title_zh: SciMDR：基准测试与推进科学多模态文档推理
authors: "Ziyu Chen, Yilun Zhao, Chengye Wang, Rilyn Han, Manasi Patwardhan, Arman Cohan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12249v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 科学多模态文档推理与问答合成
tldr: 针对科学多模态文档推理中数据集规模、忠实度与真实性难以兼得的问题，本文提出了“合成与重定位”框架，通过生成基于断言的QA并将其重嵌入完整文档，构建了包含30万个推理链的SciMDR数据集及专家标注的评估基准SciMDR-Eval。实验证明，在该数据集上微调的模型在处理复杂文档级科学推理任务时表现显著提升，为科学领域多模态大模型的发展提供了重要支撑。
selection_source: fresh_fetch
motivation: 旨在解决构建科学多模态文档推理数据集时，在规模、忠实度和真实复杂性之间存在的固有权衡挑战。
method: 提出“合成与重定位”两阶段框架，通过断言中心化QA合成与文档级重定位，生成具有显式推理链的高质量数据。
result: 成功构建了包含30万QA对的SciMDR数据集和专家标注的评估基准，显著增强了模型在复杂科学文档推理任务中的表现。
conclusion: SciMDR框架和数据集有效地推动了基础模型在处理全篇科学论文时的跨模态理解与深度推理能力。
---

## 摘要
为基础模型训练构建科学多模态文档推理数据集涉及规模、忠实度和真实性之间的内在权衡。为了应对这一挑战，我们引入了“合成与重定位”（synthesize-and-reground）框架，这是一个包含两个阶段的流水线：(1) 以主张为中心的问答合成（Claim-Centric QA Synthesis），在聚焦的片段上生成忠实的、孤立的问答对和推理；(2) 文档级重定位（Document-Scale Regrounding），通过程序化方式将这些问答对重新嵌入到全文档任务中，以确保真实的复杂性。利用该框架，我们构建了 SciMDR，这是一个用于跨模态理解的大规模训练数据集，包含分布在 2 万篇科学论文中的 30 万个带有显式推理链的问答对。我们进一步构建了 SciMDR-Eval，这是一个由专家标注的基准测试，用于评估全流程科学工作流中的多模态理解能力。实验表明，在 SciMDR 上进行微调的模型在多个科学问答基准测试中取得了显著提升，特别是在那些需要复杂文档级推理的任务中。

## Abstract
Constructing scientific multimodal document reasoning datasets for foundation model training involves an inherent trade-off among scale, faithfulness, and realism. To address this challenge, we introduce the synthesize-and-reground framework, a two-stage pipeline comprising: (1) Claim-Centric QA Synthesis, which generates faithful, isolated QA pairs and reasoning on focused segments, and (2) Document-Scale Regrounding, which programmatically re-embeds these pairs into full-document tasks to ensure realistic complexity. Using this framework, we construct SciMDR, a large-scale training dataset for cross-modal comprehension, comprising 300K QA pairs with explicit reasoning chains across 20K scientific papers. We further construct SciMDR-Eval, an expert-annotated benchmark to evaluate multimodal comprehension within full-length scientific workflows. Experiments demonstrate that models fine-tuned on SciMDR achieve significant improvements across multiple scientific QA benchmarks, particularly in those tasks requiring complex document-level reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《SciMDR: Benchmarking and Advancing Scientific Multimodal Document Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在构建用于训练大模型处理科学多模态文档（包含文本、图表、公式等）的数据集时，存在**规模（Scale）、忠实度（Faithfulness）与真实性（Realism）**之间的“不可能三角”。
*   **研究动机**：
    *   现有的科学问答（SciQA）数据集要么规模太小（人工标注），要么上下文过于简化（仅针对孤立的图表），要么在处理长文档时容易产生幻觉（直接从全文生成导致注意力稀释）。
    *   模型在真实科学工作流中表现不佳，主要是因为缺乏既具备复杂文档背景，又拥有高质量、可验证推理链的训练数据。

### 2. 方法论：合成与重定位（Synthesize-and-Reground）框架
论文提出了一个两阶段流水线，旨在解耦“忠实度”和“真实性”：
*   **第一阶段：以主张为中心的问答合成（Claim-Centric QA Synthesis）**
    *   **核心思想**：在原子化（局部）的上下文中生成高质量数据。
    *   **关键技术**：
        1.  **主张提取**：利用 LLM 从论文片段中提取核心科学主张（Claims）。
        2.  **跨模态对齐**：将文本主张与对应的视觉元素（图、表）进行匹配。
        3.  **逆向推理构建（Backward Construction）**：将主张作为“标准答案提示”，引导生成器构建从问题到主张的推理链（CoT）。这种“先有答案再造推理”的方式极大地降低了幻觉风险。
*   **第二阶段：文档级重定位（Document-Scale Regrounding）**
    *   **核心思想**：将第一阶段生成的原子化 QA 对重新嵌入到原始的长文档中。
    *   **关键技术**：
        1.  **信息定位注入**：在推理链开头显式加入定位步骤（例如：“要回答此问题，我需要先查看第 X 节和表 Y”）。
        2.  **真实复杂性模拟**：模型在训练时面对的是包含大量噪声信息的全文，但目标输出是精确的定位和推理，从而教会模型如何在复杂背景下寻找证据。

### 3. 实验设计
*   **数据集构建**：
    *   **SciMDR（训练集）**：包含 30 万个 QA 对，涵盖 2 万篇论文（来自 arXiv 和 Nature Communications）。
    *   **SciMDR-Eval（基准测试）**：由 3 名计算机专业研究生人工标注的 907 个高质量 QA 对，分为证据解释、概念映射、假设验证等 5 类。
*   **对比基准（Benchmarks）**：ChartQA、CharXiv、SPIQA 以及新提出的 SciMDR-Eval。
*   **对比方法**：
    *   **开源模型**：Qwen2.5-VL-7B（基座）、Qwen3-VL-8B、LLaVA-OV-1.5-8B、InternVL-3-8B。
    *   **闭源模型**：GPT-4o、GPT-5.1、GPT-5.2。
    *   **合成数据基准**：SPIQA（现有的合成数据集）。

### 4. 资源与算力
*   **训练配置**：使用 LLaMA-Factory 框架微调 Qwen2.5-VL-7B。
*   **参数设置**：最大序列长度 16K token，每条数据最多 8 张图像（512x512 像素）。
*   **算力说明**：论文提到了 Batch Size 为 64，学习率在 $10^{-5}$ 到 $10^{-6}$ 之间，训练 1-2 个 epoch。**但文中未明确说明具体的 GPU 型号（如 A100/H100）及具体的训练总时长。**

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流基准上进行了全面测试。
    *   进行了**受控数据质量对比**：使用 LLaVA-1.5-7B 作为探测模型，对比了 SciMDR 与 SPIQA 在相同源文档下的表现。
    *   进行了**消融实验**：分别验证了“推理链”和“定位信息”对性能的影响。
    *   进行了**噪声影响分析**：测试了模型在 Oracle（无噪声）、Standard（中等噪声）和 Full-Paper（全文档噪声）下的鲁棒性。
*   **充分性评价**：实验设计非常严谨，通过控制变量法（如使用相同的基座模型对比不同数据集）确保了公平性，消融实验深入探讨了方法论的有效性。

### 6. 主要结论与发现
*   **性能飞跃**：在 SciMDR 上微调后的 7B 模型在 SciMDR-Eval 上的表现从 19.8 提升至 49.1，**接近甚至在某些指标上匹配了 GPT-5.2 的水平**。
*   **推理链的重要性**：消融实验显示，去除推理链会导致性能大幅下降（49.1 -> 16.9），证明了显式推理引导是模型掌握复杂逻辑的关键。
*   **抗噪能力**：通过“重定位”训练，模型在面对全文档长上下文时的定位准确度显著提升，有效缓解了“迷失在中间”（Lost in the Middle）的问题。

### 7. 优点
*   **创新范式**：提出的“合成与重定位”框架巧妙地解决了合成数据中忠实度与复杂性的矛盾。
*   **高质量推理**：通过“逆向构建”确保了推理链的逻辑严密性，为科学领域提供了宝贵的微调资源。
*   **基准价值**：SciMDR-Eval 填补了真实长文档多模态科学推理评估的空白。

### 8. 不足与局限
*   **生成器依赖**：数据合成高度依赖于强大的闭源模型（如 GPT-5.1），这可能限制了该流水线在完全开源环境下的复现成本。
*   **领域覆盖**：目前主要集中在 STEM 领域（计算机科学与自然科学），对于人文社科或其他专业领域的泛化能力尚待验证。
*   **解析误差风险**：依赖 OCR 模型（MinerU2.5）进行文档解析，若 OCR 出现错误（如复杂的数学公式或重叠图表），会直接影响后续 QA 的质量。

（完）
