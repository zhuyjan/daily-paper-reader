---
title: "DocSage: An Information Structuring Agent for Multi-Doc Multi-Entity Question Answering"
title_zh: DocSage：一种用于多文档多实体问答的信息结构化智能体
authors: "Teng Lin, Yizhang Zhu, Zhengxuan Zhang, Yuyu Luo, Nan Tang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11798v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 用于多文档RAG和信息结构化的智能体框架
tldr: "针对多文档多实体问答（MDMEQA）中逻辑追踪难、检索不精准等问题，本文提出DocSage智能体框架。该框架通过动态模式发现、结构化信息提取及模式感知推理，将非结构化文本转化为关系表，利用SQL索引和关系连接实现跨文档证据链构建。实验表明，DocSage在准确率上比现有长文本模型和RAG系统提升了27%以上，有效增强了复杂实体关系的推断能力。"
selection_source: fresh_fetch
motivation: 现有RAG和长文本模型在处理跨文档复杂实体关系时，存在关键事实遗漏、缺乏模式感知及证据链构建困难等局限。
method: 提出一种集成动态模式发现、结构化提取及模式感知推理的端到端智能体框架，将非结构化文本转化为关系表进行SQL化推理。
result: "在MDMEQA基准测试中，DocSage相比最先进的长文本LLM和RAG系统实现了超过27%的准确率提升。"
conclusion: 通过结构化信息提取与关系推理相结合，DocSage显著提升了模型在多文档场景下定位精确事实和处理复杂实体关联的能力。
---

## 摘要
多文档多实体问答（MDMEQA）本质上要求模型能够追踪分布在零散文档中的多个实体之间的隐式逻辑。然而，现有的大语言模型（LLMs）和检索增强生成（RAG）框架存在严重的局限性：标准 RAG 基于向量相似性的粗粒度检索往往会遗漏关键事实；基于图的 RAG 无法有效整合碎片化的复杂关系网络；且两者都缺乏模式感知（schema awareness），导致跨文档证据链构建不足以及实体关系推断不准确。为了应对这些挑战，我们提出了 DocSage，这是一个端到端的智能体框架，集成了动态模式发现、结构化信息提取以及具有错误保证的模式感知关系推理。DocSage 通过三个核心模块运行：(1) 模式发现模块，动态推断特定于查询的最小可连接模式，以捕获关键实体和关系；(2) 提取模块，将非结构化文本转换为语义连贯的关系表，并通过错误感知纠正机制进行增强，以减少提取错误；(3) 推理模块，在结构化表上执行多跳关系推理，利用模式感知高效地对齐跨文档实体并聚合证据。这种智能体设计具有三个关键优势：通过 SQL 驱动的索引实现精确的事实定位；通过关系表自然地支持跨文档实体连接；以及通过结构化表示减轻 LLM 的注意力扩散。在两个 MDMEQA 基准测试上的评估表明，DocSage 的性能显著优于最先进的长上下文 LLM 和 RAG 系统，准确率分别提升了 27% 以上。

## Abstract
Multi-document Multi-entity Question Answering inherently demands models to track implicit logic between multiple entities across scattered documents. However, existing Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) frameworks suffer from critical limitations: standard RAG's vector similarity-based coarse-grained retrieval often omits critical facts, graph-based RAG fails to efficiently integrate fragmented complex relationship networks, and both lack schema awareness, leading to inadequate cross-document evidence chain construction and inaccurate entity relationship deduction. To address these challenges, we propose DocSage, an end-to-end agentic framework that integrates dynamic schema discovery, structured information extraction, and schema-aware relational reasoning with error guarantees. DocSage operates through three core modules: (1) A schema discovery module dynamically infers query-specific minimal joinable schemas to capture essential entities and relationships; (2) An extraction module transforms unstructured text into semantically coherent relational tables, enhanced by error-aware correction mechanisms to reduce extraction errors; (3) A reasoning module performs multi-hop relational reasoning over structured tables, leveraging schema awareness to efficiently align cross-document entities and aggregate evidence. This agentic design offers three key advantages: precise fact localization via SQL-powered indexing, natural support for cross-document entity joins through relational tables, and mitigated LLM attention diffusion via structured representation. Evaluations on two MDMEQA benchmarks demonstrate that DocSage significantly outperforms state-of-the-art long-context LLMs and RAG systems, achieving more than 27% accuracy improvements respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《DocSage: An Information Structuring Agent for Multi-Doc Multi-Entity Question Answering》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多文档多实体问答（MDMEQA）**任务。在临床研究、金融分析和法律实践等领域，答案往往隐藏在多个零散文档的复杂实体关系中。
*   **现有挑战**：
    *   **标准RAG**：基于向量相似性的检索过于粗糙，容易遗漏关键事实。
    *   **长文本LLM**：受限于上下文窗口和“注意力稀释”现象，难以在长篇幅中追踪细微的逻辑链。
    *   **GraphRAG**：构建复杂关系图的计算成本极高，且缺乏针对特定查询的模式感知（Schema Awareness）。
*   **核心目标**：提出一种智能体框架，通过动态构建结构化的关系表，将非结构化文本转化为可计算的数据库，从而实现精确的跨文档推理。

### 2. 方法论：核心思想与技术细节
DocSage 采用端到端的智能体架构，包含三个核心模块：

*   **交互式模式发现模块 (ASK 算法)**：
    *   **核心思想**：不依赖预定义模式，而是根据查询动态推断“最小可连接模式”。
    *   **流程**：首先生成初始模式假设，随后通过“主动提问”机制（如识别实体对齐冲突、属性值异常、缺失关系路径）与文档集交互，迭代更新并精简模式。
*   **逻辑感知结构化提取模块 (CLEAR 机制)**：
    *   **核心思想**：将文本填充进模式表，并引入错误纠正机制。
    *   **技术细节**：
        *   **LoRA增强校准**：微调轻量级适配器以评估提取结果的置信度。
        *   **跨记录逻辑约束**：强制执行函数依赖（如一个ID对应一个生日）、时间约束和外键完整性检查。
        *   **纠错流**：对低置信度或违反逻辑的元组，调用更强大的LLM委员会进行重新提取或上下文验证。
*   **模式引导关系推理模块**：
    *   **核心思想**：将复杂推理转化为确定性的数据库操作。
    *   **流程**：LLM 将自然语言查询编译为优化的 SQL 语句，在结构化表上执行。系统支持**证据溯源**，可将结果行映射回原始文档片段。

### 3. 实验设计
*   **数据集/Benchmark**：
    1.  **MEBench**：包含 4,780 个问题，涵盖比较、统计和关系三大类，测试模型在不同实体密度（0-100+ 实体）下的表现。
    2.  **Loong**：长文本基准，涵盖定位、比较、聚类和推理链任务，文档长度从 10K 扩展至 250K tokens。
*   **对比方法**：
    *   **原生模型**：GPT-4o。
    *   **检索增强**：GPT-4o + 标准 RAG。
    *   **结构化/图增强**：GraphRAG、StructRAG。

### 4. 资源与算力
*   **模型使用**：主要使用 GPT-4o 和 Qwen3 作为推理引擎，Mistral-7B 用于信息提取的小模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。文中提到了对 Mistral-7B 进行 LoRA 微调，但未给出具体的硬件环境参数。

### 5. 实验数量与充分性
*   **实验规模**：在两个大规模基准上进行了全面测试，涵盖了从短文本到极长文本（250K tokens）的多种场景。
*   **消融实验**：非常充分。作者分别针对“无结构化提取”、“无模式引导推理”、“无模式发现”、“无逻辑检查（CLEAR）”以及“被动模式发现”进行了对比实验。
*   **客观性**：通过准确率（Accuracy）、LLM 评分和精确匹配率（EM）多维度衡量，实验设计考虑了实体密度和文档长度的变量控制，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能飞跃**：DocSage 在 MEBench 和 Loong 上分别比最先进的基线（如 GPT-4o+RAG）提升了 **27.2%** 和 **27%** 的准确率。
*   **鲁棒性强**：随着实体数量增加或文档长度增长，传统 RAG 和 LLM 的性能会剧烈下降，而 DocSage 的性能保持相对稳定（例如在 MEBench Set3 中仍保持 87.9% 的准确率）。
*   **结构化的价值**：实验证明，将推理任务从 LLM 的注意力机制转移到 SQL 引擎，能有效消除幻觉并提高多跳推理的可靠性。

### 7. 优点（亮点）
*   **动态模式感知的创新**：ASK 算法解决了开放域问答中预定义模式缺失的问题。
*   **错误保证机制**：CLEAR 机制结合了统计置信度和符号逻辑约束，显著降低了信息提取阶段的级联错误。
*   **确定性推理**：通过 SQL 驱动的索引和连接，实现了精准的事实定位和可解释的证据链。

### 8. 不足与局限
*   **计算成本**：由于采用了多阶段、迭代式的智能体工作流，其推理延迟和 API 调用成本可能高于单次检索的 RAG。
*   **模型依赖性**：框架的效果高度依赖于底层基础模型（如 GPT-4o）的模式推断和提取能力。
*   **数据假设**：目前假设源文档具有一定的语义连贯性，在处理极端噪声、高度矛盾或极度专业化的领域文本时，性能可能会受限。

（完）
