---
title: "One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries"
title_zh: 一个主管，多种模态：面向自主查询的自适应工具编排
authors: Mayank Saini Arit Kumar Bishwas
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11545v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 用于自主多模态查询处理的智能体AI框架
tldr: 本研究提出一种用于自主多模态查询处理的代理 AI 框架，通过中央主管（Supervisor）协调文本、图像、音频和视频等多种工具。该框架摒弃了预设决策树，采用 RouteLLM 和 SLM 辅助的自适应路由策略进行任务分解与调度。实验表明，该方法在保持准确率的同时，显著降低了响应时间、对话返工率和运营成本，为多模态 AI 的高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态 AI 系统在处理跨模态复杂查询时，因缺乏灵活调度机制而导致的效率低、成本高和用户交互冗余问题。
method: 开发了一个基于中央主管的框架，利用自适应路由动态分解查询并调用特定模态工具（如 OCR、语音转录等）进行处理。
result: "实验显示该框架在 2,847 个查询中实现了 72% 的响应时间缩短、85% 的返工减少以及 67% 的成本降低。"
conclusion: 智能化的集中编排是优化多模态 AI 部署经济性并提升自主查询处理性能的关键。
---

## 摘要
我们提出了一种用于自主多模态查询处理的智能体 AI 框架，该框架协调了涵盖文本、图像、音频、视频和文档模态的专业工具。一个核心主管（Supervisor）动态分解用户查询，将子任务委派给相应的模态工具（例如目标检测、OCR、语音转录），并通过自适应路由策略而非预定义的决策树来综合结果。对于纯文本查询，该框架通过 RouteLLM 使用学习型路由；而对于非文本路径，则使用 SLM 辅助的模态分解。在涵盖 15 个任务类别的 2,847 个查询上的评估结果显示，与匹配的分层基准相比，我们的框架在保持准确率相当的情况下，获得准确答案的时间缩短了 72%，对话返工减少了 85%，成本降低了 67%。这些结果表明，智能化的集中编排从根本上改善了多模态 AI 部署的经济性。

## Abstract
We present an agentic AI framework for autonomous multimodal query processing that coordinates specialized tools across text, image, audio, video, and document modalities. A central Supervisor dynamically decomposes user queries, delegates subtasks to modality-appropriate tools (e.g., object detection, OCR, speech transcription), and synthesizes results through adaptive routing strategies rather than predetermined decision trees. For text-only queries, the framework uses learned routing via RouteLLM, while non-text paths use SLM-assisted modality decomposition. Evaluated on 2,847 queries across 15 task categories, our framework achieves 72% reduction in time-to-accurate-answer, 85% reduction in conversational rework, and 67% cost reduction compared to the matched hierarchical baseline while maintaining accuracy parity. These results demonstrate that intelligent centralized orchestration fundamentally improves multimodal AI deployment economics.

---

## 论文详细总结（自动生成）

这是一份关于论文《One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries》的深度结构化总结：

### 1. 核心问题与整体含义
该研究旨在解决现代 AI 系统在处理**多模态自主查询**时面临的“不可能三角”挑战：即如何在支持复杂多模态（文本、图像、音频、视频、文档）的同时，保持**低运营成本**和**实时响应速度**。
*   **研究动机**：现有的单体大模型（如 GPT-4）全量处理所有任务成本极高；而传统的分层路由系统依赖预设的决策树，面对非预期模式时表现得非常脆弱（Brittle），一旦出错往往需要全局重启，导致用户体验差且资源浪费。
*   **核心意义**：提出了一种基于“中央主管”（Supervisor）的智能体编排框架，通过动态任务分解和自适应路由，实现了高效、经济且鲁棒的多模态查询处理。

### 2. 方法论
该框架的核心思想是将编排逻辑从硬编码的决策树转变为**基于上下文的自主推理**。

*   **中央主管（Supervisor）**：作为核心大脑，读取工具的类型签名、前置/后置条件及延迟先验，动态构建执行图。
*   **两阶段查询分解**：
    1.  **模态检测**：结合启发式分析（文件扩展名、MIME 类型）和内容检查。
    2.  **语义分析**：利用轻量级小语言模型（SLM，如 Phi-3.5-mini）分配执行标签（如 audio, vision, complex 等）。
*   **自适应路由策略**：
    *   **文本路径**：集成 RouteLLM，通过“获胜预测模型”在强模型（GPT-4o）和弱模型（LLaMA-3 等）之间切换。
    *   **非文本路径（Couplet Framework）**：将传统感知模型（如 YOLO 用于检测、CLIP 用于检索、Tesseract 用于 OCR）与 SLM 协调器配对，避免昂贵的多模态 LLM 推理。
*   **五层记忆架构**：包括短期窗口、全历史、模态特定记忆、语义相关上下文（基于 Qdrant 向量库）和压缩记忆，确保跨模态上下文的一致性。
*   **局部修复机制**：当某个工具失败时，主管能识别置信度并动态选择替代工具或发起澄清，无需重启整个流水线。

### 3. 实验设计
*   **数据集与场景**：评估涵盖了 2,847 个查询，横跨 15 个任务类别（包括 MMLU 文本推理、VQA-v2 视觉问答、PDF 文档分析、音频转录、视频分析等）。
*   **基准测试（Benchmark）**：
    *   **Monolithic LLM**：全量使用 GPT-4 处理。
    *   **Hierarchical Routing**：基于预设决策树的分层路由系统。
    *   **Multi-Agent Frameworks**：如 AutoGen 和 LangGraph。
*   **核心指标**：准确答案获取时间（TTA）、对话返工率（Rework Rate）、单次查询成本、吞吐量（q/s）和任务准确率。

### 4. 资源与算力
*   **模型使用**：文中提到了多种模型，包括 Phi-3.5-mini (3.8B)、LLaMA-3-70B、Mixtral-8x7B、GPT-4o、YOLO、CLIP 等。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。这主要是因为该研究侧重于**推理侧的编排框架**而非模型本身的训练。

### 5. 实验数量与充分性
*   **实验规模**：2,847 组实验对于验证编排框架而言是相对充分的，覆盖了从简单文本到复杂跨模态检索的多种组合。
*   **消融实验**：研究者进行了详细的消融实验，分别验证了记忆层、验证机制、并行执行和 Couplet Framework 对系统性能的影响。
*   **客观性**：通过 95% 置信区间和显著性检验（p < 0.001）来增强结果的可信度，对比实验在相同工具集和后端下进行，确保了公平性。

### 6. 主要结论与发现
*   **效率提升**：相比分层基准，该框架将获取准确答案的时间（TTA）缩短了 **72%**。
*   **质量优化**：对话返工率降低了 **85%**，表明自主协调显著减少了用户纠错的需求。
*   **成本降低**：通过智能路由和传统模型替代，运营成本降低了 **67%**。
*   **性能平衡**：在大幅提升效率的同时，准确率保持在 ±1% 的统计等效水平。
*   **感知任务优势**：使用 YOLO 处理视频帧（180ms/帧）远快于 LLM 视觉方案（2.4s/帧）。

### 7. 优点
*   **架构灵活性**：采用“插件化”工具设计，主管可以像调用函数一样组合不同模态的工具。
*   **鲁棒性强**：局部修复机制解决了传统流水线“一处断、全线崩”的痛点。
*   **经济性**：通过“成本旋钮”（Cost Knob）策略，实现了资源分配的精细化管理。
*   **多模态深度融合**：五层记忆架构有效解决了跨模态上下文丢失的问题。

### 8. 不足与局限
*   **编排延迟**：虽然减少了执行延迟，但 LLM 驱动的主管在进行复杂任务分解时，自身会产生一定的推理开销，在极高吞吐量场景下可能成为瓶颈。
*   **工具集成门槛**：Couplet Framework 目前支持的模型集是固定的，引入新模型仍需一定的接口适配工作。
*   **路由保守性**：在某些模糊场景下，系统可能会为了保险而过度倾向于调用昂贵的高性能模型。
*   **长会话压力**：随着会话极度增长，记忆检索和压缩的计算负担会显著增加。

（完）
