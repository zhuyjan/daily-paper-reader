---
title: Pooling Engram Conditional Memory in Large Language Models using CXL
title_zh: 使用 CXL 在大语言模型中池化 Engram 条件记忆
authors: "Ruiyang Ma, Teng Ma, Zhiyuan Su, Hantian Zha, Xinpeng Zhao, Xuchun Shang, Xingrui Yi, Zheng Liu, Zhu Cao, An Wu, Zhichong Dou, Ziqian Liu, Daikang Kuang, Guojie Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10087v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 使用 CXL 进行可扩展存储的大模型 Engram 条件记忆
tldr: 本研究针对大语言模型中 Engram 条件记忆的大规模存储需求，提出利用 CXL（Compute Express Link）内存池进行存储。Engram 通过解耦静态知识检索与动态计算提升性能，但其庞大的嵌入表对内存容量提出挑战。相比 RDMA，CXL 提供了更细粒度和低延迟的访问，适合 Engram 的稀疏访问特性。通过将 CXL 存储集成至 SGLang 框架，该方案在保持接近 DRAM 性能的同时，实现了高扩展性和成本效益。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型中 Engram 条件记忆所需的大规模嵌入表在传统内存中存储成本高且扩展性受限的问题。
method: 提出利用 CXL 内存池作为 Engram 的存储层，并将其集成到 SGLang 框架中，利用 CXL 的低延迟特性支持稀疏访问。
result: 该方案在 SGLang 中实现了接近 DRAM 的端到端推理性能，同时显著降低了存储成本。
conclusion: CXL 内存池是未来集成 Engram 的大语言模型实现高性能、可扩展且低成本存储的理想选择。
---

## 摘要
Engram 条件记忆通过将静态知识查找与动态计算解耦，已成为大语言模型（LLM）中一个极具前景的组件。由于 Engram 表现出稀疏的访问模式并支持预取，其庞大的嵌入表非常适合卸载到低层级内存中。在本文中，我们提出使用 Compute Express Link (CXL) 内存池进行 Engram 存储。与 RDMA 相比，CXL 提供了 Engram 极小且离散的检索模式所需的细粒度、低延迟访问。我们将基于 CXL 的 Engram 池集成到 SGLang 中，实现了接近 DRAM 的端到端性能。这为未来集成 Engram 的大语言模型提供了一种可扩展且具有成本效益的存储解决方案，且不会损害推理性能。

## Abstract
Engram conditional memory has emerged as a promising component for LLMs by decoupling static knowledge lookup from dynamic computation. Since Engram exhibits sparse access patterns and supports prefetching, its massive embedding tables are well-suited for offloading to lower-tier memory. In this paper, we propose using Compute Express Link (CXL) memory pool for Engram storage. Compared to RDMA, CXL provides fine-grained and low-latency access required by minimal and discrete retrieval patterns of Engram. We integrate the CXL-based Engram pool into SGLang, achieving near-DRAM end-to-end performance. This provides a scalable and cost-efficient storage solution for future Engram-integrated LLMs without compromising inference performance.

---

## 论文详细总结（自动生成）

这篇论文题为《Pooling Engram Conditional Memory in Large Language Models using CXL》，由北京大学、阿里云、山东盈信、人大及港大等机构的研究人员共同完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）在处理静态知识查找时效率较低。DeepSeek 提出的 **Engram** 架构通过解耦静态 N-Gram 知识查找与动态推理，引入了“条件记忆”机制。
*   **核心挑战**：Engram 需要庞大的嵌入表（可能达数百 GB），这给 GPU 显存带来了巨大压力（Memory Wall）。
*   **核心问题**：虽然 Engram 占用空间大，但其访问模式具有**稀疏性**、**只读性**且**容忍延迟**（可预取）。论文探讨了如何利用 **CXL（Compute Express Link）** 内存池技术，在不损失推理性能的前提下，实现低成本、可扩展的 Engram 存储。

### 2. 方法论
*   **核心思想**：将 Engram 参数从昂贵的 GPU/本地 DRAM 卸载到通过 CXL Switch 连接的共享内存池中。
*   **关键技术细节**：
    *   **CXL 内存池架构**：使用 CXL 2.0/3.0 交换机（如 XConn XC50256）构建解耦的内存空间，支持多主机共享。
    *   **硬件级访问**：利用 CXL 的 `load/store` 语义，绕过传统网络协议栈，实现缓存行粒度的细粒度访问。
    *   **优化访问例程**：
        *   **CXL → CPU**：使用 OpenMP 多线程并行 `memcpy`。
        *   **CXL → GPU**：开发自定义 CUDA 内核，通过 `cudaHostRegister` 将 CXL 内存映射为 GPU 可访问地址，实现点对点（P2P）数据传输，避开 CPU 中转。
    *   **框架集成**：在 **SGLang** 推理框架中实现了初始化、异步预取和计算逻辑的集成。

### 3. 实验设计
*   **实验场景**：
    1.  **微基准测试（Micro-benchmark）**：对比本地 DRAM、RDMA 内存池（基于 Mooncake Store）和 CXL 内存池在不同 Batch Size 下的读取延迟。
    2.  **端到端推理测试**：在 SGLang 中集成 Engram 模块，测试吞吐量。
*   **对比方法**：本地 DRAM 方案、基于 RDMA 的内存池方案。
*   **测试模型**：由于 DeepSeek Engram 权重未开源，实验使用 **Qwen3-4B/8B/32B** 进行性能模拟。

### 4. 资源与算力
*   **硬件配置**：
    *   2 台服务器，每台配备双路 Intel Xeon 6766E 处理器（共 288 核）。
    *   每台服务器配备 2 块 **NVIDIA L20 GPU**（共 4 块）。
    *   **CXL 设备**：XConn XC50256 CXL 交换机，连接 256GB 三星 CXL 内存扩展卡。
*   **软件环境**：Ubuntu 22.04，SGLang 推理框架。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Engram-27B 和 Engram-40B 两种配置进行了延迟分析。
    *   Batch Size 从 1 覆盖到 1024。
    *   进行了单节点与多节点（nnode=2）、单数据并行（DP=1）与多数据并行（DP=2）的扩展性实验。
*   **充分性评价**：实验设计较为全面，涵盖了从底层硬件延迟到上层框架吞吐量的多维度评估，并包含成本效益分析。虽然节点规模较小（2 节点），但足以证明 CXL 在该场景下的可行性。

### 6. 主要结论与发现
*   **延迟优势**：CXL 的读取延迟远低于 RDMA，且非常接近本地 DRAM，能够满足 Engram 预取的时间窗口要求。
*   **端到端性能**：基于 CXL 的 Engram 池在 SGLang 中实现了**接近 DRAM 的性能**（吞吐量损失极小）。
*   **可扩展性**：系统在多节点和高并发访问下表现稳定，性能下降微乎其微。
*   **成本效益**：随着模型规模（如 400B Engram）和集群规模的增加，CXL 方案相比全本地 DRAM 方案可节省大量硬件成本（最高可达数十万美元）。

### 7. 优点
*   **首创性**：首次将 CXL 内存池应用于 LLM 的 Engram 条件记忆卸载。
*   **路径优化**：通过自定义 CUDA Kernel 实现 CXL 到 GPU 的直接 P2P 传输，显著降低了数据搬运开销。
*   **实用性强**：直接集成在主流推理框架 SGLang 中，并提供了详尽的成本对比分析。

### 8. 不足与局限
*   **模型权重限制**：由于缺乏开源的 Engram 权重，实验主要基于模拟计算，可能无法完全捕捉真实模型中的所有动态特性。
*   **硬件规模**：实验仅在 2 节点的小规模集群上完成，更大规模（如数十个节点）下的 CXL 交换机竞争和带宽瓶颈尚待验证。
*   **资源竞争**：未深入探讨 Engram 负载与 KV Cache 负载在同一个 CXL 内存池中混合存储时的资源调度与冲突问题。

（完）
