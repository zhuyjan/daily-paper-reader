---
title: "Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models"
title_zh: 边看边想：多模态大语言模型中用于多轮视频推理的在线流式片段级记忆
authors: "Lu Wang, Zhuoran Jin, Yupu Hao, Yubo Chen, Kang Liu, Yulong Ao, Jun Zhao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11896v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 用于视频推理的在线流式片段级记忆
tldr: 针对多模态大模型在在线视频流推理和多轮交互中的局限性，本文提出“边看边想”（Think While Watching）框架。该框架通过保留连续的分段级内存，克服了传统交替感知-生成模式导致的内存衰减和长程依赖建模难的问题。通过构建三阶段多轮CoT数据集和采用流式因果掩码等技术，实现了感知与生成的并行，显著提升了在线视频推理的准确性与效率。
selection_source: fresh_fetch
motivation: 现有的流式视频理解方法因感知与生成无法并行且存在内存衰减，难以支持长视频流中的高效多轮交互。
method: 提出一种基于分段级内存的流式推理框架，结合三阶段多轮CoT数据集训练，并引入流式因果掩码和位置编码以支持并行感知与思考。
result: "在StreamingBench和OVO-Bench上分别提升了2.6%和3.79%的准确率，且在多轮对话中减少了56%的输出Token。"
conclusion: 该研究证明了分段级内存和并行处理机制在提升流式视频多轮推理性能和效率方面的有效性。
---

## 摘要
多模态大语言模型（MLLMs）在离线视频理解方面表现出强大的性能，但大多数模型局限于离线推理或在线推理能力较弱，导致难以在持续到达的视频流上进行多轮交互。现有的流式方法通常采用交替的感知-生成范式，这阻碍了感知与生成的并发进行，并随着视频流的增长导致早期记忆衰减，从而损害了长程依赖建模。我们提出了“边看边想”（Think While Watching），这是一个以记忆为锚点的流式视频推理框架，能够在多轮交互过程中保持连续的片段级记忆。我们构建了一个三阶段、多轮的思维链（CoT）数据集，并采用了阶段匹配的训练策略，同时通过片段级流式因果掩码和流式位置编码来强制执行严格的因果性。在推理过程中，我们引入了一种高效的流水线，使“观看”与“思考”重叠，并自适应地选择最佳的注意力后端。在单轮和多轮流式输入协议下，我们的方法均取得了优异的结果。基于 Qwen3-VL，该方法在 StreamingBench 上的单轮准确率提升了 2.6%，在 OVO-Bench 上提升了 3.79%。在多轮设置下，它在保持性能的同时将输出 Token 减少了 56%。代码可在以下网址获取：https://github.com/wl666hhh/Think_While_Watching/

## Abstract
Multimodal large language models (MLLMs) have shown strong performance on offline video understanding, but most are limited to offline inference or have weak online reasoning, making multi-turn interaction over continuously arriving video streams difficult. Existing streaming methods typically use an interleaved perception-generation paradigm, which prevents concurrent perception and generation and leads to early memory decay as streams grow, hurting long-range dependency modeling. We propose Think While Watching, a memory-anchored streaming video reasoning framework that preserves continuous segment-level memory during multi-turn interaction. We build a three-stage, multi-round chain-of-thought dataset and adopt a stage-matched training strategy, while enforcing strict causality through a segment-level streaming causal mask and streaming positional encoding. During inference, we introduce an efficient pipeline that overlaps watching and thinking and adaptively selects the best attention backend. Under both single-round and multi-round streaming input protocols, our method achieves strong results. Built on Qwen3-VL, it improves single-round accuracy by 2.6% on StreamingBench and by 3.79% on OVO-Bench. In the multi-round setting, it maintains performance while reducing output tokens by 56%. Code is available at: https://github.com/wl666hhh/Think_While_Watching/

---

## 论文详细总结（自动生成）

### 论文总结：Think While Watching (TWW)

#### 1. 核心问题与整体含义
论文针对多模态大语言模型（MLLM）在**在线流式视频理解**和**多轮交互推理**中的局限性展开研究。
*   **研究动机**：现有的流式视频模型多采用“交替感知与生成”的串行模式。这种模式存在两大痛点：一是**记忆侵蚀（Memory Erosion）**，即在多轮对话中，模型容易遗忘早期的视觉线索或对话上下文；二是**序列化瓶颈（Serialization Bottleneck）**，即模型在生成文本回复时必须暂停视频摄取，导致响应延迟和处理不连续。
*   **核心目标**：实现一种能够“边看边想”、具备持久记忆且支持感知与生成并行的流式推理框架。

#### 2. 方法论
论文提出了 **Think While Watching (TWW)** 框架，其核心思想是将视频流转化为片段级的记忆笔记，并解耦输入与输出流。
*   **片段级记忆笔记（Segment-level Memory Notes）**：模型为每个视频片段生成简短的文本笔记并存入记忆库。在回答问题时，模型通过注意力机制隐式地检索和整合这些笔记。
*   **流式架构设计**：
    *   **片段级流式因果掩码**：确保模型在推理时只能看到当前及过去的视频片段，严禁“偷看”未来信息。
    *   **流式位置编码**：基于 MRoPE 改进，将输入流和输出流的位置编码解耦。这使得模型在生成回复（输出）的同时，仍能为新到达的视频片段（输入）分配正确的位置索引。
*   **训练策略**：采用**三阶段训练**：
    1.  **阶段1**：学习编写片段笔记并进行单轮问答。
    2.  **阶段2**：扩展至多轮对话，强化上下文一致性。
    3.  **阶段3**：长程能力训练，包括长效记忆提取、不确定性处理（证据不足时延迟回答）和抗干扰学习。
*   **推理优化**：引入**双 KV 缓存（Dual KV Cache）**实现感知与生成的并行处理，并根据查询长度自适应切换 Flash Attention 和 Memory-efficient Attention 后端。

#### 3. 实验设计
*   **数据集与场景**：
    *   **在线评测**：StreamingBench（涵盖实时理解、全源理解等）和 OVO-Bench（侧重真实世界在线视频理解）。
    *   **离线迁移评测**：Video-MME 和 LV-Bench（验证流式训练对长视频理解的泛化能力）。
*   **对比方法**：
    *   **闭源模型**：Gemini 1.5 Pro, GPT-4o。
    *   **开源流式模型**：Flash-VStream, VideoLLM-online, Dispider, StreamAgent, StreamForest 等。
    *   **基座模型**：Qwen3-VL (2B/4B/8B) 的 Instruct 和 Thinking 版本。
*   **评测协议**：分为单轮流式（Single-round）和多轮流式（Multi-round）输入协议。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA RTX A6000 (48GB)** GPU。
*   **训练细节**：采用全参数监督微调（Full-parameter SFT），使用 DeepSpeed ZeRO-3 优化器，精度为 bf16。
*   **时长**：文中未明确给出总训练时长，但详细列出了学习率（1.0e-5）、Batch Size（128）等超参数。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 2B、4B、8B 三种参数规模的基座模型，并在 4 个主流基准测试集上进行了详尽测试。
*   **消融实验**：针对记忆笔记的有效性、视频帧掩码的影响、片段分割粒度（15s 到 120s）等进行了多组对比。
*   **分析深度**：通过注意力分布图分析了模型在不同训练阶段对历史记忆的关注度变化，并进行了理论上的延迟推导（TTFT 分析）。
*   **评价**：实验设计客观、公平，对比基准涵盖了当前最先进的开源与闭源模型，实验组数充分，论证了方法在准确率与效率上的双重优势。

#### 6. 主要结论与发现
*   **性能提升**：在 Qwen3-VL-4B 基础上，TWW 在 StreamingBench 上提升了 2.6% 的准确率，在 OVO-Bench 上提升了 3.79%。
*   **效率优化**：在多轮对话中，TWW 在保持准确率的同时，将输出 Token 数量减少了 **56%**，显著降低了计算开销。
*   **延迟降低**：通过并行流水线，首 Token 延迟（TTFT）相比传统的批处理模式降低了 **92.6%**。
*   **泛化性**：流式训练不仅提升了在线表现，也增强了模型在离线长视频任务（如 Video-MME）上的推理能力。

#### 7. 优点
*   **并行化创新**：成功解决了 MLLM 在流式场景下“感知”与“生成”互斥的难题，实现了真正的“边看边想”。
*   **记忆机制高效**：片段级记忆笔记既保留了关键信息，又避免了原始视频 Token 带来的巨大计算负担。
*   **数据集贡献**：构建并开源了三阶段流式 CoT 数据集，填补了该领域高质量多轮流式训练数据的空白。

#### 8. 不足与局限
*   **分割策略固定**：目前采用固定的时间窗口分割视频，未来若能根据场景切换动态调整分割边界（Adaptive Segmentation）会更理想。
*   **模态局限**：目前主要聚焦于视觉信息，尚未整合音频和语音转录（ASR）线索。
*   **长程细节丢失**：在极长视频中，片段笔记虽然能保留宏观逻辑，但仍可能丢失某些微观的细粒度属性（如物体的具体颜色或微小动作）。

（完）
