---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程 LLM 智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 用于扩展长周期LLM智能体的索引经验记忆
tldr: 针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在长程任务中的表现。
method: 提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 框架优化智能体的读写与检索决策。
result: 在挑战性的长程任务中，Memex 智能体在保持较小工作上下文的同时，显著提高了任务成功率。
conclusion: 索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体的处理能力。
---

## 摘要
大语言模型（LLM）智能体在长程任务中从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以使用。现有的解决方案通常通过截断或运行摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，一种索引经验记忆机制，它在不丢弃证据的情况下压缩上下文。Memex 维护一个由简洁的结构化摘要和稳定索引组成的紧凑工作上下文，同时将全保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时对索引进行解引用（dereference），并恢复当前子目标所需的准确过去证据。我们利用强化学习框架 MemexRL 优化读写行为，使用针对上下文预算下索引记忆使用情况定制的奖励塑造，使智能体学会总结什么、存档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持有限解引用的同时维持决策质量的潜力，同时在历史增长时保持有效的上下文计算有界。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减小工作上下文的同时，提高了任务成功率。

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Memex** 的索引经验记忆机制及其配套的强化学习框架 **MemexRL**，旨在解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时面临的上下文窗口限制问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：LLM 智能体在执行复杂、多步骤的任务时，会产生大量的观察结果、工具输出和推理轨迹。现有的上下文管理方法（如简单的截断或文本摘要）是“有损”的，往往会丢失关键的细节（如特定的 API 返回值、代码片段或对象 ID），导致智能体在后续步骤中无法准确回溯。
*   **研究动机**：借鉴人类管理长期工作的方式（保留简洁的笔记和稳定的外部引用），作者希望建立一种既能保持工作上下文紧凑，又不丢失任何原始证据的记忆机制。

### 2. 论文提出的方法论
*   **核心思想**：将记忆分为**在上下文中的“索引摘要”**和**外部存储的“全保真经验”**。
*   **关键技术细节**：
    *   **索引经验记忆（Indexed Experience Memory）**：
        *   **工作上下文（Working Context）**：仅保留当前的进度状态和一组“索引-描述”对（Index Map）。
        *   **外部存储（External Store）**：一个键值对数据库，存储完整的工具输出、日志或代码。
    *   **核心操作**：
        *   `CompressExperience`：将当前冗长的上下文压缩，把详细内容存入外部库，并在上下文中仅保留索引摘要。
        *   `ReadExperience(index)`：通过解引用（Dereferencing）特定索引，将精确的原始证据重新注入上下文。
    *   **MemexRL 强化学习框架**：
        *   基于 **GRPO** 算法，将记忆操作（压缩、读取）视为与环境工具同等的动作空间。
        *   **奖励塑造（Reward Shaping）**：奖励函数包含任务成功分、上下文溢出惩罚、冗余工具调用惩罚和格式错误惩罚。
        *   **分段轨迹处理**：将压缩点作为边界切分轨迹，使模型学习到压缩决策对后续步骤的长期影响。
        *   **软触发机制**：通过在观察中加入“上下文状态”提示，训练智能体自主决定压缩时机，而非硬性规定。

### 3. 实验设计
*   **场景与数据集**：使用了修改后的 **ALFWorld**（一个交互式文本决策环境）。
*   **Benchmark 改进**：为了增加难度并测试记忆能力，作者修改了环境：隐藏了可用命令列表、隐藏了初始观察、限制了“查看（look）”动作的次数，并强制对摘要进行截断。
*   **对比方法**：主要对比了经过 MemexRL 训练的智能体与未经 RL 训练（仅靠 Prompt 指引）的基座模型表现。

### 4. 资源与算力
*   **模型**：使用 **Qwen3-30B-A3B-Thinking-2507**（一个约 30B 参数的 MoE 模型）。
*   **训练框架**：基于开源的 **Slime** 强化学习框架。
*   **技术细节**：采用 **INT4 量化**进行推理采样，**量化感知训练（QAT）**进行反向传播（梯度累积使用 BF16）。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量，但提到了 Batch Size 为 32，GRPO 组大小为 8。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 3553 个任务。
*   **充分性**：
    *   展示了训练过程中的成功率曲线和惩罚项下降曲线，证明了 RL 的收敛性。
    *   进行了关于记忆工具调用频率的分析（发现训练后压缩次数减少、检索次数增加）。
    *   **局限性**：实验主要集中在修改后的 ALFWorld 这一类任务上，缺乏在其他长程任务（如软件工程、复杂网络搜索）上的广泛验证。

### 6. 主要结论与发现
*   **性能提升**：MemexRL 将任务成功率从 **24.2% 提升至 85.6%**。
*   **效率优化**：在成功率大幅提升的同时，峰值工作上下文长度从约 1.7 万 token 降至约 0.96 万 token（降低了约 **43%**）。
*   **行为转变**：RL 训练使智能体从“被动压缩”转变为“主动构建可重用的索引记忆”，学会了在需要精确证据时通过 `ReadExperience` 检索，而非重复执行环境工具。

### 7. 优点
*   **低损耗压缩**：通过索引机制解决了摘要丢失细节的问题，实现了“证据不丢失”的上下文管理。
*   **理论支撑**：提供了理论分析，证明了在解引用次数有界的情况下，Memex 能够保持决策质量且计算复杂度不随历史增长。
*   **自主性**：通过 RL 让智能体自主学习记忆管理策略（何时存、存什么、怎么取），而非依赖人工设计的启发式规则。

### 8. 不足与局限
*   **任务单一性**：实验仅在 ALFWorld 环境下进行，虽然进行了难度强化，但其泛化到真实世界复杂任务（如多工具协作、长文档分析）的效果仍待验证。
*   **冷启动依赖**：MemexRL 需要从高质量的演示数据中进行热启动（Warm-start），对初始数据的质量有一定要求。
*   **检索开销**：虽然减少了上下文长度，但频繁的 `ReadExperience` 可能会增加推理轮数和总体的 Token 消耗（尽管单次推理变快了）。

（完）
