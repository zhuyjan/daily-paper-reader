---
title: "DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval"
title_zh: DEO：面向否定感知检索的免训练直接嵌入优化
authors: "Taegyeong Lee, Jiwon Park, Seunghyun Hwang, JooYoung Jang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09185v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 无需训练的多模态检索方法
tldr: 针对现有检索方法在处理否定和排除查询时表现不佳且微调成本高的问题，本文提出了一种无需训练的直接嵌入优化（DEO）方法。DEO通过将查询分解为正向和负向组件，并利用对比目标优化查询嵌入，在文本和多模态检索任务中显著提升了性能，为现实场景提供了一种高效且实用的否定感知检索方案。
selection_source: fresh_fetch
motivation: 现有的检索方法难以准确处理否定和排除类查询，且现有的改进方案通常需要高昂的微调成本。
method: 提出DEO方法，将查询分解为正负组件，并在无需训练的情况下通过对比目标直接优化查询嵌入。
result: "在NegConstraint基准测试中显著超越基准模型，并在多模态检索中将OpenAI CLIP的Recall@5提升了6%。"
conclusion: DEO是一种高效、无需训练的否定感知检索方法，具有极强的实际应用价值。
---

## 摘要
大语言模型 (LLMs) 和检索增强生成 (RAG) 的最新进展催生了多种检索方法。然而，现有的检索方法在处理否定和排除查询时往往难以准确检索结果。为了解决这一局限性，先前的方法通常依赖于嵌入适配或微调，这会引入额外的计算成本和部署复杂性。我们提出了直接嵌入优化 (DEO)，这是一种用于否定感知文本和多模态检索的免训练方法。DEO 将查询分解为正向和负向组件，并使用对比目标来优化查询嵌入。在无需额外训练数据或模型更新的情况下，DEO 在 NegConstraint 基准测试上的表现优于基线模型，nDCG@10 提升了 0.0738，MAP@100 提升了 0.1028，同时在多模态检索中相比 OpenAI CLIP 将 Recall@5 提高了 6%。这些结果证明了 DEO 在现实世界否定和排除感知检索场景中的实用性。

## Abstract
Recent advances in Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) have enabled diverse retrieval methods. However, existing retrieval methods often fail to accurately retrieve results for negation and exclusion queries. To address this limitation, prior approaches rely on embedding adaptation or fine-tuning, which introduce additional computational cost and deployment complexity. We propose Direct Embedding Optimization (DEO), a training-free method for negation-aware text and multimodal retrieval. DEO decomposes queries into positive and negative components and optimizes the query embedding with a contrastive objective. Without additional training data or model updates, DEO outperforms baselines on NegConstraint, with gains of +0.0738 nDCG@10 and +0.1028 MAP@100, while improving Recall@5 by +6\% over OpenAI CLIP in multimodal retrieval. These results demonstrate the practicality of DEO for negation- and exclusion-aware retrieval in real-world settings.

---

## 论文详细总结（自动生成）

以下是对论文《DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在检索增强生成（RAG）和多模态检索场景中，用户经常使用包含“否定”或“排除”语义的查询（例如“显示2025年的预测，但排除2024年的结果”）。
*   **核心痛点**：现有的密集检索模型（Dense Retrieval）往往难以准确捕捉否定语义，容易检索到被要求排除的内容。
*   **现有方案缺陷**：以往的改进方法通常依赖于对嵌入模型进行微调或添加适配器，这需要大量的计算资源、标注数据，且可能导致模型在通用任务上性能退化。
*   **论文目标**：提出一种**无需训练（Training-Free）**、轻量级且通用的优化方法，在推理阶段直接调整查询嵌入，以提升否定感知检索的准确性。

### 2. 方法论：直接嵌入优化 (DEO)
DEO 的核心思想是在推理时通过梯度下降直接优化查询向量，使其在嵌入空间中靠近正向意图，远离负向意图。
*   **第一阶段：查询分解**
    *   利用大语言模型（LLM，如 GPT-4）将原始查询分解为：
        1.  **正向子查询 (Positive Sub-queries)**：强化用户真正想要的内容。
        2.  **负向子查询 (Negative Sub-queries)**：明确用户想要排除的内容。
*   **第二阶段：嵌入优化**
    *   将原始查询嵌入作为初始值，并将其设为可学习的参数（冻结编码器权重）。
    *   **损失函数**：由三部分组成：
        1.  **吸引项 (Attraction)**：减小优化后的嵌入与正向子查询嵌入之间的距离。
        2.  **排斥项 (Repulsion)**：增大优化后的嵌入与负向子查询嵌入之间的距离。
        3.  **一致性正则 (Consistency)**：防止优化后的嵌入偏离原始查询语义太远。
*   **第三阶段：检索**
    *   使用优化后的嵌入向量在向量数据库中进行相似度检索。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本检索**：使用 **NegConstraint**（否定约束基准）和 **NevIR**（否定神经检索基准）。
    *   **多模态检索**：使用 **COCO-Neg**（包含否定描述的文本-图像检索数据集）。
*   **对比基准 (Baselines)**：
    *   **文本模型**：BGE-small-en-v1.5, BGE-large-en-v1.5, BGE-M3。
    *   **多模态模型**：OpenAI CLIP, CLIP-laion400m, CLIP-datacomp, 以及专门微调过的 NegCLIP。
*   **评估指标**：nDCG@10, MAP@100, Pairwise Accuracy, Recall@5。

### 4. 资源与算力
*   **算力需求**：极低。该方法不需要训练模型，仅在推理时进行少量迭代优化。
*   **硬件测试**：在 NVIDIA RTX 3060 GPU 和 AMD Ryzen 7 5800X CPU 上均进行了测试。
*   **耗时详情**：
    *   **GPU**：20步优化总耗时约 **0.033秒**。
    *   **CPU**：20步优化总耗时约 **0.016秒**。
*   这种极低的开销使其非常适合部署在资源受限的生产环境中。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种主流文本嵌入模型和 4 种多模态模型上进行了验证，结果具有一致性。
*   **消融实验**：
    *   测试了不同 LLM（GPT-4 vs Qwen-1.5B）对分解质量的影响。
    *   分析了损失函数中各项权重参数（$\lambda$）的敏感性。
    *   对比了“仅分解”与“分解+优化”的效果，证明了优化步骤是性能提升的关键。
    *   探讨了优化步数（Steps）对结果的影响。
*   **客观性**：通过 PCA 轨迹可视化展示了嵌入向量的移动过程，直观地证明了方法论的有效性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **显著提升**：在 NegConstraint 基准上，DEO 使 BGE-large 的 MAP 提升了 **16.32%**，nDCG@10 提升了 **10.34%**。
*   **多模态通用性**：在 COCO-Neg 任务中，DEO 将 OpenAI CLIP 的 Recall@5 提升了 **6%**。
*   **叠加效应**：即使是已经针对否定语义微调过的 NegCLIP，使用 DEO 后性能仍有进一步提升。
*   **效率平衡**：20 到 50 步的优化即可达到最佳性能，计算成本几乎可以忽略不计。

### 7. 优点
*   **免训练与即插即用**：无需昂贵的微调过程，可直接应用于任何现有的密集检索模型。
*   **模型与模态无关**：同时适用于纯文本和跨模态（如文生图）检索。
*   **可控性强**：通过调整正负向权重，用户可以灵活控制“排除”语义的强度。
*   **极高性能功耗比**：在 CPU 上也能实现毫秒级的优化。

### 8. 不足与局限
*   **LLM 依赖性**：DEO 的效果高度依赖于 LLM 分解查询的准确性。如果 LLM 无法正确识别否定意图，优化可能会失效。
*   **超参数微调**：虽然方法稳健，但在不同模态（如文本 vs 图像）之间，一致性正则项的权重（$\lambda_o$）需要手动调整以获得最佳效果。
*   **复杂逻辑限制**：目前主要针对显式的否定和排除，对于更复杂的逻辑推理（如多重否定或隐含的逻辑矛盾）的有效性尚待进一步验证。

（完）
