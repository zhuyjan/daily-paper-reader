---
title: "VPWEM: Non-Markovian Visuomotor Policy with Working and Episodic Memory"
title_zh: VPWEM：具有工作记忆和情节记忆的非马尔可夫视觉运动策略
authors: "Yuheng Lei, Zhixuan Liang, Hongyuan Zhang, Ping Luo"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04910v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 具有工作记忆和情节记忆的视觉运动策略，用于长期任务
tldr: "针对模仿学习中视觉运动策略难以处理长期记忆任务的问题，本文提出VPWEM框架。该方法结合了滑动窗口形式的短时工作记忆和基于Transformer压缩器的长时片段记忆，能将历史观测递归压缩为固定长度的令牌。VPWEM在扩散策略基础上实现，以近乎恒定的计算开销捕获全局信息。实验表明，该方法在MIKASA记忆密集型任务上优于现有扩散策略和VLA模型20%以上，有效解决了非马尔可夫任务中的长程依赖挑战。"
selection_source: fresh_fetch
motivation: 现有策略因上下文窗口有限而难以应对非马尔可夫任务，且盲目增加窗口长度会带来巨大的计算负担和过拟合风险。
method: 引入工作记忆与片段记忆机制，利用Transformer压缩器递归处理历史观测，实现对长期经验的高效压缩与检索。
result: 在MIKASA和MoMaRT等多个机器人操作基准测试中，VPWEM的性能显著超过了当前的SOTA基线模型。
conclusion: VPWEM证明了通过结构化记忆压缩可以有效提升机器人在复杂长程任务中的表现，并兼顾了实时推理的效率。
---

## 摘要
从人类演示中进行模仿学习在机器人控制领域取得了显著成功，但大多数视觉运动策略仍依赖于单步观测或短上下文历史，导致其在处理需要长期记忆的非马尔可夫任务时表现不佳。简单地扩大上下文窗口会产生巨大的计算和内存开销，并诱发对伪相关性的过拟合，从而在分布偏移下导致灾难性失败，并违反机器人系统的实时性约束。相比之下，人类能够将重要的过往经验压缩进长期记忆，并利用这些记忆在整个生命周期中解决任务。在本文中，我们提出了 VPWEM，这是一种配备了工作记忆和情节记忆的非马尔可夫视觉运动策略。VPWEM 保留了最近观测 token 的滑动窗口作为短期工作记忆，并引入了一个基于 Transformer 的上下文记忆压缩器，该压缩器递归地将窗口外的观测转换为固定数量的情节记忆 token。该压缩器对过去摘要 token 的缓存使用自注意力，对历史观测的缓存使用交叉注意力，并与策略进行联合训练。我们在扩散策略上实例化了 VPWEM，以利用短期和全情节信息进行动作生成，且每步的内存和计算开销几乎恒定。实验表明，在 MIKASA 的记忆密集型操作任务中，VPWEM 的表现优于包括扩散策略和视觉-语言-动作（VLA）模型在内的最先进基准模型 20% 以上，并在移动操作基准测试 MoMaRT 上实现了平均 5% 的提升。代码见 https://github.com/HarryLui98/code_vpwem。

## Abstract
Imitation learning from human demonstrations has achieved significant success in robotic control, yet most visuomotor policies still condition on single-step observations or short-context histories, making them struggle with non-Markovian tasks that require long-term memory. Simply enlarging the context window incurs substantial computational and memory costs and encourages overfitting to spurious correlations, leading to catastrophic failures under distribution shift and violating real-time constraints in robotic systems. By contrast, humans can compress important past experiences into long-term memories and exploit them to solve tasks throughout their lifetime. In this paper, we propose VPWEM, a non-Markovian visuomotor policy equipped with working and episodic memories. VPWEM retains a sliding window of recent observation tokens as short-term working memory, and introduces a Transformer-based contextual memory compressor that recursively converts out-of-window observations into a fixed number of episodic memory tokens. The compressor uses self-attention over a cache of past summary tokens and cross-attention over a cache of historical observations, and is trained jointly with the policy. We instantiate VPWEM on diffusion policies to exploit both short-term and episode-wide information for action generation with nearly constant memory and computation per step. Experiments demonstrate that VPWEM outperforms state-of-the-art baselines including diffusion policies and vision-language-action (VLA) models by more than 20% on the memory-intensive manipulation tasks in MIKASA and achieves an average 5% improvement on the mobile manipulation benchmark MoMaRT. Code is available at https://github.com/HarryLui98/code_vpwem.

---

## 论文详细总结（自动生成）

以下是对论文《VPWEM: Non-Markovian Visuomotor Policy with Working and Episodic Memory》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉运动策略（Visuomotor Policies）大多依赖单步观测或极短的上下文历史（通常少于10帧），难以处理具有长程依赖的**非马尔可夫（Non-Markovian）任务**。
*   **研究动机**：
    *   **计算瓶颈**：简单扩大上下文窗口会导致计算量随长度呈平方级增长（$O(L^2)$），增加推理延迟。
    *   **因果困惑**：长历史容易引入伪相关性，导致模型出现“复读机”问题（Copycat Problem）或在分布偏移下失效。
    *   **生物启发**：借鉴人类大脑海马体将工作记忆转化为长期情节记忆的机制，通过压缩历史信息来平衡记忆容量与计算效率。

### 2. 方法论
VPWEM 框架的核心是将扩散策略（Diffusion Policy）与双重记忆系统相结合：
*   **工作记忆（Working Memory, WM）**：采用先进先出（FIFO）的滑动窗口，保留最近 $L$ 帧的原始观测 token，用于捕捉即时的短期动态。
*   **情节记忆（Episodic Memory, EM）**：
    *   **上下文记忆压缩器**：一个基于 Transformer 的递归模块。当观测 token 移出工作窗口时，压缩器将其与历史摘要 token 进行交叉注意力（Cross-Attention）计算。
    *   **递归压缩**：通过自注意力处理过去的摘要缓存，并利用交叉注意力检索历史观测缓存，将无限增长的历史递归压缩为固定数量（$M$个）的情节记忆 token。
*   **动作生成**：将工作记忆 token 和压缩后的情节记忆 token 拼接，作为扩散模型（如基于 Transformer 的噪声预测器）的条件输入，指导动作序列的去噪生成。
*   **训练策略**：压缩器与策略网络通过行为克隆（BC）损失函数进行端到端联合训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **MIKASA**：专门评估记忆能力的基准，包括“杯中寻球”和“颜色记忆”任务。
    *   **MoMaRT**：移动操作基准，涉及洗碗机清理、餐桌布置等长程非马尔可夫任务。
    *   **Robomimic**：包含标准操作任务（Square, Transport）及一个改进的长程非马尔可夫 Square 任务。
*   **对比基线**：
    *   **扩散策略变体**：DP（标准扩散策略）、DP-PTP（带过去 token 预测的 DP）、MaIL（基于 Mamba 的扩散策略）。
    *   **VLA 模型**：OpenVLA、$\pi_0$、Octo、MemoryVLA 等。
    *   **传统模型**：基于 LSTM 的 RNN 策略。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA RTX 4090 GPU** 上运行。
*   **训练时长/规模**：
    *   MIKASA 任务训练 60万步；MoMaRT 和 Robomimic 任务训练 100万步。
    *   模型参数量：额外增加的记忆模块非常轻量（约 2.24M 参数），总参数量约 53M。
*   **效率**：相比于直接增加上下文长度的方法，VPWEM 在推理速度和显存占用上表现出近乎恒定的开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从桌面操作到移动操作的 3 个大类基准，共计约 10 个具体任务。
*   **统计显著性**：所有实验均使用 3 个随机种子运行，并报告了均值和标准差。
*   **消融实验**：非常充分，涵盖了上下文长度（$L$）、情节记忆 token 数量（$M$）、压缩器层数、缓存最大容量以及不同的缓存管理策略（FIFO, K-Means, AdjSim 等）。
*   **客观性**：通过在 Markovian（Robomimic）和 Non-Markovian 任务上同时测试，证明了该方法在提升记忆能力的同时不会损害基础任务的表现。

### 6. 主要结论与发现
*   **性能飞跃**：在记忆密集型任务（MIKASA）中，VPWEM 比现有最强基线（包括大型 VLA 模型）的成功率高出 **20% 以上**。
*   **长程有效性**：在移动操作任务中平均提升了 **5%**，证明了其在复杂长程任务中的泛化能力。
*   **效率优势**：在达到更高成功率的同时，其推理延迟远低于简单增加窗口长度的 DP-PTP 模型（例如在 128 帧上下文时，DP-PTP 推理需 0.72s，而 VPWEM 仅需 0.22s）。
*   **压缩必要性**：消融实验显示，即使只使用 1-2 个情节记忆 token，也能显著提升非马尔可夫任务的表现。

### 7. 优点
*   **架构优雅**：通过递归压缩实现了 $O(1)$ 的单步计算复杂度，解决了长序列处理的瓶颈。
*   **即插即用**：该记忆机制可以轻松集成到不同的策略骨干（如 DP 或 MaIL）中。
*   **抗过拟合**：通过压缩和梯度截断（Detach）机制，有效缓解了模仿学习中常见的因果困惑和对历史动作的过度依赖。

### 8. 不足与局限
*   **实验环境限制**：目前主要在仿真环境（MIKASA, MoMaRT）中验证，缺乏真实世界机器人的部署数据。
*   **采样频率敏感性**：压缩器使用了固定的下采样比例（Subsample Ratio），对于动作极快或环境瞬变的场景，可能会丢失关键细节。
*   **辅助任务缺失**：目前仅靠 BC 损失驱动压缩器，未来若引入重建损失或预测损失，可能会进一步增强记忆的表征质量。

（完）
