---
title: "RAGTrack: Language-aware RGBT Tracking with Retrieval-Augmented Generation"
title_zh: RAGTrack：基于检索增强生成的语言感知 RGBT 追踪
authors: "Hao Li, Yuhao Wang, Wenning Hao, Pingping Zhang, Dong Wang, Huchuan Lu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03617v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 结合检索增强生成的RGBT追踪
tldr: 针对RGBT追踪中缺乏语言引导及模态差异导致的背景干扰问题，本文提出了RAGTrack框架。该研究首先利用多模态大模型为RGBT基准自动生成文本描述，随后通过多模态Transformer编码器进行统一建模，并设计自适应令牌融合减少冗余。最后，引入检索增强生成（RAG）机制实现动态语言推理，增强了目标建模的鲁棒性，在多个基准上达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有RGBT追踪器因缺乏语言引导且受模态差异和搜索冗余干扰，难以应对复杂的外观变化和背景干扰。
method: 提出一种基于检索增强生成的RAGTrack框架，结合多模态编码器、自适应令牌融合及上下文感知推理模块进行动态建模。
result: 在四个主流RGBT追踪基准测试中，该框架在各种挑战性场景下均实现了最先进的性能。
conclusion: 通过引入语言描述和RAG机制，RAGTrack显著提升了多模态目标追踪的鲁棒性和环境适应能力。
---

## 摘要
RGB-红外（RGBT）追踪旨在通过融合可见光和热红外模态，在各种环境条件下实现鲁棒的目标定位。然而，现有的 RGBT 追踪器仅依赖初始帧的视觉信息进行目标建模，由于缺乏语言引导，无法适应外观变化。此外，当前的方法受限于冗余的搜索区域和异构模态差异，导致背景干扰。为了解决这些问题，我们首先将文本描述引入 RGBT 追踪基准。这是通过一个利用多模态大语言模型（MLLMs）自动生成文本标注的流水线实现的。随后，我们提出了 RAGTrack，一种用于鲁棒 RGBT 追踪的新型检索增强生成框架。为此，我们引入了一个多模态 Transformer 编码器（MTE）用于统一的视觉-语言建模。然后，我们设计了一个自适应令牌融合（ATF）模块，用于选择与目标相关的令牌，并基于跨模态相关性进行通道交换，从而减轻搜索冗余和模态差异。最后，我们提出了一个上下文感知推理模块（CRM）来维护动态知识库，并采用检索增强生成（RAG）来实现时序语言推理，以进行鲁棒的目标建模。在四个 RGBT 基准上的广泛实验表明，我们的框架在各种挑战性场景下均达到了最先进的性能。源代码可在 https://github.com/IdolLab/RAGTrack 获取。

## Abstract
RGB-Thermal (RGBT) tracking aims to achieve robust object localization across diverse environmental conditions by fusing visible and thermal infrared modalities. However, existing RGBT trackers rely solely on initial-frame visual information for target modeling, failing to adapt to appearance variations due to the absence of language guidance. Furthermore, current methods suffer from redundant search regions and heterogeneous modality gaps, causing background distraction. To address these issues, we first introduce textual descriptions into RGBT tracking benchmarks. This is accomplished through a pipeline that leverages Multi-modal Large Language Models (MLLMs) to automatically produce texual annotations. Afterwards, we propose RAGTrack, a novel Retrieval-Augmented Generation framework for robust RGBT tracking. To this end, we introduce a Multi-modal Transformer Encoder (MTE) for unified visual-language modeling. Then, we design an Adaptive Token Fusion (ATF) to select target-relevant tokens and perform channel exchanges based on cross-modal correlations, mitigating search redundancies and modality gaps. Finally, we propose a Context-aware Reasoning Module (CRM) to maintain a dynamic knowledge base and employ a Retrieval-Augmented Generation (RAG) to enable temporal linguistic reasoning for robust target modeling. Extensive experiments on four RGBT benchmarks demonstrate that our framework achieves state-of-the-art performance across various challenging scenarios. The source code is available https://github.com/IdolLab/RAGTrack.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RAGTrack: Language-aware RGBT Tracking with Retrieval-Augmented Generation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 RGB-红外（RGBT）追踪器主要依赖初始帧的视觉模板，缺乏高层语义引导，导致在目标外观剧烈变化、背景干扰严重或目标歧义（如分不清扫帚和人腿）时容易发生漂移。此外，搜索区域存在大量冗余信息，且 RGB 与热红外模态之间存在显著的异构鸿沟。
*   **研究动机**：引入自然语言作为高层抽象表示，利用多模态大语言模型（MLLM）的推理能力来增强目标的语义判别力，并通过检索增强生成（RAG）技术解决长时序下的目标身份保持问题。

### 2. 方法论：核心思想与关键技术
RAGTrack 提出了一个结合视觉、红外和语言的三模态追踪框架，核心组件包括：
*   **自动文本标注流水线**：利用 MLLM（如 Qwen-VL）为现有的 RGBT 数据集自动生成详细的文本描述（如“一个穿着粉色外套、黑色裤子的人”），解决了 RGBT 领域缺乏语言标注的问题。
*   **多模态 Transformer 编码器 (MTE)**：将文本 Token、视觉 Patch Token 和推理 Token 拼接，进行统一的视觉-语言建模，使语义信息直接增强视觉特征的判别性。
*   **自适应令牌融合 (ATF)**：
    *   **动态 Token 选择**：基于注意力分数（Attention Score）识别并保留与目标最相关的 Token，剔除背景噪声和冗余区域。
    *   **自适应通道交换**：根据跨模态相关性，在 RGB 和红外分支间交换特征通道，以弥合模态间的异构差距。
*   **上下文感知推理模块 (CRM) 与 RAG 机制**：
    *   **构建与检索**：维护一个动态知识库，存储历史帧的文本特征。通过余弦相似度检索最相关的历史上下文。
    *   **增强与生成**：利用检索到的信息增强当前的推理 Token，并调用 MLLM 动态生成当前帧的描述，实现时序上的语义一致性推理。

### 3. 实验设计
*   **数据集**：在四个主流 RGBT 追踪基准上进行了评估：**GTOT** (50序列)、**RGBT210** (210序列)、**RGBT234** (234序列) 和 **LasHeR** (1224序列)。
*   **对比方法**：对比了超过 20 种先进追踪器，包括基于 Prompt 的方法（ViPT, SDSTrack）、基于 Mamba 的方法（AINet, MambaVT）以及最新的 SOTA 模型（XTrack, STTrack, AETrack 等）。
*   **评价指标**：采用精确率（PR/MPR）、成功率（SR/MSR）和归一化精确率（NPR）。

### 4. 资源与算力
*   **硬件环境**：使用 **4 张 NVIDIA V100 GPU** 进行训练。
*   **训练配置**：Batch size 为 16，采用 AdamW 优化器，学习率为 $10^{-4}$。
*   **推理速度**：在 V100 上达到 **24.3 FPS**，计算量为 62.7G FLOPs。
*   **模型参数**：总参数量约为 101.8M。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验和极其详尽的消融实验。
*   **消融实验内容**：涵盖了组件有效性（MTE/ATF/CRM）、融合位置（不同层级的影响）、融合范式对比（与 TBSI, BSI 等对比）、Token 长度配置、超参数敏感性（Token 保留率 $\gamma$、通道交换率 $\sigma$、知识库大小 $n$、检索数量 $k$）等。
*   **客观性**：在 GTOT 和 RGBT234 上使用了 MPR/MSR 以应对模态对齐不准的问题，确保了评估的公平性。实验覆盖了 19 种细粒度属性（如全遮挡、视野外等），验证了模型在极端情况下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：RAGTrack 在所有四个基准上均刷新了纪录。例如在 LasHeR 上，PR 达到 76.8%，SR 达到 61.1%，显著优于之前的 SOTA 方法。
*   **语言的价值**：实验证明，引入文本描述能显著提升模型在目标被遮挡或存在相似物干扰时的表现。
*   **RAG 的有效性**：动态更新的知识库和 MLLM 生成的实时描述比静态的初始帧描述更能适应视频中的目标外观演变。

### 7. 优点（亮点）
*   **首创性**：首次将文本描述和 RAG 机制引入 RGBT 追踪领域，填补了该方向的空白。
*   **高效融合**：ATF 模块通过参数量极小的通道交换和 Token 选择，在提升性能的同时控制了计算开销。
*   **动态推理**：CRM 模块通过时序推理 Token 的传播，实现了比传统模板匹配更高级的语义追踪。
*   **数据贡献**：为 RGBT 社区提供了大规模的高质量文本标注数据。

### 8. 不足与局限
*   **MLLM 依赖性**：虽然推理时可以达到 24.3 FPS，但频繁调用 MLLM（如 Qwen-VL）生成描述可能会带来额外的延迟或对算力有特定要求（文中提到每 5 帧更新一次）。
*   **文本缺失风险**：虽然消融实验显示在缺失文本时仍有不错表现，但其核心优势高度依赖于初始文本的准确性。
*   **应用限制**：对于极小目标或缺乏显著语义特征的目标（如远处的模糊光点），语言描述的增益可能会减弱。

（完）
