---
title: Enhancing Web Agents with a Hierarchical Memory Tree
title_zh: 利用分层记忆树增强 Web 智能体
authors: "Yunteng Tan, Zhi Gao, Xinxiao Wu"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07024v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 用于Web智能体的分层记忆树
tldr: 本研究针对大语言模型 Web 智能体在跨网站泛化时面临的“工作流不匹配”问题，提出了分层记忆树（HMT）框架。HMT 通过自动化抽象流程将原始轨迹构建为意图、阶段和动作三层结构，实现了逻辑规划与具体执行的解耦。配合阶段感知的推理机制，HMT 在 Mind2Web 和 WebArena 基准测试中显著提升了智能体在跨网站和跨领域场景下的表现，增强了其鲁棒性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的扁平化记忆结构将高层逻辑与特定网站的动作细节混杂，导致智能体在面对新环境时容易产生逻辑执行不一致。
method: 提出了分层记忆树（HMT），将记忆划分为意图、阶段和动作三层，并结合规划器和执行器实现阶段感知的推理。
result: 在 Mind2Web 和 WebArena 上的实验表明，HMT 在跨网站和跨领域任务中的表现显著优于传统的扁平记忆方法。
conclusion: 结构化的记忆框架是提升 Web 智能体在复杂、多变环境下的泛化能力和执行鲁棒性的关键。
---

## 摘要
基于大语言模型的 Web 智能体通过先进的推理和指令遵循能力，在自动化 Web 交互方面展现出巨大潜力。虽然源自历史轨迹的检索式记忆使这些智能体能够处理复杂的长程任务，但现有方法在泛化到未见过的网站时仍面临挑战。我们发现，这一挑战源于扁平化的记忆结构，这种结构将高层任务逻辑与特定网站的操作细节纠缠在一起。这种纠缠在面对新环境时会导致工作流失配，即检索到的内容与当前网页发生混淆，从而导致逻辑不一致的执行。为解决这一问题，我们提出了分层记忆树（Hierarchical Memory Tree, HMT），这是一个旨在显式解耦逻辑规划与动作执行的结构化框架。HMT 通过自动化抽象流水线从原始轨迹中构建了一个三层架构：意图层（Intent level）将多样的用户指令映射为标准化的任务目标；阶段层（Stage level）定义了以可观测的前置条件和后置条件为特征的可重用语义子目标；动作层（Action level）则存储了与可迁移语义元素描述相匹配的动作模式。利用这一结构，我们开发了一种包含规划器（Planner）和执行器（Actor）的阶段感知推理机制。通过显式验证前置条件，规划器将当前状态与正确的逻辑子目标对齐以防止工作流失配，而执行器则通过将存储的语义描述与目标页面进行匹配来落实动作。在 Mind2Web 和 WebArena 上的实验结果表明，HMT 显著优于扁平记忆方法，特别是在跨网站和跨领域场景下，凸显了结构化记忆对于 Web 智能体鲁棒泛化能力的必要性。

## Abstract
Large language model-based web agents have shown strong potential in automating web interactions through advanced reasoning and instruction following. While retrieval-based memory derived from historical trajectories enables these agents to handle complex, long-horizon tasks, current methods struggle to generalize across unseen websites. We identify that this challenge arises from the flat memory structures that entangle high-level task logic with site-specific action details. This entanglement induces a workflow mismatch in new environments, where retrieved contents are conflated with current web, leading to logically inconsistent execution. To address this, we propose Hierarchical Memory Tree (HMT), a structured framework designed to explicitly decouple logical planning from action execution. HMT constructs a three-level hierarchy from raw trajectories via an automated abstraction pipeline: the Intent level maps diverse user instructions to standardized task goals; the Stage level defines reusable semantic subgoals characterized by observable pre-conditions and post-conditions; and the Action level stores action patterns paired with transferable semantic element descriptions. Leveraging this structure, we develop a stage-aware inference mechanism comprising a Planner and an Actor. By explicitly validating pre-conditions, the Planner aligns the current state with the correct logical subgoal to prevent workflow mismatch, while the Actor grounds actions by matching the stored semantic descriptions to the target page. Experimental results on Mind2Web and WebArena show that HMT significantly outperforms flat-memory methods, particularly in cross-website and cross-domain scenarios, highlighting the necessity of structured memory for robust generalization of web agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**分层记忆树（Hierarchical Memory Tree, HMT）**的框架，旨在提升基于大语言模型（LLM）的 Web 智能体在面对未见过的网站时的泛化能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现代 Web 智能体利用 LLM 处理复杂任务，通常依赖检索历史成功轨迹（记忆）来指导当前操作。
*   **核心问题**：现有的记忆机制多为“扁平化”结构，将高层的任务逻辑（如“预订机票”）与特定网站的底层动作细节（如点击特定的 HTML ID `#btn-123`）纠缠在一起。
*   **痛点**：这种“意图-执行纠缠”导致智能体在跨网站迁移时出现“工作流失配”——检索到的动作在逻辑上可能正确，但在新网站的 DOM 结构下无法执行，或者跳过了必要的导航步骤。

### 2. 方法论
HMT 的核心思想是**显式解耦逻辑规划与动作执行**，通过一个三层结构的记忆树来实现：

*   **分层记忆结构**：
    1.  **意图层 (Intent Level)**：将多样的用户指令标准化为统一的任务目标和约束（JSON 格式），消除表述差异。
    2.  **阶段层 (Stage Level)**：将任务分解为可重用的语义子目标（如“过滤结果”），并定义显式的**前置条件**和**后置条件**（基于可观测的 UI 状态）。
    3.  **动作层 (Action Level)**：存储抽象的动作模式和**可迁移的语义元素描述**（如“角色：按钮，文本包含：搜索”），而非原始的 DOM ID。
*   **自动化构建流水线**：包括指令归一化、子目标分割（带有一致性检查）和步骤抽象。
*   **阶段感知推理机制**：
    *   **规划器 (Planner)**：根据当前页面状态验证前置/后置条件，选择正确的逻辑阶段，防止工作流失配。
    *   **执行器 (Actor)**：在选定阶段内，将抽象的语义描述与当前页面的候选元素进行匹配，完成动作落地。
    *   **置信度感知回退**：当检索或阶段选择不确定时，扩大搜索范围或回退到基础策略。

### 3. 实验设计
*   **数据集与场景**：
    *   **Mind2Web**：用于离线设置，测试跨任务、跨网站和跨领域的泛化能力。
    *   **WebArena**：用于在线设置，测试智能体在部署过程中积累程序性知识的能力，涵盖购物、CMS、GitLab、地图等领域。
*   **Benchmark 与对比方法**：
    *   对比了通用智能体（MindAct, WebArena baseline）、分层/检索增强智能体（AWM, SteP, AutoEval）以及消融变体（扁平检索基准）。
*   **评估指标**：StepSR（步骤成功率）、TaskSR（任务成功率）、EA（元素准确率）等。

### 4. 资源与算力
*   **模型后端**：实验主要基于 **GPT-4** 作为骨干模型。
*   **算力说明**：论文**未明确说明**具体的本地 GPU 型号或训练时长。由于主要使用闭源模型 API，其算力消耗主要体现在 API 调用成本和 Token 消耗上。
*   **效率提升**：论文提到 HMT 相比扁平检索方法，平均上下文 Token 减少了约 **72.7%**，推理成本降低了 **71.0%**。

### 5. 实验数量与充分性
*   **实验规模**：在 Mind2Web 的三个子集和 WebArena 的多个领域进行了广泛测试。
*   **消融实验**：设计了 5 组消融实验，分别验证了分层结构、前/后置条件、语义描述、规划器模块以及回退机制的有效性。
*   **机制分析**：通过检索质量分析（Recall@K）和落地鲁棒性定量分析，深入探讨了 HMT 成功的原因。
*   **评价**：实验设计较为充分，涵盖了从离线数据集到在线交互环境的多种维度，对比基准选择合理，实验过程具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **泛化性显著提升**：HMT 在跨网站场景下的表现远超扁平记忆方法，Mind2Web 跨网站 StepSR 提升了 6.0%。
*   **解耦的必要性**：实验证明，使用语义描述而非原始 ID 是实现跨网站落地的关键（原始 ID 在新网站成功率仅 12.4%，而语义描述可达 76.8%）。
*   **效率与准确并存**：通过结构化过滤，HMT 不仅提高了准确率，还显著降低了 LLM 的输入长度和推理延迟。

### 7. 优点
*   **结构化创新**：提出了一种符合人类操作逻辑的“意图-阶段-动作”分层模型，有效解决了长程任务中的逻辑一致性问题。
*   **鲁棒的落地能力**：通过语义元素描述解决了 Web 元素频繁变动导致的记忆失效问题。
*   **即插即用与可扩展性**：该框架可应用于不同模态（如截图或 DOM）和不同动作格式的环境。

### 8. 不足与局限
*   **空间任务限制**：在依赖精确空间坐标的任务（如 WebArena 的地图领域）中，纯文本的语义抽象效果略逊于原始轨迹回放。
*   **歧义性挑战**：当页面存在多个语义相似的元素（如多个“更多”按钮）且缺乏层级约束时，可能导致落地错误。
*   **状态验证局限**：在单页应用（SPA）中，URL 不变但视觉更新的情况可能导致规划器的验证逻辑失效，引发死循环。

（完）
