---
title: "Seeing as Experts Do: A Knowledge-Augmented Agent for Open-Set Fine-Grained Visual Understanding"
title_zh: 像专家一样观察：一种用于开集细粒度视觉理解的知识增强智能体
authors: "Junhan Chen, Zilu Zhou, Yujun Tong, Dongliang Chang, Yitao Luo, Zhanyu Ma"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03762v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 用于视觉理解的知识增强推理智能体
tldr: 针对细粒度视觉理解在开放集和复杂场景下的局限性，本文提出了知识增强细粒度推理智能体（KFRA）。该框架模拟专家分析过程，通过开放词汇检测与互联网检索生成假设，并利用全局到局部的聚焦机制将文本知识转化为空间定位证据，最后由大模型进行可解释推理。在新建的FGExpertBench基准上，KFRA显著提升了推理准确性并增强了证据可追溯性。
selection_source: fresh_fetch
motivation: 现有的细粒度视觉理解方法受限于闭集分类和单标签预测，难以应对开放集或复杂上下文环境下的推理需求。
method: 提出KFRA框架，通过检索-定位耦合机制将外部知识转化为空间定位证据，并利用三阶段闭环模拟专家的证据驱动推理过程。
result: "在涵盖六个知识维度的FGExpertBench基准测试中，KFRA的推理准确率比现有大模型和智能体框架提升了高达19%。"
conclusion: KFRA成功实现了知识检索与视觉定位的深度融合，为开放集细粒度视觉理解提供了一种具备事实依据且高度可解释的通用推理方案。
---

## 摘要
细粒度视觉理解正从静态分类转向知识增强推理，模型不仅需要识别，还需要给出理由。现有方法仍受限于闭集分类体系和单标签预测，在开集或上下文相关条件下性能显著下降。我们提出了知识增强细粒度推理智能体（KFRA），这是一个将细粒度感知转化为证据驱动推理的统一框架。KFRA 通过模拟专家分析的三阶段闭环推理运行。它首先执行开放词汇检测和互联网规模的检索，以生成类别假设。随后，它通过全局到局部的聚焦机制，将文本知识与视觉证据对齐，从而进行判别性区域定位。最后，它在大型多模态模型中整合所有多模态证据，以执行可解释推理。与将检索和推理视为独立过程的现有智能体不同，KFRA 建立了检索-定位耦合机制，将检索到的知识转化为空间定位的证据以供验证。这种设计在多种细粒度场景中实现了基于事实、可解释且与任务无关的推理。为了评估这一能力，我们构建了 FGExpertBench，这是一个旨在评估六个知识维度上的推理深度和跨任务泛化能力的基准测试。大量实验表明，KFRA 一贯优于独立的大型多模态模型和当前的智能体框架，在推理准确率上提升高达 19%，并在开集细粒度视觉理解中提供了基于证据的可解释性。

## Abstract
Fine-grained visual understanding is shifting from static classification to knowledge-augmented reasoning, where models must justify as well as recognise. Existing approaches remain limited by closed-set taxonomies and single-label prediction, leading to significant degradation under open-set or context-dependent conditions. We present the Knowledge-Augmented Fine-Grained Reasoning Agent (KFRA), a unified framework that transforms fine-grained perception into evidence-driven reasoning. KFRA operates through a three-stage closed reasoning loop that emulates expert analysis. It first performs open-vocabulary detection and web-scale retrieval to generate category hypotheses. It then conducts discriminative regions localisation by aligning textual knowledge with visual evidence through a global-to-local focusing mechanism. Finally, it integrates all multimodal evidence within a large multimodal model to perform interpretable reasoning. Unlike existing agents that treat retrieval and reasoning as independent processes, KFRA establishes a retrieval-grounding coupling that converts retrieved knowledge into spatially grounded evidence for verification. This design enables factual, interpretable, and task-agnostic reasoning across diverse fine-grained scenarios. To evaluate this capability, we construct FGExpertBench, a benchmark designed to assess reasoning depth and cross-task generalisation across six knowledge dimensions. Extensive experiments demonstrate that KFRA consistently surpasses both standalone large multimodal models and current agent frameworks, achieving up to 19 percent improvement in reasoning accuracy and delivering evidence-grounded interpretability in open-set fine-grained visual understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Seeing as Experts Do: A Knowledge-Augmented Agent for Open-Set Fine-Grained Visual Understanding》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的细粒度视觉理解（FGVU）主要被视为闭集分类任务，即在固定标签体系内预测单一标签。这种方式缺乏推理能力，无法解释“为什么是这个类别”，且在面对未见过的物种（开集）或复杂上下文时，准确率会大幅下降（通常下降30-40%）。
*   **核心问题**：如何让模型像人类专家一样，通过观察、假设、检索知识、定位特征并最终进行逻辑验证，从而实现具备事实依据且可解释的细粒度推理？

### 2. 方法论：知识增强细粒度推理智能体 (KFRA)
KFRA 模拟专家的分析流程，构建了一个**三阶段闭环推理链**：
*   **阶段 1：候选列表生成 (Candidate List Generation)**
    *   利用开放词汇检测器（如 Grounding-DINO）识别图像中的实体。
    *   通过互联网规模的图像检索（Google Lens）获取相似图像及其文本描述。
    *   由大语言模型（LMM）整合检索信息，生成一组带置信度的类别假设列表。
*   **阶段 2：判别性区域定位 (Discriminative Regions Localisation)**
    *   **知识检索与解析**：针对每个假设类别，从维基百科等源检索文本知识，并解析为结构化的判别性属性（如“红色喙”、“条纹翅膀”）。
    *   **全局到局部聚焦**：利用语义对齐机制将文本描述与图像区域匹配。
    *   **细节增强**：若关键特征模糊，调用超分辨率工具（OseDiff）重建局部区域，以恢复高频细节供进一步验证。
*   **阶段 3：知识与区域引导推理 (Knowledge and Region Guided Inference)**
    *   将类别假设、文本知识、判别性属性及对应的视觉掩码（Mask）整合为证据元组。
    *   LMM 作为控制器，基于这些多模态证据进行最终推理。若置信度低，则触发自纠正循环重新检索或定位。

### 3. 实验设计
*   **自建基准 (FGExpertBench)**：为了评估推理深度而非单纯的分类，作者构建了包含 300 张图像和 1500 个 QA 对的基准。涵盖六个维度：对象识别、属性提取、动作理解、计数、推理分析、知识推理。
*   **传统数据集**：在 CUB-200-2011、Stanford Cars、Stanford Dogs、Oxford Flowers、FGVC-Aircraft 和 Oxford-IIIT Pets 六个标准细粒度分类数据集上验证泛化性。
*   **对比方法**：
    *   **开源 LMM**：如 Qwen2.5/3-VL、InternVL3.5、LLaVA-Next 等。
    *   **商业模型**：GPT-4o、Gemini-2.5-Flash、Doubao-1.5-Vision-Pro。
    *   **智能体框架**：VSA、MMSearch。

### 4. 资源与算力
*   **硬件环境**：实验在一台单卡 **NVIDIA H20 GPU** 上完成。
*   **实现细节**：控制器采用 Qwen3-A3B，检测器为 Grounding-DINO，超分模型为 OseDiff。文中未明确提及具体的训练时长，但强调了该框架在推理阶段的工具调用逻辑。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 FGExpertBench 上进行了 18 种主流模型/框架的横向对比。
    *   在 6 个传统 FGIC 数据集上进行了零样本（Zero-shot）性能测试。
    *   进行了详细的**消融实验**，验证了知识参考（KR）、视觉搜索（VS）、开放检测（OD）、全局聚焦（GF）和超分（SR）各组件的贡献。
*   **充分性与公平性**：实验涵盖了从 4B 到 72B 不同参数规模的模型，并对比了最新的商业闭源模型，评估维度全面。通过统一的 QA 格式转化，确保了不同架构模型在传统数据集上的对比公平性。

### 6. 主要结论与发现
*   **性能领先**：KFRA 在 FGExpertBench 上显著优于所有基准，配合 GLM-4.5V 时达到 74.81% 的最高准确率。
*   **显著提升**：相比于基础模型（如 Qwen2.5-VL-7B），KFRA 带来的推理准确率提升高达 **19.14%**。
*   **开集能力**：在传统 FGIC 数据集上，KFRA 在无特定任务微调的情况下，表现优于专门训练的 SOTA 模型（如 DeepPerception-7B）。
*   **可解释性**：通过“检索-定位耦合”，模型能够明确指出是基于图像中哪个具体部位的什么特征得出的结论。

### 7. 优点（亮点）
*   **范式转变**：将细粒度理解从“标签预测”提升为“证据驱动推理”，更符合专家认知逻辑。
*   **检索-定位耦合**：不同于以往只做文本检索的 RAG，KFRA 能将检索到的知识实时映射到图像的空间坐标上。
*   **高质量基准**：FGExpertBench 填补了细粒度领域缺乏多维度推理评估数据集的空白。
*   **即插即用**：作为一个智能体框架，它可以集成各种强大的 LMM 后端，具备极强的灵活性。

### 8. 不足与局限
*   **外部依赖**：高度依赖外部 API（如 Google Lens、Wikipedia），存在网络延迟风险及检索信息错误导致的连锁反应。
*   **计算开销**：多阶段的闭环推理（检测、多次检索、超分、多次 LMM 调用）相比单次推理显著增加了计算成本和响应时间。
*   **领域覆盖**：虽然涵盖了鸟类、飞机、汽车等，但对于更广泛的工业检测或医疗细粒度场景的覆盖仍有待扩展。

（完）
