---
title: "Beyond Relevance: On the Relationship Between Retrieval and RAG Information Coverage"
title_zh: 超越相关性：检索与 RAG 信息覆盖率之间的关系研究
authors: "Saron Samuel, Alexander Martin, Eugene Yang, Andrew Yates, Dawn Lawrie, Ian Soborof, Laura Dietz, Benjamin Van Durme"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08819v1"
tags: ["query:mmrag"]
score: 10.0
evidence: 分析了WikiVideo等多模态RAG基准中检索与生成之间的关系。
tldr: 本研究系统探讨了检索增强生成（RAG）系统中检索质量与生成响应信息覆盖率之间的关系。通过在TREC NeuCLIR 2024、TREC RAG 2024和WikiVideo等文本及多模态基准上进行大规模实验，分析了多种检索堆栈和评估框架。研究发现，基于覆盖率的检索指标与生成内容的信息点覆盖率在主题和系统层面均表现出强相关性，证明了检索指标可作为RAG性能的可靠早期预测指标。
selection_source: fresh_fetch
motivation: 旨在验证上游检索指标是否能作为RAG系统最终生成响应信息覆盖率的可靠早期预测指标。
method: 在多个文本和多模态基准上，通过分析25个检索堆栈和多种评估框架，系统研究了检索指标与生成覆盖率的相关性。
result: 实验证明基于覆盖率的检索指标与生成响应的信息点覆盖率之间存在显著的正相关关系。
conclusion: 研究结果为使用检索指标作为RAG性能的代理提供了实证支持，并揭示了迭代流程对这种相关性的影响。
---

## 摘要
检索增强生成 (RAG) 系统将文档检索与生成模型相结合，以解决报告生成等复杂的信息寻求任务。虽然检索质量与生成效果之间的关系看似直观，但尚未得到系统性的研究。我们研究了上游检索指标是否可以作为最终生成响应的信息覆盖率的可靠早期指标。通过在两个文本 RAG 基准测试（TREC NeuCLIR 2024 和 TREC RAG 2024）以及一个多模态基准测试 (WikiVideo) 上进行实验，我们分析了跨四个 RAG 流水线和多个评估框架（Auto-ARGUE 和 MiRAGE）的 15 个文本检索栈和 10 个多模态检索栈。我们的研究结果表明，在主题和系统层面，基于覆盖率的检索指标与生成响应中的关键信息点 (nugget) 覆盖率之间存在强相关性。当检索目标与生成目标一致时，这种关系最为显著，尽管更复杂的迭代 RAG 流水线可能会使生成质量与检索效能部分解耦。这些发现为使用检索指标作为 RAG 性能的代理指标提供了实证支持。

## Abstract
Retrieval-augmented generation (RAG) systems combine document retrieval with a generative model to address complex information seeking tasks like report generation. While the relationship between retrieval quality and generation effectiveness seems intuitive, it has not been systematically studied. We investigate whether upstream retrieval metrics can serve as reliable early indicators of the final generated response's information coverage. Through experiments across two text RAG benchmarks (TREC NeuCLIR 2024 and TREC RAG 2024) and one multimodal benchmark (WikiVideo), we analyze 15 text retrieval stacks and 10 multimodal retrieval stacks across four RAG pipelines and multiple evaluation frameworks (Auto-ARGUE and MiRAGE). Our findings demonstrate strong correlations between coverage-based retrieval metrics and nugget coverage in generated responses at both topic and system levels. This relationship holds most strongly when retrieval objectives align with generation goals, though more complex iterative RAG pipelines can partially decouple generation quality from retrieval effectiveness. These findings provide empirical support for using retrieval metrics as proxies for RAG performance.

---

## 论文详细总结（自动生成）

这篇论文《Beyond Relevance: On the Relationship Between Retrieval and RAG Information Coverage》对检索增强生成（RAG）系统中检索质量与生成效果之间的量化关系进行了深入探讨。以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然直觉上认为更好的检索结果会带来更好的 RAG 生成质量，但这一关系缺乏系统性的实证研究。此外，端到端评估 RAG 系统（尤其是长文本报告生成）成本极高且难以复用。
*   **核心问题**：上游检索指标（如相关性、多样性指标）是否可以作为下游生成响应“信息覆盖率”（Information Coverage）的可靠早期预测指标（Proxy）？
*   **研究意义**：如果相关性成立，开发者可以在不运行昂贵 LLM 生成的情况下，通过优化检索指标来预估 RAG 性能，从而降低开发成本。

### 2. 论文提出的方法论
*   **核心思想**：通过计算**检索指标**与**生成指标**之间的 **Pearson 相关系数**，在“主题级别”（Topic-level）和“系统级别”（System-level）验证两者的关联性。
*   **关键技术细节**：
    *   **信息点（Nuggets）评估**：将复杂查询拆解为原子级的信息单位（Nuggets）。
    *   **检索指标**：采用 $\alpha$-nDCG（考虑多样性和冗余）、nDCG（基于 Nugget 标注）和 Subtopic Recall（子主题召回率）。
    *   **生成指标**：使用 **Auto-ARGUE**（针对文本）和 **MiRAGE**（针对多模态）框架，测量生成文本中包含并正确引用 Nugget 的比例（Nugget Coverage）。
    *   **相关性分析**：对比不同复杂度的 RAG 流水线（线性 vs. 迭代），观察检索质量提升对最终生成质量的边际贡献。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **TREC NeuCLIR 2024**：多语言报告生成任务（中文、波斯语、俄语）。
    *   **TREC RAG 2024**：基于 MS MARCO 的问答任务。
    *   **WikiVideo**：多模态任务，根据多个视频编写百科条目。
*   **对比方法（检索堆栈）**：
    *   **文本检索**：包含 15 种组合，涵盖 BM25、PLAID-X（后期交互）、LSR（学习型稀疏检索）、Qwen3-8B Embed（稠密检索）以及 RRF 融合，并配合 Qwen3-8B 和 Rank1-7B 重排序器。
    *   **多模态检索**：包含 10 种组合，如 CLIP、LanguageBind、Video-ColBERT、OmniEmbed 等。
*   **RAG 流水线**：
    *   **线性流水线**：GPT-Researcher（单查询/多查询）、Bullet List（提取式）。
    *   **迭代流水线**：LangGraph（具备自我反思和多轮检索能力）。

### 4. 资源与算力
*   **模型使用**：文本 RAG 使用 **Llama-70B-Instruct** 作为骨干模型；多模态 RAG 使用 **Qwen2.5-VL-72B**。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。但提到迭代流水线（如 LangGraph）和多模态实验的计算成本非常高。

### 5. 实验数量与充分性
*   **实验规模**：分析了 15 个文本检索堆栈和 10 个多模态检索堆栈，跨越 4 种 RAG 架构。
*   **充分性**：实验覆盖了单语/跨语言、文本/视频多模态、简单问答/复杂报告生成等多种场景。
*   **客观性**：通过在不同任务（NeuCLIR vs. RAG24）和不同评估器（Auto-ARGUE vs. MiRAGE）之间进行交叉验证，证明了结论的稳健性。

### 6. 主要结论与发现
*   **强相关性确认**：基于 Nugget 的检索指标（尤其是 **$\alpha$-nDCG**）与生成的 Nugget 覆盖率在主题和系统层面均表现出强相关性。
*   **指标优劣**：$\alpha$-nDCG 是最可靠的代理指标，因为它惩罚了冗余信息，这与 LLM 上下文窗口有限且易受冗余干扰的特性相符。
*   **流水线复杂度的影响**：
    *   **线性流水线**：对检索质量高度敏感，检索提升直接转化为生成提升。
    *   **迭代流水线**：复杂的迭代 RAG（如 LangGraph）可以部分“解耦”这种关系，即通过多轮检索弥补初次检索的不足，但这也增加了 LLM 适配检索模型的难度。
*   **多模态一致性**：在视频 RAG 中，检索质量与生成的“事实准确性”（Precision）强相关。

### 7. 优点
*   **跨模态验证**：不仅研究了文本，还扩展到了视频多模态 RAG，具有前瞻性。
*   **实证支持**：为“以检索为中心”的 RAG 优化路径提供了坚实的理论和数据支持。
*   **实用性强**：确立了 $\alpha$-nDCG 作为 RAG 开发过程中低成本评估指标的地位。

### 8. 不足与局限
*   **维度局限**：研究重点仅在于“信息覆盖率”（Coverage），未深入探讨生成文本的**流畅度、逻辑性或幻觉问题**（虽然多模态部分提到了事实性）。
*   **模型偏差**：实验主要基于 Llama-70B 等高性能模型，较小规模的 LLM 是否遵循相同的相关性规律尚不明确。
*   **静态评估**：主要关注一次性生成的质量，对于长期的对话式 RAG 交互研究较少。

（完）
