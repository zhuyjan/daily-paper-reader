---
title: "PRAM-R: A Perception-Reasoning-Action-Memory Framework with LLM-Guided Modality Routing for Adaptive Autonomous Driving"
title_zh: PRAM-R：一种用于自适应自动驾驶的、具有 LLM 引导模态路由的感知-推理-动作-记忆框架
authors: "Yi Zhang, Xian Zhang, Saisi Zhao, Yinglei Song, Chengdong Wu, Nenad Petrovic, Alois Knoll"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04222v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 自动驾驶智能体的分层记忆模块
tldr: 本研究针对自动驾驶多模态感知计算开销大的问题，提出PRAM-R框架。该框架采用异步双环设计，利用LLM根据环境动态引导模态路由，并结合分层记忆模块确保一致性。实验表明，该方法在nuScenes数据集上显著降低了传感器使用频率和路由振荡，同时保持了与全模态基准相当的精度，实现了高效自适应感知。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶中多模态感知因全传感器常开导致的高计算成本问题，寻求鲁棒性与效率的平衡。
method: 提出一种异步双环PRAM-R框架，利用LLM进行模态路由选择，并结合分层记忆模块支持长期自适应。
result: "在nuScenes数据集上，该方法在保持轨迹精度的同时减少了6.22%的模态使用，并使路由振荡降低了87.2%。"
conclusion: 研究证明了LLM增强架构与分层记忆的结合能有效实现自动驾驶中高效且自适应的多模态感知。
---

## 摘要
多模态感知实现了鲁棒的自动驾驶，但当所有传感器都保持激活状态时，会产生不必要的计算开销。本文提出了 PRAM-R，这是一个统一的感知-推理-动作-记忆（Perception-Reasoning-Action-Memory）框架，具有用于自适应自动驾驶的 LLM 引导模态路由。PRAM-R 采用异步双环设计：一个用于感知和控制的快速反应环，以及一个用于推理驱动的模态选择和记忆更新的慢速审议环。LLM 路由器利用环境上下文和传感器诊断来选择模态并分配权重，而分层记忆模块则保持时间一致性并支持长期自适应。我们进行了两个阶段的评估：(1) 用于稳定性分析的合成压力测试，以及 (2) 在 nuScenes 数据集上的真实世界验证。合成压力测试证实，通过基于滞后的稳定化方法，路由振荡减少了 87.2%。在 nuScenes 上的真实世界验证显示，在复杂城市场景中，在保持与全模态基准相当的轨迹精度的同时，模态减少了 6.22%，记忆召回率为 20%。我们的工作表明，具有分层记忆的 LLM 增强架构在自动驾驶中实现了高效、自适应的多模态感知。

## Abstract
Multimodal perception enables robust autonomous driving but incurs unnecessary computational cost when all sensors remain active. This paper presents PRAM-R, a unified Perception-Reasoning-Action-Memory framework with LLM-Guided Modality Routing for adaptive autonomous driving. PRAM-R adopts an asynchronous dual-loop design: a fast reactive loop for perception and control, and a slow deliberative loop for reasoning-driven modality selection and memory updates. An LLM router selects and weights modalities using environmental context and sensor diagnostics, while a hierarchical memory module preserves temporal consistency and supports long-term adaptation. We conduct a two-stage evaluation: (1) synthetic stress tests for stability analysis and (2) real-world validation on the nuScenes dataset. Synthetic stress tests confirm 87.2% reduction in routing oscillations via hysteresis-based stabilization. Real-world validation on nuScenes shows 6.22% modality reduction with 20% memory recall while maintaining comparable trajectory accuracy to full-modality baselines in complex urban scenarios. Our work demonstrates that LLM-augmented architectures with hierarchical memory achieve efficient, adaptive multimodal perception in autonomous driving.

---

## 论文详细总结（自动生成）

### 论文总结：PRAM-R 框架

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶依赖多模态感知（摄像头、激光雷达、雷达）来确保鲁棒性，但始终激活所有传感器会带来巨大的计算开销、延迟以及能源浪费。现有的多模态融合方法大多采用固定策略，缺乏根据环境上下文动态调整感知配置的高级推理能力。此外，现有的 LLM 驱动方法往往面临推理延迟高和缺乏记忆机制的问题。
**PRAM-R** 旨在解决这一矛盾，通过引入大语言模型（LLM）引导的模态路由和分层记忆机制，在保证感知精度的前提下，实现传感器的自适应激活，从而优化计算效率。

#### 2. 方法论
PRAM-R 提出了一个统一的“感知-推理-动作-记忆”框架，其核心技术包括：
*   **异步双环架构**：
    *   **快速反应环（Fast Reactive Loop）**：负责高频的实时感知和车辆控制。
    *   **慢速审议环（Slow Deliberative Loop）**：负责低频的 LLM 推理、模态路由决策和记忆更新。
*   **LLM 引导的模态路由**：利用 **Qwen3-VL-8B** 作为路由器。它接收传感器的诊断指标（如图像亮度、点云密度、雷达信噪比等）和环境上下文，输出各模态的激活掩码（Usage Mask）和可靠性权重。
*   **稳定性机制**：为了防止传感器频繁切换（振荡），引入了**滞后（Hysteresis）处理**和**指数移动平均（EMA）平滑**，确保路由决策在时间上的连续性。
*   **分层记忆模块**：分为四层：
    1.  **感知层**：缓存路由上下文和语义特征。
    2.  **推理层**：存储场景记录和策略提示。
    3.  **动作层**：记录控制日志以保持执行连续性。
    4.  **知识库**：存储长期经验，支持跨场景的持续学习。

#### 3. 实验设计
*   **数据集与场景**：
    1.  **合成压力测试**：模拟极端环境（如传感器突发故障、高频噪声、逐渐退化），测试路由的稳定性。
    2.  **真实世界验证**：使用 **nuScenes** 数据集中的复杂城市道路场景。
*   **Benchmark 与对比方法**：
    *   基准模型为 **PLA**（一种不带记忆且全模态常开的框架）。
    *   对比指标包括：路由效率（RE）、路由一致性（RC）、路由稳定性指数（RSI）、记忆召回率（MRR）以及轨迹预测精度（ADE/FDE）。

#### 4. 资源与算力
*   **模型规模**：使用了 **Qwen3-VL-8B** 大模型。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。但作者在结论中提到，8B 规模的模型在嵌入式硬件上仍存在延迟和内存开销挑战，暗示了对算力有一定要求。

#### 5. 实验数量与充分性
*   **实验规模**：作者在 nuScenes 数据集中选择了 10 个具有代表性的典型场景进行聚合分析，并进行了消融实验（对比了无记忆、无双环等变体）。
*   **充分性评价**：实验涵盖了从合成压力到真实数据的闭环验证，消融实验设计合理，能够证明各组件（如滞后机制、记忆模块）的有效性。然而，10 个场景的样本量相对较小，对于自动驾驶这种长尾场景极多的领域，实验的覆盖面尚有提升空间。

#### 6. 主要结论与发现
*   **效率提升**：在 nuScenes 验证中，PRAM-R 成功减少了 **6.22%** 的模态使用，显著降低了冗余计算。
*   **稳定性增强**：通过滞后稳定化机制，路由振荡减少了 **87.2%**。
*   **性能保持**：在减少传感器使用的前提下，轨迹预测精度（ADE/FDE）与全模态常开的基准模型相当，证明了自适应路由的可靠性。
*   **记忆作用**：分层记忆实现了约 **20%** 的推理结果重用（召回），有助于减少重复推理。

#### 7. 优点
*   **架构创新**：异步双环设计巧妙地平衡了 LLM 的深度推理能力与自动驾驶的实时性要求。
*   **稳定性设计**：引入滞后和 EMA 处理，解决了 LLM 在动态决策中容易出现的“决策闪烁”问题。
*   **分层记忆**：四层记忆结构不仅提高了效率，还为自动驾驶智能体提供了长期进化的可能性。

#### 8. 不足与局限
*   **数据规模有限**：实验仅基于 10 个 nuScenes 场景，缺乏大规模、长时间跨度的路测验证。
*   **实时性挑战**：尽管采用了异步设计，但 8B 参数量的模型在车载嵌入式平台上实现真正的实时运行仍有较大压力。
*   **缺乏实车部署**：目前研究仍停留在离线数据集和仿真阶段，未在真实车辆硬件上进行集成测试。
*   **记忆消融深度不足**：虽然有整体消融，但缺乏对四层记忆中每一层具体贡献的量化剥离分析。

（完）
