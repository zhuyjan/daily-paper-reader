---
title: "Rethinking Retrieval-Augmentation as Synthesis: A Query-Aware Context Merging Approach"
title_zh: 将检索增强重新构想为合成：一种查询感知的上下文合并方法
authors: "Jiarui Guo, Yuemeng Xu, Zongwei Lv, Yangyujia Wang, Xiaolin Wang, Kan Liu, Tao Lan, Lin Qu, Tong Yang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.20286v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 将 RAG 重新构思为具有查询感知上下文合并的综合过程
tldr: 针对检索增强生成（RAG）中上下文窗口限制导致的证据丢失和冗余问题，本文提出MergeRAG框架。该框架将检索增强重新定义为动态优化问题，通过查询感知的上下文合并策略，利用对称合并恢复长尾证据，利用非对称合并消除冗余。实验表明，MergeRAG在多个基准测试中显著优于现有SOTA方法，大幅提升了信息密度和生成质量。
selection_source: fresh_fetch
motivation: 传统的Top-k检索策略容易截断长尾关键证据，且在语义冗余的块上浪费了有限的上下文窗口。
method: 提出MergeRAG框架，通过对称合并整合弱信号并利用非对称合并消除冗余，结合分层并行策略优化计算效率。
result: 在标准基准测试中，MergeRAG相比现有SOTA基线在F1分数和精确匹配度（EM）上分别提升了高达13.7和11.5个百分点。
conclusion: 将检索增强视为动态合成而非简单过滤，能有效提高上下文信息密度，是解决大语言模型长文本限制的有效途径。
---

## 摘要
检索增强生成（RAG）通过动态整合外部信息，使大语言模型（LLM）能够扩展其现有知识。然而，实际部署受到 LLM 有限上下文窗口的根本限制，迫使在信息充分性与 Token 消耗之间进行权衡。标准流水线通过“检索后选择”策略来解决这一问题，通常仅保留基于相关性的前 k 个块（chunks）。尽管如此，这种方法并非最优：它本质上截断了位于相关性分布长尾中的关键桥接证据，同时在语义冗余的高排名块上浪费了 Token 预算。在本文中，我们将检索增强重新构想为一个旨在最大化信息密度的动态优化问题。我们提出了 MergeRAG，这是一个将范式从静态过滤转变为查询感知合成的新颖框架。MergeRAG 采用评分代理（scoring agent）通过双路径机制重构检索到的上下文：1）对称合并（Symmetric Merging），整合弱信号以恢复丢失的桥接证据；2）非对称合并（Asymmetric Merging），利用熵引导锚定（entropy-guided anchoring）在不牺牲语义完整性的情况下消除冗余。我们进一步引入了一种分层并行合并策略，在最大化计算并行性的同时减轻信息损失。在标准基准测试上的广泛实验表明，MergeRAG 显著优于最先进的 RAG 基线，在 F1 分数和精确匹配（EM）上分别实现了高达 13.7 点和 11.5 点的提升。

## Abstract
Retrieval-Augmented Generation (RAG) enables Large Language Models (LLMs) to extend their existing knowledge by dynamically incorporating external information. However, practical deployment is fundamentally constrained by the LLM's finite context window, forcing a trade-off between information sufficiency and token consumption. Standard pipelines address this via a retrieve-then-select strategy, typically retaining only the top-k chunks based on relevance. Nevertheless, this approach is suboptimal: it inherently truncates critical bridging evidence located in the long tail of the relevance distribution, while simultaneously wasting the token budget on semantically redundant high-ranking chunks.   In this paper, we rethink retrieval-augmentation as a dynamic optimization problem aimed at maximizing information density. We propose MergeRAG, a novel framework that shifts the paradigm from static filtering to query-aware synthesis. MergeRAG employs a scoring agent to restructure retrieved contexts through a dual-pathway mechanism: 1) Symmetric Merging, which consolidates weak signals to recover lost bridging evidence; 2) Asymmetric Merging, which utilizes entropy-guided anchoring to eliminate redundancy without sacrificing semantic integrity. We further introduce a Hierarchical Parallel Merging strategy that mitigates information loss while maximizing computational parallelism. Extensive experiments on standard benchmarks demonstrate that MergeRAG significantly outperforms state-of-the-art RAG baselines, achieving up to 13.7 points improvement in F1 score and 11.5 points in Exact Match (EM), respectively.

---

## 论文详细总结（自动生成）

### 论文总结：MergeRAG - 将检索增强重新构想为查询感知的上下文合成

#### 1. 核心问题与研究动机
*   **核心问题**：传统检索增强生成（RAG）系统在处理有限上下文窗口时，通常采用“检索后选择（Retrieve-then-Select）”的 Top-k 策略。这种策略存在两个致命缺陷：
    1.  **信息丢失（截断误差）**：位于相关性分布长尾（低分）的块可能包含关键的“桥接证据”，直接丢弃会导致多跳推理失败。
    2.  **语义冗余（资源浪费）**：高排名的块往往包含大量重复信息，白白消耗了宝贵的 Token 预算。
*   **研究动机**：作者认为不应将检索到的块视为不可变的原子单位，而应根据用户查询动态地进行**合成（Synthesis）**，从而在有限的窗口内最大化信息密度。

#### 2. 方法论：MergeRAG 框架
MergeRAG 将上下文构建建模为一个率失真优化问题（Rate-Distortion Optimization），旨在最大化相关信息的同时最小化 Token 长度。
*   **对称合并（Symmetric Merging）**：
    *   **目标**：整合弱信号。
    *   **流程**：识别评分最低的两个块，利用 LLM 作为融合算子，在查询感知下将它们合成为一个连贯、精简的单元，从而恢复原本会被截断的潜在证据。
*   **非对称合并（Asymmetric Merging）**：
    *   **目标**：消除冗余。
    *   **流程**：利用**负对数似然（NLL）**作为条件熵的经验估计。选取一个低分块，计算其相对于其他高分“锚点块”的 NLL，找到语义重叠最大的锚点进行定向融合。LLM 保持锚点块的结构，仅从源块中提取补充性的关键细节。
*   **分层并行合并（Hierarchical Parallel Merging）**：
    *   **技术细节**：为了解决顺序合并带来的高延迟和递归生成导致的语义漂移（幻觉累积），该方法将合并过程组织为对数深度的树状结构。在每一层中，多个块对并行进行融合，显著提升了推理效率并保证了语义忠实度。

#### 3. 实验设计
*   **数据集**：
    *   **多跳推理**：2WikiMQA, HotpotQA, MuSiQue（评估碎片化证据的整合能力）。
    *   **长上下文**：TriviaQA（评估长文本中的信号识别）。
    *   **专业领域**：QASPER（评估科研论文的深度理解）。
*   **对比基线（Benchmarks）**：
    *   **选择类**：BM25, BGE-reranker-v2-M3。
    *   **压缩类**：RECOMP。
    *   **聚合类**：RAPTOR（递归聚类摘要）, Tree-RAG（分层存储）。
*   **评估指标**：精确匹配（EM）、F1 分数、准确率（Acc）。

#### 4. 资源与算力
*   **硬件环境**：使用了高性能计算集群，配备 **96 块 NVIDIA H20 Tensor Core GPU**（每块含 141GB HBM3 显存）。
*   **模型使用**：统一使用 **Qwen3-30B-A3B-Instruct** 作为生成器和合并代理；在泛化性实验中使用了 **Llama-3.1-8B-Instruct**。
*   **训练时长**：文中未明确提及训练时长，因为该方法主要基于推理侧的动态合成，不涉及大规模模型重训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流基准数据集上进行了全面测试。
*   **消融实验**：
    *   对比了对称与非对称合并在不同任务下的表现。
    *   分析了上下文预算 $k$（从 1 到 5）对性能的影响。
    *   对比了顺序合并与分层并行合并的延迟与质量。
    *   验证了在不同规模后端模型（Qwen vs Llama）上的通用性。
*   **评价**：实验设计非常充分且客观。通过控制变量法（统一检索器、统一生成器）确保了对比的公平性，且覆盖了从效率到效果的多个维度。

#### 6. 主要结论与发现
*   **性能飞跃**：MergeRAG 在多跳推理任务上表现卓越，MuSiQue 数据集的 F1 分数比最强基线提升了 12.1 点。
*   **高信息密度**：在极端的 $k=1$（仅允许一个块的长度）设定下，MergeRAG 的性能几乎是传统 Top-k 方案的两倍，证明了其合成多源信息的能力。
*   **效率优化**：分层并行策略在保持甚至提升生成质量的同时，实现了超过 **2.2 倍** 的推理加速。
*   **策略差异**：非对称合并在多跳推理中更优（去冗余强），而对称合并在长尾数据挖掘（如 QASPER）中表现更好。

#### 7. 优点与亮点
*   **范式突破**：从简单的“块过滤”转向“内容合成”，解决了 RAG 中长期存在的证据截断与冗余矛盾。
*   **理论严谨**：引入信息论中的条件熵（通过 NLL 估计）来指导非对称合并，为冗余消除提供了数学依据。
*   **工程可行性**：通过分层并行设计，克服了 LLM 多次调用带来的延迟瓶颈，使其具备实际部署的潜力。

#### 8. 不足与局限
*   **计算开销**：尽管有并行优化，但与简单的 Top-k 检索相比，MergeRAG 仍需额外的 LLM 推理步骤和 NLL 计算，首字延迟（TTFT）会更高。
*   **模型依赖性**：合并的效果高度依赖于评分代理（Scoring Agent）的准确性和合成 LLM 的指令遵循能力，较弱的模型可能在合并过程中引入幻觉。
*   **复杂性**：系统架构比标准 RAG 复杂，涉及多次推理调用和动态评分逻辑，维护成本较高。

（完）
