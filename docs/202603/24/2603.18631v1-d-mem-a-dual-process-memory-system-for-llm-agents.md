---
title: "D-Mem: A Dual-Process Memory System for LLM Agents"
title_zh: D-Mem：大语言模型智能体的双过程记忆系统
authors: "Zhixing You, Jiachen Yuan, Jason Cai"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18631v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 大语言模型智能体的双过程记忆系统
tldr: 针对LLM智能体在长程推理中记忆检索精度不足的问题，本文提出D-Mem双过程记忆系统。该系统结合了轻量级向量检索与高保全的“全审议”模块，并通过多维质量门控策略动态切换。实验表明，D-Mem在保持低计算成本的同时，显著提升了细粒度上下文理解能力，性能接近全量处理且优于传统检索基准，为持久化智能体提供了高效的记忆方案。
selection_source: fresh_fetch
motivation: 现有的基于检索的记忆框架因有损抽象常丢失关键上下文，难以处理需要精细理解的复杂查询。
method: 提出D-Mem系统，集成轻量级向量检索与高保全全审议模块，并利用多维质量门控策略实现两者的动态衔接。
result: "在LoCoMo基准测试中，D-Mem以较低成本达到了全审议性能的96.7%，且F1分数显著优于Mem0等静态检索基准。"
conclusion: D-Mem通过双过程机制有效平衡了认知经济性与记忆准确性，为构建高性能长程推理智能体提供了新方案。
---

## 摘要
随着持久、自适应自主智能体的发展，为这些系统配备用于长程推理的高保真记忆访问已成为一项关键需求。然而，流行的基于检索的记忆框架通常遵循增量处理范式，将对话记忆持续提取并更新到向量数据库中，并在查询时依赖语义检索。虽然这种方法速度较快，但其本质上依赖于有损抽象，经常遗漏上下文中的关键信息，且难以解决依赖细粒度上下文理解的查询。为了解决这一问题，我们引入了 D-Mem，一种双过程记忆系统。它保留了用于常规查询的轻量级向量检索，同时建立了一个详尽的全量审议（Full Deliberation）模块作为高保真回退方案。为了在不牺牲准确性的情况下实现认知经济性，D-Mem 采用多维质量门控策略来动态衔接这两个过程。在 LoCoMo 和 RealTalk 基准测试上使用 GPT-4o-mini 和 Qwen3-235B-Instruct 进行的实验证明了我们方法的有效性。值得注意的是，我们的多维质量门控策略在使用 GPT-4o-mini 的 LoCoMo 测试中达到了 53.5 的 F1 分数。这优于我们的静态检索基线 Mem0* (51.2)，并在计算成本显著降低的情况下，达到了全量审议性能 (55.3) 的 96.7%。

## Abstract
Driven by the development of persistent, self-adapting autonomous agents, equipping these systems with high-fidelity memory access for long-horizon reasoning has emerged as a critical requirement. However, prevalent retrieval-based memory frameworks often follow an incremental processing paradigm that continuously extracts and updates conversational memories into vector databases, relying on semantic retrieval when queried. While this approach is fast, it inherently relies on lossy abstraction, frequently missing contextually critical information and struggling to resolve queries that rely on fine-grained contextual understanding. To address this, we introduce D-Mem, a dual-process memory system. It retains lightweight vector retrieval for routine queries while establishing an exhaustive Full Deliberation module as a high-fidelity fallback. To achieve cognitive economy without sacrificing accuracy, D-Mem employs a Multi-dimensional Quality Gating policy to dynamically bridge these two processes. Experiments on the LoCoMo and RealTalk benchmarks using GPT-4o-mini and Qwen3-235B-Instruct demonstrate the efficacy of our approach. Notably, our Multi-dimensional Quality Gating policy achieves an F1 score of 53.5 on LoCoMo with GPT-4o-mini. This outperforms our static retrieval baseline, Mem0$^\ast$ (51.2), and recovers 96.7\% of the Full Deliberation's performance (55.3), while incurring significantly lower computational costs.

---

## 论文详细总结（自动生成）

### 论文总结：D-Mem 大语言模型智能体的双过程记忆系统

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在长程对话中面临的**记忆瓶颈**提出方案。现有的记忆框架（如 Mem0, Zep）多采用“增量压缩”范式，将对话摘要存入向量数据库。这种方式虽然高效，但存在**有损抽象（Lossy Abstraction）**的问题：在压缩过程中会丢失细粒度的上下文（如隐含的时间逻辑、多跳依赖关系），导致模型在处理复杂推理查询时表现不佳。论文受人类认知心理学中的“双过程理论”（System 1 与 System 2）启发，旨在构建一个既能快速检索又能深度审议的记忆系统，平衡“认知经济性”与“推理准确性”。

#### 2. 方法论：核心思想与技术细节
D-Mem 系统由三个核心组件构成，模拟人类的直觉反应与深度思考：

*   **System 1 (Mem0*)：轻量级检索基础**
    *   改进了传统的 Mem0 架构，通过向量数据库进行 Top-K 语义检索。
    *   **技术细节**：在提取阶段引入 Top-10 相似记忆作为背景，并在更新阶段加入严格的余弦相似度过滤（>0.8），以减少冗余和噪声。
*   **System 2 (Full Deliberation)：高保真全量审议**
    *   当 System 1 无法解决问题时触发。它绕过压缩后的记忆，直接处理原始对话历史。
    *   **流程**：将历史切分为 60 条消息一组的块（Chunk），利用 LLM 进行**查询引导（Query-guided）**的实时事实提取，并进行 0-10 分的关联度打分，最后筛选高分事实生成答案。
*   **多维质量门控（Multi-dimensional Quality Gating）：动态路由**
    *   作为“元认知”检查点，评估 System 1 生成的初始答案。
    *   **评估维度**：相关性（Relevance）、忠实度与一致性（Faithfulness & Consistency）、完整性（Completeness）。
    *   **逻辑**：仅当初始答案未通过任一维度测试时，才调用昂贵的 System 2。

#### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：包含长达 24K token 的长程对话，涵盖单跳、多跳、时间推理和开放域四类问题。
    *   **RealTalk**：真实的 21 天长对话数据集，平均每场对话超过 16,000 词。
*   **对比方法（Baselines）**：
    *   全上下文（Full Context）、标准 RAG、LangMem、Mem0、Zep、Nemori 等。
*   **评估指标**：F1 分数（词汇重叠）、BLEU（n-gram 忠实度）、LLM-as-a-Judge（语义等价性评分）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA GeForce RTX 4070 Laptop GPU (8GB VRAM)** 和 Intel Core i9-13900HX CPU 的笔记本电脑上完成。
*   **模型与 API**：使用了 GPT-4o-mini 和 Qwen3-235B-Instruct（通过 DashScope API）。
*   **算力消耗**：整个评估过程在 24 小时内完成，总计消耗约 **4 亿（400 million）个 token**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流长记忆基准上进行了详尽测试，涵盖了 1540 个（LoCoMo）和 728 个（RealTalk）问题。
*   **充分性**：
    *   进行了**跨模型验证**（GPT vs. Qwen），证明了方法的泛化性。
    *   设计了**消融实验**和**策略对比**（如对比“多数投票”、“共识策略”与“质量门控”）。
    *   针对不同难度的问题类别进行了深入分析。
*   **客观性**：实验对比了多种最新的 SOTA 基准，并公开了所有 Prompt 模板，具有较高的透明度和客观性。

#### 6. 主要结论与发现
*   **性能优异**：D-Mem 的质量门控策略在 LoCoMo 上达到了 53.5 的 F1 分数，优于所有静态检索基准（如 Mem0* 的 51.2）。
*   **效率平衡**：该策略成功恢复了“全量审议”模块 96.7% 的性能，但 token 消耗仅为后者的 **35.8%**，推理延迟也大幅降低。
*   **难度相关性**：实验发现，问题越难（如时间推理和多跳推理），D-Mem 相比静态检索的提升越明显，证明了 System 2 处理复杂逻辑的必要性。
*   **解决“迷失中段”**：通过分块处理和查询引导提取，有效缓解了长文本处理中的“Lost-in-the-Middle”现象。

#### 7. 优点
*   **认知启发式架构**：巧妙地将心理学双过程理论应用于记忆系统，解决了 RAG 中“快”与“准”的矛盾。
*   **高保真回退机制**：提出的“全量审议”为长记忆推理设定了新的性能上限。
*   **动态路由策略**：多维质量门控不仅提高了准确率，还通过减少不必要的深度计算实现了极佳的“认知经济性”。

#### 8. 不足与局限
*   **可扩展性挑战**：尽管减少了开销，但 System 2 仍需扫描原始历史，对于“无限长”或终身学习场景，计算成本依然可能随时间线性增长。
*   **缺乏显式状态跟踪**：目前的分块处理依赖 LLM 的原生注意力机制，缺乏跨块的显式因果或逻辑链条合成，可能影响极长跨度的推理。
*   **模型依赖性**：质量门控的效果高度依赖于底层 LLM 的判别能力，较弱的模型可能无法准确识别初始答案的缺陷。

（完）
