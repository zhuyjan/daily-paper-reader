---
title: Structured Linked Data as a Memory Layer for Agent-Orchestrated Retrieval
title_zh: 结构化链接数据作为智能体编排检索的记忆层
authors: "Andrea Volpini, Elie Raad, Beatrice Gamba, David Riccitelli"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10700v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 结构化链接数据作为智能体 RAG 的记忆层
tldr: "本研究探讨了结构化链接数据（如Schema.org和JSON-LD）在增强检索增强生成（RAG）系统中的作用。通过在四个领域进行实验，研究发现将文档从纯文本转换为包含代理指令、面包屑导航和实体链接的增强型实体页面，能显著提升标准RAG和代理化RAG的检索准确度（约30%）。该研究证明了结构化元数据作为智能体“记忆层”的巨大潜力，并开源了相关数据集和模板。"
selection_source: fresh_fetch
motivation: 传统的RAG系统通常将文档视为扁平文本，忽略了知识图谱提供的结构化元数据和实体间的链接关系。
method: 在四个领域测试了三种文档表示形式（HTML、JSON-LD、增强型实体页面）与两种检索模式（标准RAG、多跳链接遍历代理）的组合。
result: "增强型实体页面格式使标准RAG和代理化管道的准确率分别提升了29.6%和29.8%，而单纯的JSON-LD提升有限。"
conclusion: 结构化链接数据和针对智能体优化的页面设计能显著提高RAG系统的回答质量，为构建更高效的智能体记忆层提供了新方向。
---

## 摘要
检索增强生成 (RAG) 系统通常将文档视为扁平文本，忽略了知识图谱提供的结构化元数据和链接关系。在本文中，我们研究了结构化链接数据（特别是 Schema.org 标记和由链接数据平台提供的可解引用实体页面）是否能提高标准 RAG 和智能体 RAG 系统的检索准确率和回答质量。我们利用 Vertex AI Vector Search 2.0 进行检索，并使用 Google Agent Development Kit (ADK) 进行智能体推理，在四个领域（编辑、法律、旅游、电子商务）进行了受控实验。我们的实验设计测试了七种条件：三种文档表示形式（纯 HTML、带有 JSON-LD 的 HTML 以及增强的智能体优化实体页面）与两种检索模式（标准 RAG 和具有多跳链接遍历的智能体 RAG）交叉，外加一种增加了丰富导航功能和实体互联的 Enhanced+ 条件。结果显示，虽然仅靠 JSON-LD 标记只能带来微小的提升，但我们结合了 llms.txt 风格的智能体指令、面包屑导航和神经搜索能力的增强型实体页面格式实现了显著收益：标准 RAG 的准确率提升了 29.6%，完整智能体流水线的准确率提升了 29.8%。具有更丰富导航功能的 Enhanced+ 变体获得了最高的绝对分数（准确率：4.85/5，完整性：4.55/5），尽管其相对于基础增强格式的增量收益在统计上并不显著。我们发布了数据集、评估框架和增强型实体页面模板，以支持可复现性。

## Abstract
Retrieval-Augmented Generation (RAG) systems typically treat documents as flat text, ignoring the structured metadata and linked relationships that knowledge graphs provide. In this paper, we investigate whether structured linked data, specifically Schema.org markup and dereferenceable entity pages served by a Linked Data Platform, can improve retrieval accuracy and answer quality in both standard and agentic RAG systems. We conduct a controlled experiment across four domains (editorial, legal, travel, e-commerce) using Vertex AI Vector Search 2.0 for retrieval and the Google Agent Development Kit (ADK) for agentic reasoning. Our experimental design tests seven conditions: three document representations (plain HTML, HTML with JSON-LD, and an enhanced agentic-optimized entity page) crossed with two retrieval modes (standard RAG and agentic RAG with multi-hop link traversal), plus an Enhanced+ condition that adds rich navigational affordances and entity interlinking. Our results reveal that while JSON-LD markup alone provides only modest improvements, our enhanced entity page format, incorporating llms.txt-style agent instructions, breadcrumbs, and neural search capabilities, achieves substantial gains: +29.6% accuracy improvement for standard RAG and +29.8% for the full agentic pipeline. The Enhanced+ variant, with richer navigational affordances, achieves the highest absolute scores (accuracy: 4.85/5, completeness: 4.55/5), though the incremental gain over the base enhanced format is not statistically significant. We release our dataset, evaluation framework, and enhanced entity page templates to support reproducibility.

---

## 论文详细总结（自动生成）

这篇论文由 WordLift 团队撰写，探讨了如何利用结构化链接数据（Structured Linked Data）优化检索增强生成（RAG）系统，特别是针对 AI 智能体（Agent）的检索场景。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 RAG 系统通常将网页视为扁平的非结构化文本，忽略了网页中广泛存在的 Schema.org 标记和知识图谱（KG）关系。
*   **研究动机**：随着 AI 驱动的搜索引擎（如 Google AI Mode）兴起，如何让内容更好地被 AI 检索、理解并推理成为关键。作者提出将结构化链接数据作为智能体的“外部记忆层”，通过显式化实体关系来提升回答的准确性和完整性。
*   **整体含义**：论文定义了“SEO 3.0”的概念，即从单纯的文档排名转向优化 AI 的推理（Reasoning）和行动（Actions）能力。

### 2. 核心方法论
论文提出了一个结合了结构化数据与智能体编排的检索架构：
*   **增强型实体页面（Enhanced Entity Page）**：这是一种专为 AI 优化的文档格式，包含：
    *   基于结构化数据生成的自然语言摘要。
    *   嵌入式 JSON-LD 块（Schema.org 类型）。
    *   **导航启示（Navigational Affordances）**：显式的实体链接（可解引用的 URI）和面包屑导航。
    *   **智能体指令**：采用 `llms.txt` 风格，为 LLM 提供如何处理该页面的显式引导。
*   **智能体编排检索（Agent-Orchestrated Retrieval）**：
    *   使用 **ReAct 循环**（推理+行动）。
    *   **工具集**：包括向量搜索（Vertex AI）、实体链接追踪（通过 HTTP 获取 JSON-LD）和知识图谱神经搜索。
    *   智能体可以跨越实体边界进行多跳（Multi-hop）推理，模拟人类浏览网页的行为。

### 3. 实验设计
*   **场景与数据集**：涵盖四个垂直领域，共 158 个实体和 349 个测试查询：
    1.  **编辑（WordLift Blog）**：SEO 和 AI 相关文章。
    2.  **法律（Express Legal Funding）**：法律资助和诉讼概念。
    3.  **旅游（SalzburgerLand）**：餐厅、景点等地理实体。
    4.  **电子商务（BlackBriar）**：户外装备产品信息。
*   **对比方法（7 种条件）**：
    *   **文档格式**：纯 HTML、HTML+JSON-LD、增强型实体页面、Enhanced+（更丰富的链接）。
    *   **检索模式**：标准 RAG（单次检索） vs. 智能体 RAG（多步工具调用）。
*   **评估指标**：由独立 LLM（Gemini 3.0 Flash）对准确性（1-5 分）、完整性（1-5 分）和接地性（Grounding）进行评分。

### 4. 资源与算力
*   **算力平台**：实验完全在 **Google Cloud** 上运行。
*   **模型使用**：
    *   **检索**：Vertex AI Vector Search 2.0（使用 `text-embedding-005`）。
    *   **生成**：Gemini 2.5 Flash。
    *   **评估**：Gemini 3.0 Flash。
*   **说明**：文中未明确提及具体的 GPU 型号或训练时长，因为主要基于现有的云端 API 和托管服务进行推理和检索实验，而非从头训练大模型。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **2,443 次独立查询评估**（349 个查询 × 7 种条件）。
*   **充分性**：实验覆盖了从简单事实查询到复杂关系推理和比较查询的不同类型。
*   **客观性**：采用了配对 t 检验和 Bonferroni 校正来确保统计显著性。通过跨四个不同领域的测试，验证了结论的普适性。

### 6 主要结论与发现
*   **JSON-LD 效果有限**：在扁平文本检索中，单纯添加 JSON-LD 标记仅带来微小提升（+0.17 准确率），因为长文档往往在索引时被截断。
*   **增强型页面效果显著**：将结构化数据转化为“增强型实体页面”后，标准 RAG 的准确率提升了 **29.6%**，智能体管道提升了 **29.8%**。
*   **智能体的补偿作用**：智能体在处理结构化程度较差的文档时提升最明显；但当文档本身已针对 AI 优化（增强型页面）时，智能体带来的额外准确率增益较小，主要体现在检索效率的提升。
*   **领域差异**：在旅游和编辑领域（知识图谱信息远超 HTML 文本）提升最大；在电商领域（HTML 已包含大部分事实）提升较小。

### 7. 优点
*   **实战导向**：直接针对当前 AI 搜索（如 Google SGE）的底层逻辑进行优化，具有极高的商业应用价值。
*   **桥接技术鸿沟**：成功将语义网（Semantic Web）的经典理念（链接数据）与现代 LLM 智能体架构相结合。
*   **开源贡献**：发布了数据集、评估框架和页面模板，便于社区复现和改进。

### 8. 不足与局限
*   **评估偏置风险**：测试查询的“标准答案”是从知识图谱中提取的，而增强型页面也源自同一知识图谱，这可能导致评估结果对增强型页面天然有利（存在循环论证风险）。
*   **截断问题**：实验中发现 80% 以上的文档超过了向量搜索的字符限制，这可能掩盖了 JSON-LD 在更长上下文窗口下的潜在表现。
*   **模型单一性**：主要基于 Gemini 系列模型，尚未验证在 GPT-4 或开源模型（如 Llama 3）上的表现差异。
*   **成本考虑**：智能体多跳检索虽然准确，但相比标准 RAG 会消耗更多的 API Token 和推理时间。

（完）
