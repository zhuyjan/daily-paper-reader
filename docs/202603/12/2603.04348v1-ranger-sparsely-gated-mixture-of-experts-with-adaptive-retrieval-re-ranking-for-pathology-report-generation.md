---
title: "RANGER: Sparsely-Gated Mixture-of-Experts with Adaptive Retrieval Re-ranking for Pathology Report Generation"
title_zh: RANGER：基于稀疏门控混合专家网络与自适应检索重排序的病理报告生成
authors: "Yixin Chen, Ziyu Su, Hikmat Khan, Muhammad Khalid Khan Niazi"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04348v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 多模态报告生成的自适应检索重排序
tldr: 针对病理报告生成中全切片图像（WSI）规模巨大且形态异质性强的问题，本文提出RANGER框架。该框架在解码器中引入稀疏门控专家混合（MoE）机制，实现针对不同诊断模式的动态专家分工；同时设计了自适应检索重排序模块，通过筛选知识库中的冗余信息来增强语义对齐。实验证明，RANGER在PathText-BRCA数据集上显著提升了生成质量，达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的病理报告生成方法受限于同质化解码器架构和静态知识检索，难以处理复杂的形态异质性并容易引入外部噪声。
method: 提出一种结合稀疏门控MoE解码器与自适应检索重排序模块的框架，通过动态专家路由和知识精炼提升生成性能。
result: 在PathText-BRCA数据集上，RANGER在BLEU、METEOR和ROUGE-L等指标上均优于现有方法，BLEU-4达到0.1435。
conclusion: 动态专家路由与自适应知识精炼的结合，能有效提升病理报告生成的语义准确性与专业化水平。
---

## 摘要
病理报告生成仍然是一个相对未被充分探索的下游任务，这主要是由于全视野数字化切片（WSIs）的十亿像素级规模和复杂的形态异质性。现有的病理报告生成框架通常采用 Transformer 架构，依赖于同质化的解码器架构和静态知识检索集成。此类架构限制了生成的专业化，并可能在报告生成过程中引入噪声外部引导。为了解决这些局限性，我们提出了 RANGER，这是一个用于病理报告生成的、具有自适应检索重排序的稀疏门控混合专家（MoE）框架。具体而言，我们在解码器中集成了稀疏门控 MoE，并结合了噪声 top-$k$ 路由和负载均衡正则化，以实现跨各种诊断模式的动态专家专业化。此外，我们引入了一个自适应检索重排序模块，在集成前选择性地细化从知识库中检索到的记忆，从而减少噪声并基于视觉特征表示提高语义对齐。我们在 PathText-BRCA 数据集上进行了广泛的实验，并证明了在标准自然语言生成指标上，该方法相较于现有方法具有持续的改进。我们的完整 RANGER 模型在 PathText 数据集上实现了最佳性能，BLEU-1 到 BLEU-4 分数分别达到 0.4598、0.3044、0.2036 和 0.1435，METEOR 为 0.1883，ROUGE-L 为 0.3038，验证了动态专家路由和自适应知识细化对于具有语义基础的病理报告生成的有效性。

## Abstract
Pathology report generation remains a relatively under-explored downstream task, primarily due to the gigapixel scale and complex morphological heterogeneity of Whole Slide Images (WSIs). Existing pathology report generation frameworks typically employ transformer architectures, relying on a homogeneous decoder architecture and static knowledge retrieval integration. Such architectures limit generative specialization and may introduce noisy external guidance during the report generation process. To address these limitations, we propose RANGER, a sparsely-gated Mixture-of-Experts (MoE) framework with adaptive retrieval re-ranking for pathology report generation. Specifically, we integrate a sparsely gated MoE into the decoder, along with noisy top-$k$ routing and load-balancing regularization, to enable dynamic expert specialization across various diagnostic patterns. Additionally, we introduce an adaptive retrieval re-ranking module that selectively refines retrieved memory from a knowledge base before integration, reducing noise and improving semantic alignment based on visual feature representations. We perform extensive experiments on the PathText-BRCA dataset and demonstrate consistent improvements over existing approaches across standard natural language generation metrics. Our full RANGER model achieves optimal performance on PathText dataset, reaching BLEU-1 to BLEU-4 scores of 0.4598, 0.3044, 0.2036, and 0.1435, respectively, with METEOR of 0.1883, and ROUGE-L of 0.3038, validating the effectiveness of dynamic expert routing and adaptive knowledge refinement for semantically grounded pathology report generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《RANGER: Sparsely-Gated Mixture-of-Experts with Adaptive Retrieval Re-ranking for Pathology Report Generation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：自动化生成病理报告对于减轻病理医生负担、辅助临床决策具有重要意义。然而，全切片图像（WSI）具有十亿像素级的超高分辨率和复杂的形态异质性，且诊断信息在海量像素中分布极度稀疏。
*   **核心问题**：
    1.  **解码器同质化**：现有模型多采用标准 Transformer 架构，使用单一参数集处理从形态描述到生物标志物解读等多种异质推理任务，限制了生成的专业化水平。
    2.  **检索噪声**：现有的知识增强方法通常采用静态检索，直接融合外部知识，容易引入无关或弱相关的噪声信息，干扰报告生成的准确性。

### 2. 论文提出的方法论
论文提出了 **RANGER** 框架，核心思想是通过“输入端精炼”和“输出端特化”来提升报告质量。
*   **自适应检索重排序（Adaptive Retrieval Re-ranking）**：
    *   **两阶段检索**：首先通过余弦相似度从预构建的句子级记忆库（基于 PLIP 编码）中粗选候选句；随后使用一个可学习的 MLP 重排序模块，根据视觉特征与文本的兼容性进行细化筛选，减少噪声。
    *   **令牌压缩层（TC Layer）**：利用交叉注意力机制将海量的补丁级（Patch-level）视觉特征和检索到的文本特征分别压缩为紧凑的全局令牌。
*   **稀疏门控混合专家（MoE）解码器**：
    *   **专家特化**：将标准 Transformer 的前馈网络（FFN）替换为 MoE 模块（包含多个专家 FFN），使模型能针对不同的诊断模式（如分级、分期、形态描述）调用不同的专家。
    *   **噪声 Top-k 路由**：引入可学习的噪声机制进行路由选择，确保专家在训练中得到充分更新，防止“专家塌陷”。
    *   **负载均衡正则化**：通过辅助损失函数 $L_{aux}$ 强制平衡各专家的利用率，确保训练稳定性。

### 3. 实验设计
*   **数据集**：使用 **PathText-BRCA** 数据集（基于 TCGA 数据库），包含乳腺癌病理图像及对应的诊断报告。
*   **数据划分**：训练集 796 样本，验证集 88 样本，测试集 93 样本（确保患者级独立）。
*   **Benchmark 与对比方法**：
    *   **基础模型**：CNN-RNN, att-LSTM, Vanilla Transformer。
    *   **医疗专用模型**：R2Gen, R2GenCMN, MI-Gen, HistGen。
    *   **SOTA 基准**：BiGen（目前最强的知识增强病理报告生成模型）。
*   **评估指标**：BLEU-1/2/3/4, METEOR, ROUGE-L。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100 (40GB)** GPU 上完成。
*   **训练细节**：使用了 Adam 优化器，初始学习率为 $1 \times 10^{-4}$，权重衰减为 $5 \times 10^{-5}$。推理阶段采用 Beam Search（束宽为 3）。文中未明确提及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验验证，包括性能对比实验和详尽的消融实验。
*   **消融实验充分性**：
    *   验证了重排序模块、MoE 解码器、噪声路由和负载均衡各自的贡献。
    *   对超参数进行了敏感性分析，如第一阶段检索量 $K$、第二阶段精炼量 $k$、专家数量 $E$ 以及负载均衡系数 $\lambda$。
*   **客观性**：实验在标准化的数据集和协议下进行，对比了从经典到最新的多种 SOTA 模型，结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能突破**：RANGER 在所有指标上均优于现有方法，BLEU-4 达到 0.1435，ROUGE-L 达到 0.3038，显著超过了之前的基准 BiGen。
*   **MoE 的有效性**：实验发现设置 4 个专家并采用 Top-2 路由效果最佳。过多的专家（如 8 个）在小规模数据集上反而会导致性能下降。
*   **重排序的重要性**：两阶段检索比简单的余弦检索能提供更具语义一致性的引导，有效抑制了外部知识带来的噪声。

### 7. 优点
*   **架构创新**：首次将稀疏门控 MoE 引入病理报告生成领域，解决了病理诊断推理的异质性问题。
*   **知识精炼**：提出的自适应重排序机制解决了检索增强生成（RAG）中常见的噪声干扰问题，使视觉与文本的对齐更加精准。
*   **端到端优化**：重排序器与生成器共同优化，使检索过程具有任务感知能力。

### 8. 不足与局限
*   **数据规模限制**：实验仅在 PathText-BRCA（乳腺癌）单一数据集上进行，样本量（约 1000 例）相对较小，可能限制了 MoE 架构在大规模参数下的潜力发挥。
*   **泛化性验证不足**：尚未在多中心、多器官（如肺癌、前列腺癌）数据集上验证模型的通用性。
*   **临床评估缺失**：虽然 NLP 指标有所提升，但缺乏病理医生对生成报告在临床准确性和逻辑性方面的定性评价。

（完）
