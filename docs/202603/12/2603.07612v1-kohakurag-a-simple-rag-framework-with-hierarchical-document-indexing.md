---
title: "KohakuRAG: A simple RAG framework with hierarchical document indexing"
title_zh: KohakuRAG：一种具有分层文档索引的简单 RAG 框架
authors: "Shih-Ying Yeh, Yueh-Feng Ku, Ko-Wei Huang, Buu-Khang Tu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07612v1"
tags: ["query:mmrag"]
score: 9.0
evidence: RAG系统的分层文档索引
tldr: 本研究针对检索增强生成（RAG）在处理高精度引用和复杂文档结构时的局限性，提出了KohakuRAG框架。该框架通过四层树状分级索引保留文档结构，结合大模型驱动的查询规划器和带弃权机制的集成投票推理，显著提升了检索覆盖率和回答稳定性。在WattBot 2025挑战赛中，KohakuRAG凭借卓越的数值精度和归因准确性获得双榜冠军，并已开源。
selection_source: fresh_fetch
motivation: 传统RAG系统在处理高精度引用需求时，面临文档结构丢失、查询词不匹配以及推理结果不稳定等挑战。
method: 采用文档-章节-段落-句子的四层分级索引，并结合LLM查询规划、跨查询重排序及多模型集成投票机制。
result: 在WattBot 2025挑战赛中取得0.861的最高分，成为唯一在公开和私有排行榜上均保持第一的系统。
conclusion: 分级索引、重试机制和集成推理的结合能有效提升RAG系统在技术文档问答中的准确性和可靠性。
---

## 摘要
检索增强生成（RAG）系统在从文档集合中回答问题时，若要求高精度引用，则面临多重困难：扁平化分块策略牺牲了文档结构，单查询表述因词汇不匹配而遗漏相关段落，且单次推理产生的随机答案在内容和引用选择上均存在波动。我们提出了 KohakuRAG，这是一个分层 RAG 框架，它通过四层树状表示（文档 $\rightarrow$ 章节 $\rightarrow$ 段落 $\rightarrow$ 句子）及自底向上的嵌入聚合来保留文档结构，通过基于大语言模型（LLM）的查询规划器和跨查询重排序来提高检索覆盖率，并通过带有弃权感知投票的集成推理来稳定答案。我们在 WattBot 2025 挑战赛上进行了评估，该基准测试要求系统根据 32 份文档回答技术问题，数值容差为 $\pm$0.1%，并要求精确的来源归属。KohakuRAG 在公开和私有排行榜上均获得第一名（最终得分为 0.861），是唯一在两个评估分区中均保持领先地位的团队。消融研究表明，提示词排序（相对提升 +80%）、重试机制（+69%）以及带有空白过滤的集成投票（+1.2 个百分点）均做出了实质性贡献，而仅靠分层稠密检索即可与混合稀疏-稠密方法相媲美（BM25 仅增加 +3.1 个百分点）。我们将 KohakuRAG 作为开源软件发布，网址为 https://github.com/KohakuBlueleaf/KohakuRAG。

## Abstract
Retrieval-augmented generation (RAG) systems that answer questions from document collections face compounding difficulties when high-precision citations are required: flat chunking strategies sacrifice document structure, single-query formulations miss relevant passages through vocabulary mismatch, and single-pass inference produces stochastic answers that vary in both content and citation selection. We present KohakuRAG, a hierarchical RAG framework that preserves document structure through a four-level tree representation (document $\rightarrow$ section $\rightarrow$ paragraph $\rightarrow$ sentence) with bottom-up embedding aggregation, improves retrieval coverage through an LLM-powered query planner with cross-query reranking, and stabilizes answers through ensemble inference with abstention-aware voting. We evaluate on the WattBot 2025 Challenge, a benchmark requiring systems to answer technical questions from 32 documents with $\pm$0.1% numeric tolerance and exact source attribution. KohakuRAG achieves first place on both public and private leaderboards (final score 0.861), as the only team to maintain the top position across both evaluation partitions. Ablation studies reveal that prompt ordering (+80% relative), retry mechanisms (+69%), and ensemble voting with blank filtering (+1.2pp) each contribute substantially, while hierarchical dense retrieval alone matches hybrid sparse-dense approaches (BM25 adds only +3.1pp). We release KohakuRAG as open-source software at https://github.com/KohakuBlueleaf/KohakuRAG.

---

## 论文详细总结（自动生成）

这是一份关于论文《KohakuRAG: A simple RAG framework with hierarchical document indexing》的深度结构化总结：

### 1. 核心问题与整体含义
论文针对检索增强生成（RAG）系统在处理需要**高精度引用**和**极高数值准确性**的技术问答任务时的三大局限性：
*   **结构丢失**：传统的扁平化分块（Flat Chunking）破坏了文档的层级逻辑，导致引用归属困难。
*   **词汇不匹配**：单次查询往往无法覆盖文档中不同表述的相关段落（如缩写与全称）。
*   **推理不稳定**：单次 LLM 推理具有随机性，容易在有证据的情况下产生不必要的“弃权”（Abstention）或错误的引用。

研究背景设定在 **WattBot 2025 挑战赛**，该比赛要求对 AI 能耗文档进行问答，数值误差需控制在 $\pm$0.1% 以内，并要求精确到文档级的引用。

### 2. 方法论：核心思想与关键技术
KohakuRAG 提出了一个包含三个阶段的系统框架：

*   **分层文档索引（Hierarchical Indexing）**：
    *   将文档解析为四层树状结构：**文档 $\rightarrow$ 章节 $\rightarrow$ 段落 $\rightarrow$ 句子**。
    *   **自底向上聚合**：先计算叶子节点（句子）的嵌入，再通过长度加权平均（Length-weighted aggregation）生成父节点（段落/章节）的嵌入。
    *   **多模态支持**：利用 VLM（如 Qwen-VL）为图表生成描述，作为特殊段落节点存入索引。
*   **多查询检索与重排序**：
    *   **查询规划器**：利用 LLM 将原始问题扩展为 $n$ 个语义相关的查询。
    *   **跨查询重排序（Cross-Query Reranking）**：结合节点被检索到的频率（Frequency）和相似度得分（Score）进行排序。
    *   **上下文扩展**：检索到特定节点后，自动引入其父节点和兄弟节点以提供完整的上下文。
*   **集成推理与重试机制**：
    *   **重试机制**：若 LLM 返回“证据不足”，系统会增加检索深度 $k$ 并重新尝试。
    *   **弃权感知投票（Abstention-aware Voting）**：进行 $m$ 次独立推理，在投票前过滤掉“空白/弃权”响应，防止保守预测主导结果。

### 3. 实验设计
*   **数据集/场景**：WattBot 2025 挑战赛数据集，包含 32 份关于 AI 能源消耗的技术文档（约 50 万 token），共约 300 个高精度技术问题。
*   **Benchmark**：官方评分标准（75% 数值准确度 + 15% 引用精确度 + 10% 幻觉规避）。
*   **对比方法**：
    *   **嵌入模型**：Jina v3（纯文本） vs Jina v4（多模态）。
    *   **生成模型**：GPT-5 系列、Gemini 3 Pro、Grok 4.1、Mistral-large 等。
    *   **检索策略**：纯稠密检索 vs BM25 混合检索。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号、数量或训练时长。
*   **推理支持**：文中提到 Comfy Org 资助了 LLM 推理成本。由于该框架主要基于现成的 API 和向量数据库（sqlite-vec），其核心贡献在于推理流程的设计而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的消融实验，涵盖提示词顺序、重试次数、集成规模（$n=1$ 到 $15$）、重排序策略等。
    *   **错误分析**：对 **2,583 项预测** 进行了人工/自动分类分析，识别出失败模式（如 26.8% 为不必要的弃权）。
*   **充分性评价**：实验设计非常充分且具有实战导向。通过公开和私有排行榜的双重验证，证明了系统的鲁棒性和泛化能力，避开了过拟合单一测试集的风险。

### 6. 主要结论与发现
*   **关键增益**：**提示词重排**（将上下文置于问题之前）带来了 **+80%** 的相对提升；**重试机制**在低检索深度下提升了 **+69%**。
*   **检索发现**：分层稠密检索表现强劲，加入 BM25 仅带来 +3.1pp 的微小提升，说明结构化索引已捕捉了大部分关键信息。
*   **集成效果**：集成投票（尤其是 $n=9$ 时）能显著稳定数值输出，过滤空白响应是提升得分的关键。
*   **最终成绩**：KohakuRAG 以 **0.861** 的得分获得 WattBot 2025 冠军，是唯一在两个榜单均保持第一的团队。

### 7. 优点
*   **结构化思维**：通过树状结构优雅地解决了 RAG 中常见的上下文断裂和引用丢失问题。
*   **鲁棒性设计**：重试机制和弃权感知投票极大地降低了 LLM 在面对复杂技术文档时的“胆怯”心理。
*   **实用性强**：系统设计简单且完全开源，不依赖昂贵的模型微调，易于在工业界部署。

### 8. 不足与局限
*   **解析依赖**：高度依赖基于规则的 PDF 解析，对于排版极度复杂的非标准文档可能失效。
*   **引用偏差**：错误分析显示，LLM 仍倾向于引用综述性文档而非原始数据来源（引用匹配错误占 23.6%）。
*   **成本与延迟**：多次集成推理（如 15 次）虽然提升了精度，但也成倍增加了 API 调用成本和响应延迟。
*   **领域局限**：目前仅在 AI 能源这一特定技术领域验证，跨领域的通用性有待进一步测试。

（完）
