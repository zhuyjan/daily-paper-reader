---
title: "Beyond Relevance: On the Relationship Between Retrieval and RAG Information Coverage"
title_zh: 超越相关性：论检索与 RAG 信息覆盖率之间的关系
authors: "Saron Samuel, Alexander Martin, Eugene Yang, Andrew Yates, Dawn Lawrie, Ian Soboroff, Laura Dietz, Benjamin Van Durme"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08819v2"
tags: ["query:mmrag"]
score: 10.0
evidence: 多模态基准中检索与 RAG 信息覆盖率的关系
tldr: 本研究系统探讨了检索增强生成（RAG）系统中检索质量与生成内容信息覆盖率之间的关系。通过在TREC NeuCLIR 2024、TREC RAG 2024和WikiVideo等文本及多模态基准上进行大规模实验，分析了多种检索技术栈和RAG流水线。研究发现，基于覆盖率的检索指标与生成响应中的关键信息点覆盖率在主题和系统层面均表现出强相关性，证明了检索指标可作为评估RAG性能的可靠代理。
selection_source: fresh_fetch
motivation: 尽管直觉上检索质量会影响生成效果，但目前缺乏对检索指标能否作为生成质量可靠预测指标的系统性研究。
method: 在多个文本和多模态基准上，通过分析25种检索技术栈和4种RAG流水线，利用Auto-ARGUE等框架量化评估检索与生成的相关性。
result: 实验证明基于覆盖率的检索指标与生成响应的信息覆盖率之间存在强相关性，且这种关系在检索与生成目标一致时最为显著。
conclusion: 研究结果支持将检索指标作为RAG性能的有效代理，并指出复杂的迭代流水线可能会部分解耦检索与生成之间的直接联系。
---

## 摘要
检索增强生成 (RAG) 系统将文档检索与生成模型相结合，以处理如报告生成等复杂的信息寻求任务。尽管检索质量与生成效果之间的关系看似直观，但尚未得到系统性的研究。我们研究了上游检索指标是否可以作为最终生成响应信息覆盖率的可靠早期指标。通过在两个文本 RAG 基准测试（TREC NeuCLIR 2024 和 TREC RAG 2024）以及一个多模态基准测试 (WikiVideo) 上进行实验，我们分析了跨四个 RAG 流水线和多个评估框架 (Auto-ARGUE 和 MiRAGE) 的 15 个文本检索栈和 10 个多模态检索栈。我们的研究结果表明，在主题和系统层面，基于覆盖率的检索指标与生成响应中的关键信息点 (nugget) 覆盖率之间存在强相关性。当检索目标与生成目标一致时，这种关系最为显著，尽管更复杂的迭代 RAG 流水线可能会使生成质量与检索效能部分解耦。这些发现为将检索指标作为 RAG 性能的代理指标提供了实证支持。

## Abstract
Retrieval-augmented generation (RAG) systems combine document retrieval with a generative model to address complex information seeking tasks like report generation. While the relationship between retrieval quality and generation effectiveness seems intuitive, it has not been systematically studied. We investigate whether upstream retrieval metrics can serve as reliable early indicators of the final generated response's information coverage. Through experiments across two text RAG benchmarks (TREC NeuCLIR 2024 and TREC RAG 2024) and one multimodal benchmark (WikiVideo), we analyze 15 text retrieval stacks and 10 multimodal retrieval stacks across four RAG pipelines and multiple evaluation frameworks (Auto-ARGUE and MiRAGE). Our findings demonstrate strong correlations between coverage-based retrieval metrics and nugget coverage in generated responses at both topic and system levels. This relationship holds most strongly when retrieval objectives align with generation goals, though more complex iterative RAG pipelines can partially decouple generation quality from retrieval effectiveness. These findings provide empirical support for using retrieval metrics as proxies for RAG performance.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了检索增强生成（RAG）系统中，上游检索质量与下游生成内容质量之间的量化关系。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 RAG 系统从简单的问答转向复杂的报告生成，评价标准已从单纯的“文档相关性”转向“信息覆盖率”（即生成内容是否涵盖了所有必要的知识点且无冗余）。
*   **核心问题**：上游检索指标（如 nDCG、Recall）能否作为下游生成响应质量的可靠早期预测指标？如果成立，开发者可以仅通过优化检索指标来提升 RAG 性能，从而节省昂贵的端到端生成和人工评估成本。
*   **背景**：目前缺乏系统性研究来证明检索与生成在“信息覆盖率”这一维度上的相关性，且 LLM 在处理长上下文时存在信息丢失和幻觉问题。

### 2. 方法论
*   **核心思想**：通过对比多种检索技术栈和 RAG 流水线，计算检索阶段的“关键信息点（Nugget）覆盖指标”与最终生成文本的“Nugget 覆盖率”之间的相关性。
*   **关键技术细节**：
    *   **信息点（Nuggets）**：将复杂答案拆解为原子化的信息单位。
    *   **检索指标**：采用 $\alpha$-nDCG（考虑多样性并惩罚冗余）、nDCG（基于 Nugget 标签）和子主题召回率（StRecall）。
    *   **RAG 流水线分类**：
        *   **线性流水线**：简单的“检索-生成”模式（如 GPT-Researcher 单查询版）。
        *   **复杂/迭代流水线**：包含子查询分解、自我反思和多轮检索（如 LangGraph、Bullet List）。
    *   **评估框架**：使用 Auto-ARGUE（基于问答对的 Nugget 评估）和 MiRAGE（多模态 Nugget 评估）。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **TREC NeuCLIR 2024**：多语言报告生成任务（1000万+新闻文章）。
    *   **TREC RAG 2024**：基于 MS MARCO 的复杂问答任务。
    *   **WikiVideo**：多模态任务，根据多个视频编写事件文章。
*   **对比方法**：
    *   **检索端**：15 种文本检索组合（BM25、PLAID-X、LSR/SPLADE、Qwen3 嵌入与重排序器）和 10 种多模态检索组合（CLIP、Video-ColBERT、OmniEmbed 等）。
    *   **生成端**：Llama-70B-Instruct（文本）和 Qwen2.5-VL-72B（多模态）。

### 4. 资源与算力
*   **模型使用**：明确提到了使用 Llama-70B-Instruct 作为文本 RAG 的骨干模型，Qwen2.5-VL-72B 用于多模态任务。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。但考虑到涉及 15 种检索栈在多个基准上的端到端生成，其实验规模暗示了较高的推理算力需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   文本实验涵盖了 15 种检索栈 $\times$ 4 种流水线 $\times$ 2 个数据集。
    *   多模态实验涵盖了 10 种检索栈 $\times$ 1 种流水线。
    *   进行了**主题级别（Topic-level）**和**系统级别（System-level）**的双重相关性分析。
*   **充分性与公平性**：实验设计非常充分，跨越了不同语言（中、俄、波斯、英）、不同模态（文本、视频）和不同复杂度的 RAG 架构。通过固定生成阶段的文档处理预算（50个文档）确保了对比的公平性。

### 6. 主要结论与发现
*   **强相关性**：基于 Nugget 的检索指标（尤其是 $\alpha$-nDCG）与生成响应的信息覆盖率在主题和系统层面均表现出强相关性。
*   **指标有效性**：$\alpha$-nDCG 是最稳健的代理指标，因为它能有效衡量检索结果的多样性和冗余度。
*   **流水线复杂度的影响**：简单的线性流水线对检索质量的依赖性极强；而复杂的迭代流水线（如 LangGraph）可以部分“解耦”这种关系，即通过多次尝试和反思来弥补初始检索的不足，但这也将性能瓶颈从检索模型转移到了 LLM 的自适应能力上。
*   **多模态一致性**：在视频 RAG 中，检索质量与生成内容的“事实性（Precision）”相关性更强，因为模型倾向于利用检索结果来验证其参数化知识。

### 7. 优点
*   **实证支持**：为“检索指标可作为 RAG 性能代理”这一直觉提供了坚实的实证数据。
*   **跨模态验证**：研究不仅限于文本，还扩展到了前沿的多模态视频 RAG 领域。
*   **实用性强**：为 RAG 系统的开发者提供了优化路径建议，即在开发早期应优先关注基于覆盖率的检索指标。

### 8. 不足与局限
*   **评估维度单一**：研究主要集中在“信息覆盖率”，未深入探讨流畅度、逻辑性或安全性等其他生成维度。
*   **标签偏差风险**：在 TREC RAG 2024 实验中，由于原始数据缺乏 Nugget 与文档的对应关系，作者使用了 LLM 生成的标签，这可能引入一定的模型偏见。
*   **应用限制**：结论主要适用于报告生成和复杂问答，对于极短的事实性问答（Single-hop QA），传统的相关性指标可能依然足够。

（完）
