---
title: "HiMemVLN: Enhancing Reliability of Open-Source Zero-Shot Vision-and-Language Navigation with Hierarchical Memory System"
title_zh: HiMemVLN：利用分层记忆系统增强开源零样本视觉语言导航的可靠性
authors: "Kailin Lyu, Kangyi Wu, Pengna Li, Xiuyu Hu, Qingyi Si, Cui Miao, Ning Yang, Zihang Wang, Long Xiao, Lianyu Hu, Jingyuan Sun, Ce Hao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14807v1"
tags: ["query:multi-modal-memory"]
score: 10.0
evidence: 视觉语言导航的层级记忆系统
tldr: 针对开源大模型在零样本视觉语言导航（VLN）中存在的“导航失忆”问题，本文提出了HiMemVLN框架。该框架通过引入分层记忆系统，增强了多模态大模型的视觉感知召回和长期定位能力，有效缓解了导航过程中的遗忘与定位偏差。实验证明，HiMemVLN在模拟和真实环境中的表现达到了现有开源最先进方法的近两倍，显著提升了开源导航智能体的可靠性。
selection_source: fresh_fetch
motivation: 开源大模型在零样本导航中存在严重的“导航失忆”现象，导致其性能远逊于闭源模型且难以处理长期定位任务。
method: 提出了一种名为HiMemVLN的框架，通过在多模态大模型中构建分层记忆系统来增强视觉感知召回和空间定位能力。
result: 实验结果显示，HiMemVLN在多种环境下的导航性能接近开源SOTA方法的两倍，显著缩小了与闭源模型的差距。
conclusion: 通过引入分层记忆机制，可以有效解决开源模型在复杂导航任务中的失忆问题，大幅提升其零样本导航的成功率与可靠性。
---

## 摘要
基于大语言模型（LLM）的智能体在视觉语言导航（VLN）任务中展现出令人印象深刻的零样本性能。然而，大多数零样本方法主要依赖闭源 LLM 作为导航器，这面临着高昂的 token 成本和潜在的数据泄露风险。最近的研究尝试通过结合开源 LLM 与时空思维链（CoT）框架来解决这一问题，但与闭源模型相比仍有很大差距。在这项工作中，我们通过对导航过程的详细分析，发现了一个关键问题：导航失忆症（Navigation Amnesia）。这一问题导致了导航失败，并扩大了开源与闭源方法之间的差距。为了解决这个问题，我们提出了 HiMemVLN，它将分层记忆系统集成到多模态大模型中，以增强视觉感知召回和长期定位，从而缓解失忆问题并提高智能体的导航性能。在模拟和真实环境中的广泛实验表明，HiMemVLN 的性能几乎是现有开源最先进方法的两倍。代码可在 https://github.com/lvkailin0118/HiMemVLN 获取。

## Abstract
LLM-based agents have demonstrated impressive zero-shot performance in vision-language navigation (VLN) tasks. However, most zero-shot methods primarily rely on closed-source LLMs as navigators, which face challenges related to high token costs and potential data leakage risks. Recent efforts have attempted to address this by using open-source LLMs combined with a spatiotemporal CoT framework, but they still fall far short compared to closed-source models. In this work, we identify a critical issue, Navigation Amnesia, through a detailed analysis of the navigation process. This issue leads to navigation failures and amplifies the gap between open-source and closed-source methods. To address this, we propose HiMemVLN, which incorporates a Hierarchical Memory System into a multimodal large model to enhance visual perception recall and long-term localization, mitigating the amnesia issue and improving the agent's navigation performance. Extensive experiments in both simulated and real-world environments demonstrate that HiMemVLN achieves nearly twice the performance of the open-source state-of-the-art method. The code is available at https://github.com/lvkailin0118/HiMemVLN.

---

## 论文详细总结（自动生成）

以下是对论文《HiMemVLN: Enhancing Reliability of Open-Source Zero-Shot Vision-and-Language Navigation with Hierarchical Memory System》的结构化深入总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：开源大语言模型（LLM）在零样本视觉语言导航（VLN）任务中表现远逊于闭源模型（如 GPT-4o），且存在严重的“导航失忆症”（Navigation Amnesia）。
*   **背景**：
    *   **闭源模型缺陷**：依赖云端 API 导致高成本、高延迟，且存在隐私泄露风险（需上传实时环境图像）。
    *   **开源模型痛点**：在连续环境中，开源模型难以维持长期的状态估计和任务进度感知。
    *   **导航失忆症**：表现为**短期感知缺失**（无法识别重复访问的场景，导致陷入局部循环）和**长期规划漂移**（随着步骤增加，逐渐忘记全局指令约束，偏离预定路线）。

### 2. 方法论：HiMemVLN 框架
该方法受神经生物学“海马体记忆索引理论”启发，构建了一个分层记忆系统：

*   **短期定位器系统（Short-Term Localer）**：
    *   **视觉图记忆（Visual Graph Memory）**：构建一个在线更新的拓扑图，节点存储由 CLIP 提取的视觉特征。
    *   **定位与更新**：通过余弦相似度匹配当前观测与历史节点。若相似度高于动态阈值则判定为“重访”，并利用动量更新机制稳定节点表示。
    *   **软约束注入**：将重访统计和方向优先级转化为文本提示词，引导 MLLM 优先探索未访问区域，抑制无效循环。
*   **长期全局器系统（Long-Term Globaler）**：
    *   **全局模式提取**：在任务开始时提取指令中的主方向、目标地标和导航模式。
    *   **原点追踪（Origin Tracking）**：显式维护“来向”（CameFrom）变量，增强智能体的方向感。
    *   **反射推理**：在每一步决策时，将全局目标与近期轨迹摘要结合，校准局部动作，防止长期任务执行中的意图漂移。
*   **决策流程**：感知（路标预测） -> 记忆检索与注入 -> MLLM 推理（如 Qwen2-VL） -> 低级动作执行。

### 3. 实验设计
*   **模拟环境**：使用 Habitat 模拟器，在 MP3D 数据集的 val-unseen 划分上进行 R2R-CE（连续环境导航）任务测试。
*   **真实环境**：部署在 Unitree Go2W 轮足机器人上，在咖啡馆、实验室走廊、图书馆等真实室内场景进行 20 组复杂指令测试。
*   **对比方法**：
    *   **监督学习方法**：CMA, RecBERT, ETPNav 等。
    *   **闭源零样本方法**：基于 GPT-4/GPT-4o 的 NavGPT, SmartWay, Open-Nav-GPT4。
    *   **开源零样本方法**：Open-Nav (Llama3.1/Qwen2)。
*   **评估指标**：成功率 (SR)、成功加权路径长度 (SPL)、导航误差 (NE)、Oracle 成功率 (OSR) 等。

### 4. 资源与算力
*   **硬件设备**：
    *   **模拟实验**：使用 vLLM 框架部署。
    *   **真实环境**：机器人搭载 Intel RealSense D435i 摄像头；计算端使用配备**双 NVIDIA L40 GPU** 的工作站进行推理。
*   **模型配置**：测试了 Qwen2-VL-72B（4-bit 量化）、InternVL-3.5-8B、LLaVA-OV-1.5-8B 和 Qwen2.5-VL-7B。
*   **训练时长**：由于是零样本（Zero-shot）方法，**无需针对导航任务进行微调训练**，主要开销在于推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：模拟环境测试了 100 个 Episode；真实环境测试了 20 组涵盖简单到复杂的指令。
*   **消融实验**：详细对比了“无 MLLM”、“无短期定位器”、“无长期全局器”以及“全系统结合”的效果，验证了各组件的必要性。
*   **模型横评**：对比了四种主流开源多模态大模型作为导航器的表现。
*   **评价**：实验设计较为全面，涵盖了从仿真到实体的迁移验证，对比基准涵盖了监督学习与零样本学习的 SOTA 方法，结果具有客观性。

### 6. 主要结论与发现
*   **性能飞跃**：HiMemVLN 在 R2R-CE 上的成功率（SR）达到 30%，相比之前的开源 SOTA 方法（Open-Nav, 16%）提升了近一倍。
*   **缩小差距**：该方法显著缩小了开源模型与闭源模型（如 SmartWay 的 29% SR）之间的差距，甚至在部分指标上实现超越。
*   **解决失忆**：可视化结果证明，分层记忆系统能有效消除局部循环行为，并使智能体在长距离导航中保持意图一致性。

### 7. 优点（亮点）
*   **认知启发**：将神经科学的记忆理论转化为可实现的工程架构，逻辑清晰。
*   **隐私与部署友好**：证明了通过结构化设计，开源模型可以在不依赖云端 API 的情况下实现高可靠导航。
*   **即插即用**：作为一种训练无关（Training-free）的框架，可以轻松适配不同的开源 MLLM。
*   **视觉-语义双重增强**：短期靠视觉特征匹配，长期靠语义意图对齐，互补性强。

### 8. 不足与局限
*   **与监督学习仍有差距**：虽然在零样本领域领先，但相比于在海量数据上微调的监督学习模型（如 BEVBert 的 60% SR），性能仍有较大提升空间。
*   **实时性挑战**：使用 72B 参数级别的模型进行实时导航，对本地算力（如双 L40）要求较高，在小型嵌入式平台上部署仍有难度。
*   **真实环境样本量**：真实世界实验仅 20 组指令，虽然具有代表性，但统计显著性略弱于大规模仿真测试。

（完）
