---
title: "InViC: Intent-aware Visual Cues for Medical Visual Question Answering"
title_zh: InViC：面向医疗视觉问答的意图感知视觉提示
authors: "Zhisong Wang, Ziyang Chen, Zanting Ye, Hongze Zhu, Yefeng Zheng, Yong Xia"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16372v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 用于医疗视觉问答的意图感知视觉线索
tldr: 针对医疗视觉问答（Med-VQA）中模型过度依赖语言先验而忽视视觉证据的问题，本文提出了一种轻量级插件框架 InViC。该框架通过提取与问题意图相关的视觉提示（Cue Tokens），并结合两阶段瓶颈注意力微调策略，强制模型关注关键视觉信息。在多个公开数据集上的实验证明，InViC 显著提升了 Med-VQA 的准确性和可靠性。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在医疗问答中常出现“捷径回答”现象，即过度依赖文本偏见而忽略了对临床诊断至关重要的细微视觉特征。
method: 提出 InViC 框架，利用 CTE 模块提取问题相关的视觉提示，并通过两阶段瓶颈注意力掩码策略引导模型深度融合视觉证据。
result: 在 VQA-RAD、SLAKE 和 ImageCLEF 等主流数据集上，InViC 的表现均优于零样本推理和标准的 LoRA 微调方法。
conclusion: 通过引入意图感知的视觉提示和瓶颈训练策略，可以有效减少模型对语言先验的依赖，提升医疗视觉问答的可信度。
---

## 摘要
医疗视觉问答（Med-VQA）旨在根据医学图像回答临床相关问题。然而，现有的多模态大语言模型（MLLMs）往往表现出“捷径回答”行为，即通过利用语言先验或数据集偏差生成看似合理的回答，而对视觉证据的关注不足。这种行为损害了临床可靠性，尤其是在细微的影像发现具有决定性意义时。我们提出了一个名为“意图感知视觉提示”（InViC）的轻量级插件框架，旨在显式增强医疗 VQA 中基于图像的答案生成。InViC 引入了一个提示标记提取（CTE）模块，将密集的视觉标记提炼为一组紧凑的 K 个以问题为条件的提示标记，这些标记作为结构化的视觉中间体注入 LLM 解码器，以促进与意图对齐的视觉证据。为了防止视觉信息被绕过，我们进一步设计了一种带有提示瓶颈注意力掩码的两阶段微调策略。在第一阶段，我们使用注意力掩码来阻断 LLM 对原始视觉特征的直接观察，从而使所有视觉证据都通过提示路径传输。在第二阶段，恢复标准因果注意力，以训练 LLM 共同利用视觉标记和提示标记。我们在多个代表性 MLLM 上，针对三个公开的 Med-VQA 基准测试（VQA-RAD、SLAKE 和 ImageCLEF VQA-Med 2019）对 InViC 进行了评估。InViC 相比零样本推理和标准 LoRA 微调均有持续提升，证明了带有瓶颈训练的意图感知视觉提示是提高可信 Med-VQA 的一种实用且有效的策略。

## Abstract
Medical visual question answering (Med-VQA) aims to answer clinically relevant questions grounded in medical images. However, existing multimodal large language models (MLLMs) often exhibit shortcut answering, producing plausible responses by exploiting language priors or dataset biases while insufficiently attending to visual evidence. This behavior undermines clinical reliability, especially when subtle imaging findings are decisive. We propose a lightweight plug-in framework, termed Intent-aware Visual Cues (InViC), to explicitly enhance image-based answer generation in medical VQA. InViC introduces a Cue Tokens Extraction (CTE) module that distills dense visual tokens into a compact set of K question-conditioned cue tokens, which serve as structured visual intermediaries injected into the LLM decoder to promote intent-aligned visual evidence. To discourage bypassing of visual information, we further design a two-stage fine-tuning strategy with a cue-bottleneck attention mask. In Stage I, we employ an attention mask to block the LLM's direct view of raw visual features, thereby funneling all visual evidence through the cue pathway. In Stage II, standard causal attention is restored to train the LLM to jointly exploit the visual and cue tokens. We evaluate InViC on three public Med-VQA benchmarks (VQA-RAD, SLAKE, and ImageCLEF VQA-Med 2019) across multiple representative MLLMs. InViC consistently improves over zero-shot inference and standard LoRA fine-tuning, demonstrating that intent-aware visual cues with bottlenecked training is a practical and effective strategy for improving trustworthy Med-VQA.

---

## 论文详细总结（自动生成）

### 论文总结：InViC - 面向医疗视觉问答的意图感知视觉提示

#### 1. 核心问题与研究动机
*   **核心问题**：医疗视觉问答（Med-VQA）模型中普遍存在的**“捷径回答”（Shortcut Answering）**现象。
*   **背景与动机**：现有的多模态大语言模型（MLLM）在处理医学影像时，往往过度依赖文本先验（如问题的固定格式或数据集中的语言偏差），而忽视了对临床诊断至关重要的细微视觉证据。这种行为在临床应用中极具风险。作者认为，现有的去偏方法（如数据增强或提示工程）未能从架构上强制约束视觉信息的流动，因此需要一种显式的机制来确保模型必须通过视觉证据来生成答案。

#### 2. 核心方法论
论文提出了一种名为 **InViC (Intent-aware Visual Cues)** 的轻量级插件框架，核心包含两个部分：

*   **提示标记提取模块 (CTE Module)**：
    *   **初始化**：利用可学习的种子槽位（Seed Slots）结合问题的池化表示进行初始化，使提示标记具备初步的意图感知。
    *   **跨模态检索**：通过交叉注意力机制（Cross-Attention），让提示标记先后与文本标记和视觉标记进行交互，从而从密集的视觉特征中提炼出与问题相关的紧凑视觉证据（K个Cue Tokens）。
    *   **残差校准**：采用类似 Adapter 的结构和门控机制对提取的提示标记进行稳定化处理。
*   **两阶段瓶颈训练策略**：
    *   **第一阶段（瓶颈训练）**：引入一种特殊的注意力掩码（Attention Mask），**阻断** LLM 解码器直接观察原始视觉标记的路径。此时，视觉信息只能通过 CTE 模块生成的 Cue Tokens 传递给 LLM，强制模型学习如何利用这些视觉线索。
    *   **第二阶段（全上下文微调）**：恢复标准的因果注意力掩码，让模型在保留第一阶段学到的视觉路径基础上，协同利用原始视觉特征和提示标记进行最终优化。

#### 3. 实验设计
*   **数据集**：
    *   **VQA-RAD**：放射学数据集，包含解剖、属性和异常等临床问题。
    *   **SLAKE**：涵盖 CT、MRI、X射线的多模态双语（中英）数据集。
    *   **ImageCLEF VQA-Med 2019**：专注于开放式医学问答的基准测试。
*   **对比方法**：
    *   **闭源模型**：GPT-4o-mini 等。
    *   **医疗专用模型**：Hulu-Med、Lingshu-7B。
    *   **通用 MLLM 基座**：Qwen3-VL (4B/8B)、InternVL3.5 (4B/8B)。
    *   **微调基准**：标准的 LoRA 微调。
*   **评估指标**：准确率（Accuracy）、召回率（Recall）、BLEU 分数，并引入 GPT-4o-mini 作为评委处理开放式问题。

#### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **8 张 NVIDIA GeForce RTX 4090 GPU** 的服务器。
*   **训练细节**：
    *   优化器：AdamW。
    *   Batch Size：8。
    *   第一阶段：训练 2 个 Epoch（仅训练 CTE 模块，冻结主干和 LoRA）。
    *   第二阶段：训练 3 个 Epoch（联合训练 CTE 和 LoRA）。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，涵盖了从 4B 到 8B 参数规模的多种模型架构。
*   **消融实验**：
    *   验证了“两阶段训练”相对于“仅第二阶段训练”的优越性。
    *   对比了“可学习标记”与“意图感知提示标记（CTE）”的效果。
    *   探讨了提示标记数量 $K$（4, 8, 16, 32）对性能的影响。
*   **评价**：实验设计较为全面，通过跨模型、跨数据集的对比验证了框架的泛化能力，消融实验逻辑严密，能够客观证明各组件的有效性。

#### 6. 主要结论与发现
*   **性能提升**：InViC 在所有测试基准上均显著优于零样本推理和标准的 LoRA 微调。例如，在 SLAKE 数据集上，Qwen3-VL-4B 的准确率从 0.826（LoRA）提升至 0.849。
*   **瓶颈机制的必要性**：第一阶段的瓶颈掩码对于缓解“捷径回答”至关重要，它迫使模型建立起从视觉线索到答案的实质性联系。
*   **最优参数**：实验发现 $K=16$ 是提示标记数量的最佳平衡点，过多会导致冗余，过少则信息承载不足。

#### 7. 优点与亮点
*   **架构创新**：通过“瓶颈掩码”从物理路径上限制信息流，相比单纯的损失函数约束，更能有效解决语言偏见问题。
*   **轻量化与通用性**：作为插件式框架，InViC 易于集成到各种现有的 MLLM 中，且增加的参数量较少。
*   **意图对齐**：CTE 模块通过问题引导视觉特征提取，使模型生成的视觉线索具有高度的针对性。

#### 8. 不足与局限
*   **超参数敏感性**：提示标记的数量 $K$ 需要针对不同任务进行微调，缺乏自适应调整机制。
*   **计算开销**：虽然 CTE 模块轻量，但在推理阶段仍增加了额外的交叉注意力计算步骤。
*   **覆盖范围**：实验主要集中在放射学相关的 VQA 任务，对于病理学或其他更复杂的临床推理场景（如长文本报告生成）的适用性尚待验证。
*   **解释性局限**：虽然提出了 Cue Tokens，但这些 Token 内部编码的具体视觉语义仍缺乏直观的可解释性分析。

（完）
