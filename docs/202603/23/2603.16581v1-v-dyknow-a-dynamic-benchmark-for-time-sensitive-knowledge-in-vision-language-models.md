---
title: "V-DyKnow: A Dynamic Benchmark for Time-Sensitive Knowledge in Vision Language Models"
title_zh: V-DyKnow：视觉语言模型中时效性知识的动态基准
authors: "Seyed Mahed Mousavi, Christian Moiola, Massimo Rizzoli, Simone Alghisi, Giuseppe Riccardi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16581v1"
tags: ["query:mmrag"]
score: 10.0
evidence: 评估多模态RAG方法在时效性知识上的有效性
tldr: 视觉语言模型（VLM）通常在静态数据上训练，难以应对现实世界中随时间变化的动态知识。本文提出了V-DyKnow，一个专门用于评估VLM中时效性事实知识的动态基准。通过该基准，研究者分析了模型在不同模态下的可靠性、知识编辑与RAG方法的有效性，以及预测过时的根源。研究发现VLM普遍存在知识滞后问题，且现有更新方法在跨模态一致性上表现不佳，为未来构建动态更新的VLM提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的VLM训练和评估多基于静态数据，忽略了现实世界事实的时效性，导致模型输出容易过时。
method: 提出了V-DyKnow基准，通过多模态输入评估模型对动态知识的掌握程度，并测试了知识编辑和RAG等更新技术的表现。
result: 实验显示VLM常输出过时事实，视觉模态的可靠性低于文本，且现有的对齐和更新方法难以实现跨模态的知识同步。
conclusion: 当前VLM在获取和更新跨模态时效性知识方面存在根本性局限，亟需开发更有效的动态知识维护机制。
---

## 摘要
视觉语言模型（VLMs）是在包含图像和文本的文档数据快照上进行训练的。它们的训练数据和评估基准通常是静态的，隐含地将事实知识视为时间不变的。然而，现实世界的事实本质上具有时效性，并受不规则和周期性变化的影响，导致模型预测变得过时。我们提出了 V-DyKnow，这是一个用于评估 VLM 中时效性事实知识的视觉动态知识基准。利用 V-DyKnow，我们对闭源和开源 VLM 进行了基准测试，并分析了：a) 模型响应在不同模态和输入扰动下的可靠性（正确性和一致性）；b) 知识编辑和多模态 RAG 方法在跨模态知识更新中的有效性；c) 通过数据和机制分析探究过时预测的来源。我们的结果表明，VLM 经常输出过时的事实，反映了（预）训练阶段使用的过时快照。即使实体被正确识别，事实可靠性也会从文本刺激向视觉刺激退化。此外，现有的对齐方法无法在不同模态间一致地更新模型的知识。总之，这些发现突显了当前 VLM 在跨模态获取和更新时效性知识方面的根本局限性。我们发布了该基准、代码和评估数据。

## Abstract
Vision-Language Models (VLMs) are trained on data snapshots of documents, including images and texts. Their training data and evaluation benchmarks are typically static, implicitly treating factual knowledge as time-invariant. However, real-world facts are intrinsically time-sensitive and subject to erratic and periodic changes, causing model predictions to become outdated. We present V-DyKnow, a Visual Dynamic Knowledge benchmark for evaluating time-sensitive factual knowledge in VLMs. Using V-DyKnow, we benchmark closed- and open-source VLMs and analyze a) the reliability (correctness and consistency) of model responses across modalities and input perturbations; b) the efficacy of knowledge editing and multi-modal RAG methods for knowledge updates across modalities; and c) the sources of outdated predictions, through data and mechanistic analysis. Our results show that VLMs frequently output outdated facts, reflecting outdated snapshots used in the (pre-)training phase. Factual reliability degrades from textual to visual stimuli, even when entities are correctly recognized. Besides, existing alignment approaches fail to consistently update the models' knowledge across modalities. Together, these findings highlight fundamental limitations in how current VLMs acquire and update time-sensitive knowledge across modalities. We release the benchmark, code, and evaluation data.

---

## 论文详细总结（自动生成）

以下是对论文《V-DyKnow: A Dynamic Benchmark for Time-Sensitive Knowledge in Vision Language Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）通常在静态的数据快照上进行预训练，而现实世界的事实（如公司CEO、运动员所属俱乐部、国家领导人）是随时间动态变化的。
*   **研究动机**：现有的评估基准大多是静态的，无法衡量模型在面对时效性知识（Time-Sensitive Knowledge）时的表现。这导致模型往往输出过时的信息，且在视觉刺激（如看到Logo或头像）下的事实检索能力与文本刺激下存在显著差异。
*   **整体含义**：本文旨在通过建立动态基准 V-DyKnow，系统性地评估 VLM 处理动态事实的能力，并探讨现有知识更新技术（如知识编辑和 RAG）在多模态场景下的有效性。

### 2. 论文提出的方法论
*   **核心思想**：利用 Wikidata 这一持续更新的知识图谱，构建一个能够根据推理时间动态验证事实正确性的基准。
*   **关键技术细节**：
    *   **事实表示**：采用 `(主体, 属性, 取值)` 三元组，并关联 Wikidata 中的时间有效区间（Validity Intervals）。
    *   **多模态提示设计**：为每个主体设计配对的视觉提示（如国徽/国旗、运动员头像、组织Logo）和文本提示（直接提及名称），以对比不同模态下的知识召回。
    *   **评估策略**：引入“上界策略”（Upper-Bound Strategy），通过三种语义相同但表述略异的提示进行测试，取最佳结果以减少模型对特定提示词的敏感性。
    *   **错误分类**：将模型响应细分为“正确”（当前有效）、“过时”（历史上有效但现在无效）和“无关”（从未有效）。
    *   **辅助任务**：设计了“视觉实体识别”任务，以区分模型错误是由于“看不出是谁”还是“不知道相关事实”。

### 3. 实验设计
*   **数据集/场景**：包含 139 个时效性事实，涵盖 47 个国家（领导人）、28 名运动员（所属俱乐部）和 22 个组织（CEO/主席）。
*   **Benchmark**：V-DyKnow（基于 Wikidata 实时数据）。
*   **对比模型**：
    *   **开源模型**：LLaVA-1.5, LLaVA-OneVision, PaliGemma 2, Molmo, Qwen2-VL, Qwen2.5-VL, InternVL3.5（多为 7B/8B 规模）。
    *   **闭源模型**：GPT-4, GPT-5（作为性能上界参考）。
*   **对比方法**：
    *   **知识编辑**：GRACE（适配器内存）、WISE（双内存架构）、IKE（上下文学习）。
    *   **多模态 RAG**：对比“检索到的文档”与“金标准文档”作为上下文的效果。

### 4. 资源与算力
*   **推理算力**：开源模型的本地推理在单张 **NVIDIA RTX 3090 (24GB)** 上执行，使用 `bfloat16` 精度。
*   **编辑/训练算力**：知识编辑方法（WISE, GRACE）的训练在单张 **NVIDIA A100 (80GB)** 上完成。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到使用了 EasyEdit 框架的默认配置。

### 5. 实验数量与充分性
*   **实验规模**：对 9 种 VLM 及其对应的基座 LLM 进行了全面测试。
*   **分析维度**：
    *   **跨模态对比**：视觉 vs. 文本提示。
    *   **一致性分析**：不同提示词下的输出稳定性。
    *   **更新有效性**：5 种不同更新方法的对比。
    *   **深入分析**：包括数据时间区间近似、预训练数据关联分析、以及基于层级概率的机械解释性分析。
*   **充分性评价**：实验设计较为全面，不仅关注宏观分数，还深入探讨了模型内部层级的变化和训练数据的影响，实验结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **知识滞后普遍存在**：即使是最新发布的模型，其内部知识也大多停留在 2020 年之前，频繁输出过时事实。
*   **模态鸿沟**：视觉提示下的事实检索准确率显著低于文本提示，即使模型能正确识别图像中的实体，也往往无法提取出最新的事实。
*   **更新方法局限**：
    *   传统的知识编辑方法（GRACE, WISE）在 VLM 上效果极差（成功率低于 6%），且容易引发幻觉。
    *   IKE（上下文学习）和 RAG 效果较好，但 RAG 仍会受到模型内部过时先验知识的干扰。
*   **机制发现**：机械解释性分析显示，知识编辑往往只影响模型的最后几层，且难以彻底消除深层嵌入的过时关联。

### 7. 优点
*   **动态性**：解决了静态基准无法评估时效性知识的痛点，使评估结果与现实世界同步。
*   **细粒度分析**：通过区分“过时”和“无关”错误，提供了比单纯“准确率”更深入的洞察。
*   **多维度解构**：通过实体识别辅助任务和机械解释性分析，成功定位了 VLM 知识失效的具体环节。

### 8. 不足与局限
*   **样本规模较小**：139 个事实虽然涵盖典型类别，但样本量相对较小，且主要集中在高频知名实体（Head Entities）。
*   **模型规模限制**：受限于算力，开源模型仅测试了 7B 左右的规模，未探讨更大参数量模型（如 70B+）是否存在相同问题。
*   **副作用评估缺失**：在进行知识编辑实验时，未评估编辑操作是否对模型原有的通用能力（如逻辑推理、其他不相关事实）产生负面影响（即“副作用”）。
*   **应用限制**：目前仅针对特定类型的三元组事实，对于更复杂的时效性事件或推理任务覆盖不足。

（完）
