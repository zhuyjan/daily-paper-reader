---
title: "AndroTMem: From Interaction Trajectories to Anchored Memory in Long-Horizon GUI Agents"
title_zh: AndroTMem：从交互轨迹到长程 GUI 智能体中的锚定记忆
authors: "Yibo Shi, Jungang Li, Linghao Zhang, Zihao Dongfang, Biao Wu, Sicheng Tao, Yibo Yan, Chenxi Qin, Weiting Liu, Zhixin Lin, Hanqian Li, Yu Huang, Song Dai, Yonghua Hei, Yue Ding, Xiang Li, Shikang Wang, Chengdong Xu, Jingqi Liu, Xueying Ma, Zhiwen Zheng, Xiaofei Zhang, Bincheng Wang, Nichen Yang, Jie Wu, Lihua Tian, Chen Li, Xuming Hu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18429v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 长程GUI智能体的锚定记忆
tldr: "本研究针对长程 Android GUI 智能体在交互记忆方面的不足，提出了 AndroTMem 诊断框架及包含 1,069 个任务的基准测试。研究发现长程任务的失败主因是记忆失效而非感知错误。为此，作者提出了锚点状态记忆（ASM）方法，通过因果链接的中间状态锚点来精简记忆并保留关键依赖。实验表明，ASM 在 12 个智能体上均显著提升了任务完成率，有效解决了长程交互中的记忆瓶颈问题。"
selection_source: fresh_fetch
motivation: 现有的 GUI 智能体记忆机制在处理长程任务时，要么因全量回放引入过多噪声，要么因摘要化丢失了关键的因果依赖信息。
method: 建立了 AndroTMem-Bench 基准测试，并提出锚点状态记忆（ASM），利用因果关联的中间状态锚点进行目标导向的检索与决策。
result: "ASM 在 12 种智能体上将任务完成率提升了 5% 至 30.16%，显著优于全序列回放和摘要基准。"
conclusion: 结构化且具备因果关联的锚点记忆是提升长程 GUI 智能体可靠性和决策准确性的有效途径。
---

## 摘要
长程 GUI 智能体是迈向现实世界部署的关键一步，然而在现有范式下，有效的交互记忆仍未得到充分探索。重放完整的交互序列存在冗余并会放大噪声，而摘要往往会抹除关键的依赖信息和可追溯性。我们提出了 AndroTMem，这是一个针对长程 Android GUI 智能体中锚定记忆的诊断框架。其核心基准测试 AndroTMem-Bench 包含 1,069 个任务，共计 34,473 个交互步骤（每个任务平均 32.1 步，最高 65 步）。我们使用任务完成率（TCR）来评估智能体，重点关注那些需要传递关键中间状态才能完成的任务；AndroTMem-Bench 旨在强化步骤间的强因果依赖，使稀疏但必不可少的中间状态对后续操作起决定性作用，从而将交互记忆作为评估的核心。在开源和闭源 GUI 智能体中，我们观察到一个一致的模式：随着交互序列变长，性能下降主要源于任务内的记忆失效，而非孤立的感知错误或局部动作失误。在此诊断的启发下，我们提出了锚定状态记忆（ASM），它将交互序列表示为一组具有因果联系的紧凑中间状态锚点，以实现面向子目标的检索和归因感知的决策。在多种设置和 12 个评估的 GUI 智能体中，ASM 始终优于全序列重放和基于摘要的基准方法，将 TCR 提高了 5%-30.16%，将 AMS 提高了 4.93%-24.66%，这表明锚定且结构化的记忆能有效缓解长程 GUI 任务中的交互记忆瓶颈。代码、基准测试及相关资源已在 https://github.com/CVC2233/AndroTMem 公开。

## Abstract
Long-horizon GUI agents are a key step toward real-world deployment, yet effective interaction memory under prevailing paradigms remains under-explored. Replaying full interaction sequences is redundant and amplifies noise, while summaries often erase dependency-critical information and traceability. We present AndroTMem, a diagnostic framework for anchored memory in long-horizon Android GUI agents. Its core benchmark, AndroTMem-Bench, comprises 1,069 tasks with 34,473 interaction steps (avg. 32.1 per task, max. 65). We evaluate agents with TCR (Task Complete Rate), focusing on tasks whose completion requires carrying forward critical intermediate state; AndroTMem-Bench is designed to enforce strong step-to-step causal dependencies, making sparse yet essential intermediate states decisive for downstream actions and centering interaction memory in evaluation. Across open- and closed-source GUI agents, we observe a consistent pattern: as interaction sequences grow longer, performance drops are driven mainly by within-task memory failures, not isolated perception errors or local action mistakes. Guided by this diagnosis, we propose Anchored State Memory (ASM), which represents interaction sequences as a compact set of causally linked intermediate-state anchors to enable subgoal-targeted retrieval and attribution-aware decision making. Across multiple settings and 12 evaluated GUI agents, ASM consistently outperforms full-sequence replay and summary-based baselines, improving TCR by 5%-30.16% and AMS by 4.93%-24.66%, indicating that anchored, structured memory effectively mitigates the interaction-memory bottleneck in long-horizon GUI tasks. The code, benchmark, and related resources are publicly available at [https://github.com/CVC2233/AndroTMem](https://github.com/CVC2233/AndroTMem).

---

## 论文详细总结（自动生成）

### 论文总结：AndroTMem - 长程 GUI 智能体中的锚定记忆

#### 1. 核心问题与研究动机
*   **研究背景**：多模态大语言模型（MLLM）正从静态图像理解转向复杂的 GUI 交互。然而，在处理长程（Long-Horizon）任务时，智能体表现会剧烈下降。
*   **核心问题**：现有的交互记忆机制存在两难困境：**全量回放（Raw Replay）**虽然完整但冗余且噪声大；**粗略摘要（Coarse Summary）**虽然简洁但容易丢失关键的因果依赖信息（如提取的验证码、中间价格等）。
*   **研究动机**：诊断长程任务失败的根本原因，并提出一种既能保留关键因果状态又能保持简洁的记忆机制。

#### 2. 方法论：锚定状态记忆（ASM）
*   **核心思想**：将交互历史转化为一组**“状态锚点（State Anchors）”**，这些锚点不仅记录发生了什么，还通过**因果链接（Causal Links）**记录了步骤间的依赖关系。
*   **关键技术细节**：
    *   **锚点类型**：定义了 6 种锚点：子目标达成（Subgoal）、状态变更（State Change）、因果依赖（Dependency）、异常处理（Exception）、上下文信息（Context Info）和完成标识（Finish）。
    *   **因果结构**：每个锚点包含类型、内容、证据（截图/坐标）和链接（指向前序锚点）。
    *   **算法流程（检索-决策-更新）**：
        1.  **检索**：根据当前 UI 状态和指令，从记忆库中检索相关的锚点。
        2.  **决策**：结合当前 UI 和检索到的锚点预测下一步动作。
        3.  **更新**：执行动作后，分析是否产生了新的语义状态，并据此更新记忆库和因果链接。

#### 3. 实验设计
*   **基准测试（AndroTMem-Bench）**：
    *   包含 **1,069 个任务**，涉及 50 个常用 App。
    *   总计 **34,473 个交互步骤**，平均每任务 32.1 步，最长 65 步。
    *   任务分为 8 类意图（如比价决策、预订、社交分享等），强调跨 App 协作和强因果依赖。
*   **对比方法**：评估了 12 个主流智能体，包括：
    *   **闭源模型**：GPT-4o, GPT-5, Gemini-2.5/3 系列。
    *   **开源模型**：Qwen2.5-VL, UI-TARS, AgentCPM-GUI 等。
    *   **多智能体框架**：Mobile-Agent-E, COLA。
*   **评估指标**：动作匹配得分（AMS）和基于锚点的任务完成率（TCR）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号、数量或时长）。
*   **原因分析**：该研究侧重于推理侧的记忆机制设计和基准测试评估，许多实验基于闭源模型的 API 调用或开源模型的零样本/少样本推理，而非大规模从头训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 种不同的智能体架构上进行了广泛测试，涵盖了从 10 步到 60 步以上的不同长度区间。
*   **消融实验**：对比了“无历史”、“全量历史”、“粗略摘要”与“ASM”四种模式，验证了 ASM 的有效性。
*   **充分性评价**：实验设计较为**充分且客观**。通过跨模型、跨任务类型、跨长度的对比，证明了 ASM 的改进具有普适性，而非依赖于特定模型的特性。

#### 6. 主要结论与发现
*   **记忆是瓶颈**：诊断显示，长程任务的性能下降主要由**任务内记忆失效**驱动，而非感知或局部动作错误。
*   **ASM 的优越性**：ASM 在所有 12 个智能体上均实现了性能提升，TCR 提升幅度达 5%~30.16%。
*   **效率平衡**：ASM 在 Token 消耗和推理时间上与摘要法相当，远低于全量回放，实现了效率与效果的平衡。
*   **失败模式分析**：识别了长程任务中的五大记忆失效模式：状态丢失、状态误绑定、上下文漂移、未验证进度和中断处理失败。

#### 7. 优点与亮点
*   **诊断性视角**：不仅提出了方法，还深入分析了长程 GUI 任务失败的底层逻辑。
*   **结构化记忆设计**：引入因果链接（Causal Links），使智能体能够像人类一样“回溯”关键决策点。
*   **高质量数据集**：AndroTMem-Bench 的步骤长度和因果复杂度显著超过了 AITW、AndroidWorld 等现有基准。

#### 8. 不足与局限
*   **跨会话限制**：目前仅关注单次任务内的长程记忆，尚未涉及跨天、跨周的超长程跨会话记忆。
*   **环境动态性**：基准测试基于相对稳定的 App 状态，对 UI 频繁变动、网络波动等随机环境因素的覆盖有限。
*   **应用限制**：ASM 的锚点生成目前依赖于模型自身的推理能力，对于推理能力较弱的小模型，生成高质量锚点本身可能成为新的挑战。

（完）
