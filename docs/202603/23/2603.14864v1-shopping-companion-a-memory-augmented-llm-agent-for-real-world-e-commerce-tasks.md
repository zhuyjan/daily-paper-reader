---
title: "Shopping Companion: A Memory-Augmented LLM Agent for Real-World E-Commerce Tasks"
title_zh: Shopping Companion：一种用于真实世界电子商务任务的记忆增强型 LLM 智能体
authors: "Zijian Yu, Kejun Xiao, Huaipeng Zhao, Tao Luo, Xiaoyi Zeng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14864v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 用于长期偏好的记忆增强LLM智能体
tldr: 本研究针对电商领域中LLM智能体难以捕捉长期用户偏好的问题，提出了Shopping Companion框架。该框架通过统一内存检索与购物辅助，并结合双重奖励强化学习策略，解决了现有系统偏好识别与任务执行脱节的挑战。同时，研究发布了一个包含120万真实商品、侧重长期记忆的新基准测试。实验证明，该轻量级模型在偏好捕捉和任务成功率上优于GPT-4等强基线模型。
selection_source: fresh_fetch
motivation: 现有电商LLM智能体缺乏评估长期偏好的基准，且偏好识别与购物辅助功能相互分离，导致无法进行端到端优化。
method: 提出了一个集成内存检索、购物辅助及用户干预的统一框架，并采用带有工具级奖励的双重奖励强化学习策略进行训练。
result: "实验显示即使是顶尖模型在复杂基准上的成功率也低于70%，而Shopping Companion在偏好捕捉和任务表现上均显著优于强基线。"
conclusion: 统一的设计架构和针对性的强化学习策略能有效提升LLM智能体在处理真实世界复杂电商任务时的性能。
---

## 摘要
在电子商务领域，大语言模型（LLM）智能体在推荐、预算编制和捆绑销售等购物任务中展现出巨大潜力，其中从长期对话中准确捕捉用户偏好至关重要。然而，实现这一潜力面临两大挑战：（1）缺乏用于评估长期偏好感知购物任务的基准测试；（2）由于现有设计将偏好识别与购物辅助视为独立组件，导致缺乏端到端优化。在本文中，我们引入了一个具有长期记忆设置的新型基准测试，涵盖了涉及 120 万个真实世界产品的两项购物任务，并提出了 Shopping Companion，这是一个统一的框架，在支持用户干预的同时，共同处理记忆检索和购物辅助。为了训练这些能力，我们开发了一种带有工具级奖励的双重奖励强化学习策略，以处理多轮交互中固有的稀疏且不连续的奖励。实验结果表明，即使是最先进的模型（如 GPT-5）在我们的基准测试中成功率也低于 70%，这突显了该领域的重大挑战。值得注意的是，我们使用 Shopping Companion 训练的轻量级 LLM 始终优于强大的基准模型，实现了更好的偏好捕捉和任务性能，验证了我们统一设计的有效性。

## Abstract
In e-commerce, LLM agents show promise for shopping tasks such as recommendations, budgeting, and bundle deals, where accurately capturing user preferences from long-term conversations is critical. However, two challenges hinder realizing this potential: (1) the absence of benchmarks for evaluating long-term preference-aware shopping tasks, and (2) the lack of end-to-end optimization due to existing designs that treat preference identification and shopping assistance as separate components. In this paper, we introduce a novel benchmark with a long-term memory setup, spanning two shopping tasks over 1.2 million real-world products, and propose Shopping Companion, a unified framework that jointly tackles memory retrieval and shopping assistance while supporting user intervention. To train such capabilities, we develop a dual-reward reinforcement learning strategy with tool-wise rewards to handle the sparse and discontinuous rewards inherent in multi-turn interactions. Experimental results demonstrate that even state-of-the-art models (such as GPT-5) achieve success rates under 70% on our benchmark, highlighting the significant challenges in this domain. Notably, our lightweight LLM, trained with Shopping Companion, consistently outperforms strong baselines, achieving better preference capture and task performance, which validates the effectiveness of our unified design.

---

## 论文详细总结（自动生成）

以下是对论文《Shopping Companion: A Memory-Augmented LLM Agent for Real-World E-Commerce Tasks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在电子商务领域，LLM 智能体需要处理复杂的购物任务（如推荐、预算管理、捆绑销售）。这些任务的成功高度依赖于从长期对话历史中捕捉用户的隐式偏好（如品牌偏好、尺码历史、审美风格）。
*   **研究动机**：现有的研究存在两个主要瓶颈：
    1.  **缺乏基准测试**：现有的电商基准测试要么缺乏长期记忆（LTM）评估，要么缺乏多轮交互和真实任务的结合。
    2.  **系统设计脱节**：现有的系统通常将“偏好识别”和“购物辅助”视为独立模块，缺乏端到端的联合优化，导致整体效率和准确率受限。
*   **核心目标**：建立一个包含长期记忆和真实购物场景的基准，并提出一个能够联合优化记忆检索与任务执行的统一智能体框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **Shopping Companion** 框架，其核心在于将购物流程拆分为两个有机结合的阶段，并通过强化学习（RL）进行端到端训练。
*   **两阶段智能体架构**：
    *   **阶段 1（偏好识别）**：调用记忆工具（如 `mem_search`, `mem_view`）从历史对话中提取偏好，并呈现给用户确认，支持用户干预（纠错或补充）。
    *   **阶段 2（购物辅助）**：基于确认的偏好，调用商品工具（如 `product_search`, `product_view`）在 120 万商品的数据库中检索并验证约束条件，最终给出建议。
*   **双重奖励强化学习（Dual-Reward RL）**：
    *   采用 **GRPO** 算法，针对两个阶段分别设计奖励函数 $R_1$（偏好覆盖率）和 $R_2$（任务成功率，包括商品匹配、预算符合等）。
*   **工具级奖励（Tool-Wise Reward）**：
    *   为了解决多轮交互中奖励稀疏和不连续的问题，论文引入了针对每个工具调用的中间奖励。例如，如果 `mem_search` 检索到了包含关键偏好的历史会话，则给予正向反馈。这显著改善了信用分配（Credit Assignment）。

### 3. 实验设计
*   **数据集与基准**：
    *   构建了一个包含 **120 万个真实世界产品** 的沙盒环境。
    *   **任务类型**：单品推荐（Single Product）和加购优惠/捆绑销售（Add-on Deals）。
    *   **数据规模**：1,000 条指令（800 训练，200 测试），每条指令配有 15-50 轮包含隐式偏好的历史对话。
*   **对比方法**：
    *   **闭源模型**：GPT-5, GPT-4.1, GPT-4o, Qwen3-Max。
    *   **开源模型**：Qwen3 系列（4B, 30B, 80B）。
    *   **消融变体**：SFT 基础模型、仅含双重奖励的 RL、含双重+工具级奖励的 RL。
*   **评估指标**：偏好提取准确率（Acc.）和任务最终成功率（Succ.）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：使用 LLaMA-Factory 框架，对 Qwen3-4B 进行 LoRA 微调，训练 3 个 epoch。
    *   **RL 阶段**：使用 VeRL 框架，采用 GRPO 算法，训练约 2.6 个 epoch。
    *   **推理**：最大输出长度设为 32,768 token，每条轨迹最多允许 20 轮智能体交互。

### 5. 实验数量与充分性
*   **实验规模**：针对两类核心任务进行了详尽测试，并进行了多维度的消融实验。
*   **充分性分析**：
    *   **消融实验**：对比了单阶段 vs 两阶段、不同程度的用户干预（无提示、低提示、高提示）以及不同奖励机制的效果。
    *   **客观性**：引入了“LLM-as-Judge”（GPT-5 作为裁判），并通过人工校验验证了裁判的一致性（>90%），确保了评估的客观性。
    *   **公平性**：所有开源和闭源模型均在相同的检索工具和环境下进行测试。

### 6. 主要结论与发现
*   **任务难度极高**：即使是 GPT-5，在复杂的“加购优惠”任务中成功率也仅为 54%，平均成功率低于 70%，证明了该基准的挑战性。
*   **联合优化的优越性**：经过 Shopping Companion 训练的 **4B 轻量级模型**，在偏好捕捉（90% Acc.）和任务成功率上显著超过了参数量更大的开源模型，甚至在某些指标上逼近 GPT-5。
*   **工具级奖励的作用**：引入工具级奖励不仅提升了成功率，还显著缩短了响应长度并减少了冗余的工具调用，使智能体行为更高效。
*   **用户干预的价值**：实验证明，即使是简单的用户反馈也能大幅提升最终任务的成功率。

### 7. 优点与亮点
*   **端到端闭环**：首次将长期记忆检索与下游购物决策在同一个策略梯度框架下进行联合优化。
*   **创新的奖励设计**：通过工具级奖励解决了长程任务中的信用分配难题，为工具增强型智能体（Tool-augmented Agents）提供了参考。
*   **高价值基准**：提供了包含 120 万真实商品和复杂历史对话的大规模数据集，填补了电商 LTM 评估的空白。

### 8. 不足与局限
*   **复杂约束处理仍有空间**：在涉及预算和多商品组合的“加购优惠”任务中，所有模型的表现依然不尽如人意，说明组合优化能力仍是瓶颈。
*   **工具奖励的通用性**：工具级奖励的设计依赖于特定领域的先验知识（如知道哪些历史会话是“正确的”），在缺乏标注的通用领域可能难以迁移。
*   **隐私风险**：长期记忆涉及用户历史，论文虽提及了隐私保护，但在实际部署中如何平衡个性化与数据脱敏仍是挑战。

（完）
