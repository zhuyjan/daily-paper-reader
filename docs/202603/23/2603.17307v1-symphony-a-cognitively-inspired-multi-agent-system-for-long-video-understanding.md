---
title: "Symphony: A Cognitively-Inspired Multi-Agent System for Long-Video Understanding"
title_zh: Symphony：一种受认知启发的长视频理解多智能体系统
authors: "Haiyang Yan, Hongyun Zhou, Peng Xu, Xiaoxue Feng, Mengyi Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17307v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 利用类人认知和推理进行长视频理解的多智能体系统。
tldr: 针对长视频理解（LVU）中信息密度高和时间跨度大的挑战，本文提出了受人类认知启发的名为 Symphony 的多智能体系统。该系统通过细粒度的任务分解、增强反思的深度推理协作机制，以及基于视觉语言模型（VLM）的定位方法，有效提升了对复杂长视频的推理和关键片段定位能力。实验证明 Symphony 在多个主流长视频基准测试中达到 SOTA 水平，显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的多模态大模型智能体在处理长视频时，简单的任务分解和嵌入式检索难以应对复杂的长链推理及隐性意图定位。
method: 提出 Symphony 系统，通过模拟人类认知模式进行细粒度任务分解，并结合反思机制的深度推理协作与基于 VLM 的视频片段定位。
result: "在 LVBench、LongVideoBench、VideoMME 和 MLVU 等多个基准测试中取得领先，其中在 LVBench 上比现有最优方法提升了 5.0%。"
conclusion: Symphony 证明了结合认知启发式分解与增强型定位机制能显著提升长视频理解任务的性能。
---

## 摘要
尽管多模态大语言模型（MLLM）智能体发展迅速且应用广泛，但在处理具有高信息密度和长跨度时间特征的长视频理解（LVU）任务时仍面临挑战。近期关于 LVU 智能体的研究表明，简单的任务分解和协作机制不足以应对长链推理任务。此外，通过基于嵌入的检索直接缩减时间上下文可能会丢失复杂问题的关键信息。在本文中，我们提出了 Symphony，一个旨在缓解这些局限性的多智能体系统。通过模拟人类认知模式，Symphony 将 LVU 分解为细粒度的子任务，并结合了由反思增强的深度推理协作机制，从而有效提升了推理能力。此外，Symphony 提供了一种基于视觉语言模型（VLM）的定位方法，用于分析 LVU 任务并评估视频片段的相关性，这显著增强了定位具有隐性意图和长跨度时间复杂问题的能力。实验结果表明，Symphony 在 LVBench、LongVideoBench、VideoMME 和 MLVU 上均达到了最先进的性能，在 LVBench 上比之前的最优方法提升了 5.0%。代码可在 https://github.com/Haiyang0226/Symphony 获取。

## Abstract
Despite rapid developments and widespread applications of MLLM agents, they still struggle with long-form video understanding (LVU) tasks, which are characterized by high information density and extended temporal spans. Recent research on LVU agents demonstrates that simple task decomposition and collaboration mechanisms are insufficient for long-chain reasoning tasks. Moreover, directly reducing the time context through embedding-based retrieval may lose key information of complex problems. In this paper, we propose Symphony, a multi-agent system, to alleviate these limitations. By emulating human cognition patterns, Symphony decomposes LVU into fine-grained subtasks and incorporates a deep reasoning collaboration mechanism enhanced by reflection, effectively improving the reasoning capability. Additionally, Symphony provides a VLM-based grounding approach to analyze LVU tasks and assess the relevance of video segments, which significantly enhances the ability to locate complex problems with implicit intentions and large temporal spans. Experimental results show that Symphony achieves state-of-the-art performance on LVBench, LongVideoBench, VideoMME, and MLVU, with a 5.0% improvement over the prior state-of-the-art method on LVBench. Code is available at https://github.com/Haiyang0226/Symphony.

---

## 论文详细总结（自动生成）

以下是对论文《Symphony: A Cognitively-Inspired Multi-Agent System for Long-Video Understanding》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：长视频理解（LVU）面临信息密度极高和时间跨度巨大的挑战。现有的多模态大模型（MLLM）在处理长序列时，指令遵循和推理能力会随输入长度增加而下降。
*   **现有局限**：
    *   **单智能体瓶颈**：当任务复杂度超过模型推理极限时，单智能体倾向于采取简单行动而非深度推理。
    *   **检索精度不足**：传统的基于嵌入（Embedding）或 CLIP 的检索（RAG）难以处理具有隐性意图、抽象概念或多步推理要求的复杂查询，容易丢失关键信息。
*   **核心目标**：通过模拟人类认知模式，构建一个能够进行细粒度任务分解、深度推理协作及精准视频定位的多智能体系统。

### 2. 方法论
Symphony 采用了一种受认知心理学启发的**能力维度解耦范式**，将系统分为五个专业智能体：
*   **核心架构**：
    *   **规划智能体 (Planning Agent)**：作为中央协调者，负责全局任务规划、多智能体调度及信息整合。
    *   **定位智能体 (Grounding Agent)**：模拟人类的“注意”功能。它利用 LLM 对复杂问题进行语义扩展和歧义消除，并结合 VLM（视觉语言模型）对视频片段进行相关性评分（1-4分），而非仅依赖简单的向量相似度。
    *   **视觉感知智能体 (Visual Perception Agent)**：提供全局摘要、帧检查（细粒度分析）和多片段对比分析工具。
    *   **字幕智能体 (Subtitle Agent)**：负责文本语义分析，如实体识别、情感分析和主题建模。
    *   **反思智能体 (Reflection Agent)**：模拟“自我监督”。它评估推理链的逻辑严密性，若发现证据不足或逻辑矛盾，则生成批评意见（Critique）引导规划智能体重新探索。
*   **协作机制**：采用类似 Actor-Critic 的动态协作框架。规划智能体生成子任务，专业智能体执行并返回观察结果，反思智能体验证结果。这一过程循环迭代，直到获得高置信度的答案。

### 3. 实验设计
*   **数据集**：
    *   **LVBench**：平均时长 68 分钟，侧重长程时间定位和综合推理。
    *   **LongVideoBench**：评估细粒度信息检索和跨片段逻辑推理。
    *   **Video-MME (Long)**：多领域时空复合推理。
    *   **MLVU**：涵盖推理、摘要、识别等 9 类任务。
*   **对比方法**：
    *   **商业模型**：GPT-4o, Gemini-1.5-Pro, OpenAI o3。
    *   **开源模型**：InternVL2.5-78B, Qwen2.5-VL-72B。
    *   **智能体框架**：VideoTree, VideoAgent, DVD, VideoDeepResearch, LvAgent 等。

### 4. 资源与算力
*   **模型配置**：
    *   推理与规划：**DeepSeek R1**。
    *   字幕分析：**DeepSeek V3**。
    *   视觉感知与定位：**Doubao Seed 1.6 VL**。
*   **输入规格**：最大 40 帧，分辨率上限 720p。
*   **算力说明**：文中未明确提及具体的 GPU 硬件数量（如 A100 数量），但提到每个查询在 LVBench 上的平均成本为 **0.124 美元**（消耗约 22 万个 Token），相比使用 OpenAI o3 的 DVD 系统降低了 41.8% 的成本。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流长视频基准测试上进行了全面评估。
*   **消融实验**：非常充分。分别验证了规划、字幕、视觉感知、反思四个智能体的独立贡献；对比了 VLM 评分与 CLIP 检索的定位效果；测试了不同采样频率（FPS）和片段长度对性能的影响。
*   **客观性**：通过在不同基础模型（如 Qwen2.5-VL, Seed 1.6 VL）上运行 Symphony 框架，证明了性能提升源于系统架构设计而非单一模型的强大。

### 6. 主要结论与发现
*   **性能领先**：Symphony 在所有基准测试中均达到 SOTA。在最难的 LVBench 上，比之前的最优方法（DVD）提升了 **5.0%**。
*   **定位是关键**：基于 VLM 的相关性评分显著优于传统的 CLIP 检索，尤其是在处理抽象概念（如“贿赂”）和长时序动作时。
*   **反思机制的价值**：独立的反思智能体比单智能体自我修正更有效，能显著减少“过度自信”导致的错误，提升推理可靠性。
*   **投票策略**：引入多实例投票（Symphony-Vote）可进一步提升性能 2-4 个百分点。

### 7. 优点
*   **认知启发式设计**：将复杂任务按功能维度（而非简单的模态维度）解耦，降低了单个智能体的认知负荷。
*   **强大的定位能力**：通过 LLM 扩展查询意图，解决了长视频中“大海捞针”式的定位难题。
*   **动态协作**：非线性、基于反馈的推理路径比传统的线性流水线更具灵活性。
*   **成本效益**：通过合理的智能体分工，利用开源高性能模型达到了超越闭源商业模型的性能。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮智能体交互和 VLM 逐片段评分，系统的端到端推理时间可能较长（定位过程需并行处理多个片段）。
*   **依赖基础模型**：系统的上限仍受限于所使用的 LLM 推理能力和 VLM 感知能力。
*   **复杂场景覆盖**：虽然在基准测试表现优异，但在极度嘈杂或缺乏字幕的实时监控等极端现实场景下的鲁棒性仍待验证。

（完）
