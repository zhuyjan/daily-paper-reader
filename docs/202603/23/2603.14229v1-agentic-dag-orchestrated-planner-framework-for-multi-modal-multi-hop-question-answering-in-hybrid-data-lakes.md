---
title: "Agentic DAG-Orchestrated Planner Framework for Multi-Modal, Multi-Hop Question Answering in Hybrid Data Lakes"
title_zh: 混合数据湖中用于多模态、多跳问答的代理式 DAG 编排规划器框架
authors: "Kirushikesh D B, Manish Kesarwani, Nishtha Madaan, Sameep Mehta, Aldrin Dennis, Siddarth Ajay, Rakesh B R, Renu Rajagopal, Sudheesh Kairali"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14229v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 基于RAG系统的混合数据湖多模态多跳问答。
tldr: 针对混合数据湖中多跳问答效率低且缺乏逻辑推理的问题，本文提出 A.DOT 框架。该框架将自然语言查询编译为有向无环图（DAG）执行计划，实现了跨结构化与非结构化数据源的并行子查询分解与协同检索。通过结合模式感知推理、语义验证及缓存机制，A.DOT 在提升问答准确性的同时提供了可追溯的证据链，显著增强了系统在复杂异构环境下的处理能力与用户信任度。
selection_source: fresh_fetch
motivation: 现有的 RAG 系统在处理混合数据湖的多跳推理时，往往依赖简单的暴力检索且缺乏跨源逻辑编排，导致检索效率低下且难以处理复杂的关联查询。
method: 提出 A.DOT 框架，利用 Agent 将查询分解为 DAG 执行计划，协调异构数据源的并发检索、中间结果路由及结构化验证。
result: "在基准数据集上，该框架相比基准模型在正确性上提升了 14.8%，在完整性上提升了 10.7%，并显著降低了响应延迟。"
conclusion: A.DOT 证明了基于 DAG 的 Agent 编排能有效解决复杂混合数据环境下的多跳问答难题，并为系统提供了良好的可解释性与数据溯源能力。
---

## 摘要
企业对结合了结构化表格和非结构化文档的混合数据湖上的自然语言 (NL) 问答需求日益增长。目前部署的解决方案（包括基于 RAG 的系统）通常依赖于从每个存储库进行暴力检索并进行事后合并。这种方法效率低下且存在信息遗漏，更关键的是，它们缺乏对多跳推理的显式支持，即查询被分解为连续的步骤（跳），这些步骤可能在结构化和非结构化源之间来回穿梭。我们提出了代理式 DAG 编排 Transformer (A.DOT) 规划器，这是一个用于多模态、多跳问答的框架，它将用户自然语言查询编译为跨越结构化和非结构化存储的有向无环图 (DAG) 执行计划。该系统将查询分解为可并行的子查询，结合了模式感知推理，并在执行前应用结构和语义验证。执行引擎遵循生成的 DAG 计划，协调跨异构源的并发检索，将中间输出路由到依赖的子查询，并严格按照计划的逻辑依赖关系合并最终结果。先进的缓存机制结合了释义感知的模板匹配，使系统能够检测等效查询并重用先前的 DAG 执行计划以实现快速重新执行，而 DataOps 系统则处理验证反馈或执行错误。所提出的框架不仅提高了准确性并降低了延迟，还产生了显式的证据链，从而能够验证检索内容、追踪数据血缘，并增强用户对系统输出的信任。在基准数据集上，A.DOT 在正确性上比基准模型实现了 14.8% 的绝对提升，在完整性上提升了 10.7%。

## Abstract
Enterprises increasingly need natural language (NL) question answering over hybrid data lakes that combine structured tables and unstructured documents. Current deployed solutions, including RAG-based systems, typically rely on brute-force retrieval from each store and post-hoc merging. Such approaches are inefficient and leaky, and more critically, they lack explicit support for multi-hop reasoning, where a query is decomposed into successive steps (hops) that may traverse back and forth between structured and unstructured sources. We present Agentic DAG-Orchestrated Transformer (A.DOT) Planner, a framework for multi-modal, multi-hop question answering, that compiles user NL queries into directed acyclic graph (DAG) execution plans spanning both structured and unstructured stores. The system decomposes queries into parallelizable sub-queries, incorporates schema-aware reasoning, and applies both structural and semantic validation before execution. The execution engine adheres to the generated DAG plan to coordinate concurrent retrieval across heterogeneous sources, route intermediate outputs to dependent sub-queries, and merge final results in strict accordance with the plan's logical dependencies. Advanced caching mechanisms, incorporating paraphrase-aware template matching, enable the system to detect equivalent queries and reuse prior DAG execution plans for rapid re-execution, while the DataOps System addresses validation feedback or execution errors. The proposed framework not only improves accuracy and latency, but also produces explicit evidence trails, enabling verification of retrieved content, tracing of data lineage, and fostering user trust in the system's outputs. On benchmark dataset, A.DOT achieves 14.8% absolute gain in correctness and 10.7% in completeness over baselines.

---

## 论文详细总结（自动生成）

### 混合数据湖中多模态多跳问答的代理式 DAG 编排规划器框架 (A.DOT) 总结

#### 1. 核心问题与整体含义
*   **研究背景**：企业数据通常以混合形式存在，既有结构化表（SQL），也有非结构化文档（PDF/文本）。
*   **核心问题**：现有的检索增强生成（RAG）系统在处理此类混合数据时，通常采用“暴力检索”后合并的方法。这种方式在处理**多跳推理**（例如：先从表中查到某人 ID，再根据 ID 去文档中查其背景）时效率低下、容易产生幻觉，且缺乏数据溯源能力，难以满足企业级应用对准确性和可审计性的要求。
*   **研究意义**：本文提出了 **A.DOT (Agentic DAG-Orchestrated Transformer)** 框架，旨在通过将自然语言查询转化为有向无环图（DAG）执行计划，实现跨模态的高效、精准推理。

#### 2. 方法论
*   **核心思想**：将复杂的自然语言查询分解为一系列原子子查询，并以 DAG 形式组织，通过代理（Agent）协调不同数据源的检索与逻辑依赖。
*   **关键技术细节**：
    *   **计划生成器 (Plan Generator)**：利用大模型（LLM）单次生成包含多个子任务的 DAG，每个节点标注了工具（SQL 或向量数据库）、查询内容及变量标签。
    *   **双重验证机制**：
        *   **结构验证**：检查模式（Schema）合规性、变量引用是否闭环、是否存在循环依赖。
        *   **语义验证**：确保分解后的子任务与原始查询意图一致。
    *   **DataOps 系统**：包含诊断器、推荐器、修复器和重规划器。当验证失败或执行出错时，系统会自动尝试局部修复或全局重规划。
    *   **变量绑定与精简**：在节点间传递数据时，仅传递必要的键值（如 ID），而非全量数据，以节省上下文窗口并减少数据泄露风险。
    *   **并行执行引擎**：根据 DAG 的拓扑顺序，并行执行无依赖的节点，缩短端到端延迟。
    *   **计划缓存**：支持精确匹配、模板匹配和语义匹配，允许重用已验证的执行计划。

#### 3. 实验设计
*   **数据集**：使用 **HybridQA** 基准数据集（包含 3,466 个开发集样本），该数据集要求结合维基百科表格及其关联文本进行多跳推理。
*   **场景构建**：将 HybridQA 重新构建为混合存储架构，表格存入 SQL 数据库，文本存入 Milvus 向量数据库，并建立跨模态关联。
*   **对比方法 (Baselines)**：
    *   **Standard RAG**：简单的检索后生成。
    *   **ReAct**：基于思维链和工具调用的迭代推理框架。
    *   **LLM Compiler**：另一种基于 DAG 的并行函数调用框架。
*   **评估指标**：使用 Unitxt 库，通过 LLM-as-a-judge（Mistral Large 2）评估“答案正确性”和“答案完整性”。

#### 4. 资源与算力
*   **模型使用**：核心框架（计划生成、执行、DataOps）使用 **LLaMA-3 70B**。
*   **评估模型**：使用 **Mistral Large 2** 作为评估裁判。
*   **算力说明**：文中未明确提及具体的 GPU 型号、数量或训练/推理的总时长，但指出系统基于 LangGraph 框架实现。

#### 5. 实验数量与充分性
*   **实验规模**：在 HybridQA 的 3,466 个样本上进行了完整测试。
*   **消融实验**：针对 500 个样本的子集，分别验证了“无计划验证器”和“无 DataOps 系统”的情况。
*   **充分性评价**：实验设计较为充分，涵盖了主流的代理框架对比，并通过消融实验证明了验证和修复机制的必要性。错误分析部分详细分类了失败案例，体现了实验的客观性。

#### 6. 主要结论与发现
*   **性能提升**：A.DOT 在正确性上比最强基准（Standard RAG）高出 **14.8%**，在完整性上高出 **10.7%**。
*   **验证的重要性**：消融实验显示，仅有验证而无 DataOps 修复会导致性能大幅下降（因为系统会直接报错停止），只有两者结合才能实现最优性能。
*   **多跳优势**：相比于 ReAct 等顺序执行框架，A.DOT 的 DAG 结构在处理复杂依赖和并行任务时更具鲁棒性和效率。

#### 7. 优点
*   **可解释性与溯源性**：通过显式的 DAG 计划和执行日志，用户可以清晰追踪每个答案的来源（数据血缘）。
*   **容错性强**：引入了 DataOps 闭环，能够自动纠正 LLM 生成计划时的语法或逻辑错误。
*   **效率优化**：并行执行和变量精简机制有效解决了长上下文和高延迟问题。
*   **企业级就绪**：设计了完善的缓存和模式感知机制，且已计划集成至 IBM Watsonx.data 产品中。

#### 8. 不足与局限
*   **模型依赖**：系统性能高度依赖于底层 LLM（如 LLaMA-3 70B）的规划能力，若模型较弱，生成的 DAG 质量会受限。
*   **多轮对话限制**：目前主要针对单次复杂查询，尚未充分扩展到多轮对话上下文。
*   **知识局限**：错误分析显示，当问题涉及模型本身缺乏的常识或外部实时信息时，系统仍可能失败。
*   **复杂度开销**：对于极简单的查询，DAG 生成和验证的开销可能高于直接检索。

（完）
