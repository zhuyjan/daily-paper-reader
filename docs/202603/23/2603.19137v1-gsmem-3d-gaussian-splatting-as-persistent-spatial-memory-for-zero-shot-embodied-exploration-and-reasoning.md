---
title: "GSMem: 3D Gaussian Splatting as Persistent Spatial Memory for Zero-Shot Embodied Exploration and Reasoning"
title_zh: GSMem：将 3D 高斯泼溅作为持久化空间记忆，用于零样本具身探索与推理
authors: "Yiren Lu, Yi Du, Disheng Liu, Yunlai Zhou, Chen Wang, Yu Yin"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19137v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 3D 高斯泼溅作为智能体的持久空间记忆
tldr: GSMem 提出将 3D 高斯泼溅（3DGS）作为具身智能体的持久空间记忆，解决了传统场景表示中观测缺失不可恢复的问题。通过 3DGS 的连续几何和外观参数化，智能体能够实现“空间回想”，从最优视角渲染新视图以供 VLM 推理。结合并行场景图与语义语言场，GSMem 实现了鲁棒的目标定位与混合探索策略，在零样本具身问答和导航任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的场景表示（如离散场景图或静态快照）缺乏事后重新观测的能力，导致初始观测中的遗漏难以恢复。
method: 利用 3DGS 构建持久空间记忆以实现空间回想，并结合并行场景图、语义语言场及混合探索策略进行目标定位与推理。
result: 在具身问答和终身导航任务中的实验表明，该框架能有效生成高保真视图供 VLM 推理，显著提升了任务成功率。
conclusion: GSMem 通过引入 3DGS 作为连续且可重观测的空间记忆，为零样本具身探索和复杂推理提供了强大的支撑。
---

## 摘要
有效的具身探索要求智能体能够随时间积累并保留空间知识。然而，现有的场景表示（如离散场景图或基于静态视图的快照）缺乏“事后可重观测性”（post-hoc re-observability）。如果初始观测遗漏了目标，由此产生的记忆缺失通常是不可恢复的。为了弥补这一差距，我们提出了 GSMem，这是一个基于 3D 高斯泼溅（3DGS）的零样本具身探索与推理框架。通过显式参数化连续几何和密集外观，3DGS 作为一种持久化空间记忆，赋予了智能体“空间回忆”（Spatial Recollection）能力：即从先前未占据的最佳视点渲染出逼真的新视图的能力。为了实现这一点，GSMem 采用了一种检索机制，同时利用并行的对象级场景图和语义级语言场。这种互补设计能够稳健地定位目标区域，使智能体能够为高保真视觉语言模型（VLM）推理“幻觉”出最佳视图。此外，我们引入了一种混合探索策略，将 VLM 驱动的语义评分与基于 3DGS 的覆盖目标相结合，平衡了任务感知探索与几何覆盖。在具身问答和终身导航方面的广泛实验证明了我们框架的稳健性和有效性。

## Abstract
Effective embodied exploration requires agents to accumulate and retain spatial knowledge over time. However, existing scene representations, such as discrete scene graphs or static view-based snapshots, lack \textit{post-hoc re-observability}. If an initial observation misses a target, the resulting memory omission is often irrecoverable. To bridge this gap, we propose \textbf{GSMem}, a zero-shot embodied exploration and reasoning framework built upon 3D Gaussian Splatting (3DGS). By explicitly parameterizing continuous geometry and dense appearance, 3DGS serves as a persistent spatial memory that endows the agent with \textit{Spatial Recollection}: the ability to render photorealistic novel views from optimal, previously unoccupied viewpoints. To operationalize this, GSMem employs a retrieval mechanism that simultaneously leverages parallel object-level scene graphs and semantic-level language fields. This complementary design robustly localizes target regions, enabling the agent to ``hallucinate'' optimal views for high-fidelity Vision-Language Model (VLM) reasoning. Furthermore, we introduce a hybrid exploration strategy that combines VLM-driven semantic scoring with a 3DGS-based coverage objective, balancing task-aware exploration with geometric coverage. Extensive experiments on embodied question answering and lifelong navigation demonstrate the robustness and effectiveness of our framework

---

## 论文详细总结（自动生成）

### GSMem：基于 3D 高斯泼溅的持久化空间记忆具身探索与推理总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
具身智能体在复杂 3D 环境中探索时，需要积累并保留空间知识。现有的场景表示方法存在局限性：
*   **对象中心化表示（如场景图）：** 依赖实时感知，一旦检测失败，记忆遗漏不可恢复。
*   **视图中心化表示（如 2D 快照）：** 具有稀疏性和视角依赖性，若初始观测角度不佳或存在遮挡，智能体无法事后获取细节。
*   **核心痛点：** 缺乏“事后可重观测性”（Post-hoc Re-observability）。
**GSMem 的动机：** 利用 3D 高斯泼溅（3DGS）构建连续、密集的辐射场作为持久空间记忆，使智能体具备“空间回想”能力，即能从从未物理到达的最优视角渲染出高保真视图，供视觉语言模型（VLM）进行精确推理。

#### 2. 论文提出的方法论
GSMem 提出了一个零样本（Zero-shot）框架，核心包含以下三个部分：
*   **3DGS 映射与在线语言场生成：**
    *   使用 RGB-D 输入增量更新 3DGS 地图，通过滑动窗口优化参数。
    *   **关键技术：** 提出一种无需优化的语言场生成方法，通过“权重一致的反向聚合”，将 2D CLIP 特征根据渲染权重直接分配给 3D 高斯点，实现语义特征的实时提升。
*   **多级检索-渲染机制：**
    *   **对象级检索：** 利用 3D 场景图（ConceptGraphs）提取候选目标。
    *   **语义级检索：** 在 3D 语言场中通过 CLIP 嵌入相似度定位感兴趣区域（ROI）。
    *   **最优视角选择：** 采用“采样-评分”范式，综合考虑可见性（Ray Marching）、投影面积和 3DGS 累积不透明度（Opacity），为 VLM 渲染出信息量最大的视图。
*   **混合探索策略：**
    *   结合 VLM 驱动的**语义相关性评分**和基于 3DGS 的**几何覆盖目标**。
    *   利用 Fisher 信息矩阵（FIM）的迹（T-optimality）来量化高斯场的不确定性，引导智能体探索信息增益最高的区域。

#### 3. 实验设计
*   **数据集/场景：** 使用了 Habitat-Matterport3D (HM3D) 场景。
*   **Benchmark：**
    1.  **Active EQA (OpenEQA)：** 包含 184 个关于物体识别、空间理解和功能推理的问题。
    2.  **Multimodal Lifelong Navigation (GOAT-Bench)：** 涉及 36 个场景，超过 2600 个子任务，评估长程交互中的记忆可靠性。
*   **对比方法：** 包括 Blind LLMs (GPT-4o)、LLM+Captions、纯 VLM (Qwen3-VL)、以及先进的具身导航方法（Explore-EQA, ConceptGraphs, 3D-Mem, TANGO, MTU3D 等）。

#### 4. 资源与算力
*   **硬件配置：** 导航框架运行在单张 **NVIDIA RTX 4090** GPU 上；VLM 推理（Qwen3-VL-8B）部署在 **H100** GPU 上。
*   **运行效率：** 在多进程设置下，每步平均处理时间约为 **1.2 秒**（包括地图更新、检索渲染和推理）。
*   **模型调用：** 实验中主要使用 GPT-4o 作为 VLM 后端。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 63 个 HM3D 场景的 184 个问答对，以及 GOAT-Bench 的大规模长程导航测试，实验样本量充足。
*   **消融实验：** 针对语言场、视角选择、扩散增强、混合探索策略以及超参数（如语义阈值 $\tau_s$、候选对象数 $K_{obj}$）进行了详细的消融研究。
*   **客观性：** 对比了多种不同类型的基线模型，并使用了 LLM-Match 和 SPL 等标准化指标，评估过程较为客观公平。

#### 6. 论文的主要结论与发现
*   **性能领先：** GSMem 在 Active EQA 和终身导航任务中均达到了 SOTA 性能，尤其在长程任务中优势更明显。
*   **记忆优势：** 3DGS 作为持久记忆，能有效弥补目标检测器的漏检和误检。
*   **视角重要性：** 通过“空间回想”生成的最优视角视图显著提升了 VLM 的推理准确率，解决了视角依赖导致的识别错误。
*   **探索效率：** 混合探索策略平衡了任务导向和环境覆盖，提高了导航效率（SPL 指标提升）。

#### 7. 优点
*   **创新性：** 首次将 3DGS 的新视图合成能力转化为具身智能体的“空间回想”记忆功能。
*   **鲁棒性：** 通过并行利用场景图和语言场，增强了对开放词汇目标的定位能力。
*   **高效性：** 提出的在线语言场生成方法无需额外优化，适合实时具身应用。
*   **视觉质量：** 引入单步扩散模型增强渲染效果，进一步提升了 VLM 的感知质量。

#### 8. 不足与局限
*   **计算开销：** 虽然达到了 1.2s/step，但对于需要极高实时响应的机器人任务，3DGS 的实时优化和 VLM 推理仍有加速空间。
*   **环境动态性：** 论文主要针对静态环境，未深入探讨 3DGS 在处理频繁变动的动态场景（如家具移动）时的记忆更新问题。
*   **依赖性：** 系统的最终表现仍高度依赖于底层 VLM（如 GPT-4o）的推理能力和 CLIP 的语义对齐精度。

（完）
