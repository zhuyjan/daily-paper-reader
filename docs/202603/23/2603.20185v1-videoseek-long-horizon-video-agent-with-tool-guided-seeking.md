---
title: "VideoSeek: Long-Horizon Video Agent with Tool-Guided Seeking"
title_zh: VideoSeek：具有工具引导寻踪能力的长时程视频智能体
authors: "Jingyang Lin, Jialian Wu, Jiang Liu, Ximeng Sun, Ze Wang, Xiaodong Yu, Jiebo Luo, Zicheng Liu, Emad Barsoum"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20185v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 具有工具引导寻踪证据收集的长程视频智能体
tldr: "VideoSeek 是一款长视频智能体，通过“思考-行动-观察”循环和多粒度工具包，利用视频逻辑流主动寻找关键证据，解决了传统密集采样导致的高计算成本问题。该方法支持查询感知探索，在显著减少帧数（最高减少93%）的同时，大幅提升了长视频推理的准确性与效率。"
selection_source: fresh_fetch
motivation: 现有的视频智能体通常依赖对密集采样帧的贪婪解析，导致在处理长视频时计算成本极高且效率低下。
method: 提出一种基于视频逻辑流的主动寻证机制，通过“思考-行动-观察”循环和多粒度工具包实现对关键信息的精准定位。
result: "在多个基准测试中表现优异，特别是在 LVBench 上比基础模型提升了 10.2 个百分点，且仅使用了 7% 的帧数。"
conclusion: 通过工具引导的主动寻证和逻辑流利用，可以在大幅降低计算开销的同时，显著增强长视频的理解与推理能力。
---

## 摘要
视频智能体模型在具有挑战性的视频-语言任务中取得了显著进展。然而，大多数智能体方法仍然严重依赖于对密集采样视频帧的贪婪解析，导致了高昂的计算成本。我们提出了 VideoSeek，这是一种长时程视频智能体，它利用视频逻辑流主动寻找对回答至关重要的证据，而不是穷举式地解析整个视频。这一见解使得模型在保持甚至提高其视频理解能力的同时，能够使用更少的帧。VideoSeek 在“思考-行动-观察”循环中运行，并配备了一个精心设计的工具包，用于收集多粒度的视频观测。这种设计实现了对累积观测结果的查询感知探索，并支持实际的视频理解与推理。在四个具有挑战性的视频理解和推理基准测试上的实验表明，VideoSeek 在使用比之前的视频智能体和独立大型多模态模型（LMMs）少得多的帧的情况下，实现了极高的准确率。值得注意的是，VideoSeek 在 LVBench 上相比其基础模型 GPT-5 实现了 10.2 个百分点的绝对提升，同时减少了 93% 的帧使用量。进一步的分析强调了利用视频逻辑流、强大的推理能力以及工具包设计的互补作用的重要性。

## Abstract
Video agentic models have advanced challenging video-language tasks. However, most agentic approaches still heavily rely on greedy parsing over densely sampled video frames, resulting in high computational cost. We present VideoSeek, a long-horizon video agent that leverages video logic flow to actively seek answer-critical evidence instead of exhaustively parsing the full video. This insight allows the model to use far fewer frames while maintaining, or even improving, its video understanding capability. VideoSeek operates in a think-act-observe loop with a well-designed toolkit for collecting multi-granular video observations. This design enables query-aware exploration over accumulated observations and supports practical video understanding and reasoning. Experiments on four challenging video understanding and reasoning benchmarks demonstrate that VideoSeek achieves strong accuracy while using far fewer frames than prior video agents and standalone LMMs. Notably, VideoSeek achieves a 10.2 absolute points improvement on LVBench over its base model, GPT-5, while using 93% fewer frames. Further analysis highlights the significance of leveraging video logic flow, strong reasoning capability, and the complementary roles of toolkit design.

---

## 论文详细总结（自动生成）

以下是对论文《VideoSeek: Long-Horizon Video Agent with Tool-Guided Seeking》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频智能体（Video Agents）在处理长视频时，通常依赖于对视频帧的**密集采样和贪婪解析**（如每秒 0.2-2 帧），这导致了极高的计算成本和冗余。
*   **研究动机**：作者观察到，人类在回答视频问题时并不会从头到尾看完每一帧，而是利用视频的**逻辑流（时间与因果结构）**来定位关键证据。统计显示，在长视频基准测试（如 LVBench）中，超过 80% 的问题仅需查看不到 5% 的视频内容即可回答。
*   **整体含义**：VideoSeek 旨在模拟人类的寻踪行为，通过主动、有选择地寻找关键证据，在大幅降低计算开销（减少帧处理量）的同时，提升长时程视频理解和复杂推理的能力。

### 2. 方法论
VideoSeek 采用 **“思考-行动-观察”（Think-Act-Observe）** 的循环机制，核心思想是将视频理解建模为一个长时程的证据搜寻过程。

*   **核心流程**：
    1.  **思考（Thought）**：LLM（如 GPT-5）根据用户查询和已积累的观测轨迹，分析当前证据是否充分，并规划下一步行动。
    2.  **行动（Act）**：从工具包中选择一个工具并设定参数（如时间区间、查询词）。
    3.  **观察（Observe）**：工具返回新的视频观测结果，并将其存入推理轨迹 $\tau$ 中。
*   **多粒度工具包（Toolkit）**：
    *   **`<overview>`（全局概览）**：均匀采样极少量帧（如 16 帧），构建视频的粗略故事线。
    *   **`<skim>`（快速浏览）**：对较长的候选片段进行低成本扫描，用于定位感兴趣的区域。
    *   **`<focus>`（精细检查）**：以高帧率（1 FPS）检查短片段，捕捉关键细节（如文字、面部、微小动作）。
*   **数学表达**：将任务分解为轨迹预测 $p(\tau | X, Q)$ 和基于轨迹的答案生成 $p(Y | X, Q, \tau)$，通过查询感知的探索实现高效推理。

### 3. 实验设计
*   **数据集/场景**：
    *   **长视频理解**：LVBench（平均 67 分钟）、Video-MME（长视频子集）、LongVideoBench。
    *   **复杂视频推理**：Video-Holmes（基于悬疑短片的复杂推理）。
*   **Benchmark 与对比方法**：
    *   **独立 LMMs**：GPT-4o, Gemini 1.5 Pro, Gemini 2.0 Flash, Qwen2.5-VL-72B, 以及基础模型 GPT-5。
    *   **视频智能体**：VideoAgent, VideoTree, DrVideo, VCA, MR. Video, DVD。

### 4. 资源与算力
*   **算力说明**：论文未明确给出训练所需的具体 GPU 型号和训练时长，因为 VideoSeek 主要是一个**基于 API 的推理框架**（Model-agnostic Agentic Framework）。
*   **推理成本**：附录中对比了 Token 消耗和运行时间。VideoSeek 在 LVBench 上平均消耗约 49K-57K Tokens，虽然由于多轮交互导致运行时间（约 90-136s）长于单次解析的 GPT-5（约 66-71s），但其处理的**帧数显著减少**（从 384 帧降至最低 15.9 帧）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流长视频/推理基准上进行了全面测试，涵盖了有无字幕两种场景。
*   **消融实验**：
    *   验证了三个工具（Overview, Skim, Focus）的必要性，发现 `<overview>` 对捕捉逻辑流最关键。
    *   测试了不同基础模型（GPT-5, o4-mini, GPT-4.1）作为“大脑”的影响，证明了强大的推理能力是智能体有效性的前提。
    *   分析了采样预算参数 $\alpha$ 的影响。
*   **客观性**：实验对比了当前最强的闭源和开源模型，并提供了详细的案例分析（Case Study），展示了智能体如何逐步缩小搜索范围，实验设计较为客观公平。

### 6. 主要结论与发现
*   **高效性**：VideoSeek 在 LVBench 上比基础模型 GPT-5 准确率提升 **10.2%**，但使用的帧数减少了 **93%**。
*   **性能领先**：在所有测试的基准测试中，VideoSeek 均达到了 SOTA 或同等水平，且帧效率通常是第二名智能体的 **30-300 倍**。
*   **逻辑流的重要性**：引入字幕后，帧使用量进一步大幅下降，证明了文本故事线能极大地引导智能体跳过冗余内容。
*   **推理驱动**：性能提升不仅源于视觉证据，更源于智能体在长时程交互中的中间推理过程。

### 7. 优点（亮点）
*   **拟人化策略**：成功将人类“先看大概、再找细节”的直觉转化为可操作的工具化流程。
*   **极高的帧效率**：解决了长视频处理中“计算量随视频长度线性增长”的痛点，极具实用价值。
*   **灵活性**：作为模型无关的框架，可以随底层 LMM 能力的提升而自动增强。

### 8. 不足与局限
*   **意外事件检测**：对于那些不符合逻辑流、具有高度局部性或突发性的事件（如异常检测），逻辑驱动的寻踪可能不如全量扫描有效。
*   **推理延迟**：由于采用多轮“思考-行动”循环，推理的实时性（Latency）可能不如单次解析的方法，且依赖于 API 的响应速度。
*   **对“大脑”要求高**：实验显示，如果底层 LLM 推理能力不足（如 GPT-4.1），智能体会表现出过度自信或判断失误，导致性能大幅下降。

（完）
