---
title: "Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents"
title_zh: Memori：一种用于高效、上下文感知 LLM 智能体的持久化记忆层
authors: "Luiz C. Borro, Luiz A. B. Macarini, Gordon Tindall, Michael Montero, Adam B. Struck"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19935v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 上下文感知 LLM 智能体的持久记忆层
tldr: "Memori 是一个与模型无关的持久化记忆层，旨在解决 LLM 智能体在多轮对话中因注入原始对话导致的成本高和性能下降问题。它通过将非结构化对话转化为语义三元组和摘要，实现了高效的上下文感知。在 LoCoMo 基准测试中，Memori 以极低的 Token 消耗实现了 81.95% 的准确率，显著降低了部署成本并提升了推理效率。"
selection_source: fresh_fetch
motivation: 现有的 LLM 记忆方案通常依赖于注入大量原始对话，导致 Token 成本高昂、厂商锁定且容易造成模型性能退化。
method: 开发了一种高级增强流水线，将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确的记忆检索和推理。
result: "在 LoCoMo 基准测试中达到 81.95% 的准确率，Token 使用量仅为完整上下文的 5%，比现有竞争方案节省了 67% 的成本。"
conclusion: 研究证明有效的 LLM 记忆应依赖于结构化的数据表示而非单纯扩大上下文窗口，这为实现可扩展且低成本的智能体部署奠定了基础。
---

## 摘要
随着大语言模型（LLM）演变为自主智能体，API 层的持久化记忆对于实现跨 LLM 和多会话交互的上下文感知行为至关重要。现有方法强制绑定特定供应商，并依赖于将大量原始对话注入提示词，导致高昂的 token 成本和性能下降。我们介绍了 Memori，这是一个与 LLM 无关的持久化记忆层，它将记忆视为一个数据结构化问题。其高级增强流水线将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确检索和连贯推理。在 LoCoMo 基准测试中，Memori 达到了 81.95% 的准确率，在每次查询仅使用 1,294 个 token（约占全上下文的 5%）的情况下，性能优于现有的记忆系统。这显著降低了成本，包括比竞争方法减少了 67% 的 token 使用量，且与全上下文方法相比节省了 20 倍以上的成本。这些结果表明，LLM 智能体中的有效记忆取决于结构化表示而非更大的上下文窗口，从而实现了可扩展且具有成本效益的部署。

## Abstract
As large language models (LLMs) evolve into autonomous agents, persistent memory at the API layer is essential for enabling context-aware behavior across LLMs and multi-session interactions. Existing approaches force vendor lock-in and rely on injecting large volumes of raw conversation into prompts, leading to high token costs and degraded performance.   We introduce Memori, an LLM-agnostic persistent memory layer that treats memory as a data structuring problem. Its Advanced Augmentation pipeline converts unstructured dialogue into compact semantic triples and conversation summaries, enabling precise retrieval and coherent reasoning.   Evaluated on the LoCoMo benchmark, Memori achieves 81.95% accuracy, outperforming existing memory systems while using only 1,294 tokens per query (~5% of full context). This results in substantial cost reductions, including 67% fewer tokens than competing approaches and over 20x savings compared to full-context methods.   These results show that effective memory in LLM agents depends on structured representations instead of larger context windows, enabling scalable and cost-efficient deployment.

---

## 论文详细总结（自动生成）

### Memori：一种用于高效、上下文感知 LLM 智能体的持久化记忆层

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）向自主智能体演进，如何实现跨会话、跨模型的**持久化记忆**成为核心挑战。现有的记忆方案（如简单的 RAG 或直接注入历史对话）存在三大痛点：
*   **成本高昂**：注入大量原始对话会导致 Token 消耗剧增。
*   **性能退化（Context Rot）**：长上下文会导致模型忽略关键信息或产生幻觉。
*   **供应商锁定**：记忆往往绑定在特定模型或平台上。
论文提出 **Memori**，旨在将记忆从简单的“存储问题”转变为“**数据结构化问题**”，通过解耦的记忆层实现高效、低成本的上下文感知。

#### 2. 论文提出的方法论
Memori 的核心是其**高级增强（Advanced Augmentation）**流水线，它将非结构化的原始对话转化为结构化的知识表示：
*   **语义三元组生成（Semantic Triples）**：将对话拆解为“主语-谓语-宾语”形式的原子知识单元。这能过滤掉口语废话，实现极高的数据压缩率和检索精度。
*   **对话摘要（Conversation Summaries）**：在提取事实的同时，生成高层级的意图和叙事摘要，以保留对话的演进逻辑和时间背景。
*   **关联索引**：将每个三元组与其来源对话的摘要进行链接。检索时，系统不仅能提供精确事实，还能提供其背后的背景故事。
*   **混合检索**：结合了基于 Gemma-300 嵌入模型的向量搜索（余弦相似度）和 BM25 关键词匹配，确保检索的全面性。

#### 3. 实验设计
*   **数据集**：使用 **LoCoMo (Long Conversation Memory)** 基准测试，该测试专门评估智能体在多会话、长周期对话中的状态跟踪、时间推理和偏好提取能力。
*   **评估维度**：涵盖单跳推理（Single-Hop）、多跳推理（Multi-Hop）、时间推理（Temporal）和开放域推理（Open-Domain）。
*   **对比方法**：
    *   **主流记忆框架**：Zep、LangMem、Mem0。
    *   **性能上限**：全上下文（Full-Context）方法（将所有历史直接输入模型）。
*   **评估方式**：采用 **LLM-as-a-Judge** 模式，使用 GPT-4o-mini 作为裁判，对生成答案的准确性进行评分。

#### 4. 资源与算力
*   **模型使用**：实验中使用 **GPT-4o-mini** 作为生成模型和裁判模型；使用 **Gemma-300** 进行向量嵌入。
*   **存储与检索**：使用 **FAISS** 进行本地索引和相似度搜索。
*   **算力说明**：文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/处理的总时长。由于其主要依赖 API 调用和轻量级向量库，推测其对本地算力要求较低。

#### 5. 实验数量与充分性
*   **实验规模**：针对 LoCoMo 数据集中的 1529 个问题进行了测试（不含对抗性类别）。
*   **充分性**：Memori 的结果取自 3 轮运行的平均值，并提供了标准差，显示了结果的稳定性。
*   **客观性**：对比了当前市面上最流行的开源记忆框架，并使用了公认的第三方基准测试，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **性能领先**：Memori 在检索类系统中取得最高准确率（**81.95%**），显著优于 Zep (79.09%)、LangMem (78.05%) 和 Mem0 (62.47%)。
*   **极致能效比**：Memori 每次查询仅需 **1,294 个 Token**，仅为全上下文方法的 **5%**。
*   **成本优势**：比竞争对手 Zep 节省了 **67%** 的 Token 成本，比全上下文方法节省了 **20 倍以上**。
*   **结构化胜过规模**：实验证明，记忆的质量取决于其**结构化程度**，而非上下文窗口的大小。

#### 7. 优点
*   **架构解耦**：作为独立的记忆层，不依赖特定 LLM，具有极强的通用性。
*   **双层表示法**：通过“三元组+摘要”的组合，兼顾了事实的精确性（低噪声）和叙事的连贯性（上下文）。
*   **显著降本增效**：在保持高准确率的同时，极大地压缩了输入 Token，解决了生产环境中的成本痛点。

#### 8. 不足与局限
*   **时间推理仍有提升空间**：在时间推理（Temporal）维度上，Memori (80.37%) 略逊于 LangMem (86.92%)，说明纯三元组结构在处理复杂时间演变时仍有局限。
*   **开放域挑战**：与所有检索系统一样，在缺乏明确检索锚点的开放域问题上表现一般（63.54%）。
*   **依赖提取质量**：记忆的准确性高度依赖于“高级增强”流水线中提取模型的性能，若提取阶段遗漏信息，后续无法弥补。

（完）
