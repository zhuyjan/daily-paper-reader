---
title: "CurveStream: Boosting Streaming Video Understanding in MLLMs via Curvature-Aware Hierarchical Visual Memory Management"
title_zh: CurveStream：通过曲率感知的分层视觉记忆管理增强 MLLM 中的流式视频理解
authors: "Chao Wang, Xudong Tan, Jianjian Cao, Kangcong Li, Tao Chen"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19571v1"
tags: ["query:multi-modal-memory"]
score: 10.0
evidence: 流式视频的层次化视觉记忆管理
tldr: 针对多模态大模型在流式视频理解中面临的视觉标记爆炸和内存溢出问题，本文提出 CurveStream 框架。该方法无需训练，通过几何曲率识别特征轨迹中的关键语义转换点，并利用动态阈值实现分级视觉记忆管理。在严格的标记预算下，CurveStream 显著提升了流式视频的感知能力，在多个基准测试中刷新了 SOTA 记录。
selection_source: fresh_fetch
motivation: 现有的流式视频内存管理方法缺乏语义感知，难以捕捉关键的语义转换并维持上下文连贯性。
method: 提出一种基于曲率感知的层次化视觉记忆管理框架，利用曲率得分和动态阈值将帧自适应分配至清晰或模糊记忆状态。
result: "在 StreamingBench 和 OVOBench 等多个时间尺度基准上，该方法比基线性能提升超过 10%，达到了当前领先水平。"
conclusion: CurveStream 证明了利用几何曲率进行语义感知的视觉记忆管理是提升 MLLM 流式视频理解能力的有效且轻量化的途径。
---

## 摘要
多模态大语言模型（MLLMs）在离线视频理解方面取得了显著成功，但其在流式视频中的应用受到视觉 Token 线性爆炸的严重限制，这通常会导致显存溢出（OOM）错误或灾难性遗忘。现有的视觉保留和记忆管理方法通常依赖于均匀采样、低级物理指标或被动缓存淘汰。然而，这些策略往往缺乏内在的语义感知，可能会破坏上下文连贯性，并模糊瞬态但关键的语义转换。为了解决这些局限性，我们提出了 CurveStream，这是一个无需训练的、曲率感知的分层视觉记忆管理框架。我们的方法源于一个关键观察：连续特征轨迹上的高曲率区域与关键的全局语义转换密切相关。基于这一几何洞察，CurveStream 通过曲率评分（Curvature Score）评估实时语义强度，并集成在线 K-Sigma 动态阈值，在严格的 Token 预算下自适应地将帧路由到清晰和模糊的记忆状态。在不同时间尺度上的评估证实，CurveStream 这一轻量级框架在各自基准测试上始终产生超过 10% 的绝对性能提升（例如，在 StreamingBench 上提升 10.69%，在 OVOBench 上提升 13.58%），为流式视频感知建立了新的最先进结果。代码将发布于 https://github.com/streamingvideos/CurveStream。

## Abstract
Multimodal Large Language Models have achieved significant success in offline video understanding, yet their application to streaming videos is severely limited by the linear explosion of visual tokens, which often leads to Out-of-Memory (OOM) errors or catastrophic forgetting. Existing visual retention and memory management methods typically rely on uniform sampling, low-level physical metrics, or passive cache eviction. However, these strategies often lack intrinsic semantic awareness, potentially disrupting contextual coherence and blurring transient yet critical semantic transitions. To address these limitations, we propose CurveStream, a training-free, curvature-aware hierarchical visual memory management framework. Our approach is motivated by the key observation that high-curvature regions along continuous feature trajectories closely align with critical global semantic transitions. Based on this geometric insight, CurveStream evaluates real-time semantic intensity via a Curvature Score and integrates an online K-Sigma dynamic threshold to adaptively route frames into clear and fuzzy memory states under a strict token budget. Evaluations across diverse temporal scales confirm that this lightweight framework, CurveStream, consistently yields absolute performance gains of over 10% (e.g., 10.69% on StreamingBench and 13.58% on OVOBench) over respective baselines, establishing new state-of-the-art results for streaming video perception.The code will be released at https://github.com/streamingvideos/CurveStream.

---

## 论文详细总结（自动生成）

### CurveStream：通过曲率感知的分层视觉记忆管理增强 MLLM 中的流式视频理解

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多模态大语言模型（MLLMs）在离线视频理解上表现优异，但在处理理论上长度无限的**流式视频**时面临严峻挑战。
*   **核心问题**：视觉 Token 随时间线性爆炸，导致显存溢出（OOM）或因简单的截断/采样策略导致的“灾难性遗忘”。
*   **现有局限**：目前的视觉保留策略（如均匀采样、基于相似度的剔除）大多缺乏**语义感知**。它们往往只关注低级的物理运动（如像素变化），容易受到局部噪声干扰，且会抹除瞬时但关键的语义转折点（如新物体的出现或动作的切换）。

#### 2. 论文提出的方法论
CurveStream 是一个**无需训练（Training-free）**的框架，其核心思想是利用特征空间中轨迹的**几何曲率**来识别语义强度。
*   **曲率感知评分器 (CAS)**：
    *   **一阶运动变化**：计算相邻帧特征的余弦相似度。
    *   **二阶几何曲率**：计算特征位移向量之间的夹角。高曲率（轨迹急转弯）通常对应于全局语义的重大转变（如镜头切换、新事件开始）。
    *   **综合评分**：结合一阶和二阶指标，精准定位高价值信息。
*   **分层视觉记忆管理 (HVMM)**：
    *   **在线分布估计**：利用指数移动平均（EMA）实时更新曲率得分的均值和方差。
    *   **动态 K-Sigma 阈值**：根据历史分布自动生成双阈值（$g_1, g_2$），将帧划分为三种状态：
        1.  **清晰记忆 (Clear Memory)**：高曲率帧，保留原始高分辨率，作为关键语义锚点。
        2.  **模糊记忆 (Blurred Memory)**：中等曲率帧，下采样至低分辨率（如 224x224），以极低成本维持时序连贯性。
        3.  **丢弃 (Discard)**：低曲率冗余帧，直接剔除。
    *   **FIFO 淘汰**：在固定 Token 预算内，采用先进先出原则管理内存队列。

#### 3. 实验设计
*   **数据集与场景**：
    *   **流式基准**：StreamingBench（10项实时理解任务）、OVOBench（6项实时感知任务）。
    *   **离线/长视频基准**：EgoSchema（第一视角推理）、VideoMME（超长视频理解）、MVBench（细粒度动作感知）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 1.5 Pro。
    *   **开源模型基座**：Qwen2-VL, Qwen2.5-VL, Qwen3-VL (4B/7B/8B/32B), LLaVA-OneVision, InternVL2。
    *   **专门的流式/长视频框架**：Flash-VStream, HERMES, ReKV, FreshMem, StreamForest 等。

#### 4. 资源与算力
*   **训练时长**：**无**。该方法是插件式的，无需任何参数微调或重新训练。
*   **推理算力**：论文提到所有基准测试均在**单张 GPU** 上独立执行，以验证其在严格显存限制下的鲁棒性。
*   **轻量化**：前端特征提取使用轻量级的 DINOv2-small，计算开销极低。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 32B 不同参数规模的多个模型系列，验证了方法的**可扩展性**。
*   **消融实验**：
    *   验证了曲率指标优于余弦相似度和光流法。
    *   分析了清晰/模糊记忆比例对性能与 Token 成本的平衡影响。
    *   测试了超参数（如 K-Sigma 中的 $k$ 值）的敏感度，证明了其鲁棒性。
*   **充分性评价**：实验设计非常全面，不仅在流式任务上表现突出，还测试了其在离线长视频任务中的泛化能力，对比对象涵盖了当前最顶尖的商业和开源模型，结果客观公正。

#### 6. 主要结论与发现
*   **性能飞跃**：在 StreamingBench 和 OVOBench 上，相比基线模型实现了超过 **10%** 的绝对精度提升（最高达 13.58%）。
*   **超越闭源**：搭载 CurveStream 的 7B/8B 开源模型在流式理解指标上多次超越了 GPT-4o 和 Gemini 1.5 Pro。
*   **几何洞察有效性**：证明了特征轨迹的“曲率”是衡量语义转折的可靠几何指标，能有效过滤物理噪声。
*   **显存友好**：在固定 Token 预算下，通过分层管理成功消除了 OOM 风险，同时保持了长程上下文的连贯性。

#### 7. 优点
*   **即插即用**：无需训练，适配性极强，可直接应用于各种基于 Transformer 的 MLLMs。
*   **几何启发式创新**：将复杂的语义感知简化为特征空间的几何曲率计算，既有理论深度又具备计算效率。
*   **分层管理策略**：通过“高分辨率关键帧 + 低分辨率过渡帧”的组合，巧妙解决了“看得清”与“记得久”之间的矛盾。

#### 8. 不足与局限
*   **极端长视频的细节权衡**：在处理数小时的超长视频时，为了维持固定内存，系统仍需剔除早期 Token，可能导致极远距离的细粒度细节丢失。
*   **依赖预训练编码器**：曲率计算依赖于冻结的视觉编码器（如 DINOv2）提取的特征质量，若编码器对某些特定领域的特征不敏感，可能会影响采样精度。
*   **应用限制**：目前主要针对视觉 Token 管理，尚未深入探讨 KV Cache 在语言模型内部的非线性压缩。

（完）
