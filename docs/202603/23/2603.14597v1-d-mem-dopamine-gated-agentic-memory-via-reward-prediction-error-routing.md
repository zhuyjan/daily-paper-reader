---
title: "D-MEM: Dopamine-Gated Agentic Memory via Reward Prediction Error Routing"
title_zh: D-MEM：通过奖励预测误差路由实现的多巴胺门控智能体记忆
authors: "Yuru Song, Qi Xin"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14597v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 受生物启发的大模型智能体长期记忆架构
tldr: "针对自主LLM智能体长期记忆系统存在的O(N^2)写入延迟和高昂Token成本问题，本文提出受生物学启发的D-MEM架构。该系统利用奖励预测误差（RPE）机制实现快慢路径路由：常规输入进入O(1)快速缓存，而高RPE（如事实矛盾或偏好转变）则触发“多巴胺”信号进行O(N)记忆重构。实验表明，D-MEM在降低80%成本的同时，显著提升了多步推理能力和抗噪性。"
selection_source: fresh_fetch
motivation: 现有的长期记忆系统在处理海量交互时面临计算复杂度过高和Token消耗巨大的瓶颈。
method: 引入基于奖励预测误差的路由机制，通过轻量级评论器筛选高价值信息触发记忆演化，实现快慢路径解耦。
result: "在LoCoMo-Noise基准测试中，D-MEM减少了超过80%的Token消耗，并消除了O(N^2)性能瓶颈。"
conclusion: D-MEM通过选择性认知重构，为构建可扩展、低成本且具备终身学习能力的智能体记忆系统奠定了基础。
---

## 摘要
自主大语言模型（LLM）智能体需要结构化的长期记忆，但目前的“追加与演进”系统（如 A-MEM）面临 O(N^2) 的写入延迟和过高的 token 成本。我们提出了 D-MEM（多巴胺门控智能体记忆），这是一种受生物学启发的架构，通过基于奖励预测误差（RPE）的快/慢路由系统，将短期交互与认知重构解耦。一个轻量级的评论家路由器（Critic Router）评估刺激的惊喜度（Surprise）和效用（Utility）。常规的、低 RPE 的输入会被绕过或缓存在 O(1) 的快速访问缓冲区中。相反，高 RPE 输入（如事实矛盾或偏好转变）会触发“多巴胺”信号，激活 O(N) 记忆演进流水线，以重塑智能体的知识图谱。为了在现实条件下评估性能，我们引入了 LoCoMo-Noise 基准测试，该基准在长期会话中注入受控的对话噪声。评估结果表明，D-MEM 减少了超过 80% 的 token 消耗，消除了 O(N^2) 瓶颈，并在多跳推理和对抗韧性方面优于基准模型。通过选择性地对认知重构进行门控，D-MEM 为终身智能体记忆提供了一个可扩展且具有成本效益的基础。

## Abstract
Autonomous LLM agents require structured long-term memory, yet current "append-and-evolve" systems like A-MEM face O(N^2) write-latency and excessive token costs. We introduce D-MEM (Dopamine-Gated Agentic Memory), a biologically inspired architecture that decouples short-term interaction from cognitive restructuring via a Fast/Slow routing system based on Reward Prediction Error (RPE). A lightweight Critic Router evaluates stimuli for Surprise and Utility. Routine, low-RPE inputs are bypassed or cached in an O(1) fast-access buffer. Conversely, high-RPE inputs, such as factual contradictions or preference shifts, trigger a "dopamine" signal, activating the O(N) memory evolution pipeline to reshape the agent's knowledge graph. To evaluate performance under realistic conditions, we introduce the LoCoMo-Noise benchmark, which injects controlled conversational noise into long-term sessions. Evaluations demonstrate that D-MEM reduces token consumption by over 80%, eliminates O(N^2) bottlenecks, and outperforms baselines in multi-hop reasoning and adversarial resilience. By selectively gating cognitive restructuring, D-MEM provides a scalable, cost-efficient foundation for lifelong agentic memory.

---

## 论文详细总结（自动生成）

### D-MEM: 基于奖励预测误差路由的多巴胺门控智能体记忆论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的自主大语言模型（LLM）智能体长期记忆框架（如 A-MEM）通常采用“同步追加与全量演进”模式。这意味着每一句对话（无论是否有意义）都会触发复杂的记忆构建和知识图谱更新，导致：
    *   **计算瓶颈**：随着交互增长，更新复杂度呈 $O(N^2)$ 增长，产生严重的写入延迟。
    *   **成本高昂**：API Token 消耗随对话长度爆炸式增长。
    *   **噪声污染**：现实对话中的闲聊、状态更新等噪声会污染向量数据库，导致检索质量下降和上下文窗口“中毒”。
*   **研究背景**：借鉴生物学中哺乳动物大脑的记忆巩固机制——并非所有感官输入都进入长时记忆，而是由腹侧被盖区（VTA）通过多巴胺释放（奖励预测误差 RPE）来门控。只有“意外”或“高价值”的信息才会触发神经元结构的重塑。

#### 2. 方法论：D-MEM 架构
D-MEM 核心思想是引入一个**快/慢路由系统**，将短期交互与长期认知重构解耦。
*   **智能体奖励预测误差 (Agentic RPE)**：
    *   **语义惊喜度 (Surprise)**：通过计算当前输入与已有记忆的余弦相似度，并进行 Z-score 标准化（解决嵌入向量各向异性问题），衡量信息的新颖性。
    *   **长期效用 (Utility)**：利用轻量级 LLM 调用将输入分类为瞬时（Transient）、短期（Short-term）或持久（Persistent）。
    *   **公式化门控**：$RPE = \min(1.0, I(Utility \ge \tau) \cdot [Utility \times (Surprise + \beta)])$。只有效用超过阈值且具有一定惊喜度的信息才会获得高 RPE。
*   **三层分级路由**：
    1.  **SKIP (低 RPE)**：视为噪声或冗余，直接跳过记忆演进，存入 $O(1)$ 的**影子缓冲区 (Shadow Buffer)**。
    2.  **CONSTRUCT_ONLY (中 RPE)**：常规事实，仅构建原子节点存入短期记忆缓冲区，不触发复杂的图链接更新。
    3.  **FULL_EVOLUTION (高 RPE)**：识别为范式转变或矛盾，触发“多巴胺释放”，执行 $O(N)$ 的深度记忆演进和全局知识图谱重构。
*   **零成本检索增强**：结合 BM25 稀疏检索与向量检索（RRF 融合），并利用影子缓冲区作为对抗性查询的兜底，确保不丢失琐碎细节。

#### 3. 实验设计
*   **数据集与基准**：
    *   **LoCoMo**：标准的长期对话记忆基准。
    *   **LoCoMo-Noise (新提出)**：在原数据集基础上系统性注入 75% 的对话噪声（包括填充语、状态更新、离题话），模拟真实的人机交互。
*   **对比方法**：
    *   **A-MEM**（同步演进的最先进框架）。
    *   **MemGPT**（基于操作系统分页思想的记忆管理）。
    *   **MemoryBank**（基于遗忘曲线的记忆增强）。
    *   **Full Context**（将全部历史放入上下文的上限模型）。
*   **评估指标**：F1 分数、BLEU-1、Token 消耗量、写入延迟。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：实验主要基于 API 调用（使用 **GPT-4o-mini** 作为骨干模型和评论家路由器）。由于 D-MEM 侧重于推理侧的架构优化而非模型训练，其核心开销在于 API Token 和向量数据库操作。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在标准 LoCoMo 和高噪声 LoCoMo-Noise 上进行了完整对比。
    *   包含针对 700 轮对话的 RPE 组件分解分析。
    *   进行了 UMAP 流形可视化，展示了长短期记忆在潜在空间中的分离。
    *   **消融实验**：对比了线性 RPE、严格剪枝、带安全网的路由等 4 个变体（v1-v4）。
*   **充分性评价**：实验设计较为充分，特别是在噪声鲁棒性和长程依赖（多跳推理）方面的分析非常深入。通过可视化手段（注意力热图、UMAP）客观地证明了架构的有效性。

#### 6. 主要结论与发现
*   **极致高效**：相比 A-MEM，D-MEM 减少了 **80% 以上**的 API Token 消耗。
*   **性能反超**：在极端噪声环境下，D-MEM 的多跳推理能力显著优于同步基准（F1 提升约 15.7%），证明了“少即是多”的记忆纯化逻辑。
*   **抗噪性强**：通过影子缓冲区和 RPE 门控，D-MEM 在对抗性问题（询问被跳过的琐碎信息）上依然保持了高准确率。
*   **复杂度优化**：成功将大多数回合的记忆处理复杂度从 $O(N^2)$ 降低到 $O(1)$ 或稀疏的 $O(N)$。

#### 7. 优点与亮点
*   **生物启发式创新**：将神经科学中的 RPE 和多巴胺门控机制巧妙转化为工程化的路由算法。
*   **解决实际痛点**：直接针对长期记忆系统在生产环境中的成本和延迟瓶颈。
*   **基准贡献**：提出的 LoCoMo-Noise 更贴近真实应用场景，填补了记忆系统抗噪性评估的空白。
*   **架构解耦**：快慢路径的设计兼顾了实时响应与深度认知的需求。

#### 8. 不足与局限
*   **单跳事实损失**：在标准（无噪声）任务中，单跳检索性能低于 A-MEM。这是因为激进的效用过滤可能误判了一些看似琐碎但属于单跳考点的事实。
*   **阈值敏感性**：RPE 的门控阈值（$\theta_{low}, \theta_{high}$）需要根据场景手动调优，缺乏自动化的动态调整机制。
*   **分类器开销**：虽然评论家路由器是轻量级的，但每轮对话仍需一次额外的 LLM 调用，对于极低延迟要求的场景仍有优化空间（如蒸馏到更小的本地模型）。
*   **评估局限**：目前主要基于 GPT-4o-mini，在更小规模或不同系列的开源模型上的泛化表现尚待验证。

（完）
