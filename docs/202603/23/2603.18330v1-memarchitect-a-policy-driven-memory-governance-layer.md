---
title: "MemArchitect: A Policy Driven Memory Governance Layer"
title_zh: MemArchitect：一种策略驱动的内存治理层
authors: "Lingavasan Suresh Kumar, Yang Ba, Rong Pan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18330v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 自主智能体的记忆治理层
tldr: 针对持久化大模型智能体在内存管理中存在的冲突、隐私及过时信息等治理难题，本文提出MemArchitect。这是一个将内存生命周期管理与模型权重解耦的治理层，通过引入衰减、冲突解决和隐私控制等显式规则策略，显著提升了自主系统的可靠性与安全性。
selection_source: fresh_fetch
motivation: 现有的RAG框架将内存视为被动存储，缺乏处理矛盾、保护隐私和清理过时信息的有效治理机制。
method: 提出MemArchitect治理层，通过解耦内存管理并实施基于规则的策略（如内存衰减和冲突解决）来管控内存。
result: 实验证明，在智能体场景下，受治理的内存表现持续优于未经管理的内存。
conclusion: 结构化的内存治理对于构建可靠且安全的自主大模型系统至关重要。
---

## 摘要
持久化大语言模型（LLM）智能体揭示了内存管理中一个关键的治理空白。标准的检索增强生成（RAG）框架将内存视为被动存储，缺乏解决矛盾、执行隐私保护或防止过时信息（“僵尸记忆”）污染上下文窗口的机制。我们引入了 MemArchitect，这是一个将内存生命周期管理与模型权重解耦的治理层。MemArchitect 执行显式的、基于规则的策略，包括内存衰减、冲突解决和隐私控制。我们证明了在智能体场景下，受治理的内存始终优于未管理的内存，强调了结构化内存治理对于构建可靠且安全的自主系统的必要性。

## Abstract
Persistent Large Language Model (LLM) agents expose a critical governance gap in memory management. Standard Retrieval-Augmented Generation (RAG) frameworks treat memory as passive storage, lacking mechanisms to resolve contradictions, enforce privacy, or prevent outdated information ("zombie memories") from contaminating the context window.   We introduce MemArchitect, a governance layer that decouples memory lifecycle management from model weights. MemArchitect enforces explicit, rule-based policies, including memory decay, conflict resolution, and privacy controls.   We demonstrate that governed memory consistently outperforms unmanaged memory in agentic settings, highlighting the necessity of structured memory governance for reliable and safe autonomous systems.

---

## 论文详细总结（自动生成）

以下是对论文《MemArchitect: A Policy Driven Memory Governance Layer》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从简单的聊天机器人演变为持久化的自主智能体，它们面临着严重的**“治理鸿沟”（Governance Gap）**。
*   **现有问题**：传统的检索增强生成（RAG）将内存视为被动的、只增不减的存储桶，导致了“内存幻觉”（如检索到过时的矛盾事实）、上下文污染以及缺乏隐私保护（如无法彻底执行“被遗忘权”）。
*   **核心目标**：本文提出 **MemArchitect**，一个独立于模型权重的策略驱动内存治理层。它将内存管理从被动存储转变为主动裁决，通过显式规则管理内存的生命周期、一致性、溯源和效率。

### 2. 论文提出的方法论
MemArchitect 的核心思想是在用户和智能体之间建立一个“分诊与竞价”（Triage & Bid）经济体系，其治理引擎包含四个支柱：

*   **生命周期与卫生（Lifecycle & Hygiene）**：
    *   **FSRS 衰减**：引入自由间隔重复调度算法（FSRS v4）模拟生物遗忘曲线，根据记忆的稳定性和时间计算可检索性（R）。
    *   **熵触发整合**：后台监控信息密度，当冗余过高时，将衰减的片段记忆压缩为稳定的语义事实，并删除低价值噪声。
*   **一致性与真实性（Consistency & Truth）**：
    *   **卡尔曼滤波（Kalman Filter）**：追踪每条记忆的“信任分数”，有用记忆增加权重，幻觉信息被数学化降权。
    *   **交叉编码器否决门**：即使通过了向量搜索，若不满足严格的语义蕴含阈值，也会被剔除。
*   **自适应检索（Adaptive Retrieval）**：
    *   **多跳分解**：将复杂查询拆分为子问题。
    *   **赫布图扩展（Hebbian Graph Expansion）**：基于“共同触发”原则，自动拉取关联记忆。
*   **效率（Efficiency）**：
    *   **自适应令牌预算**：根据检索置信度动态分配上下文窗口（推理预留 vs. 召回预留）。

### 3. 实验设计
*   **数据集/场景**：使用 **LoCoMo-10** 基准测试，这是一个包含 1,986 个问答对的长程对话数据集，涵盖单跳、多跳、时间推理和开放域任务。
*   **对比方法（Baselines）**：
    *   **MemOS**：代表系统级治理的“最大化”方案，将内存视为操作系统资源。
    *   **SimpleMem**：代表算法效率方案，侧重于语义压缩。
*   **骨干模型**：使用了 Qwen-3B 和 Llama-3.1-8B 进行评估。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到使用了 BGE-M3 和 nomic-embed-text-v1.5 作为嵌入模型，并使用 LLM-as-a-Judge（Llama-3.1-8B 和 Qwen2.5-3B）进行自动化评估。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模的骨干模型（3B 和 8B）上进行了跨类别的对比实验。
*   **充分性评价**：实验涵盖了长程对话的主要挑战（如时间敏感性、多跳逻辑）。通过与系统级（MemOS）和算法级（SimpleMem）基准对比，验证了其作为“治理层”的独特性。然而，部分策略（如冲突解决和毒性过滤）在文中标记为“计划中”，尚未在本次实验中完全体现。

### 6. 论文的主要结论与发现
*   **治理优于压缩**：MemArchitect 在平均准确率上比 SimpleMem 提升了 **27.5%**（在 Qwen-3B 上），证明主动治理比单纯的语义压缩更能保持上下文的忠实度。
*   **性能权衡**：相比于保留所有日志的 MemOS，MemArchitect 在开放域和多跳任务中表现更好，但在单跳和时间推理中准确率有所下降（-44.9%）。
*   **结论**：主动衰减策略虽然牺牲了部分“一次性事实”的精确记忆，但显著增强了抽象合成能力和长程连贯性，将“上下文污染”的风险转化为可控的“过度修剪”。

### 7. 优点
*   **解耦设计**：作为外部治理层，不修改模型参数，具有模型无关性（Model-agnostic）。
*   **生物学启发**：引入 FSRS 遗忘曲线，使智能体的记忆管理更接近人类的认知规律。
*   **主动性**：从被动检索转向主动裁决，解决了 RAG 系统中长期存在的“僵尸记忆”污染问题。

### 8. 不足与局限
*   **过度修剪风险**：激进的衰减策略可能导致智能体遗忘某些低频但关键的时间戳信息。
*   **功能完整性**：论文中提到的“冲突解决”、“毒性过滤”和“被遗忘权级联”等关键治理策略仍处于“计划中”或初步阶段，缺乏完整的实验验证。
*   **计算开销**：虽然提高了令牌效率，但后台的“卫生引擎”（清理和压缩）在超大规模内存库中的实时计算开销尚未详细讨论。

（完）
