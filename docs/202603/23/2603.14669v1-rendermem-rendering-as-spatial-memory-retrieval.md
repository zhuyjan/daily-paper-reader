---
title: "RenderMem: Rendering as Spatial Memory Retrieval"
title_zh: RenderMem：将渲染视为空间记忆检索
authors: "JooHyun Park, HyeongYeop Kang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14669v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 具身智能体的空间记忆框架
tldr: RenderMem 是一种新型空间记忆框架，旨在解决具身智能推理中视点依赖的问题。传统方法难以处理遮挡和视线等几何推理，而 RenderMem 将渲染作为 3D 世界表示与推理之间的接口。它通过维护 3D 场景表示，并根据查询需求从特定视角渲染视觉证据，使智能体能直接推理任意视角的可见性。该框架与现有视觉语言模型兼容，在 AI2-THOR 环境中显著提升了视点相关任务的性能。
selection_source: fresh_fetch
motivation: 现有的具身智能空间记忆系统缺乏显式的几何基础，难以处理与视点相关的可见性和遮挡推理。
method: 提出 RenderMem 框架，将渲染作为 3D 场景表示与空间推理的接口，根据查询动态生成特定视角的视觉证据。
result: 在 AI2-THOR 环境下的实验表明，RenderMem 在处理视点依赖的可见性和遮挡查询方面优于现有的记忆基准模型。
conclusion: RenderMem 通过将渲染引入记忆检索过程，为具身智能体提供了一种更具几何感知能力的 3D 空间推理方案。
---

## 摘要
具身推理本质上是视点相关的：什么是可见的、被遮挡的或可到达的，关键取决于智能体所处的位置。然而，现有的具身智能体空间记忆系统通常存储多视图观测或以对象为中心的抽象，这使得进行具有显式几何基础的推理变得困难。我们引入了 RenderMem，这是一个将渲染视为 3D 世界表示与空间推理之间接口的空间记忆框架。RenderMem 不存储固定的观测结果，而是维护一个 3D 场景表示，并通过从查询所隐含的视点渲染场景来生成基于查询条件的视觉证据。这使得具身智能体能够直接从任意视角对视线、可见性和遮挡进行推理。RenderMem 与现有的视觉语言模型完全兼容，且无需对标准架构进行任何修改。在 AI2-THOR 环境中的实验表明，与之前的记忆基准相比，RenderMem 在视点相关的可见性和遮挡查询方面表现出持续的改进。

## Abstract
Embodied reasoning is inherently viewpoint-dependent: what is visible, occluded, or reachable depends critically on where the agent stands. However, existing spatial memory systems for embodied agents typically store either multi-view observations or object-centric abstractions, making it difficult to perform reasoning with explicit geometric grounding. We introduce RenderMem, a spatial memory framework that treats rendering as the interface between 3D world representations and spatial reasoning. Instead of storing fixed observations, RenderMem maintains a 3D scene representation and generates query-conditioned visual evidence by rendering the scene from viewpoints implied by the query. This enables embodied agents to reason directly about line-of-sight, visibility, and occlusion from arbitrary perspectives. RenderMem is fully compatible with existing vision-language models and requires no modification to standard architectures. Experiments in the AI2-THOR environment show consistent improvements on viewpoint-dependent visibility and occlusion queries over prior memory baselines.

---

## 论文详细总结（自动生成）

### 论文总结：RenderMem —— 将渲染视为空间记忆检索

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能（Embodied AI）的推理本质上是**视点依赖（Viewpoint-dependent）**的。智能体需要根据其所处位置判断物体的可见性、遮挡关系或可达性。然而，现有的空间记忆系统存在局限：
*   **基于视图的记忆**：仅存储历史观测图像，无法处理新视角或物体中心视角的查询。
*   **以对象为中心的记忆**：将场景抽象为图结构，丢失了精细的几何信息，难以进行视线（Line-of-sight）推理。
*   **3D 场景表示**：虽然包含几何信息，但难以直接与大语言模型（LLM）集成。

**RenderMem 的核心动机**是将“渲染”定义为空间记忆的“读取操作”。它不存储固定观测，而是维护一个可渲染的 3D 场景，根据查询动态生成视觉证据，从而解决视点相关的几何推理难题。

#### 2. 方法论
RenderMem 提出了一个将 3D 几何与语言推理相解耦的框架，核心流程如下：
*   **场景表示**：维护一个统一的可渲染 3D 场景（如 Mesh、NeRF 或 3D 高斯泼溅），并辅以轻量级的对象列表（包含对象 ID 和边界球）。
*   **两阶段查询管线**：
    1.  **渲染决策（Rendering Decision）**：判断问题是否需要视觉证据（如计数问题可能直接通过对象列表回答，而属性问题需要渲染）。
    2.  **渲染规范（Rendering Specification）**：确定渲染模式和对象锚点。
*   **渲染模式**：
    *   **环绕渲染（Surround Rendering）**：在目标对象周围采样多个视角，用于观察物体状态或属性。
    *   **定向渲染（Directional Rendering）**：模拟从源对象到目标对象的视线，用于推理可见性和遮挡。
*   **证据推理**：将生成的渲染图像输入视觉语言模型（VLM，如 Qwen2.5-VL），结合原始问题得出答案。

#### 3. 实验设计
*   **数据集/场景**：基于 **AI2-THOR** 仿真环境，涵盖 iTHOR、RoboTHOR 和 ProcTHOR，共计 180 个场景。
*   **Benchmark 任务**：
    1.  静态属性与计数。
    2.  动态属性与计数（涉及物体状态改变，如开关电视）。
    3.  视点依赖的可见性推理（核心挑战任务）。
*   **对比方法**：
    *   **Multi-view retrieval**：基于 CLIP 的多视图检索。
    *   **Concept Graphs**：以对象为中心的开放词汇 3D 场景图。
    *   **3D-Mem**：结合对象列表和视图快照的混合记忆系统。
*   **评估指标**：采用 **Binary LLM Match**，利用外部 LLM 判断模型回答与标准答案的语义一致性。

#### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该框架主要依赖预训练的 VLM（如 Qwen2.5-VL-7B）进行推理，且无需对模型架构进行修改或重新训练，其算力消耗主要集中在 3D 场景的实时渲染和 VLM 的推理阶段。

#### 5. 实验数量与充分性
*   **实验规模**：在 180 个场景中进行了大量测试，涵盖了静态、动态及复杂的可见性查询。
*   **消融实验**：对决策步骤（1步、2步、3步查询）进行了对比，验证了“两阶段管线”的有效性。
*   **鲁棒性测试**：针对不完美的 3D 重建（模拟模糊、重影）和定位误差（边界球扰动）进行了压力测试。
*   **评价**：实验设计较为全面，对比了当前主流的几种空间记忆范式，且通过引入动态场景和噪声模拟，增强了实验的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：RenderMem 在视点依赖的可见性任务上显著优于所有基准模型（平均得分 0.79，而 3D-Mem 仅为 0.43）。
*   **动态适应性**：由于渲染是基于当前 3D 状态实时生成的，RenderMem 无需显式更新记忆即可自然支持动态环境中的状态变化。
*   **几何感知**：通过定向渲染，模型能够准确捕捉物体间的遮挡关系，这是传统视图检索方法难以企及的。

#### 7. 优点
*   **即插即用**：完全兼容现有的 VLM，无需修改模型架构或进行微调。
*   **几何与语义解耦**：将复杂的 3D 几何推理转化为 VLM 擅长的 2D 图像理解。
*   **鲁棒性强**：实验证明即使在 3D 重建质量较低或定位存在偏差的情况下，依然能保持较高的推理准确率。

#### 8. 不足与局限
*   **实例歧义**：当场景中存在多个同类物体（如“窗户边的椅子”）时，仅靠类别 ID 和位置可能难以精确区分特定实例。
*   **存储开销**：高质量的可渲染 3D 场景（如 Mesh 或 3DGS）占用空间较大，在大规模环境部署时可能面临存储瓶颈。
*   **依赖重建质量**：虽然具有一定鲁棒性，但极度退化的 3D 重建仍会直接影响渲染质量，进而导致推理失败。

（完）
