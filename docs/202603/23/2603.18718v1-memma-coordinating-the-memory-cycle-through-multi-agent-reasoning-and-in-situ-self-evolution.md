---
title: "MemMA: Coordinating the Memory Cycle through Multi-Agent Reasoning and In-Situ Self-Evolution"
title_zh: MemMA：通过多智能体推理与原位自我演化协调记忆周期
authors: "Minhua Lin, Zhiwei Zhang, Hanqing Lu, Hui Liu, Xianfeng Tang, Qi He, Xiang Zhang, Suhang Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18718v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 具有外部记忆库的记忆增强LLM智能体，支持长程交互。
tldr: MemMA是一个即插即用的多智能体框架，旨在解决大模型智能体在长程交互中内存管理（构建、检索、利用）脱节的问题。它通过Meta-Thinker在正向路径提供策略指导，并在反向路径引入原位自进化机制，利用探针问答对验证并修复内存，显著提升了内存循环的协调性与准确性。
selection_source: fresh_fetch
motivation: 现有内存增强型智能体将内存构建与检索视为孤立子任务，缺乏全局策略指导且难以从下游失败中直接修复内存。
method: 提出MemMA框架，通过Meta-Thinker协调内存周期，并结合原位自进化机制，在内存最终确定前进行自我验证与修复。
result: 在LoCoMo基准测试中，MemMA在多种大模型底座上均优于现有基准，并能以即插即用方式提升不同存储后端的性能。
conclusion: MemMA通过多智能体协同与自进化修复，有效解决了内存管理中的策略盲目性和监督稀疏性问题。
---

## 摘要
记忆增强型大语言模型（LLM）智能体通过维护外部记忆库来支持长程交互，但现有大多数系统将记忆的构建、检索和利用视为孤立的子程序。这带来了两个耦合的挑战：一是记忆周期前向路径上的策略盲目性，即构建和检索是由局部启发式方法而非显式策略推理驱动的；二是后向路径上的稀疏且延迟的监督，即下游任务的失败很少能转化为对记忆库的直接修复。为了应对这些挑战，我们提出了 MemMA，这是一个即插即用的多智能体框架，可在前向和后向路径上协调记忆周期。在前向路径上，元思考器（Meta-Thinker）生成结构化指导，在构建过程中引导记忆管理器（Memory Manager），并在迭代检索过程中指导查询推理器（Query Reasoner）。在后向路径上，MemMA 引入了原位自我演化记忆构建，它通过合成探测问答对、验证当前记忆，并在记忆最终确定前将失败转化为修复动作。在 LoCoMo 上的广泛实验表明，MemMA 在多个 LLM 主干网络上始终优于现有基准，并以即插即用的方式改进了三种不同的存储后端。我们的代码已在 https://github.com/ventr1c/memma 公开。

## Abstract
Memory-augmented LLM agents maintain external memory banks to support long-horizon interaction, yet most existing systems treat construction, retrieval, and utilization as isolated subroutines. This creates two coupled challenges: strategic blindness on the forward path of the memory cycle, where construction and retrieval are driven by local heuristics rather than explicit strategic reasoning, and sparse, delayed supervision on the backward path, where downstream failures rarely translate into direct repairs of the memory bank. To address these challenges, we propose MemMA, a plug-and-play multi-agent framework that coordinates the memory cycle along both the forward and backward paths. On the forward path, a Meta-Thinker produces structured guidance that steers a Memory Manager during construction and directs a Query Reasoner during iterative retrieval. On the backward path, MemMA introduces in-situ self-evolving memory construction, which synthesizes probe QA pairs, verifies the current memory, and converts failures into repair actions before the memory is finalized. Extensive experiments on LoCoMo show that MemMA consistently outperforms existing baselines across multiple LLM backbones and improves three different storage backends in a plug-and-play manner. Our code is publicly available at https://github.com/ventr1c/memma.

---

## 论文详细总结（自动生成）

### 论文总结：MemMA —— 通过多智能体推理与原位自我演化协调记忆周期

#### 1. 核心问题与研究动机
论文关注的是**长程交互中大语言模型（LLM）智能体的记忆管理问题**。
*   **研究背景**：现有的记忆增强型智能体通常将记忆的“构建”、“检索”和“利用”视为孤立的步骤，忽略了它们之间的耦合关系（即“记忆周期效应”）。
*   **核心挑战**：
    *   **正向路径的策略盲目性**：记忆构建（存什么）和检索（找什么）往往依赖局部启发式搜索，缺乏全局策略，导致“近视构建”（存入冗余或冲突信息）和“盲目检索”（无法通过迭代缩小信息差）。
    *   **反向路径的监督稀疏与延迟**：只有在最终回答错误时才能发现记忆问题，此时很难追溯并修复早期的构建错误。

#### 2. 方法论：MemMA 框架
MemMA 是一个**即插即用**的多智能体框架，通过协调记忆周期的正向和反向路径来解决上述问题。

*   **核心思想**：采用“计划者-执行者”架构，将高层策略推理与底层操作分离。
*   **关键组件（多智能体角色）**：
    *   **元思考器 (Meta-Thinker)**：负责高层策略。在构建时提供指导（存、删、改）；在检索时诊断证据是否充分，并指导下一步搜索。
    *   **记忆管理器 (Memory Manager)**：根据元思考器的指导执行具体的原子编辑操作。
    *   **查询推理器 (Query Reasoner)**：执行迭代检索，根据诊断结果精细化查询语句。
    *   **回答智能体 (Answer Agent)**：基于最终检索到的证据生成答案。
*   **关键技术：原位自我演化 (In-Situ Self-Evolution)**：
    *   在每个会话结束后，系统自动合成**探测问答对 (Probe QA)**。
    *   利用这些探测对对当前记忆进行即时验证。
    *   若验证失败，则生成修复建议，并在记忆正式提交前进行**语义整合**（去重、合并或插入），实现“即时修复”。

#### 3. 实验设计
*   **数据集**：**LoCoMo**（长程对话记忆基准），包含单步、多步、时间推理和开放域四类问题。
*   **实验设置**：使用 GPT-4o-mini 和 Claude-Haiku-4.5 作为主干模型。为了公平比较，所有方法的回答生成和评估均固定使用 GPT-4o-mini。
*   **对比方法**：
    *   **被动基准**：Full Text（全文本拼接）、Naive RAG。
    *   **主动记忆系统**：LangMem、Mem0、A-Mem、LightMem。
*   **评估指标**：Token 级别的 F1 分数、BLEU-1 (B1) 以及 LLM-as-a-Judge 的准确率 (ACC)。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该框架主要基于现有的闭源 LLM（如 GPT-4o-mini, Claude 系列）通过 API 进行推理和多智能体协作，其核心开销在于 **API 调用次数和推理延迟**，而非传统的模型训练算力。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **主实验**：在两种不同的 LLM 主干上验证了 MemMA 的优越性。
*   **灵活性测试**：证明了 MemMA 可以作为插件提升 Single-Agent、A-Mem 和 LightMem 三种不同存储后端的性能。
*   **消融实验**：分别移除了构建指导、迭代检索和自我演化模块，验证了各组件的必要性。
*   **参数敏感性**：分析了检索预算 $k$ 和迭代次数 $H$ 对性能的影响。
*   **探测模型分析**：探讨了不同规模模型（Haiku, Sonnet, Opus）生成探测对对修复质量的影响。

#### 6. 主要结论与发现
*   **MemMA 显著提升性能**：在 LoCoMo 基准上，MemMA 配合 LightMem 后端达到了 81.58% 的准确率，远超原始 LightMem（75.66%）。
*   **协调胜过孤立优化**：单纯增加检索次数（无指导）效果有限，而元思考器的诊断能引导检索快速收敛。
*   **原位修复至关重要**：通过探测问答对进行的即时修复能有效捕捉并纠正构建阶段的遗漏，防止错误累积。
*   **即插即用性**：MemMA 不依赖特定的存储架构，具有很强的通用性。

#### 7. 优点
*   **闭环设计**：首次系统性地通过多智能体协作协调了记忆的整个生命周期。
*   **主动修复机制**：引入“原位自我演化”，解决了长程记忆中反馈延迟的顽疾。
*   **模块化与兼容性**：作为一种包装器（Wrapper），能轻松集成到现有的记忆系统中。
*   **诊断驱动**：将“盲目搜索”转变为“诊断驱动的迭代”，提高了检索效率。

#### 8. 不足与局限
*   **成本与延迟**：多轮迭代检索和自我演化过程涉及多次 LLM 调用，会显著增加推理成本和响应延迟。
*   **会话依赖**：自我演化机制假设交互可以划分为清晰的“会话（Sessions）”，在边界模糊的连续流数据中可能难以应用。
*   **基准局限**：实验主要集中在对话类任务，对于更复杂的具身智能或多模态长程任务的泛化能力尚待验证。
*   **探测对质量**：系统性能高度依赖于合成探测问答对的质量，若探测模型本身存在偏见，可能引入错误的修复。

（完）
