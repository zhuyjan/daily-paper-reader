---
title: Advancing Multimodal Agent Reasoning with Long-Term Neuro-Symbolic Memory
title_zh: 以长期神经符号记忆提升多模态智能体推理能力
authors: "Rongjie Jiang, Jianwei Wang, Gengda Zhao, Chengyang Luo, Kai Wang, Wenjie Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15280v1"
tags: ["query:multi-modal-memory"]
score: 10.0
evidence: 多模态智能体的长期神经符号记忆
tldr: 针对多模态智能体在长期推理中缺乏演绎推理能力的问题，本文提出NS-Mem框架。该框架结合了神经表示与显式符号结构，通过三层存储架构（情节、语义、逻辑规则）、SK-Gen知识整合机制以及混合检索方式，显著提升了智能体在复杂决策中的分析推理能力，在多项基准测试中表现优于传统纯神经存储系统。
selection_source: fresh_fetch
motivation: 现有的多模态智能体存储系统多依赖神经向量检索，难以支持现实决策中关键的分析性演绎推理。
method: 提出NS-Mem框架，通过三层存储架构、SK-Gen自动知识整合机制以及结合相似度与符号查询的混合检索实现神经符号协同。
result: "在真实多模态推理基准测试中，NS-Mem比纯神经存储系统平均准确率提升4.35%，在受限推理任务中提升高达12.5%。"
conclusion: NS-Mem通过整合神经与符号存储，有效增强了多模态智能体的长期推理和结构化决策能力。
---

## 摘要
大语言模型的最新进展推动了在开放世界、多模态环境中运行的智能体的出现。为了支持长期推理，此类智能体通常配备有外部记忆系统。然而，大多数现有的多模态智能体记忆主要依赖于神经表示和基于向量的检索，这虽然适用于归纳式、直觉式推理，但在支持对现实世界决策至关重要的分析式、演绎式推理方面存在根本局限。为了解决这一局限，我们提出了 NS-Mem，这是一个长期神经符号记忆框架，旨在通过将神经记忆与显式符号结构和规则相结合，提升多模态智能体的推理能力。具体而言，NS-Mem 围绕记忆系统的三个核心组件运行：(1) 由情节层、语义层和逻辑规则层组成的三层记忆架构；(2) 由 SK-Gen 实现的记忆构建与维护机制，该机制可从积累的多模态经验中自动整合结构化知识，并增量更新神经表示和符号规则；(3) 一种混合记忆检索机制，结合了基于相似性的搜索与确定性符号查询函数，以支持结构化推理。在真实世界多模态推理基准测试上的实验表明，神经符号记忆在整体推理准确率上比纯神经记忆系统平均提高了 4.35%，在受限推理查询上的提升高达 12.5%，验证了 NS-Mem 的有效性。

## Abstract
Recent advances in large language models have driven the emergence of intelligent agents operating in open-world, multimodal environments. To support long-term reasoning, such agents are typically equipped with external memory systems. However, most existing multimodal agent memories rely primarily on neural representations and vector-based retrieval, which are well-suited for inductive, intuitive reasoning but fundamentally limited in supporting analytical, deductive reasoning critical for real-world decision making. To address this limitation, we propose NS-Mem, a long-term neuro-symbolic memory framework designed to advance multimodal agent reasoning by integrating neural memory with explicit symbolic structures and rules. Specifically, NS-Mem is operated around three core components of a memory system: (1) a three-layer memory architecture that consists episodic layer, semantic layer and logic rule layer, (2) a memory construction and maintenance mechanism implemented by SK-Gen that automatically consolidates structured knowledge from accumulated multimodal experiences and incrementally updates both neural representations and symbolic rules, and (3) a hybrid memory retrieval mechanism that combines similarity-based search with deterministic symbolic query functions to support structured reasoning. Experiments on real-world multimodal reasoning benchmarks demonstrate that Neural-Symbolic Memory achieves an average 4.35% improvement in overall reasoning accuracy over pure neural memory systems, with gains of up to 12.5% on constrained reasoning queries, validating the effectiveness of NS-Mem.

---

## 论文详细总结（自动生成）

以下是对论文《Advancing Multimodal Agent Reasoning with Long-Term Neuro-Symbolic Memory》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态智能体记忆系统主要依赖**神经表示（向量检索）**，这类似于人类思维的“系统1”（直觉、感性），擅长语义匹配和模糊检索，但在处理需要**演绎推理、约束满足和因果逻辑**的“系统2”（理性、分析）任务时表现欠缺。
*   **研究背景**：在现实场景中，智能体不仅需要记住发生了什么，还需要理解步骤间的依赖关系（如：做沙拉前碗碎了，应先买碗而非直接搅拌）。纯向量检索难以处理这种带有显式逻辑约束的复杂决策。

### 2. 方法论：NS-Mem 框架
NS-Mem 通过整合神经表示与符号规则，构建了一个三层记忆架构及配套的维护与检索机制：
*   **三层记忆架构**：
    1.  **情节层 (Episodic Layer)**：存储带时间戳的细粒度多模态观察（文本描述+嵌入）。
    2.  **语义层 (Semantic Layer)**：抽象实体属性和关系，通过强化/削弱机制维护实体一致性。
    3.  **逻辑层 (Logic Layer)**：核心创新点。每个逻辑节点包含**双级索引向量**（目标级和步骤级，用于神经检索）和**程序化有向无环图 (DAG)**（用于符号查询）。
*   **SK-Gen 自动构建与维护机制**：
    *   **提取与挖掘**：从情节记忆中提取动作序列，利用 **PrefixSpan** 算法挖掘频繁模式。
    *   **逻辑蒸馏**：通过 LLM 过滤无效模式，构建 Procedural DAG。
    *   **增量更新**：使用**指数移动平均 (EMA)** 更新神经索引，通过转移概率统计更新符号结构，避免全量重构。
*   **混合检索与推理**：
    *   **查询分类**：将查询分为事实型、约束型和角色型。
    *   **多粒度检索**：结合目标和步骤相似度定位逻辑节点。
    *   **符号增强**：在检索到的 DAG 上执行确定性函数（如路径枚举、约束过滤），直接输出满足条件的逻辑步骤。

### 3. 实验设计
*   **数据集**：使用 **M3-Bench** 基准，包含两个子集：
    *   **M3-Bench-robot**：100 个机器人视角真实视频。
    *   **M3-Bench-web**：920 个网络来源视频。
*   **对比方法**：
    *   **苏格拉底模型**：GPT-4o, Gemini-1.5-Pro, Qwen2.5-VL 等。
    *   **在线视频理解模型**：MovieChat, MA-LMM, Flash-VStream。
    *   **智能体方法**：**M3-Agent**（目前最先进的纯向量存储智能体）。
*   **评估指标**：使用 GPT-4o 作为裁判评估回答准确率，并统计对话轮数和响应时间。

### 4. 资源与算力
*   **硬件环境**：服务器配备 Intel Xeon Silver 4314 CPU，512GB 内存。
*   **GPU**：使用 **NVIDIA RTX A5000** GPU。
*   **模型参数**：隐藏层维度设为 512，EMA 系数 $\beta=0.9$。
*   **训练/运行耗时**：文中未详细给出总训练时长，但强调了增量更新的效率，单步维护复杂度为 $O(d \log |N|)$，比批处理重训更高效。

### 5. 实验数量与充分性
*   **实验规模**：在 Robot 子集（703 个问题）和 Web 子集（2066 个问题）上进行了大规模测试。
*   **实验维度**：
    *   **整体性能对比**：验证了 NS-Mem 在所有基准上的领先地位。
    *   **推理类型分析**：针对多跳推理、常识知识等 5 类任务进行了细分评估。
    *   **消融实验**：分别验证了符号组件和神经组件的贡献。
    *   **超参数分析**：对验证阈值 $\tau$、门控阈值 $\delta$ 和检索权重 $\alpha$ 进行了敏感度测试。
*   **客观性**：实验涵盖了从感知到推理的完整链路，对比了当前主流的闭源和开源模型，评估过程较为客观公平。

### 6. 主要结论与发现
*   **准确率提升**：NS-Mem 相比纯神经存储系统（M3-Agent）平均准确率提升 **4.35%**。
*   **复杂推理优势**：在**受限推理 (Constrained Reasoning)** 任务中，准确率提升高达 **12.5%**（相对提升 50%）。
*   **效率优化**：由于符号函数能一次性返回完整步骤，减少了智能体与记忆的交互轮数（Robot 场景减少 **15.8%**），总响应时间缩短约 **4.1% - 7.4%**。

### 7. 优点与亮点
*   **神经符号融合**：成功将非结构化的多模态流转化为可查询、可进化的结构化逻辑图，兼顾了检索的灵活性和推理的严谨性。
*   **增量维护**：SK-Gen 机制支持在线学习，能够随观察流动态修正逻辑规则，具有较强的工程实用性。
*   **理论支撑**：论文为转移概率的后验一致性和符号查询的确定性提供了数学证明。

### 8. 不足与局限
*   **LLM 依赖性**：在逻辑蒸馏阶段高度依赖 LLM 的过滤能力，若 LLM 判断失误，可能引入错误的符号规则。
*   **冷启动问题**：PrefixSpan 挖掘需要一定的样本积累，在观察初期（数据稀疏时）逻辑层可能无法有效工作。
*   **计算开销**：虽然检索高效，但多模态特征提取（人脸/声音识别）和频繁的 VLM 调用在实时系统中仍存在较大的计算压力。

（完）
