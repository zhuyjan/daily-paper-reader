---
title: Retrieving Counterfactuals Improves Visual In-Context Learning
title_zh: 检索反事实样本提升视觉上下文学习
authors: "Guangzhi Xiong, Sanchit Sinha, Zhenghao He, Aidong Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16737v1"
tags: ["query:mmrag"]
score: 9.0
evidence: 使用反事实进行视觉语境学习的检索增强方法。
tldr: 针对视觉语言模型（VLM）在上下文学习中难以处理细粒度属性和因果关系的问题，本文提出 CIRCLES 框架。该方法摒弃了传统的被动相似性检索，转而利用属性引导的组合图像检索技术来主动获取反事实风格的示例。通过引入这些对比示例，CIRCLES 引导模型学习属性与结果间的因果联系，减少虚假关联。实验证明，该方法在多种架构和数据集上均取得显著提升，尤其增强了小规模模型的推理鲁棒性。
selection_source: fresh_fetch
motivation: 传统的相似性检索方法容易导致模型学习到虚假的统计相关性，而非真正的因果逻辑。
method: 提出 CIRCLES 框架，利用组合图像检索技术主动筛选并提供具有反事实特征的上下文示例。
result: 在四个多样化数据集上的实验显示，CIRCLES 持续优于现有方法，在小模型和数据稀缺场景下优势尤为明显。
conclusion: 通过检索反事实示例进行上下文学习，可以显著增强视觉语言模型的因果推理能力和对细粒度属性的理解。
---

## 摘要
视觉语言模型 (VLMs) 在广泛的多模态推理任务中取得了显著的性能，但它们往往难以解耦细粒度的视觉属性并推理潜在的因果关系。上下文学习 (ICL) 为 VLMs 适应新任务提供了一条充满前景的途径，但其有效性关键取决于演示示例的选择。现有的检索增强方法通常依赖于基于相似性的被动检索，这往往会选择相关但非因果的示例，从而放大伪相关性并限制模型的鲁棒性。我们引入了 CIRCLES (Composed Image Retrieval for Causal Learning Example Selection)，这是一个通过有针对性的、属性引导的组合图像检索来检索反事实风格示例，从而主动构建演示集的新型框架。通过引入反事实风格的示例，CIRCLES 使 VLMs 能够隐式地推理属性与结果之间的因果关系，超越表面的相关性，并促进更稳健、更有根据的推理。在四个不同数据集上的全面实验表明，CIRCLES 在多种架构上始终优于现有方法，特别是在小规模模型上，在信息稀缺的情况下增益尤为显著。此外，CIRCLES 检索到了更多样化且具有因果信息量的示例，为模型如何利用上下文演示来改进推理提供了定性见解。我们的代码可在 https://github.com/gzxiong/CIRCLES 获取。

## Abstract
Vision-language models (VLMs) have achieved impressive performance across a wide range of multimodal reasoning tasks, but they often struggle to disentangle fine-grained visual attributes and reason about underlying causal relationships. In-context learning (ICL) offers a promising avenue for VLMs to adapt to new tasks, but its effectiveness critically depends on the selection of demonstration examples. Existing retrieval-augmented approaches typically rely on passive similarity-based retrieval, which tends to select correlated but non-causal examples, amplifying spurious associations and limiting model robustness. We introduce CIRCLES (Composed Image Retrieval for Causal Learning Example Selection), a novel framework that actively constructs demonstration sets by retrieving counterfactual-style examples through targeted, attribute-guided composed image retrieval. By incorporating counterfactual-style examples, CIRCLES enables VLMs to implicitly reason about the causal relations between attributes and outcomes, moving beyond superficial correlations and fostering more robust and grounded reasoning. Comprehensive experiments on four diverse datasets demonstrate that CIRCLES consistently outperforms existing methods across multiple architectures, especially on small-scale models, with pronounced gains under information scarcity. Furthermore, CIRCLES retrieves more diverse and causally informative examples, providing qualitative insights into how models leverage in-context demonstrations for improved reasoning. Our code is available at https://github.com/gzxiong/CIRCLES.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieving Counterfactuals Improves Visual In-Context Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）在进行视觉上下文学习（Visual ICL）时，现有的检索策略（如 RICES）主要依赖于**表面相似性**。这种“被动检索”容易引入与查询图像高度相关但并非因果决定因素的示例，导致模型学习到**虚假相关性**（Spurious Associations），在面对细粒度属性辨析或分布偏移时表现脆弱。
*   **研究动机**：作者认为，有效的推理应基于对关键属性的因果理解。通过引入**反事实（Counterfactual）**示例（即：如果某个关键属性改变了，结果会如何变化），可以引导模型识别真正决定答案的特征，从而提升推理的鲁棒性和准确性。

### 2. 方法论：CIRCLES 框架
CIRCLES（Composed Image Retrieval for Causal Learning Example Selection）的核心思想是结合“相关性理解”与“因果理解”。
*   **核心思想**：利用**组合图像检索（CIR）**技术，主动构建包含属性干预的演示集。
*   **关键流程**：
    1.  **关键属性识别**：利用 VLM 提取与当前问题最相关的属性-值对（如：鸟的腹部颜色是灰色）。
    2.  **反事实描述生成**：通过提示词（Prompting）让 VLM 修改其中一个属性值，生成反事实标题（如：将腹部颜色改为黄色，其他特征不变）。
    3.  **反事实样本检索（Causal Retrieval）**：使用生成的标题在训练集中检索最匹配的图像。检索评分函数结合了视觉忠实度（图像-标题相似度）和语义约束（问题-问题相似度）：$S_j = s_{img}^j + s_{txt}^j$。
    4.  **标准相似性检索（Correlational Retrieval）**：同时保留传统的基于图像相似度的检索结果。
    5.  **检索增强推理**：将上述两类示例（反事实+相似）合并作为上下文，输入 VLM 生成最终答案。

### 3. 实验设计
*   **数据集**：
    *   **细粒度分类**：CUB（鸟类）、Flowers（花卉）。
    *   **视觉问答（VQA）**：OK-VQA（基于知识的问答）、VizWiz（真实场景问答）。
*   **Benchmark 与对比方法**：
    *   **None**：零样本（Zero-shot）推理。
    *   **Random**：随机选择上下文示例。
    *   **RICES**：基于 CLIP 图像相似度的标准检索。
    *   **MUIER / MMICES**：现有的多模态相似性检索增强方法。
*   **测试模型**：Gemma3 (4B, 12B) 和 Qwen2.5-VL (3B, 7B)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实现细节**：提到使用 `vLLM` 库进行推理，采用 `BFloat16` 精度。检索后端使用 `CLIP ViT-g/14`。
*   **开销分析**：相比 RICES，CIRCLES 增加了属性提取和反事实标题生成的步骤，导致 Token 使用量平均增加约 **10%**，但由于这些调用通常较短，主要的计算开销仍集中在最终的长上下文推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集和 4 个不同规模的模型上进行了全面测试。
*   **消融实验**：
    *   **信息稀缺性分析**：模拟训练集数据丢失（0% 到 75%），验证 CIRCLES 在数据匮乏时的鲁棒性。
    *   **检索预算分配**：研究了 IR（相似检索）与 CIR（反事实检索）数量比例对性能的影响。
    *   **属性质量影响**：对比了 VLM 自动提取属性与使用数据集标注（Ground-truth）属性的效果。
    *   **CIR 实现方式**：对比了不同的训练外（Training-free）CIR 算法（如 CIReVL vs OSrCIR）。
*   **评价**：实验设计较为充分且客观，通过多维度消融实验证明了反事实样本的独立贡献，而非仅仅依靠增加示例数量。

### 6. 主要结论与发现
*   **性能提升**：CIRCLES 在所有数据集和模型上均优于基线。在小模型（如 Gemma3-4B）上提升尤为显著，平均相对提升可达 0.76% 至 94.02%。
*   **数据稀缺下的韧性**：当训练集样本减少时，CIRCLES 与基线的差距进一步拉大，说明反事实样本在信息不足时能提供更关键的判别信号。
*   **因果引导**：定性分析显示，反事实示例能清晰地向模型展示“属性改变导致类别变化”的过程，使推理过程更具可解释性。

### 7. 优点（亮点）
*   **无需训练（Training-free）**：该方法完全基于检索和提示词工程，不需要对 VLM 进行微调，易于部署。
*   **因果视角**：首次将反事实推理与视觉 ICL 检索相结合，解决了传统检索方法难以处理虚假相关性的痛点。
*   **通用性强**：在分类和问答任务中均表现出一致的改进，且对不同架构的模型（Gemma, Qwen）均有效。

### 8. 不足与局限
*   **依赖属性提取质量**：如果 VLM 无法准确识别图像中的关键属性，或者生成的反事实描述不切实际，检索到的示例可能具有误导性。
*   **推理延迟**：虽然 Token 增加不多，但多次调用 VLM（提取属性、生成标题）会增加端到端的推理延迟。
*   **检索库限制**：反事实检索的效果受限于原始训练库的覆盖范围。如果库中缺乏特定属性组合的真实图像，CIR 只能找到“最接近”的替代品，而非完美的反事实样本。

（完）
