---
title: "All-Mem: Agentic Lifelong Memory via Dynamic Topology Evolution"
title_zh: All-Mem：通过动态拓扑演化的智能体终身记忆
authors: "Can Lv, Heng Chang, Yuchen Guo, Shengyu Tao, Shiji Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19595v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 交互式智能体的终身记忆框架
tldr: 针对长期交互智能体在处理海量历史记忆时面临的冗余和噪声问题，本文提出 All-Mem 框架。该框架通过动态拓扑演化维护记忆库，利用 LLM 在离线状态下执行分裂、合并和更新操作，在保持证据不可变性的同时优化检索效率。实验证明其在长程记忆检索和问答任务上优于现有基准。
selection_source: fresh_fetch
motivation: 现有的记忆系统在处理长期历史时容易产生冗余和信息丢失，难以在固定上下文预算内提供精准检索。
method: 提出一种基于动态拓扑演化的记忆框架，通过 LLM 诊断器执行非破坏性的拓扑编辑（分裂、合并、更新）来整合记忆。
result: 在 LOCOMO 和 LONGMEMEVAL 数据集上的实验表明，All-Mem 在检索精度和问答性能上均显著优于代表性基准模型。
conclusion: All-Mem 通过显式的拓扑管理实现了高效且可追溯的终身记忆维护，为长期交互智能体提供了更可靠的记忆方案。
---

## 摘要
终身交互式智能体有望在数月或数年内为用户提供协助，这要求在固定的上下文和延迟预算下，持续写入长期记忆，并为每个新查询检索正确的证据。现有的记忆系统往往随着历史记录的增长而退化，产生冗余、过时或带有噪声的检索上下文。我们提出了 All-Mem，这是一个在线/离线终身记忆框架，它通过显式的、非破坏性的整合来维护拓扑结构的记忆库，避免了基于摘要压缩所特有的不可逆信息损失。在在线运行中，它将检索锚定在有界的可见表面上，以保持粗略搜索成本的有界性。在定期离线状态下，LLM 诊断器会提出带有置信度评分的拓扑编辑建议，并使用 SPLIT、MERGE 和 UPDATE 三个算子通过门控机制执行，同时保留不可变的证据以实现可追溯性。在查询时，类型化链接支持在需要时从活动锚点到归档证据进行跳数受限且有预算限制的扩展。在 LOCOMO 和 LONGMEMEVAL 上的实验表明，与代表性基准模型相比，该方法在检索和问答方面均有提升。

## Abstract
Lifelong interactive agents are expected to assist users over months or years, which requires continually writing long term memories while retrieving the right evidence for each new query under fixed context and latency budgets. Existing memory systems often degrade as histories grow, yielding redundant, outdated, or noisy retrieved contexts. We present All-Mem, an online/offline lifelong memory framework that maintains a topology structured memory bank via explicit, non destructive consolidation, avoiding the irreversible information loss typical of summarization based compression. In online operation, it anchors retrieval on a bounded visible surface to keep coarse search cost bounded. Periodically offline, an LLM diagnoser proposes confidence scored topology edits executed with gating using three operators: SPLIT, MERGE, and UPDATE, while preserving immutable evidence for traceability. At query time, typed links enable hop bounded, budgeted expansion from active anchors to archived evidence when needed. Experiments on LOCOMO and LONGMEMEVAL show improved retrieval and QA over representative baselines.

---

## 论文详细总结（自动生成）

### 论文总结：All-Mem: Agentic Lifelong Memory via Dynamic Topology Evolution

#### 1. 核心问题与整体含义（研究动机和背景）
终身交互式智能体（Lifelong Agents）需要处理跨越数月甚至数年的长程历史记忆。现有的记忆系统在历史增长时面临三大挑战：
*   **实时性约束：** 复杂的记忆重组计算量大，难以在低延迟的在线交互循环中完成。
*   **结构性漂移：** 随着时间推移，记忆项会变得纠缠、冗余或过时，导致检索到的上下文质量下降。
*   **维护安全性：** 传统的摘要或重写方法往往是不可逆的，容易造成关键证据的永久丢失。
**All-Mem** 的研究动机在于建立一个既能保持检索效率，又能通过非破坏性编辑实现记忆自我进化的系统。

#### 2. 方法论：核心思想与关键技术
All-Mem 提出了基于**动态拓扑演化**的记忆框架，核心包含三个阶段：
*   **在线/离线解耦（Online/Offline Decoupling）：**
    *   **在线阶段：** 仅进行轻量级的写入和初步链接，将搜索范围限制在“可见表面”（Visible Surface），确保交互低延迟。
    *   **离线阶段：** 定期触发“智能体拓扑整合”（ATC），处理缓存的记忆片段。
*   **智能体拓扑整合（Agentic Topology Consolidation, ATC）：**
    *   利用 LLM 诊断器提出拓扑编辑建议，并通过置信度门控执行。
    *   **三大非破坏性算子：**
        1.  **SPLIT（分裂）：** 将包含多个主题的纠缠单元拆分为多个兄弟单元。
        2.  **MERGE（合并）：** 将冗余的重复信息整合为一个规范单元，同时保留原始证据的链接。
        3.  **UPDATE（更新）：** 识别过时信息，建立新旧版本间的演化链接。
    *   所有操作均保留指向原始不可变证据的“版本链接”，确保可追溯性。
*   **拓扑感知检索（Topology-Aware Retrieval）：**
    *   **锚定：** 先在可见表面进行向量搜索找到锚点。
    *   **扩展：** 沿着类型化链接（如版本、兄弟、时间链接）进行有预算限制的跳数扩展。
    *   **重排：** 对扩展后的候选集进行最终筛选。

#### 3. 实验设计
*   **数据集：** 
    *   **LOCOMO：** 评估极长程对话记忆。
    *   **LONG MEM EVAL-S：** 评估长程交互记忆的基准。
*   **对比方法（Baselines）：** 
    *   基础方法：Full History, Naive RAG。
    *   先进记忆框架：MemGPT, A-Mem, HippoRAG2, Mem0, LightMem。
*   **评估指标：** 问答质量（GPT-4o-Judge, F1, BLEU, ROUGE）和检索质量（R@5, N@5）。

#### 4. 资源与算力
*   **模型使用：** 生成器和诊断器使用了 **GPT-4o-mini**，嵌入模型使用了 **All-MiniLM-L6-v2**。
*   **算力细节：** 论文未明确说明具体的 GPU 型号和训练时长。由于该方法主要基于 LLM API 调用和向量检索，其核心开销在于 API Token 消耗和离线整合的频率。论文通过“摊销成本”分析了离线维护的开销，指出其仅占在线成本的约 9%。

#### 5. 实验数量与充分性
*   **实验规模：** 在两个主流长程记忆基准上进行了全面测试，涵盖了数百个会话和数千个查询。
*   **消融实验：** 进行了非常详尽的消融实验（见附录 H），分别验证了可见表面门控、各个拓扑算子（SPLIT/MERGE/UPDATE）、以及检索扩展机制的有效性。
*   **公平性：** 实验在相同的 Token 预算（2048 tokens）下对比了不同方法，并绘制了“准确率-Token”帕累托曲线，证明了其在相同成本下的优越性。实验设计客观且对比充分。

#### 6. 主要结论与发现
*   **性能提升：** All-Mem 在检索精度和下游问答任务上均显著优于 Mem0 和 A-Mem 等强基准。
*   **可扩展性：** 通过将搜索空间限制在“可见表面”，检索延迟随历史增长的斜率明显低于全量搜索。
*   **可追溯性：** 实验证明 95% 的归档证据可以通过 4 跳以内的拓扑链接从可见表面找回，验证了非破坏性维护的有效性。
*   **成本效益：** 相比于频繁重写记忆的方法，All-Mem 通过离线异步整合降低了在线交互的 Token 负担。

#### 7. 优点
*   **非破坏性设计：** 创新的版本链接机制避免了信息丢失，解决了 LLM 总结容易产生幻觉或遗漏细节的问题。
*   **架构优雅：** 在线/离线解耦符合生物学中的互补学习系统（CLS）理论，兼顾了响应速度和深度整理。
*   **精细化控制：** 引入类型化链接和拓扑算子，使记忆管理从简单的“增删改”进化为“结构化演化”。

#### 8. 不足与局限
*   **LLM 依赖：** 记忆的诊断和计划高度依赖 LLM 的推理能力，若底层模型能力较弱，可能导致错误的拓扑编辑。
*   **冷启动与同步：** 离线整合存在滞后性，对于刚刚发生的、尚未整合的复杂记忆纠缠，在线阶段可能处理不足。
*   **超参数敏感：** 算子的置信度阈值（$\theta$）和检索跳数（$H_q$）等参数可能需要针对不同应用场景进行微调。

（完）
