---
title: "LongVidSearch: An Agentic Benchmark for Multi-hop Evidence Retrieval Planning in Long Videos"
title_zh: LongVidSearch：长视频中多跳证据检索规划的智能体基准测试
authors: "Rongyi Yu, Chenyuan Duan, Wentao Zhang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14468v1"
tags: ["query:multi-modal-memory"]
score: 9.0
evidence: 长视频中的智能体多跳证据检索
tldr: 针对长视频问答中智能体多步证据检索规划能力的评估难题，本文提出了 LongVidSearch 基准。该基准包含 3000 个问题，涵盖 447 个长视频，强制要求多步检索（2-4步）且缺少任一证据均无法解题。通过统一工具接口隔离检索规划与答案生成，揭示了当前顶级模型在复杂检索规划上的巨大挑战。
selection_source: fresh_fetch
motivation: 现有长视频基准缺乏对多步证据检索规划的严格约束和标准化评估接口，导致难以区分检索规划与答案生成的失败原因。
method: 构建包含 3000 个问题的 LongVidSearch 基准，采用统一工具接口并强制要求多步（2-4步）必要证据检索，涵盖四种推理类别。
result: "即使是表现最好的 GPT-5 准确率也低于 50%，而使用黄金证据时表现接近完美，表明检索规划是当前长视频问答的主要瓶颈。"
conclusion: 该基准为评估和提升智能体在长视频中的迭代检索规划能力提供了标准化平台，凸显了当前模型在处理复杂多步任务时的局限性。
---

## 摘要
长视频问答（Long-Video QA）越来越依赖于智能体工具的使用，以从长视频中检索证据。在现实场景中，这一过程通常需要多跳检索，即智能体必须迭代地收集多个不连续的证据片段。然而，现有的长视频基准测试大多是静态的：它们很少强制执行严格的多跳检索，且通常缺乏标准化的证据访问接口，这使得难以将检索规划中的失败与答案生成中的失败区分开来。为了填补这一空白，我们推出了 LongVidSearch，这是一个在标准化访问约束下评估长视频中智能体多跳证据检索规划的基准测试。LongVidSearch 强调检索的必要性：一个 Hop-k 问题恰好需要 k 个必要的证据片段，移除其中任何一个片段都会导致问题无法解决。该基准测试包含针对 447 个长视频（平均长度 26 分钟）的 3,000 个问题，涵盖四种推理类别：状态演变（State Mutation）、因果推理（Causal Inference）、全局总结（Global Summary）和视觉追踪（Visual Tracking），并包含 2 跳、3 跳和 4 跳的证据需求。为了确保公平且受控的评估，所有智能体都通过统一的工具接口与 LongVidSearch 交互，该接口固定了检索后端，并隔离了智能体构建查询和规划迭代检索的能力。除了答案准确率外，我们还衡量了工具调用成本，以分析在相同访问条件下的准确率与效率之间的权衡。我们使用三评判员多数投票法，评估了基于多种骨干大语言模型（LLM）的 VideoAgent 风格问答智能体。GPT-5 达到了最高的准确率（42.43），优于 Gemini 3 Pro（30.97）和 GPT-4o（19.20），但仍低于 50%，这凸显了多跳检索规划的难度。在提供黄金证据片段的情况下，性能接近完美，证实了检索规划是主要的瓶颈。

## Abstract
Long video question answering (Long-Video QA) increasingly relies on agentic tool use to retrieve evidence from long videos. In realistic settings, this process often requires multi-hop retrieval, where agents must iteratively gather multiple discontinuous evidence clips. However, existing long-video benchmarks are largely static: they rarely enforce strict multi-hop retrieval and typically lack a standardized evidence-access interface, making it difficult to separate failures in retrieval planning from those in answer generation. To address this gap, we introduce LongVidSearch, a benchmark for evaluating agentic multi-hop evidence retrieval planning in long videos under standardized access constraints. LongVidSearch enforces retrieval necessity: a Hop-k question requires exactly k necessary evidence clips, and removing any single clip renders the question unsolvable. The benchmark contains 3,000 questions over 447 long videos (average length 26 minutes), covering four reasoning categories: State Mutation, Causal Inference, Global Summary, and Visual Tracking, with 2-hop, 3-hop, and 4-hop evidence requirements. To ensure fair and controlled evaluation, all agents interact with LongVidSearch through a unified tool interface, which fixes the retrieval backend and isolates the agent's ability to formulate queries and plan iterative retrieval. In addition to answer accuracy, we measure tool-call cost to analyze the accuracy-efficiency trade-off under identical access conditions. We evaluate VideoAgent-style QA agents with multiple backbone LLMs using three-judge majority voting. GPT-5 achieves the highest accuracy (42.43), outperforming Gemini 3 Pro (30.97) and GPT-4o (19.20), yet remaining below 50 %, highlighting the difficulty of multi-hop retrieval planning. With gold evidence clips, performance becomes near-perfect, confirming retrieval planning as the primary bottleneck.

---

## 论文详细总结（自动生成）

### LongVidSearch：长视频中多跳证据检索规划的智能体基准测试总结

#### 1. 核心问题与研究动机
*   **研究背景**：长视频理解（如监控、讲座、纪录片）要求模型能够从长达数十分钟甚至数小时的内容中提取分布在不同时间点的证据。
*   **核心问题**：现有的长视频问答（Video QA）基准测试存在两个主要缺陷：
    1.  **必要性缺失（快捷学习）**：许多所谓的“多跳”问题可以通过单帧视觉线索或语言先验（Shortcut）解决，无需真正的多步检索。
    2.  **交互缺失（静态评估）**：传统评估多为一键式生成，无法衡量智能体（Agent）在长时程推理中的子目标分解、迭代查询和自适应工具调用能力。
*   **研究目的**：建立一个强制要求多跳检索、且能隔离检索规划能力与答案生成能力的标准化基准。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：通过“检索必要性（Retrieval Necessity）”原则构建数据集，即对于一个 $k$ 跳问题，必须检索到全部 $k$ 个证据片段才能解题，缺失任何一个片段都会导致问题不可解。
*   **智能体化构建流水线（7阶段）**：
    1.  **生成阶段**：利用 GPT-5.2 基于详细视频描述生成原始问答对。
    2.  **规则过滤**：确保证据片段在时间上不重叠（非连续性）。
    3.  **语义泄露审计**：剔除问题中已包含答案信息的“同义反复”样本。
    4.  **逻辑可行性验证**：通过思维链（CoT）检查推理逻辑是否严密。
    5.  **对抗性必要性检查（N-1 烧蚀）**：**关键步骤**。尝试在遮蔽任一证据片段的情况下回答问题，若仍能回答则剔除该样本。实验显示 46% 的逻辑有效样本因此被剔除。
    6.  **视觉对齐与精炼**：利用 Qwen3-VL-235B 检查视频像素内容，修正描述中的幻觉。
    7.  **人工审计**：专家进行“对抗性证伪”，确保最终质量。
*   **标准化工具接口**：所有智能体通过统一接口交互：`Search_Clips`（检索）、`Get_Clip_Detail`（获取片段描述）、`FINAL_ANSWER`（提交答案）。这固定了检索后端，专门测试智能体的查询构建和规划能力。

#### 3. 实验设计
*   **数据集规模**：基于 LoVR 数据集，包含 447 个长视频（平均 26 分钟），共 3,000 个 QA 对。
*   **任务分类**：
    *   **视觉追踪**（实体+聚合）：跨时长识别同一物体。
    *   **状态演变**（实体+转换）：对比物体属性的变化（如：完好 vs 破碎）。
    *   **因果推理**（叙事+转换）：理解事件间的因果逻辑。
    *   **全局总结**（叙事+聚合）：整合分散信息得出结论。
*   **对比方法**：评估了多种骨干 LLM 驱动的智能体，包括闭源（GPT-5, Gemini 3 Pro, GPT-4o, GPT-4-mini）和开源（Qwen3-VL 系列, Qwen2.5 系列, Llama-3-8B）。
*   **评估指标**：答案准确率（LLM 多数投票法）、检索成本（工具调用次数）。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号、数量或训练时长。
*   **模型使用**：在数据构建阶段使用了 GPT-5.2 和 Qwen3-VL-235B 等超大规模模型进行生成与验证；在评估阶段，使用了 GPT-5、Gemini 3 Pro 和 GPT-4o 作为自动评判员。

#### 5. 实验数量与充分性
*   **实验规模**：对 10 种主流 LLM 骨干进行了全面测试，涵盖了从 7B 到数百 B 参数规模的模型。
*   **消融与验证**：
    *   **Oracle 实验**：提供“黄金证据”片段，证明模型在已知证据时准确率接近 100%，从而确认失败主因是“检索规划”。
    *   **稳定性验证**：进行了 6,172 次人工与 LLM 评判的对比，不一致率仅为 0.87%，证明了评估协议的客观性。
    *   **难度梯度**：实验覆盖了 2-hop、3-hop、4-hop 不同难度，结果显示性能随跳数增加单调下降，符合预期。

#### 6. 主要结论与发现
*   **检索规划是瓶颈**：即使是性能最强的 GPT-5，在标准工具接口下的准确率也仅为 42.43%，远低于其在拥有黄金证据时的表现。
*   **性能鸿沟**：闭源模型（GPT-5, Gemini 3 Pro）在复杂规划上显著优于开源模型，但所有模型在 4 跳任务上均表现乏力。
*   **成本与准确率权衡**：更强的模型通常会进行更多次的工具调用（检索更深），但也存在调用次数多却准确率低的情况（规划效率低）。

#### 7. 优点
*   **严谨的必要性验证**：通过 N-1 烧蚀实验彻底杜绝了“快捷学习”，确保基准测试衡量的是真正的多步推理。
*   **解耦评估**：通过统一工具接口，成功将“检索器的性能”与“智能体的规划能力”分离，为长视频智能体研究提供了纯净的测试环境。
*   **高质量数据**：结合了最强模型生成与严格的人工对抗性审计，数据可靠性极高。

#### 8. 不足与局限
*   **检索后端固定**：虽然固定后端有利于公平比较规划能力，但也限制了对“检索-规划协同进化”能力的考察。
*   **模态损失**：智能体获取证据主要依赖于片段的文本描述（Caption），虽然经过视觉模型校验，但相比直接处理原始视频流，仍存在一定的信息压缩损失。
*   **领域覆盖**：虽然视频长度较长，但 447 个视频的场景多样性仍有提升空间，未来可扩展至更多专业领域（如医疗、法律监控）。

（完）
