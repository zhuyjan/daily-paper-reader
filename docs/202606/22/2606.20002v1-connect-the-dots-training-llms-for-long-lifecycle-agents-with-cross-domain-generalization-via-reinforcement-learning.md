---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连接点：通过强化学习训练具有跨域泛化能力的长期生命周期智能体的语言模型
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 端到端强化学习与长程轨迹，支持生命周期智能体自更新
tldr: 现有LLM代理缺乏长期部署中连续学习、自我更新上下文的元能力。提出CoD框架，采用端到端强化学习和细粒度信用分配的GRPO算法，训练LLM在长序列任务中迭代优化。实验验证该框架有效，并展现出域内、跨域及到Ralph-loop设置的泛化能力。该工作连接多项前序研究，为发展长期生命周期AI代理开辟新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 现有LLM代理缺乏在长期生命周期中连续学习、自我更新上下文以实现跨任务持续改进的元能力。
method: 提出CoD框架，通过端到端强化学习，结合长序列rollout和细粒度信用分配的GRPO算法，训练LLM获得跨域泛化的元能力。
result: 实验验证了RL训练的有效性，并展示了在训练域内、跨域以及向Ralph-loop设置的分布外泛化能力。
conclusion: CoD框架连接前序工作，为训练长期生命周期AI代理提供了新范式，释放了LLM在连续、自适应环境中的潜力。
---

## 摘要
本工作提出一个通用框架，用于训练大型语言模型（LLMs）以“连接点”（CoD），这是一种长期生命周期智能体所需的元能力：当基于LLM的AI智能体部署到环境中时，它解决一系列长期任务，同时不断探索环境、从自身经验中学习，并迭代地自我更新关于环境的上下文，从而在更新上下文的条件下在未来的任务上逐步获得更好的性能。CoD框架的主要组成部分包括：（1）用于端到端强化学习（RL）的算法设计和基础设施，其中长展开序列交织了解决任务和更新上下文的片段；（2）用于在训练过程中激励和引出LLMs中目标元能力的任务和环境，以及在评估中忠实衡量进展的任务和环境。我们展示了CoD框架的概念验证实现，包括带细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非领域特定的LLM能力或标准逐任务RL）定制的任务和环境。实证结果验证了CoD设置中端到端RL训练的有效性，并展示了引出元能力在分布外泛化（训练域内、跨不同域以及从CoD到Ralph循环设置）的潜力。我们对CoD的研究连接了之前的多条工作线，并为推进LLMs和AI智能体开辟了新的机会。为促进进一步研究和应用，我们在\url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}发布了我们的实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的基于LLM的AI智能体在长期生命周期部署中，缺少一种关键的元能力——即在同一环境中连续解决一系列不同但相关的任务时，能够主动探索、从经验中学习并自我更新上下文（context），从而在未来任务中表现更好。这种能力被定义为“连接点”（Connect the Dots, CoD）。
- **研究动机**：尽管LLM在推理和智能体任务上已有进步，但它们在长期部署中容易迷失（特别是当环境存在欠规定性时），且通常依赖人工设计的复杂脚手架才能稳定运行。作者认为，需要通过端到端强化学习（RL）显式训练LLM获得这种元能力，类似于RL对推理和智能体行为的提升。
- **整体含义**：该工作奠定了基础框架和概念验证，旨在通过RL后训练使LLM具备长期生命周期中自我改进的能力，并期望这种元能力能跨不同领域泛化。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出CoD框架，包含两个部分：
  - **CoD-Deploy**：抽象了长期生命周期部署过程，其中智能体在一个环境中交替执行“解决任务”片段（solve-task episode）与“更新上下文”片段（update-context episode）。上下文（如“hint”）被维护并用于后续任务。
  - **CoD-Train**：对应的RL后训练过程，其rollout模式与CoD-Deploy完全一致，但覆盖多样化的环境以促进泛化。训练目标不是像标准逐任务RL那样从零开始解决每个任务，而是最大化每个片段的即时奖励加上未来奖励（基于动态规划原则）。
- **关键技术细节**：
  - **RL算法**：采用GRPO风格的算法，但针对CoD-Train进行了调整。关键在于细粒度信用分配：由于每个轨迹包含多个片段（每个解决任务片段有结果奖励，每个更新上下文片段只有格式奖励），定义每个片段的回报（Return）为当前片段及未来解决任务片段的平均奖励。然后按位置分组，用组内均值作为基线计算优势（Advantage）。具体公式见附录A。
  - **梯度计算**：采用REC-OneSide-NoIS（丢弃重要性采样权重），并加入了自适应RED-Weight重加权机制（当批次平均优势为负时，通过指数软化重新平衡正负梯度），以解决训练不稳定性。
  - **基础设施**：基于Trinity-RFT框架实现，支持环境级元工作流，当前采用简单的单文本“hint”作为跨片段上下文。
  - **环境设计**：为训练和评估CoD元能力专门设计了三个环境：FrozenLake-Obscure（动作-方向映射未知，需通过试错发现）、Alchemy-Random（配方随机，需探索发现）、TerminalSimulator（模拟终端任务，需根据系统特性调整策略）。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - **训练环境**：设置A：仅FrozenLake-Obscure域，任务序列长度4；设置B：混合FrozenLake-Obscure和Alchemy-Random域，任务序列长度4。
  - **评估环境**：
    - 域内泛化：使用更难的实例（如更大地图、更多元素）和更长的序列（长度8）。
    - 跨域泛化：在训练中未见过的TerminalSimulator域上评估。
    - Ralph-loop设置：在TerminalSimulator上重复尝试同一任务，作为CoD-Deploy的特例。
- **基准模型**：初始模型为Qwen3-8B-Instruct（禁用思考）。
- **对比方法**：
  - 主要对比了不同RL算法变体：GRPO（带重要性采样权重）、REC-OneSide-NoIS、以及本文提出的最终算法（REC-OneSide-NoIS + 自适应RED-Weight）。
  - 没有与其他现有智能体方法（如LaMer、MAGE、Orbit）直接对比，因为问题设置不同。
- **消融实验**：论文附录A提供了三种RL算法的对比实验，展示了其最终算法在训练稳定性和奖励曲线上的优势。

### 4. 资源与算力

- **文中未明确说明**：没有提及使用的GPU型号、数量、训练时长或具体算力开销。作者仅提到基于Trinity-RFT框架实现，超参数列表在附录B中（如学习率1e-6、rollout batch size 32、group size 8等），但未给出训练总步数或时间。

### 5. 实验数量与充分性

- **实验数量**：
  - 两个主要训练设置（A和B），每个设置展示了训练奖励曲线（平滑后）以及在多个评估集（域内困难、跨域TerminalSimulator、Ralph-loop）上的奖励曲线。
  - 附录A中比较了三种RL算法的训练曲线和平均优势变化（约1000训练步）。
  - 未提供多次运行的标准差或置信区间，仅展示单次运行曲线。
- **充分性与公平性**：
  - **优点**：评估覆盖了域内泛化（不同难度）、跨域泛化（全新域）和Ralph-loop设置，验证了元能力的迁移性。环境设计专门针对CoD，与标准RL基准有区分。
  - **不足**：
    - 缺乏与现有方法（如LaMer、MAGE、Orbit）的直接比较，这些方法虽然在问题设置上不完全一致，但可以对比类似长序列学习场景。
    - 缺少消融实验分析不同组件（如RED-Weight、信用分配方式）的贡献。
    - 仅使用Qwen3-8B作为初始模型，未验证在更大模型或其他家族模型上的效果。
    - 训练曲线显示在混合设置（B）中Alchemy-Random性能波动，但未深入分析原因。
    - 没有统计显著性检验，可能受随机性影响。

### 6. 论文的主要结论与发现

- **有效性**：端到端RL训练（CoD-Train）能够有效引出CoD元能力。例如，在FrozenLake-Obscure上，从零解决任务的奖励从18%提升到45%，而利用自更新上下文解决第4个任务的奖励从28%提升到76%。
- **泛化性**：引出的元能力展现出域内泛化（更难实例、更长序列）、跨域泛化（从FrozenLake-Obscure/Alchemy-Random到TerminalSimulator）以及到Ralph-loop设置的泛化。
- **算法稳定性**：提出的自适应RED-Weight机制相比GRPO和标准REC-OneSide-NoIS在Alchemy-Random域上带来了更稳定的训练。
- **框架可行性**：提出的CoD框架（包括算法、环境设计）可以作为长期生命周期智能体训练的可行范式，连接了元强化学习、推理扩展和终身智能体等研究方向。

### 7. 优点：方法或实验设计上的亮点

- **问题明确**：清晰地定义了长期生命周期智能体所需的“连接点”元能力，并将其与标准逐任务RL区分。
- **方法论创新**：端到端RL训练与细粒度信用分配（基于位置平均回报）的结合，而非简单地累加奖励，使得长序列训练成为可能。
- **环境设计**：专门设计了FrozenLake-Obscure和Alchemy-Random环境，通过信息论限制（如动作映射未知、配方随机）强制激励智能体更新和维护上下文，从而精确测试CoD元能力。
- **泛化验证**：实验不仅验证了域内泛化，还跨域到TerminalSimulator，并且考虑了Ralph-loop设置，展示了元能力的多样适用性。
- **开源实现**：提供了完整代码和提示模板，有利于复现和后续研究。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅在一种模型（Qwen3-8B）和三种环境（均为游戏/模拟）上验证，未在真实世界场景（如个人助理、代码仓库维护）中测试。
  - 缺乏与现有长期智能体方法（如Voyager、Reflexion、Meta-RL for LLMs等）的对比。
  - 未执行大量重复实验以证明统计显著性，可能存在单次运行的偏差。
- **算法局限**：
  - 当前上下文管理局限于简单的静态“hint”文本，未涉及更复杂的持久化记忆、技能库或Markdown文件，可能限制了在复杂环境中的性能。
  - RL算法仍含启发式组件（如自适应RED-Weight），缺乏严格理论保证，且在混合训练中观察到性能波动。
  - 回报定义为平均奖励，可能不利于处理非常长的序列（如超过8个任务）。
- **方法局限**：
  - CoD训练假设每个任务都有结果奖励，这在真实环境中不总是可得。
  - 更新上下文片段没有直接奖励信号，仅靠格式奖励，可能导致更新质量不稳定。
  - 未探讨如何在训练中平衡探索与利用的隐性学习。
- **资源与可复现性**：未报告训练所需计算资源，使得其他团队难以估算成本或复现。
- **偏差风险**：环境设计可能偏向作者意图引出的行为，而在真实杂环境中CoD元能力可能退化。

（完）
