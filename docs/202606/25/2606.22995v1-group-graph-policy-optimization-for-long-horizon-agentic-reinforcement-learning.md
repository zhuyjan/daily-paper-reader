---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 长视界智能体强化学习的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 长视界智能体强化学习的组图策略优化
tldr: "长程智能体强化学习面临奖励稀疏延迟和信用分配粗粒度问题。现有方法将交互视为线性轨迹，忽略状态转移的图结构。本文提出G2PO，将轨迹转化为全局状态图，通过组聚合状态价值估计降低方差，并基于边定义优势估计以聚焦关键转换。在WebShop等基准上，G2PO相比GRPO取得最高22.2%的成功率提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 线性轨迹视角忽略状态转移图结构，导致信用分配粗粒度、价值估计方差大。
method: 将交互轨迹转化为全局状态转移图，组聚合状态价值估计，基于边的优势估计全局标准化TD误差。
result: "在WebShop、ALFWorld、AppWorld上优于先进基线，成功率最高提升22.2%。"
conclusion: 通过图结构建模和组聚合显著改善长程智能体强化学习的信用分配与性能。
---

## 摘要
基于组的强化学习显著增强了智能体场景中的大语言模型。为了实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步骤级训练。然而，长视界智能体强化学习面临严重的奖励稀疏和延迟问题，因为反馈通常需要数十个交互步骤才能获得。现有步骤级框架虽然细化了训练粒度，但其信用分配仍然是粗粒度的，并且将智能体探索视为孤立的线性轨迹。这种过于简化的视角忽略了状态转移固有的图结构，导致高方差的状态值估计和短视、局部的信用分配。为了克服这些关键瓶颈，我们提出了组图策略优化，这是一种专为多轮智能体任务设计的新型基于组的强化学习算法。G2PO 明确地将线性交互轨迹转化为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了组聚合状态值估计，从而降低了采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点之间的转移，并提出了以边为中心的广义优势估计策略。通过全局标准化整个图上的时序差分误差，G2PO 明确识别并优先处理驱动绝对任务进展的关键转移。在代表性长视界基准测试——WebShop、ALFWorld 和 AppWorld 上的大量实验表明，G2PO 显著优于最先进的基于提示和基于强化学习的基线，相较于 GRPO 实现了高达 22.2% 的成功率提升。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 长视界智能体强化学习的组图策略优化（G2PO）——论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有长视界智能体强化学习面临两大瓶颈：（1）**奖励稀疏与延迟**——最终反馈往往在数十步后才给出，中间步骤无即时奖励；（2）**信用分配粗粒度**——现有步骤级方法仍将智能体探索视为孤立的线性轨迹，忽略了状态转移的自然图结构。这导致状态价值估计方差高、优势评估短视且局部化。
- **核心问题**：如何将线性交互轨迹转化为全局状态转移图，从而提供更准确、全局化的信用分配，提升长程任务性能。
- **整体含义**：论文提出的 G2PO 框架通过显式建模状态图、组聚合价值估计和边中心优势估计，显著降低了价值估计方差和轨迹偏差，并能优先识别到达目标的关键转换，从而在多个长视界基准上大幅超越现有方法。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将采样的多条轨迹中的相同观测合并为**状态组节点**，动作作为节点间的**有向边**，构建全局状态转移图。
- 利用组聚合方法估计每个状态组的期望价值，降低单轨迹抽样带来的方差。
- 重新定义动作优势为边上的价值增量（1-step TD误差），并在全局范围内标准化，从而衡量每个步骤对全局目标的绝对贡献。

### 关键技术细节
1. **状态组图构建**：
   - 给定任务 \(x\)，采样 \(N\) 条完整轨迹 \(\tau_i\)，收集所有中间观测 \(o_{ij}\)。
   - 将相同观测归入同一状态组 \(G_k\)，并以状态组为节点、动作为边构建图 \(\mathcal{V},\mathcal{E}\)。
2. **组聚合状态值估计**：
   - 对每个步骤 \(j\) 计算折现回报 \(v_{ij} = \gamma^{T-j+1} R_i\)（式 (2)）。
   - 状态组价值定义为组内所有步骤价值的均值：\(V(G_k) = \frac{1}{|G_k|} \sum_{o_{ij}\in G_k} v_{ij}\)（式 (3)）。
3. **多粒度优势估计**：
   - **节点中心优势** \(A^{NC}\)：在同一状态组内，将动作导向的下一个状态组价值与本地所有可能的下一个状态组价值均值进行标准化（式 (4)）。
   - **边中心优势** \(A^{EC}\)：计算每个边上的 TD 误差 \(\delta_{ij} = V(G_{k'}) - V(G_k)\)，然后在整个图的所有边上全局标准化（式 (6)）。
   - **回合级优势** \(A^{EP}\)：沿用 GRPO 方式，基于最终奖励进行全局标准化（式 (7)）。
   - 最终优势：\(A_{ij} = A^{EP}(a_{ij}) + w \cdot (A^{NC}(a_{ij}) + A^{EC}(a_{ij}))\)（式 (8)）。
4. **目标函数**：类似于 GRPO，但优势为步骤级，使用重要性采样比和 KL 散度惩罚（式 (9)）。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - **WebShop**（电商环境，需多步搜索和选择）。
  - **ALFWorld**（具身物理环境，包括 Pick、Look、Clean、Heat、Cool、Pick2 等子任务）。
  - **AppWorld**（复杂软件生态，通过 Python API 调用完成多步交互）。
- **基准**：各任务的**成功率**（Score 或 Success Rate）。
- **对比方法**：
  - **前沿商用模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro（仅提示）。
  - **提示方法**：Qwen2.5 直接提示、ReAct、Reflexion。
  - **RL 训练方法**：PPO（带 critic）、RLOO、GiGPO、GRPO，以及 G2PO。

## 4. 资源与算力

- **WebShop / ALFWorld**：使用 **4×H100 GPU**，基础模型为 Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct，各训练 **100 次迭代**。
- **AppWorld**：使用 **8×H100 GPU**，基础模型为 Qwen2.5-14B-Instruct，训练 **50 次迭代**。
- 论文指出 G2PO 在优势计算阶段仅增加约 **1 秒额外时间**（CPU 计算），几乎无额外 GPU 开销。

## 5. 实验数量与充分性

- **主实验**：在三个基准（WebShop、ALFWorld、AppWorld）上，对 1.5B 和 7B 模型（以及 14B）进行了完整对比，包含多次随机种子平均。
- **消融实验**：从基线 \(A^{EP}\) 逐步加入 \(A^{NC}\)、组聚合 (GA)、\(A^{EC}\)，验证每个组件贡献（图 3）。
- **超参数分析**：分析平衡系数 \(w\) 的影响（图 4a）。
- **方差分析**：展示状态价值估计的方差分布，证明组聚合的必要性（图 4b）。
- **效率分析**：对比 G2PO、GRPO、GiGPO 的任务完成步数（图 4c）。
- **计算开销分析**：逐步时间拆解（图 5）。
- **组大小分析**：训练过程中平均组大小变化（附录 D）。

**充分性**：实验覆盖三个典型长视界场景，多种规模模型，与多个代表性基线对比，并进行了消融、超参数、方差、效率等多维度分析，设计合理、结论可信。但仅在同一框架（verl-agent）上验证，未在更多 RL 框架或更大模型上测试。

## 6. 论文的主要结论与发现

- G2PO 在所有基准上**一致优于所有基线**，包括前沿模型和现有 RL 方法。
- 在 WebShop 上相对 GRPO 提升成功率高达 **22.2%**（1.5B）和 **19.3%**（7B）；在 ALFWorld 上提升 **14.4%**（1.5B）和 **12.2%**（7B）。
- 组聚合价值估计能有效降低状态价值的估计方差（附录 B 理论证明）。
- 边中心优势通过全局标准化 TD 误差，能明确识别和奖励驱动关键进展的动作。
- 模型训练后完成任务的**步数更少**，推理效率更高。
- 额外计算开销极低（仅约 0.4% 的总训练时间）。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次将线性轨迹显式建模为状态转移图，利用图结构进行组聚合和全局优势标准化，突破了已有步骤级方法的局部性瓶颈。
- **理论扎实**：给出了价值估计方差和优势估计方差降低的理论证明（附录 B）。
- **工程高效**：所有额外计算在 CPU 上完成，几乎不增加 GPU 负担，且支持大规模并行采样。
- **实验全面**：涵盖三个不同领域的长视界任务，与多种最新方法对比，消融实验清晰验证组件有效性。
- **结果显著**：在多个基准上取得了大幅度提升，且可复现（代码公开）。

## 8. 不足与局限

- **框架依赖**：仅基于 verl-agent 框架实现，未在更多智能体 RL 框架（如 RAGEN、CoSo）上验证，通用性有待进一步检验。
- **采样策略有限**：仅使用独立轨迹采样，未探索树采样、异步采样等更丰富的 rollout 策略，可能限制性能提升空间。
- **图构建依赖精确观测匹配**：要求环境观测相等才能归为同一状态组，对于观测噪声大或连续状态空间的任务，可能无法直接应用。
- **模型规模验证**：最大模型为 14B，未在更大模型（如 70B）上测试，扩展性未知。
- **超参数敏感性**：平衡系数 \(w\) 对性能影响较大，需要调参；论文未提供自适应调节策略。
- **任务领域局限**：仅测试了电商、具身和软件 API 三类任务，其他如对话、游戏等长视界场景未涵盖。

（完）
