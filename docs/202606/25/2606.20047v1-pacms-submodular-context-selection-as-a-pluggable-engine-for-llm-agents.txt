Title: PACMS: Submodular Context Selection as a Pluggable Engine for LLM Agents

URL Source: https://arxiv.org/pdf/2606.20047v1

Published Time: Fri, 19 Jun 2026 00:45:50 GMT

Number of Pages: 6

Markdown Content:
# PACMS: Submodular Context Selection as a Pluggable Engine for LLM Agents 

## Manu Ghulyani 

manu@nasiko.com Nasiko Bengaluru, India 

## Arunabh Singh 

arunabh@nasiko.com Nasiko Bengaluru, India 

## Karan Bharadwaj 

karan@nasiko.com Nasiko Palo Alto, USA 

## Ankit Nath 

ankit@nasiko.com Nasiko Palo Alto, USA 

## Suranjan Goswami 

suranjan@nasiko.com Nasiko Bengaluru, India 

Abstract 

Long-running LLM agents accumulate context from conversation turns, persistent memory, and tool-call outputs, routinely exceeding the model’s token budget. The default remedy, recency truncation, is topic-blind: it discards old-but-relevant facts while retaining recent noise. We present PACMS , a budget-aware submodular selector that maximizes query-relevant coverage over the pooled candidate context while shedding redundancy, installed as a drop-in context engine for OpenClaw , an open agent framework. On a shared 100-question LongMemEval sample, PACMS matches LangChain’s production MMR on evidence-round recall but leads every baseline on end-to-end QA accuracy under two GPT-5-family readers ( +8 to 

+12 points over MMR despite recall parity), suggesting that facility-location coverage produces more extractable prompts than pairwise diversification. The ranking PACMS > top-𝑘 > MMR ≥ last-𝑘 is preserved across both readers. We demonstrate PACMS live with real-time keep/drop visualization, adjustable budget, and swappable strategy. 

CCS Concepts 

• Information systems → Information retrieval ; • Computing methodologies → Natural language processing .

Keywords 

context management, LLM agents, submodular optimization, mem-ory, retrieval 

1 Introduction 

Conversational and tool-using LLM agents operate over a con-text window that fills from several directions simultaneously. As a session proceeds, the agent accumulates user and assistant turns, entries drawn from a persistent memory store, and often largest of all, the verbatim outputs of tool calls such as file reads, search results, and API responses. Once the cumulative context exceeds the model’s token budget, the framework must decide what to keep. The prevailing mechanism is recency truncation, sometimes paired with periodic summarization. This is topic-blind: a fact es-tablished early in a session is discarded simply because it is old, even when the current user query is about exactly that fact; con-versely, verbose but irrelevant recent material is retained. Agents Candidate sources Persistent memory Conversation turns Tool outputs   

> PACMS context engine assemble(): submodular selection mem turn turn mem dup tool mem turn halos cover the relevant region; dup = redundant query
> subset Model prompt halo = coverage region of a pick

Figure 1: PACMS overview. Candidates (memory, turns, tool outputs) form a query-anchored similarity graph. PACMS selects a token-budgeted subset (dark nodes) whose coverage halos account for all query-relevant items; the redundant duplicate ( dup , dashed) lies inside a halo and is skipped. 

that must recall information across many turns, the defining case for memory, are precisely where recency truncation fails. Existing alternatives sit outside the agent’s assembly step. Re-trieval augmented generation fetches external documents into the prompt but does not arbitrate the agent’s already-present pooled context. Context-compression methods reduce token count by rewrit-ing or pruning text, but operate query-blind and lossily. Neither treats memory entries, conversation turns, and tool outputs as a sin-gle candidate pool to be selected from by relevance at the moment the prompt is assembled. 

Contributions. (1) We formulate agent context assembly as budget-constrained submodular selection over a pooled candidate set [ 6 , 7,13 ]. (2) We realize this as PACMS , a pluggable context engine inside OpenClaw that owns the assembly step and delegates compaction to the host runtime- the engine abstraction is the central systems contribution. (3) We present an interactive demo with real-time keep/drop visualization and an attendee-controllable budget. (4) We report a 100-question LongMemEval evaluation with controlled redundancy injection, recall under three budgets, and end-to-end QA accuracy under two GPT-5-family readers. 

> arXiv:2606.20047v1 [cs.IR] 18 Jun 2026

Manu Ghulyani, Arunabh Singh, Karan Bharadwaj, Ankit Nath, and Suranjan Goswami 

2 Related Work 

Retrieval and diversification. RAG [ 1 ] fetches external passages; PACMS arbitrates context already present in the agent loop. The canonical redundancy-penalizing selector is MMR [ 6]; a larger IR literature on search-result diversification includes submodular for-mulations [ 13 ] and query-dependent trade-offs [ 7, 8]. PACMS uses a facility-location objective in the same spirit but contributes its application as a pluggable context engine for LLM agents, selecting uniformly over heterogeneous pooled candidates at the assembly boundary, not as a search reranker. 

Context compression and agent memory. Prompt compression [ 27 ,28 ] prunes tokens query-blind and lossily; RECOMP [ 29 ] learns selective compression but still rewrites text. PACMS selects whole units verbatim, conditioned on the live query; no information is lost from retained items. Liu et al. [ 26 ] show that LLMs struggle to use information from the middle of long contexts; PACMS’s coverage-based selection mitigates this by keeping only query-relevant items, avoiding the dilution that long unselected contexts cause. Memory frameworks [ 18 , 19 ] manage a durable store; benchmarks such as LongMemEval [ 23 ], LoCoMo [ 24 ], and MemoryAgentBench [ 25 ]measure multi-session recall. PACMS is complementary: it is the assembly-time selection policy over whatever the store surfaces, and is engine-pluggable beneath any of these stores. 

3 System Design 3.1 Selection objective 

Let 𝐶 ={𝑐 1, . . . , 𝑐 𝑛 } be the pooled candidates, 𝑞 the latest query, 𝐵 atoken budget, and 𝑀 ⊆𝐶 a mandatory set. We define relevance as 

rel (𝑖, 𝑞 )= max (0, cos (𝑒 𝑖 , 𝑒 𝑞 )) , and coverage weight as 

𝑤 𝑖 𝑗 =rel (𝑖, 𝑞 )· max (0, cos (𝑒 𝑖 , 𝑒 𝑗 )) . Given this, PACMS maximizes 

𝐹 (𝑆 ) =∑︁ 

> 𝑖

max   

> 𝑗 ∈𝑆

𝑤 𝑖 𝑗 s.t. ∑︁  

> 𝑗 ∈𝑆

tok ( 𝑗 ) ≤ 𝐵, 𝑀 ⊆ 𝑆, (1) a monotone submodular facility-location objective, via CELF lazy-greedy [ 17 ] with a constant-factor approximation guarantee [ 14 ,15]. Three differences between PACMS and the canonical MMR base-line are worth noting. Scope: MMR’s redundancy penalty looks only at the selected set ( max 𝑖 ∈𝑆 ); PACMS’s coverage term sums over the entire pool (Í𝑖 ), rewarding items that cover regions the selected set does not yet account for. Relevance weighting: PACMS weights coverage by each target item’s relevance ( rel (𝑖 )), so it only counts covering relevant regions; MMR’s penalty is pure similarity, relevance-blind. Guarantees: PACMS’s objective is a monotone sub-modular function with a constant-factor greedy guarantee; MMR is a pairwise-penalty heuristic with no comparable bound. As §5 shows, this distinction does not yield a consistent recall edge-PACMS and MMR are comparable on recall, but PACMS does out-perform MMR substantially on end-to-end QA accuracy, suggesting that facility-location coverage produces more extractable prompts than pairwise diversification. 

3.2 Engine integration 

OpenClaw exposes context handling as a swappable engine with hooks ingest() , assemble() , and compact() . PACMS implements OpenClaw agent turn loop tool calls, memory conversation state node_modules/openclaw pacms-context-engine v0.1.0 (kind: context-engine) index.ts → fetch(/select) assemble() kept context POST /select 127.0.0.1:8077 selected_indices pacms_service.py FastAPI · port 8077 /select /select_debug PACMSSelector CELF lazy greedy 5 strategies: pacms · topk · mmr lastk · rag embedding cache (warm) embed(text) vectors Ollama local nomic-embed-text port 11434 browser demo UI keep / drop grid · token meter budget slider · strategy pills single-file HTML 

> polls /select_debug AGENT SELECTION SERVICE EMBEDDINGS

Figure 2: Runtime architecture. The OpenClaw plugin calls 

/select over local HTTP; the service runs CELF lazy-greedy with cached embeddings from a local Ollama instance. The demo UI polls /select_debug .

assemble() , sets ownsCompaction=false (delegating summariza-tion to the runtime), and treats tool outputs as first-class candidates, the same selection logic applies to a 10-token user message and a 5,000-token tool output. The selector ( PACMSSelector ) is a single Python class dispatching across five strategies ( pacms , topk , lastk ,

rag , mmr ) over shared embeddings and a shared token estimator, so observed behaviour differences are policy-level, not implemen-tation drift. It is wrapped by a FastAPI service ( 127.0.0.1:8077 )exposing /select (production) and /select_debug (per-candidate scores for the demo UI). A single selector instance persists for the life of the service so its embedding cache stays warm across turns; selection sits on the agent’s hot path, so cache warmth matters. The plugin fails soft: if the service is unreachable, OpenClaw falls back to recency truncation rather than stalling. Figure 2 shows the runtime architecture. 

4 Demonstration 4.1 Walkthrough 

At the booth, an attendee drives a live multi-turn OpenClaw ses-sion. They (1) ask about several distinct topics, each triggering a tool call (e.g., reading a file) whose output enters the context pool; (2) interleave unrelated turns; and (3) re-query an earlier topic. For every turn the screen shows, in real time: a keep/drop grid coloring each candidate by whether PACMS retained it, a token meter (total pooled vs. kept vs. dropped tokens), and a budget slider the attendee drags to watch selection tighten or relax. The scripted moment: under a tight budget, a fact established many turns earlier, one that recency truncation has dropped visibly returns to the kept set when the attendee re-asks about it. A screenshot from a real OpenClaw workspace session is in Appendix A. 

4.2 What it illustrates 

The visualization maps directly to the claims: selection is relevance-driven rather than recency-driven (old relevant facts return); tool outputs are selectable units, not opaque blobs; the budget is an ex-plicit, observable control; and behavior under pressure is graceful (information-dense originals are retained while redundant restate-ments are shed first). PACMS: Submodular Context Selection as a Pluggable Engine for LLM Agents 

Table 1: Evidence-round recall (100 questions, template-based redundancy injection). Bold = best; underline = second-best. At 20% budget PACMS trails top-𝑘 ; at 45 –70% , PACMS and lc-mmr overtake top-𝑘 as 𝑅 grows. PACMS is consistently first or second. 

𝑅 top-𝑘 lc-mmr last-𝑘 PACMS 

Budget = 20% 

0 0.790 0.421 † 0.238 0.704 2 0.800 0.552 † 0.000 0.737 4 0.808 0.668 † 0.000 0.760 8 0.817 0.836 0.001 0.804 

Budget = 45% 

0 0.928 0.743 0.521 0.860 2 0.933 0.895 0.221 0.909 4 0.938 0.966 0.003 0.952 8 0.940 0.994 0.003 0.972 

Budget = 70% 

0 0.974 0.922 0.744 0.940 2 0.978 0.993 0.620 0.991 4 0.982 0.999 0.489 0.999 

8 0.982 1.000 0.139 1.000      

> †lc-mmr at LangChain default 𝜆 =0.5; likely over-diversifies at tight budget.

5 Evaluation 

Dataset and setup. We evaluate on LongMemEval [ 23 ], a bench-mark of multi-session conversational histories designed to test long-term memory in chat assistants. Each question is paired with a multi-session haystack of interleaved user and assistant turns; the benchmark annotates which rounds contain the evidence needed to answer. LongMemEval spans six question types (single-session-user, single-session-assistant, single-session-preference, temporal-reasoning, knowledge-update, multi-session). We draw a shared sample of 100 questions uniformly at random across all six types; the same 100 questions are used for both recall (§5.1) and QA (§5.2), so the two metrics are directly comparable. We fix 𝑛 =100 as a com-pute constraint: the QA evaluation requires ∼1,600 API calls across two readers, each sending 30 –60k input tokens; sweeping addi-tional budgets or redundancy levels per reader would multiply this cost linearly. All selectors share nomic-embed-text embeddings via local Ollama; budgets are set as a per-question fraction of total context tokens. 

Metric. Evidence-round recall = |𝑆 ∩𝐸 |/| 𝐸 |: the fraction of anno-tated evidence rounds retained by selector 𝑆 under budget 𝐵 .

Baselines. (i) top-𝑘 cosine relevance; (ii) LangChain MMR (lc-mmr ), budget-bounded by walking its output ranking; (iii) last-𝑘 

(recency, query-blind). 

5.1 Evidence-round recall under redundancy 

We inject template-based paraphrases of filler rounds at levels 

𝑅 ∈{ 0, 2, 4, 8} and sweep three budgets ( 20% , 45% , 70% ). 

Recall findings. (1) Coverage’s advantage scales with budget. At 

20% budget, PACMS trails top-𝑘 at every 𝑅 (e.g. 0.704 vs 0.790 at 

𝑅 =0): when the budget forces selection of only a small handful of items, pure relevance ranking is hard to improve on. At 45% and 

70% , PACMS and lc-mmr actively suppress injected paraphrases and overtake top-𝑘 as 𝑅 growsreaching 0.972 /0.994 vs top-𝑘 ’s 0.940 

at 𝑅 =8, 45% budget. (2) PACMS and lc-mmr track closely on recall 

across all budgets and 𝑅 , within a few percentage points; we do not claim recall superiority over MMR. (3) Recency truncation collapses under redundancy at every budget: last-𝑘 drops to near zero as 𝑅 

grows, confirming that topic-blind truncation is the wrong default for long-history agents. 

5.2 End-to-end QA accuracy 

We run QA on the same 100 questions at 𝑅 =2, 45% budget. A reader model answers from the selected context; a GPT-4o-mini judge scores with the official LongMemEval CORRECT /WRONG prompt [ 23 ]. Two readers: GPT-5-mini ( reasoning_effort=minimal ) and GPT-5.4-mini ( reasoning_effort=low ). 

Table 2: QA accuracy (%) at 𝑅 =2, 45% budget, two readers. PACMS leads under both; the gap to lc-mmr widens with the stronger reader ( +8→ + 12 pts). 

Method GPT-5-mini GPT-5.4-mini top-𝑘 50.0 64.0 

lc-mmr 44.0 56.0 last-𝑘 44.0 42.0 

PACMS 52.0 68.0 

Δ (PACMS − top-𝑘 ) +2.0 +4.0

Δ (PACMS − lc-mmr ) +8.0 +12 .0

Δ (PACMS − last-𝑘 ) +8.0 +26 .0

QA findings. (1) PACMS leads under both readers. PACMS scores 

52 .0% with the GPT-5-mini reader and 68 .0% with GPT-5.4-mini higher than every other budget-constrained selector under both readers. Top-𝑘 is the closest competitor at +2 and +4 points behind, respectively; the gap widens with the stronger reader, suggesting that the coverage objective produces context that more capable readers exploit better. 

(2) The central finding: PACMS outperforms lc-mmr on QA despite comparable recall. At 45% budget and 𝑅 =2 on recall (Table 1), PACMS (0.909 ) and lc-mmr (0.895 ) are within 0.014 ; both trail top-𝑘 (0.933 ). Yet on end-to-end QA, PACMS leads by +8 points under GPT-5-mini and +12 points under GPT-5.4-mini. Even more strikingly, PACMS leads top-𝑘 on QA ( +2/+4 pts) while trailing it on recall (0.909 vs 0.933 ). Recall measures evidence retention; QA measures extractability from the assembled prompt. The two metrics diverge, and PACMS wins on the metric that matters for end users. 

(3) Recency truncation collapses under stronger readers too. last-𝑘 

scores 44 .0% and 42 .0% , essentially flat ( ±2 pts). Where every other selector benefits from the GPT-5.4-mini upgrade ( +8 to +16 pts), last-𝑘 does not- a stronger reader cannot recover from query-blind selection. Manu Ghulyani, Arunabh Singh, Karan Bharadwaj, Ankit Nath, and Suranjan Goswami 

Scope and limitations. The redundancy in §5.1 is template-based natural variation (synonym swaps, hedges, conversational framing), not LLM-generated paraphrase or real-user data; it is a controlled stress test, not a benchmark. The footnote on the 20% lc-mmr rows reflects an open question (likely an interaction with LangChain’s default 𝜆 mult =0.5 at very tight budget) that we have not yet investi-gated. For QA accuracy (Table 2), we report one budget ( 45% ) and one redundancy level ( 𝑅 =2) across two readers; broader sweeps are deferred to future work. We omit a full-context QA row because LongMemEval haystacks reach 750k characters, exceeding our 300k -character reader truncation; under that truncation, a full-context row would chop evidence systematically and be uninformative as a ceiling. All absolute QA numbers are lower than LongMemEval’s published full-context baselines (e.g., GPT-4o “Offline Reading” at 

91 .8% [23 ]), which use optimized retrieval pipelines with no bud-get constraint; our numbers reflect selection under a strict 45% 

budget and are meaningful as relative comparisons among budget-constrained methods. 

6 Conclusion and Future Work 

PACMS recasts agent context assembly as budget-constrained sub-modular selection and delivers it as a pluggable engine in the Open-Claw agent framework, selecting uniformly over memory, conversa-tion, and tool outputs at the assembly boundary. The demonstration makes the resulting keep/drop and token trade-offs directly observ-able and manipulable. On a shared 100-question LongMemEval sample, PACMS matches the canonical MMR baseline on evidence-round recall but leads on end-to-end QA accuracy by +8 points with GPT-5-mini and +12 points with GPT-5.4-mini at 45% budget-and even outperforms top-𝑘 on QA despite trailing it on recall. This divergence between recall and QA is the paper’s central find-ing: facility-location coverage produces prompts that downstream readers extract from more effectively than either pairwise diver-sification or pure relevance ranking. The ranking PACMS > top-

𝑘 > lc-mmr ≥ last-𝑘 holds across both readers. The selector, evaluation harnesses (recall and QA), and the Open-Claw context-engine plugin are released as open source for repro-ducibility. Future work includes learned/adaptive relevance in place of fixed embedding similarity; cross-session memory curation (using selection statistics to decide which items to persist, compress, or evict from the memory store); extension to multi-agent settings where several agents share a context pool; and on-policy evaluation in live agent deployments, as recent work argues that static memory benchmarks understate interactive agent behavior. 

References 

[1] P. Lewis, E. Perez, A. Piktus, F. Petroni, V. Karpukhin, N. Goyal, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS, 2020. [2] V. Karpukhin, B. Oğuz, S. Min, P. Lewis, L. Wu, S. Edunov, D. Chen, W.-T. Yih. Dense Passage Retrieval for Open-Domain Question Answering. EMNLP, 2020. [3] G. Izacard, M. Caron, L. Hosseini, S. Riedel, P. Bojanowski, A. Joulin, E. Grave. Unsupervised Dense Information Retrieval with Contrastive Learning. TMLR, 2022. [4] S. Xiao, Z. Liu, P. Zhang, N. Muennighoff. C-Pack: Packed Resources For General Chinese Embeddings. SIGIR, 2024. [5] Z. Nussbaum, J. X. Morris, B. Duderstadt, A. Mulyar. Nomic Embed: Training a Reproducible Long Context Text Embedder. arXiv:2402.01613, 2024. [6] J. Carbonell and J. Goldstein. The Use of MMR, Diversity-Based Reranking for Reordering Documents and Producing Summaries. SIGIR, pp. 335–336, 1998. [7] R. L. T. Santos, C. Macdonald, I. Ounis. Selectively Diversifying Web Search Results. CIKM, 2010. [8] R. L. T. Santos, C. Macdonald, I. Ounis. Exploiting Query Reformulations for Web Search Result Diversification. WWW, 2010. [9] R. L. T. Santos, C. Macdonald, I. Ounis. Search Result Diversification. Foundations and Trends in Information Retrieval, 9(1), 2015. [10] R. Agrawal, S. Gollapudi, A. Halverson, S. Ieong. Diversifying Search Results. WSDM, 2009. [11] C. L. A. Clarke, M. Kolla, G. V. Cormack, O. Vechtomova, A. Ashkan, S. Büttcher, I. MacKinnon. Novelty and Diversity in Information Retrieval Evaluation. SIGIR, 2008. [12] M. Drosou, E. Pitoura. Search Result Diversification. SIGMOD Record, 39(1), 2010. [13] H. Lin and J. Bilmes. A Class of Submodular Functions for Document Summa-rization. ACL-HLT, 2011. [14] G. L. Nemhauser, L. A. Wolsey, M. L. Fisher. An Analysis of Approximations for Maximizing Submodular Set Functions. Mathematical Programming, 14(1), 1978. [15] S. Khuller, A. Moss, J. S. Naor. The Budgeted Maximum Coverage Problem. Information Processing Letters, 70(1), 1999. [16] A. Krause, D. Golovin. Submodular Function Maximization. In Tractability: Prac-tical Approaches to Hard Problems. Cambridge University Press, 2014. [17] J. Leskovec, A. Krause, C. Guestrin, C. Faloutsos, J. VanBriesen, N. Glance. Cost-Effective Outbreak Detection in Networks. KDD, 2007. [18] C. Packer, V. Fang, S. G. Patil, K. Lin, S. Wooders, J. E. Gonzalez. MemGPT: Towards LLMs as Operating Systems. arXiv:2310.08560, 2023. [19] Mem0: A Memory Layer for LLM Applications. https://github.com/mem0ai/ mem0, 2024. [20] J. S. Park, J. C. O’Brien, C. J. Cai, M. R. Morris, P. Liang, M. S. Bernstein. Generative Agents: Interactive Simulacra of Human Behavior. UIST, 2023. [21] G. Wang, Y. Xie, Y. Jiang, A. Mandlekar, C. Xiao, Y. Zhu, L. Fan, A. Anandkumar. Voyager: An Open-Ended Embodied Agent with Large Language Models. TMLR, 2024. [22] S. Yao, J. Zhao, D. Yu, N. Du, I. Shafran, K. Narasimhan, Y. Cao. ReAct: Synergizing Reasoning and Acting in Language Models. ICLR, 2023. [23] D. Wu, H. Wang, W. Yu, Y. Zhang, K.-W. Chang, D. Yu. LongMemEval: Bench-marking Chat Assistants on Long-Term Interactive Memory. ICLR, 2025. [24] A. Maharana, D.-H. Lee, S. Tulyakov, M. Bansal, F. Barbieri, Y. Fang. Evaluating Very Long-Term Conversational Memory of LLM Agents. ACL, 2024. [25] Y. Hu, Y. Wang, J. McAuley. Evaluating Memory in LLM Agents via Incremental Multi-Turn Interactions (MemoryAgentBench). ICLR, 2026. arXiv:2507.05257. [26] N. F. Liu, K. Lin, J. Hewitt, A. Paranjape, M. Bevilacqua, F. Petroni, P. Liang. Lost in the Middle: How Language Models Use Long Contexts. TACL, 2024. [27] H. Jiang, Q. Wu, C.-Y. Lin, Y. Yang, L. Qiu. LLMLingua: Compressing Prompts for Accelerated Inference of Large Language Models. EMNLP, 2023. [28] Z. Pan, Q. Wu, H. Jiang, M. Xia, X. Luo, J. Zhang, Q. Lin, V. Rühle, Y. Yang, C.-Y. Lin, H. V. Zhao, L. Qiu, D. Zhang. LLMLingua-2: Data Distillation for Efficient and Faithful Task-Agnostic Prompt Compression. ACL Findings, 2024. [29] F. Xu, W. Shi, E. Choi. RECOMP: Improving Retrieval-Augmented LMs with Compression and Selective Augmentation. ICLR, 2024. [30] H. Chase. LangChain. https://github.com/langchain-ai/langchain, 2022. [31] J. Liu. LlamaIndex. https://github.com/run-llama/llama_index, 2022. [32] M. Douze, A. Guzhva, C. Deng, J. Johnson, G. Szilvasy, P.-E. Mazaré, M. Lomeli, L. Hosseini, H. Jégou. The Faiss Library. arXiv:2401.08281, 2024. 

A Demo writeup: PACMS as the OpenClaw context engine on a real session A.1 System summary 

LLM agent frameworks accumulate multi-source context across each user session- persistent memory, conversation turns, tool outputs, retrieved documents. The combined context routinely exceeds the underlying model’s effective window long before any single source does. Existing agent platforms handle this through ad-hoc heuris-tics: drop the oldest turns (recency truncation), keep only the top-𝑘 

retrieved memories (relevance truncation), or summarize. None of these treat all sources as a single budget-constrained selection problem; none have a principled objective; none expose the decision to the user. PACMS is a context-engine plugin for the OpenClaw agent frame-work. On every model invocation, OpenClaw pools all candidate context items: memory entries, conversation turns, tool outputs, PACMS: Submodular Context Selection as a Pluggable Engine for LLM Agents 

and asks PACMS which subset fits the model’s token budget. PACMS treats this as a submodular maximum-coverage problem under a knapsack constraint, solved by a cost-aware greedy with a constant-factor approximation guarantee. The selector is pluggable: the same interface accepts top-𝑘 , MMR, recency, RAG, or PACMS strategies, so practitioners can A/B test selection policies without changing the rest of the stack. 

A.2 Concrete integration 

PACMS is shipped as a published OpenClaw plugin ( pacms-context -engine v0.1.0, kind: "context-engine" ) that delegates com-paction to the OpenClaw runtime ( ownsCompaction: false ) and owns assembly only. Each assemble() call POSTs the candidate pool, query, and budget to a local FastAPI service ( pacms_service.py )which runs the selection in Python and returns the kept indices. The service is independent of OpenClaw, it can also be driven by an inspection UI for debugging selection behavior, which is what Figure 3 shows. 

A.3 What the figure shows 

Figure 3 shows a real OpenClaw workspace session. Across four sessions over eleven days, the user added Rust to their coding envi-ronment notes (writing to TOOLS.md and USER.md ) and then asked the assistant to verify Rust was actually installed. The assistant ran 

rustc --version and rustup show ; both returned command not found ; the final assistant turn (highlighted) offers to help install Rust. Before producing that response, PACMS selected 17 of 168 

pooled candidates to fit the model’s budget. The keep/drop panel on the right shows which candidates were retained: the relevant tool failures, the prior Rust addition, the closest preceding user turns. Earlier session content unrelated to the Rust query (model-identity questions, file-listing exchanges from prior days) was dropped. Manu Ghulyani, Arunabh Singh, Karan Bharadwaj, Ankit Nath, and Suranjan Goswami OpenClaw — workspace /Users/manu/.openclaw/workspace 

OC OpenClaw 

with pacms-context-engine v0.1.0 PACMS active 

CONVERSATION 11 days · 4 sessions · 168 messages 

… earlier sessions: identity questions, file listings, agent-config exploration 

USER · SUN 12:27 

How many files are there in my workspace 

ASSISTANT · TOOL: EXEC(LS -1 | WC -L) 

There are 25 files in your workspace. 

USER · SUN 12:27 

which files are related to coding 

ASSISTANT · SUN 12:27 

The files related to coding in your workspace include: 1. USER.md — preferences related to your coding style (e.g., language preferences). 2. TOOLS.md — notes about your coding environment, like preferred libraries or tools. 

USER · SUN 12:28 

i want to add rust to my coding env 

ASSISTANT · TOOLS: WRITE(TOOLS.MD), EDIT(USER.MD) 

I've added Rust to your coding environment notes and updated your favorite languages. If you need any specific configurations or additional information about Rust, just let me know! 

USER · SUN 12:28 

i meant check whether rust is there 

TOOL CALLS · EXEC(RUSTC --VERSION) ; EXEC(RUSTUP SHOW) 

zsh:1: command not found: rustc zsh:1: command not found: rustup 

ASSISTANT · SUN 12:28 · USING 17 OF 168 CANDIDATES / ≈3,800 TOKENS KEPT 

PACMS CONTEXT ENGINE last selection · 356 ms Strategy pacms topk mmr lastk 

Budget 3,800 tok 

168 

POOLED 

17 

KEPT 

151 

DROPPED 

3,800 kept (36%) 6,750 dropped (64%) 

KEEP / DROP GRID conversational content shown 167 KEPT 

166 KEPT 

164 KEPT 

162 KEPT 

156 KEPT 

154 KEPT 

155 KEPT 

152 KEPT 

151 KEPT 

150 KEPT 

149 DROP 

146 DROP 

> ASST

"It looks like Rust is not currently installed on this system…" 

> TOOL

zsh:1: command not found: rustup 

> TOOL

zsh:1: command not found: rustc 

> USER

"i meant check whether rust is there" 

> ASST

"I've added Rust to your coding environment notes…" 

> TOOL

Successfully wrote 106 bytes to TOOLS.md 

> TOOL

Successfully replaced 1 block(s) in USER.md. 

> USER

"i want to add rust to my coding env" 

> ASST

"USER.md — coding preferences; TOOLS.md — coding env notes" 

> USER

"which files are related to coding" 

> ASST

"There are 25 files in your workspace." 

> USER

Figure 3: PACMS as the OpenClaw context engine on a real workspace session. The user added Rust to their coding environment notes, then asked the assistant to verify Rust was installed; the tool calls ( rustc , rustup ) returned command not found ; PACMS selected 17 of 168 pooled candidates to assemble the model’s prompt. The right-hand panel visualizes the keep/drop verdict per candidate, the strategy pill ( pacms ), the token budget, and the kept/dropped split. Transport-level heartbeat messages are omitted from the keep/drop grid for visual clarity; the underlying /select call ran on the full 168 -message pool. All visible text in the figure is verbatim from a captured OpenClaw session.