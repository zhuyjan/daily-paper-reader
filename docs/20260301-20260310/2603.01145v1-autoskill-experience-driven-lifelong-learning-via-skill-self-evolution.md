---
title: "AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution"
title_zh: AutoSkill：通过技能自我演化的经验驱动型终身学习
authors: "Yutao Yang, Junsong Li, Qianjun Pan, Bihao Zhan, Yuxuan Cai, Lin Du, Jie Zhou, Kai Chen, Qin Chen, Xin Li, Bo Zhang, Liang He"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01145v1"
tags: ["query:main"]
score: 10.0
evidence: LLM智能体技能的持续自我演化
tldr: 针对大语言模型在交互中难以积累个性化能力的局限，本文提出AutoSkill框架。该框架通过从对话轨迹中自动提取、维护和演化“技能”，将零散的交互经验转化为可重用的显式知识。AutoSkill作为模型无关的插件层，支持在不重新训练的情况下动态注入相关技能，实现了智能体的终身学习与个性化能力的持续进化，为构建个性化数字代理提供了新路径。
selection_source: fresh_fetch
motivation: 现有的LLM智能体难以从重复的交互经验中沉淀个性化需求，导致无法跨会话积累长期能力。
method: 提出一种经验驱动的终身学习框架，通过抽象、自演化和动态注入机制，将交互轨迹转化为标准化的可重用技能。
result: 实现了一个模型无关的插件层，能够跨代理、用户和任务共享并复用持续进化的个性化技能。
conclusion: AutoSkill为构建可扩展的终身个性化智能体提供了一条无需重训练且具备显式知识沉淀能力的实用路径。
---

## 摘要
在实际的大语言模型（LLM）应用中，用户会反复表达稳定的偏好和需求，例如减少幻觉、遵循机构写作规范或避免过于专业的措辞，然而这类交互经验很少被整合为可重用的知识。因此，LLM 智能体往往无法在不同会话之间积累个性化能力。我们提出了 AutoSkill，这是一个经验驱动的终身学习框架，使 LLM 智能体能够从对话和交互轨迹中自动推导、维护和重用技能。AutoSkill 从用户经验中抽象出技能，支持其持续的自我演化，并在不重新训练底层模型的情况下，将相关技能动态注入到未来的请求中。作为一个与模型无关的插件层，它与现有的 LLM 兼容，并引入了标准化的技能表示，以便在智能体、用户和任务之间进行共享和迁移。通过这种方式，AutoSkill 将短暂的交互经验转化为显式、可重用且可组合的能力。本文描述了 AutoSkill 的动机、架构、技能生命周期和实现，并将其与先前关于记忆、检索、个性化和智能体系统的研究进行了对比定位。AutoSkill 为实现终身个性化智能体和个人数字代理提供了一条实用且可扩展的路径。

## Abstract
In practical LLM applications, users repeatedly express stable preferences and requirements, such as reducing hallucinations, following institutional writing conventions, or avoiding overly technical wording, yet such interaction experience is seldom consolidated into reusable knowledge. Consequently, LLM agents often fail to accumulate personalized capabilities across sessions. We present AutoSkill, an experience-driven lifelong learning framework that enables LLM agents to automatically derive, maintain, and reuse skills from dialogue and interaction traces. AutoSkill abstracts skills from user experience, supports their continual self-evolution, and dynamically injects relevant skills into future requests without retraining the underlying model. Designed as a model-agnostic plugin layer, it is compatible with existing LLMs and introduces a standardized skill representation for sharing and transfer across agents, users, and tasks. In this way, AutoSkill turns ephemeral interaction experience into explicit, reusable, and composable capabilities. This paper describes the motivation, architecture, skill lifecycle, and implementation of AutoSkill, and positions it with respect to prior work on memory, retrieval, personalization, and agentic systems. AutoSkill highlights a practical and scalable path toward lifelong personalized agents and personal digital surrogates.

---

## 论文详细总结（自动生成）

以下是对论文《AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体在与用户交互时，虽然能通过记忆机制保留部分对话片段，但难以将用户反复表达的稳定偏好（如写作风格、特定工作流、避坑指南等）转化为**可重用的显式能力**。这导致用户在不同会话中往往需要重复陈述相同的要求。
*   **研究动机**：
    *   **经验流失**：短暂的交互经验未能沉淀为长期能力。
    *   **现有方案局限**：参数微调成本高且难以控制；基于检索的记忆（RAG）通常只处理文本事实，而非操作性的行为模式。
    *   **目标**：构建一个无需重训练、可自我演化、且具备显式表示的“技能层”，实现智能体的终身个性化学习。

### 2. 方法论
AutoSkill 提出了一个**无需训练（Training-free）**的插件式框架，核心思想是将交互经验转化为结构化的“技能卡片”（`SKILL.md`）。

*   **核心架构：双环机制**
    1.  **技能增强响应环（下环）**：
        *   **查询重写**：将用户当前输入重写为利于检索的形式。
        *   **混合检索**：结合语义向量（Dense）和关键词（BM25）计算相关性分数：$Rel(q, s) = \lambda \hat{d}(q, s) + (1-\lambda) \hat{b}(q, s)$。
        *   **动态注入**：将 Top-K 相关技能以 Prompt 形式注入上下文，引导模型生成符合用户习惯的回复。
    2.  **技能演化环（上环）**：
        *   **技能提取**：仅基于用户侧的查询序列（避免模型幻觉干扰），通过 Prompt 抽象出可重用的规则、约束或工作流。
        *   **技能管理（Judge）**：将新提取的候选技能与现有技能库对比，由 LLM 判定执行三种操作：**新增（Add）**、**合并（Merge）**或**丢弃（Discard）**。
        *   **版本化合并**：若判定为合并，则更新现有技能的版本号（如从 v0.1.0 升至 v0.1.34），实现能力的持续精炼。

*   **关键技术细节**：
    *   **标准化表示**：技能包含名称、描述、触发器、标签、Prompt 指令、示例和版本号。
    *   **模块化设计**：所有功能（重写、提取、判定、合并）均由特定 Prompt 驱动的 LLM 担任，具有模型无关性。

### 3. 实验设计
*   **数据集**：使用大规模真实用户交互语料库 **WildChat-1M**。
*   **实验场景**：
    *   筛选出轮次超过 8 轮的高质量对话。
    *   划分为四个子集：中文 GPT-3.5、英文 GPT-3.5、中文 GPT-4、英文 GPT-4。
*   **对比与分析维度**：
    *   **统计分析**：统计了提取技能的数量、类别分布（编程、写作、数据处理等）、高频标签及平台相关性。
    *   **演化分析**：通过版本号的变化展示技能如何随交互深入而不断优化。
    *   **案例研究**：展示了“顶级心理咨询师”、“专业文本改写”、“小红书种草”等具体技能卡片，验证其跨领域、跨语言的有效性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **原因分析**：由于该框架是**无需训练（Training-free）**的，其核心开销在于推理时的 API 调用或本地模型推理。实验主要侧重于对 WildChat 数据集的离线处理和技能库构建，而非模型参数的更新。

### 5. 实验数量与充分性
*   **实验规模**：处理了数万场对话，最终从不同子集中提取了数百至上千个显式技能（如英文 GPT-3.5 子集提取了 631 个技能）。
*   **充分性评价**：
    *   **数据量充分**：基于百万级真实对话日志，覆盖了多样化的用户需求。
    *   **定性分析深入**：通过详细的技能卡片展示和版本演化路径，直观证明了框架的可行性。
    *   **定量指标略显单一**：实验主要集中在统计分布和案例展示，缺乏与传统 RAG 或其他个性化方法在特定任务（如指令遵循准确率）上的端到端定量对比基准（Benchmark）。

### 6. 主要结论与发现
*   **经验可转化性**：证明了可以从非结构化的对话中自动提取出高质量、结构化的操作性技能。
*   **持续演化能力**：通过版本化合并机制，技能库能够随着用户反馈的增加而变得更加精准，而非简单的信息堆积。
*   **跨模型兼容性**：AutoSkill 作为插件层，在 GPT-3.5 和 GPT-4 上均表现出良好的适配性，能够显著增强智能体的个性化表现。

### 7. 优点与亮点
*   **显式且可控**：技能以 Markdown 形式存储，人类用户可直接查看、编辑或手动修正，解决了“黑盒”记忆难以干预的问题。
*   **低成本部署**：无需微调模型，通过 Prompt 工程即可实现终身学习，适合快速部署。
*   **版本管理**：引入了类似软件工程的版本控制思想，使能力的演化过程透明且可追溯。
*   **开源贡献**：提供了 Web UI、SDK 和 OpenAI 兼容的代理接口，具有极高的实用价值。

### 8. 不足与局限
*   **评估指标主观性**：目前主要依赖 LLM 作为评判者（Judge）和案例展示，缺乏客观的自动化性能评估指标。
*   **提取噪声风险**：如果用户输入本身存在错误或矛盾，提取出的技能可能包含错误指令，需要更强的过滤机制。
*   **推理延迟**：在线生成时增加了查询重写和检索环节，背景运行的提取和维护任务也会消耗额外的推理资源。
*   **长尾覆盖**：对于极低频的交互，系统可能难以形成有效的技能沉淀。

（完）
