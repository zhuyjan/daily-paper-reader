---
title: "AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution"
title_zh: AutoSkill：通过技能自我演进实现的经验驱动型终身学习
authors: "Yutao Yang, Junsong Li, Qianjun Pan, Bihao Zhan, Yuxuan Cai, Lin Du, Jie Zhou, Kai Chen, Qin Chen, Xin Li, Bo Zhang, Liang He"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01145v2"
tags: ["query:main"]
score: 9.0
evidence: 通过技能自我进化实现智能体的终身学习
tldr: 针对大语言模型在交互中难以积累个性化能力的局限，本文提出AutoSkill框架。该框架通过从对话轨迹中自动提取、维护和演化“技能”，将零散的交互经验转化为可重用的显式知识。AutoSkill作为模型无关的插件层，支持在不重新训练的情况下动态注入相关技能，实现了智能体的终身学习与个性化能力沉淀，为构建个人数字代理提供了可扩展路径。
selection_source: fresh_fetch
motivation: 现有的LLM智能体难以从重复的交互经验中巩固并复用用户特定的偏好和需求。
method: 提出一种经验驱动的终身学习框架，通过技能自演化机制从对话中抽象、维护并动态注入标准化技能。
result: 实现了无需重新训练即可在不同会话、用户和任务间共享与复用个性化能力。
conclusion: AutoSkill为实现具备终身学习能力的个性化智能体提供了一种实用且可扩展的技术方案。
---

## 摘要
在实际的大语言模型（LLM）应用中，用户会反复表达稳定的偏好和需求，例如减少幻觉、遵循机构写作规范或避免过于专业的措辞，然而这类交互经验很少被整合为可重用的知识。因此，LLM 智能体往往无法在不同会话之间积累个性化能力。我们提出了 AutoSkill，这是一个经验驱动的终身学习框架，使 LLM 智能体能够从对话和交互轨迹中自动推导、维护和重用技能。AutoSkill 从用户经验中抽象出技能，支持其持续的自我演进，并在不重新训练底层模型的情况下，将相关技能动态注入到未来的请求中。作为一个与模型无关的插件层，它与现有的 LLM 兼容，并引入了一种标准化的技能表示，以便在智能体、用户和任务之间进行共享和迁移。通过这种方式，AutoSkill 将短暂的交互经验转化为显式、可重用且可组合的能力。本文描述了 AutoSkill 的动机、架构、技能生命周期和实现，并阐述了其相对于内存、检索、个性化和智能体系统等先前工作的定位。AutoSkill 为实现终身个性化智能体和个人数字代理提供了一条实用且可扩展的路径。

## Abstract
In practical LLM applications, users repeatedly express stable preferences and requirements, such as reducing hallucinations, following institutional writing conventions, or avoiding overly technical wording, yet such interaction experience is seldom consolidated into reusable knowledge. Consequently, LLM agents often fail to accumulate personalized capabilities across sessions. We present AutoSkill, an experience-driven lifelong learning framework that enables LLM agents to automatically derive, maintain, and reuse skills from dialogue and interaction traces. AutoSkill abstracts skills from user experience, supports their continual self-evolution, and dynamically injects relevant skills into future requests without retraining the underlying model. Designed as a model-agnostic plugin layer, it is compatible with existing LLMs and introduces a standardized skill representation for sharing and transfer across agents, users, and tasks. In this way, AutoSkill turns ephemeral interaction experience into explicit, reusable, and composable capabilities. This paper describes the motivation, architecture, skill lifecycle, and implementation of AutoSkill, and positions it with respect to prior work on memory, retrieval, personalization, and agentic systems. AutoSkill highlights a practical and scalable path toward lifelong personalized agents and personal digital surrogates.

---

## 论文详细总结（自动生成）

以下是对论文《AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在实际应用中，用户常会重复表达稳定的偏好（如“不要幻觉”、“遵循特定写作风格”），但现有的 LLM 智能体通常将这些交互视为瞬时的对话上下文，无法将其转化为可持久、可复用的“能力”。
*   **研究动机**：
    *   **现有方法的局限**：参数微调（Fine-tuning）成本高且难以频繁更新；基于记忆（Memory-based）的方法通常只存储事实或对话片段，而非可操作的行为逻辑。
    *   **目标**：建立一种无需重新训练模型、能够从交互经验中自动提取、维护并演化“显式技能”的终身学习框架，使智能体真正成为个性化的数字代理。

### 2. 方法论：核心思想与技术细节
AutoSkill 采用**无需训练（Training-free）**的架构，通过两个耦合的循环实现：
*   **技能增强响应循环（在线）**：
    1.  **查询重写**：将用户输入重写为适合检索的独立查询。
    2.  **混合检索**：结合语义向量（Dense）和关键词（BM25）进行检索，公式为：$Rel(q_t, s) = \lambda \hat{d}(q_t, s) + (1-\lambda) \hat{b}(q_t, s)$。
    3.  **技能注入**：将检索到的 Top-K 技能以 Markdown 格式注入 Prompt，引导模型生成符合用户习惯的回复。
*   **技能演进循环（后台异步）**：
    1.  **技能提取**：仅基于用户侧的查询序列（避免模型幻觉干扰），抽象出具有持久性、可复用性的技能候选。
    2.  **技能管理决策**：通过 LLM 判断新候选技能与现有技能库的关系，做出 **Add（新增）**、**Merge（合并）** 或 **Discard（丢弃）** 的决策。
    3.  **版本化合并**：若选择合并，则进行语义融合并更新版本号（如从 v0.1.0 升至 v0.1.1），实现技能的持续精炼。
*   **核心载体**：定义了标准化的 `SKILL.md` 格式，包含名称、描述、执行指令（Prompt）、触发器、标签、示例和版本号。

### 3. 实验设计
*   **数据集**：使用大规模真实用户交互语料库 **WildChat-1M**。
*   **实验场景**：筛选出轮次超过 8 轮的高质量对话，划分为四个子集：中文 GPT-3.5、英文 GPT-3.5、中文 GPT-4、英文 GPT-4。
*   **对比与分析**：
    *   **统计分析**：分析了提取出的技能在编程、写作、数据处理等领域的分布。
    *   **案例研究**：展示了如“顶级心理咨询师”、“专业文本重写”、“小红书种草笔记”等具体技能卡的演进过程。
    *   **版本演进分析**：通过版本号的变化（如某技能经历 34 次迭代）证明了框架的持续学习能力。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：由于该框架是**无需训练（Training-free）**的，其核心开销在于调用 LLM API（如 GPT-4o/GPT-3.5）进行推理、重写、提取和合并操作。系统实现支持 Docker 部署，主要资源消耗在于向量数据库的维护和 LLM 的 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：处理了数万场对话（共计约 60 万条消息），从中提取并分析了约 1858 个显式技能。
*   **充分性评价**：
    *   **广度**：覆盖了中英双语和不同能力的模型家族，实验数据量较大。
    *   **客观性**：使用了真实世界的用户日志而非合成数据，具有较高的实际参考价值。
    *   **局限性**：实验部分侧重于**定性分析和统计描述**，缺乏与传统 RAG 或 Long-context 方法在特定任务上的硬性指标（如准确率、召回率）的定量对比实验。

### 6. 主要结论与发现
*   **经验可转化性**：证明了可以将零散的交互经验转化为结构化的显式技能。
*   **技能演进规律**：高频任务（如编程、写作）的技能演进速度最快，版本更新频繁；而特定领域（如心理咨询）的技能更趋向于行为风格的稳定。
*   **框架有效性**：AutoSkill 能够作为现有 LLM 的插件层，在不改变模型参数的前提下，显著提升智能体的个性化表现和任务一致性。

### 7. 优点与亮点
*   **显式化与可解释性**：技能以 Markdown 形式存储，用户和开发者可直接查看、编辑和干预，解决了“黑盒”记忆的问题。
*   **版本控制机制**：引入了类似软件工程的版本管理，使技能的每一次优化都可追溯。
*   **模型无关性**：作为插件层，可以轻松适配各种底层大模型，具有极强的工程落地潜力。
*   **异步演进**：将耗时的技能提取与维护放在后台，不影响前端响应延迟。

### 8. 不足与局限
*   **定量评估缺失**：缺乏针对“技能注入后任务成功率提升”的端到端定量 Benchmark 测试。
*   **依赖底层模型**：技能提取的质量高度依赖于所使用的 LLM 的抽象能力，若底层模型较弱，可能生成冗余或错误的技能。
*   **噪声风险**：虽然有 Discard 机制，但在长期的开放域交互中，如何彻底过滤无效或冲突的技能仍是一个挑战。
*   **应用限制**：目前主要针对文本交互，对于涉及多模态或复杂工具调用的技能演进讨论较少。

（完）
