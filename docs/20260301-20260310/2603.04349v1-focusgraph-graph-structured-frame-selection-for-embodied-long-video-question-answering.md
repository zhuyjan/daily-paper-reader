---
title: "FocusGraph: Graph-Structured Frame Selection for Embodied Long Video Question Answering"
title_zh: FocusGraph：面向具身长视频问答的图结构帧选择
authors: "Tatiana Zemskova, Solomon Andryushenko, Ilya Obrubov, Viktoriia Khoruzhaia, Ekaterina Eroshenko, Ekaterina Derevyanka, Dmitry Yudin"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04349v1"
tags: ["query:main"]
score: 9.0
evidence: 具身长视频问答的图结构帧选择
tldr: 针对具身智能体在长视频理解中面临的帧数过多导致多模态大模型性能下降和推理延迟问题，本文提出FocusGraph框架。该框架利用基于图结构的文本描述进行关键片段筛选，并结合免训练的补丁级稀疏流保留技术提取关键帧。FocusGraph在FindingDory和HourVideo等长视频问答基准上取得了SOTA性能，在提升准确性的同时显著降低了推理时间。
selection_source: fresh_fetch
motivation: 多模态大模型在处理长视频时，过多的输入帧会引发模型性能退化并显著增加推理计算成本。
method: 采用基于图结构场景描述的轻量级LLM选择器筛选片段，并配合补丁级稀疏流保留方法提取关键帧。
result: 在FindingDory和HourVideo等长视频问答基准测试中，该方法在保持高效推理的同时实现了最先进的准确率。
conclusion: FocusGraph证明了通过紧凑的文本表征和高效的帧筛选机制，可以有效解决长视频理解中的效率与精度平衡问题。
---

## 摘要
理解长视频的能力对于具身智能体至关重要，因为其有效性取决于它们积累、组织和利用长时感知记忆的能力。近年来，多模态大语言模型（MLLM）因其理解自然语言和利用世界知识的通用能力，在解决长视频理解任务方面日益受到关注。然而，随着提供给 MLLM 的帧数增加，其响应质量往往会下降，且推理时间会增长。因此，在使用 MLLM 进行长视频理解时，从视频中选择关键帧以回答用户查询是一个关键步骤。在这项工作中，我们开发了 FocusGraph，这是一个用于长第一视角视频问答的关键帧选择框架。它利用一个轻量级的可训练场景字幕 LLM 选择器（Scene-Caption LLM Selector），根据基于图的字幕选择与查询相关的片段，并采用一种无需训练的方法从这些片段中选择关键帧。与现有方法不同，所提出的场景字幕 LLM 选择器不依赖于原始的低分辨率帧序列；相反，它在场景的紧凑文本表示上运行。随后，我们设计了一种无需训练的分块稀疏流保留（Patch-wise Sparse-Flow Retention, PSFR）方法，从生成的片段序列中选择关键帧，并将其输入 MLLM 以生成最终答案。这些组件共同使 FocusGraph 在包括 FindingDory 和 HourVideo 在内的具有挑战性的第一视角长视频问答基准测试中取得了最先进的结果，同时与基准方法相比显著减少了推理时间。

## Abstract
The ability to understand long videos is vital for embodied intelligent agents, because their effectiveness depends on how well they can accumulate, organize, and leverage long-horizon perceptual memories. Recently, multimodal LLMs have been gaining popularity for solving the long video understanding task due to their general ability to understand natural language and to leverage world knowledge. However, as the number of frames provided to an MLLM increases, the quality of its responses tends to degrade, and inference time grows. Therefore, when using MLLMs for long video understanding, a crucial step is selecting key frames from the video to answer user queries.   In this work, we develop FocusGraph, a framework for keyframe selection for question answering over long egocentric videos. It leverages a lightweight trainable Scene-Caption LLM Selector that selects query-relevant clips based on their graph-based captions, and a training-free method for selecting keyframes from these clips. Unlike existing methods, the proposed Scene-Caption LLM Selector does not rely on the original sequence of low-resolution frames; instead, it operates on a compact textual representation of the scene. We then design a training-free Patch-wise Sparse-Flow Retention (PSFR) method to select keyframes from the resulting sequence of clips, which are fed into an MLLM to produce the final answer. Together, these components enable FocusGraph to achieve state-of-the-art results on challenging egocentric long-video question answering benchmarks, including FindingDory and HourVideo, while significantly reducing inference time relative to baseline approaches.

---

## 论文详细总结（自动生成）

这是一份关于论文《FocusGraph: Graph-Structured Frame Selection for Embodied Long Video Question Answering》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：具身智能体（如机器人）在现实环境中需要处理长达数小时的视频流。虽然多模态大语言模型（MLLM）在视频理解上表现出色，但直接处理长视频面临两大挑战：一是**推理成本极高**，二是随着输入帧数增加，模型会出现**注意力“稀释”效应**，导致回答质量下降。
*   **核心问题**：如何在保证理解准确性的前提下，从冗余的长视频中高效筛选出与用户查询（Query）最相关的关键帧？
*   **研究动机**：现有的两阶段方法通常将视频视为图像序列处理，效率较低。本文旨在通过“文本化场景图”来压缩视频表征，并结合底层视觉特征进行快速帧提取。

### 2. 方法论
FocusGraph 采用模块化设计，将长视频理解解耦为两个阶段：

*   **核心思想**：先利用轻量级 LLM 在压缩的文本空间进行片段筛选，再利用免训练的视觉算法在选定片段内提取关键帧。
*   **关键技术细节**：
    1.  **片段级场景图构建**：将视频切分为固定长度的片段（Clips），利用预训练 MLLM（如 Qwen2.5-VL）为每个片段生成包含对象列表、空间关系和动作描述的层次化文本场景图。
    2.  **场景字幕 LLM 选择器（Scene-Caption LLM Selector）**：
        *   将文本化的场景描述通过 ModernBERT 转换为嵌入向量。
        *   使用轻量级适配器（MLP 或 GAT）将嵌入对齐到 LLM 的潜在空间。
        *   LLM 根据查询指令，从文本序列中识别出包含答案的 M 个候选片段。
    3.  **补丁级稀疏流保留（PSFR）算法**：
        *   **第一阶段**：在选定片段内，利用稀疏光流（Lucas-Kanade）追踪图像补丁中的角点。当大量补丁丢失追踪目标时，判定为场景发生了显著结构变化（PSFR 事件）。
        *   **第二阶段**：结合边缘密度、灰度熵、运动幅度和低保留率计数等特征，计算质量分数，在固定预算（如 K=16 帧）内提取最具代表性的关键帧。
    4.  **程序进化（Program Evolution）**：利用自动化工具（OpenEvolve）优化 PSFR 的选择逻辑，在给定的计算预算内最大化证据覆盖率。

### 3. 实验设计
*   **数据集与场景**：
    *   **FindingDory**：专注于具身操作任务的基准，包含移动导航、空间任务和时间推理任务。
    *   **HourVideo**：包含长达 1 小时的第一视角视频，涵盖导航、感知、视觉推理和总结等任务。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括均匀采样（Uniform Sampling）、MaxInfo（基于体积最大化的免训练方法）、ReMEmbR（基于智能体的检索增强生成）、GenS（生成式帧采样器）和 ViaRL（基于强化学习的迭代放大方法）。
    *   **评估指标**：Relaxed Accuracy（放松准确率）、Overall Score、推理时间、Token 消耗量。

### 4. 资源与算力
*   **训练阶段**：
    *   **硬件**：4 台 NVIDIA A100 GPU。
    *   **时长**：约 6 小时。
    *   **数据量**：在 GenS-Video-150K 数据集及额外的 170K 场景字幕数据上进行全参数微调。
*   **推理阶段**：
    *   **硬件**：NVIDIA RTX 4090。
    *   **模型规模**：LLM 选择器使用 Qwen2.5-3B，最终 QA 使用 Qwen2.5-7B。
    *   **效率**：PSFR 算法完全在 CPU 上运行，处理单帧仅需约 0.021 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流长视频基准上进行了全面测试，FindingDory 验证集包含数百个复杂任务，HourVideo 包含 1182 个问题。
*   **消融实验**：
    *   测试了不同适配器（GAT vs MLP）的影响。
    *   验证了时间范围标注（Time Range）对时间推理任务的增益。
    *   对比了 PSFR 算法在不同优化目标（Intersection vs Inclusion）下的表现。
*   **客观性与公平性**：实验对比了从轻量级开源模型到顶级闭源模型（GPT-4o, Gemini 2.0）的多种配置，并详细记录了推理延迟和 Token 成本，评估维度较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：FocusGraph 在 FindingDory 和 HourVideo 上达到了 SOTA 或接近 SOTA 的水平，尤其在具身导航和空间推理任务中表现优异。
*   **效率极高**：相比于 GenS（103s）和 ReMEmbR（80s），FocusGraph 的片段选择器推理仅需 0.6s，且每帧消耗的 Token 数小于 1 个，显著降低了长视频处理的开销。
*   **解耦有效性**：证明了将“语义相关性筛选”与“视觉冗余消除”解耦是处理长视频的有效路径。

### 7. 优点
*   **创新性表征**：引入层次化文本场景图，将复杂的视觉搜索转化为高效的文本向量检索。
*   **免训练视觉提取**：PSFR 算法无需训练且运行极快，能够捕捉到 MLLM 容易忽略的底层运动和结构变化。
*   **自动化优化**：利用程序进化自动搜索最优的关键帧选择策略，减少了人工调参的偏差。

### 8. 不足与局限
*   **预处理开销**：虽然推理很快，但第一步构建“片段级场景图”需要调用 MLLM 处理所有视频帧，这在实时流处理中可能存在初始延迟。
*   **信息丢失风险**：将视频压缩为文本描述不可避免地会丢失某些细微的视觉细节（如物体的精确纹理或微小动作），可能影响极高精度要求的问答。
*   **场景依赖性**：在视觉极其单调或光照剧烈变化的环境中，基于光流的 PSFR 算法可能会产生较多噪声。

（完）
