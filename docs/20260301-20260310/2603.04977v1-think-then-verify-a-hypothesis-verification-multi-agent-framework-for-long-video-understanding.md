---
title: "Think, Then Verify: A Hypothesis-Verification Multi-Agent Framework for Long Video Understanding"
title_zh: 先思考，后验证：一种用于长视频理解的假设-验证多智能体框架
authors: "Zheng Wang, Haoran Chen, Haoxuan Qin, Zhipeng Wei, Tianwen Qian, Cong Bai"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04977v1"
tags: ["query:main"]
score: 9.0
evidence: 基于假设验证的多智能体长视频理解框架
tldr: 针对长视频理解中存在的视觉冗余和语义漂移问题，本文提出了一种名为 VideoHV-Agent 的多智能体框架。该框架遵循“先思考后验证”的原则，将视频问答重构为结构化的假设验证过程。通过 Thinker 生成假设、Judge 提取线索、Verifier 进行细粒度验证以及 Answer 整合证据，该方法在多个长视频基准测试中取得了 SOTA 性能，并显著提升了推理的可解释性与逻辑性。
selection_source: fresh_fetch
motivation: 长视频推理中常见的链式思考和检索机制容易积累语义漂移，需要一种先制定任务目标再进行针对性验证的主动推理模式。
method: 构建了一个包含 Thinker、Judge、Verifier 和 Answer 智能体的框架，将问答过程转化为假设重写、线索提取、细粒度验证和证据整合的结构化流程。
result: 在三个长视频理解基准测试中均达到了最先进的准确率，且具备更低的计算成本和更强的逻辑一致性。
conclusion: 研究表明，通过结构化的假设验证机制可以有效克服长视频理解中的冗余干扰，实现更精准且可解释的视频推理。
---

## 摘要
长视频理解具有挑战性，原因在于密集的视觉冗余、长程时间依赖，以及思维链（chain-of-thought）和基于检索的智能体容易积累语义漂移和相关性驱动的错误。我们认为，长视频推理不应始于反应式检索，而应始于深思熟虑的任务制定：模型必须首先阐明，为了使每个候选答案成立，视频中必须满足哪些条件。这种“先思考后寻找”的原则促使了 VideoHV-Agent 的诞生，该框架将视频问答重新表述为一个结构化的假设-验证过程。基于视频摘要，Thinker（思考者）将候选答案重写为可测试的假设，Judge（评判者）推导出指定必须检查哪些证据的判别性线索，Verifier（验证者）利用局部的细粒度视频内容对线索进行定位和测试，最后 Answer（回答）智能体整合经过验证的证据以生成最终答案。在三个长视频理解基准测试上的实验表明，VideoHV-Agent 在实现最先进准确率的同时，还提供了增强的可解释性、更高的逻辑合理性以及更低的计算成本。我们在 https://github.com/Haorane/VideoHV-Agent 公开了代码。

## Abstract
Long video understanding is challenging due to dense visual redundancy, long-range temporal dependencies, and the tendency of chain-of-thought and retrieval-based agents to accumulate semantic drift and correlation-driven errors. We argue that long-video reasoning should begin not with reactive retrieval, but with deliberate task formulation: the model must first articulate what must be true in the video for each candidate answer to hold. This thinking-before-finding principle motivates VideoHV-Agent, a framework that reformulates video question answering as a structured hypothesis-verification process. Based on video summaries, a Thinker rewrites answer candidates into testable hypotheses, a Judge derives a discriminative clue specifying what evidence must be checked, a Verifier grounds and tests the clue using localized, fine-grained video content, and an Answer agent integrates validated evidence to produce the final answer. Experiments on three long-video understanding benchmarks show that VideoHV-Agent achieves state-of-the-art accuracy while providing enhanced interpretability, improved logical soundness, and lower computational cost. We make our code publicly available at: https://github.com/Haorane/VideoHV-Agent.

---

## 论文详细总结（自动生成）

以下是对论文《Think, Then Verify: A Hypothesis-Verification Multi-Agent Framework for Long Video Understanding》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长视频理解（Long Video Understanding）面临视觉冗余严重、长程时间依赖复杂以及推理过程中容易产生“语义漂移”和“相关性驱动错误”的问题。
*   **研究动机**：现有的多智能体框架大多是“相关性驱动”的，即先进行反应式检索（寻找语义相关的片段），再进行推理。这种方式容易导致早期检索错误在推理链中累积。
*   **核心理念**：提出“先思考，后寻找”的原则。模型不应盲目搜索，而应先明确：**“如果某个候选答案是正确的，视频中必须出现什么证据？”** 将视频问答（VideoQA）重构为一个结构化的“假设-验证”过程。

### 2. 方法论：VideoHV-Agent 框架
该框架包含三个阶段，由四个专门的智能体协作完成：
*   **阶段一：上下文摘要（Context Summarization）**
    *   将视频帧转换为文本描述（Captioning），并根据问题生成一个简明、查询相关的视频摘要。
*   **阶段二：两步推理（Two-step Reasoning）—— 核心创新**
    *   **假设生成（Thinker Agent）**：将每个候选选项重写为显式的、可测试的**假设**（包含实体、动作、时间/因果约束）。
    *   **线索提取（Judge Agent）**：对比不同假设，提取出一个**判别性线索（Clue）**，即区分这些假设所需的最小视觉观察。
    *   **假设验证（Verifier Agent）**：根据线索定位视频片段，调用细粒度工具（如详细描述）获取证据，并输出验证状态（已验证、部分验证、未验证）。
    *   **自优化循环（Self-Refinement）**：如果验证状态为“未验证”，则触发循环，重新生成更具体或更具辨别力的假设和线索。
*   **阶段三：证据整合（Evidence Integration）**
    *   **答案生成（Answer Agent）**：整合视频摘要和经过验证的细粒度证据，输出最终答案及透明的推理链。

### 3. 实验设计
*   **数据集**：
    *   **EgoSchema**：大规模第一视角长视频推理（3分钟视频）。
    *   **NextQA**：侧重因果和时间推理（平均43秒）。
    *   **IntentQA**：侧重角色意图理解。
    *   **VideoMME-L**：全方位长视频评估（包含超长视频）。
*   **对比方法**：包括 VideoAgent、VideoTree、VideoMultiAgents、VideoAgent2 等最先进的智能体框架，以及 TS-LLAVA、LVNet 等模型。
*   **评估指标**：准确率（Accuracy %）和推理效率（耗时）。

### 4. 资源与算力
*   **模型后端**：所有智能体均使用 **GPT-4o** 作为大语言模型（LLM）后端。
*   **视觉工具**：使用 LaViLa 或 CogAgent 生成帧级描述。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到视频按 1 fps 采样，且由于采用了针对性的线索验证机制，其推理延迟（123.66s/题）低于 VideoTree 等同类方法。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试和超长视频测试集上进行了全面对比。
*   **消融实验**：非常充分。分别验证了“假设生成”、“线索提取”和“验证状态”对性能的影响，结果显示移除任何组件都会导致 2.4% 至 7% 的性能下降。
*   **分析维度**：涵盖了不同问题类型（因果、时间、描述）、循环次数分析、效率对比以及定性案例分析。
*   **客观性**：通过控制变量法（使用相同的 LLM 和 Captioner 后端）与现有方法进行公平对比，证明了架构本身的优越性。

### 6. 主要结论与发现
*   **性能领先**：VideoHV-Agent 在所有测试的数据集上均达到了零样本（Zero-shot）下的 SOTA 性能。
*   **逻辑稳健**：在 NextQA 的 ATP-hard（困难子集）上提升显著，证明该方法在处理复杂因果推理时比传统检索方法更有效。
*   **高效性**：通过“线索引导”的局部验证，避免了对长视频的反复全量扫描，显著降低了计算成本。
*   **可解释性**：框架输出的“假设-线索-证据”链条为最终答案提供了清晰的逻辑支撑。

### 7. 优点（亮点）
*   **范式转变**：从“相关性检索”转向“逻辑假设验证”，有效解决了长视频推理中的语义漂移问题。
*   **模块化设计**：四个智能体分工明确，Judge 智能体提取“判别性线索”的设计非常精巧，减少了冗余验证。
*   **自修复能力**：引入自优化循环，使模型在证据不足时能够主动修正推理方向。

### 8. 不足与局限
*   **后端依赖**：高度依赖 GPT-4o 等强力 LLM 的推理能力，在较弱的开源模型上表现可能受限。
*   **采样频率**：固定 1 fps 的采样可能会丢失极短时间内的关键动作（虽然这在长视频任务中不常见）。
*   **成本风险**：虽然推理时间缩短，但多智能体多次调用 API 可能会产生较高的 Token 消耗成本。
*   **初始描述质量**：如果第一阶段的帧级 Caption 质量极差，可能会影响后续 Thinker 的判断。

（完）
