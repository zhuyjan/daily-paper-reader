---
title: "Think, Then Verify: A Hypothesis-Verification Multi-Agent Framework for Long Video Understanding"
title_zh: 先思考，后验证：一种用于长视频理解的假设-验证多智能体框架
authors: "Zheng Wang, Haoran Chen, Haoxuan Qin, Zhipeng Wei, Tianwen Qian, Cong Bai"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04977v1"
tags: ["query:main"]
score: 9.0
evidence: 用于长视频理解的假设验证多智能体框架
tldr: 针对长视频理解中视觉冗余和语义漂移的挑战，本文提出 VideoHV-Agent 多智能体框架。该框架倡导“先思后找”原则，将视频问答转化为结构化的假设验证过程：由 Thinker 生成假设，Judge 提取关键线索，Verifier 进行细粒度验证，最后由 Answer 整合证据。实验证明该方法在多个长视频基准测试中达到 SOTA 水平，显著提升了推理的逻辑性与可解释性。
selection_source: fresh_fetch
motivation: 长视频推理中传统的链式思考或检索方法容易积累语义漂移和相关性错误，缺乏主动的任务规划与验证机制。
method: 构建了一个包含 Thinker、Judge、Verifier 和 Answer 智能体的框架，通过将候选答案重写为可验证假设并进行针对性证据核实来完成推理。
result: 在三个长视频理解基准测试上取得了最先进的准确率，同时具备更低的计算成本和更强的逻辑合理性。
conclusion: 结构化的假设验证流程是解决长视频复杂推理问题的有效途径，能显著提高模型处理长程时空依赖的能力。
---

## 摘要
长视频理解具有挑战性，原因在于密集的视觉冗余、长程时间依赖，以及思维链（chain-of-thought）和基于检索的智能体容易积累语义漂移和相关性驱动的错误。我们认为，长视频推理不应始于反应式检索，而应始于深思熟虑的任务制定：模型必须首先阐明，为了使每个候选答案成立，视频中必须满足哪些条件。这种“先思考后寻找”的原则促使了 VideoHV-Agent 的诞生，该框架将视频问答重新表述为一个结构化的假设-验证过程。基于视频摘要，Thinker（思考者）将候选答案重写为可测试的假设，Judge（评判者）推导出指定必须检查哪些证据的判别性线索，Verifier（验证者）利用局部的细粒度视频内容对线索进行定位和测试，最后 Answer（回答）智能体整合经过验证的证据以生成最终答案。在三个长视频理解基准测试上的实验表明，VideoHV-Agent 在实现最先进准确率的同时，还提供了增强的可解释性、更高的逻辑合理性以及更低的计算成本。我们在 https://github.com/Haorane/VideoHV-Agent 公开了代码。

## Abstract
Long video understanding is challenging due to dense visual redundancy, long-range temporal dependencies, and the tendency of chain-of-thought and retrieval-based agents to accumulate semantic drift and correlation-driven errors. We argue that long-video reasoning should begin not with reactive retrieval, but with deliberate task formulation: the model must first articulate what must be true in the video for each candidate answer to hold. This thinking-before-finding principle motivates VideoHV-Agent, a framework that reformulates video question answering as a structured hypothesis-verification process. Based on video summaries, a Thinker rewrites answer candidates into testable hypotheses, a Judge derives a discriminative clue specifying what evidence must be checked, a Verifier grounds and tests the clue using localized, fine-grained video content, and an Answer agent integrates validated evidence to produce the final answer. Experiments on three long-video understanding benchmarks show that VideoHV-Agent achieves state-of-the-art accuracy while providing enhanced interpretability, improved logical soundness, and lower computational cost. We make our code publicly available at: https://github.com/Haorane/VideoHV-Agent.

---

## 论文详细总结（自动生成）

以下是对论文《Think, Then Verify: A Hypothesis-Verification Multi-Agent Framework for Long Video Understanding》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长视频理解（Long Video Understanding）面临三大挑战：**视觉冗余严重**、**长程时间依赖复杂**以及**推理错误累积**。
*   **现有方法的局限：** 传统的思维链（CoT）或基于检索的智能体通常是“相关性驱动”的，即先根据问题检索相关片段再进行推理。这种“反应式检索”容易导致语义漂移（Semantic Drift），且无法有效处理复杂的因果或时序逻辑。
*   **核心动机：** 作者提出推理应始于“深思熟虑的任务制定”，即**先思考（Think）后验证（Verify）**。在寻找证据前，模型应先明确：若某个答案正确，视频中必须出现哪些具体证据。

### 2. 方法论：VideoHV-Agent 框架
该框架将视频问答（VideoQA）重构为一个结构化的“假设-验证”过程，包含四个核心智能体：
1.  **上下文摘要（Context Summarization）：** 首先将视频帧转化为文本描述，并生成一个与问题相关的简明摘要，用于初步筛选候选答案。
2.  **Thinker（思考者）智能体：** 将每个候选答案重写为显式的、可测试的**假设（Hypothesis）**，明确其中涉及的实体、动作及因果约束。
3.  **Judge（评判者）智能体：** 对比不同假设，提取出最具判别性的**线索（Clue）**，即区分不同选项所需的最小视觉观察点。
4.  **Verifier（验证者）智能体：** 根据线索定位视频片段，调用细粒度描述工具获取证据，并输出验证状态（已验证、部分验证、未验证）。
5.  **自我修正循环（Self-Refinement Loop）：** 若验证结果为“未验证”，则触发循环，重新生成更具体或更具辨别力的假设和线索。
6.  **Answer（回答）智能体：** 整合摘要信息与经过验证的证据，生成最终答案及透明的推理链。

### 3. 实验设计
*   **数据集：**
    *   **EgoSchema：** 评估 3 分钟第一视角视频的深度推理能力。
    *   **NextQA：** 侧重于日常视频中的因果和时序推理。
    *   **IntentQA：** 关注叙事视频中的角色意图理解。
    *   **VideoMME-L：** 涵盖超长视频（平均 40 分钟以上）的综合基准。
*   **对比方法：** 包括 VideoAgent、VideoTree、VideoMultiAgents、VideoAgent2 等最先进的智能体框架，以及 TS-LLAVA、LLaVA-OV 等多模态大模型。

### 4. 资源与算力
*   **模型骨干：** 使用 **GPT-4o** 作为所有智能体的核心 LLM。
*   **视觉工具：** 使用 LaViLa 或 CogAgent 生成帧级描述。
*   **算力细节：** 论文**未明确说明**具体的 GPU 硬件型号、数量或总训练时长。由于该框架主要基于预训练模型和 API 调用（Agent 模式），其核心开销在于推理时的 API 调用和视频处理。论文给出了推理效率对比：在 EgoSchema 上平均每题耗时 123.66 秒，优于其他智能体方法。

### 5. 实验数量与充分性
*   **实验规模：** 在四个主流长视频基准上进行了测试，涵盖了从短时（43s）到超长时（2h）的视频范围。
*   **消融实验：** 针对“假设生成”、“线索提取”和“验证状态”分别做了消融，证明了每个模块对准确率的贡献（分别提升了 5% 到 7%）。
*   **多样性分析：** 进行了循环次数分析、不同 LLM 后端（Claude-3, GPT-3.5）的鲁棒性测试，以及针对不同问题类型（因果、时序、描述）的性能分析。
*   **评价：** 实验设计全面、客观，通过控制变量法排除了模型后端对架构优越性的干扰，论证充分。

### 6. 主要结论与发现
*   **性能领先：** VideoHV-Agent 在所有测试的零样本（Zero-shot）基准上均达到了 SOTA 水平（例如在 EgoSchema 上达到 81.0% 的准确率）。
*   **逻辑稳健：** 相比于相关性检索，假设验证机制能显著减少幻觉，尤其在处理复杂的因果关系（ATP-hard 子集）时表现更优。
*   **效率提升：** 通过精准定位和减少全视频扫描，该方法在保持高精度的同时，推理时间比同类智能体框架更短。

### 7. 优点（亮点）
*   **范式创新：** 从“被动检索”转向“主动验证”，符合人类处理复杂信息的认知逻辑。
*   **高可解释性：** 提供了从假设到证据再到结论的完整推理链，每一步决策都有据可查。
*   **模块化强：** 智能体分工明确，易于集成更强大的视觉工具或语言模型。
*   **自适应性：** 引入自我修正机制，能够处理初始信息不足或线索模糊的情况。

### 8. 不足与局限
*   **依赖高质量初始摘要：** 如果初始的粗粒度摘要完全遗漏了关键信息，Thinker 可能会生成错误的假设，虽然有循环机制，但仍存在失败风险。
*   **API 成本：** 作为一个多智能体框架，多次调用 GPT-4o 等闭源模型 API 会产生较高的经济成本。
*   **实时性限制：** 尽管比其他 Agent 快，但对于需要即时响应的场景（如实时监控），百秒级的推理延迟仍然过高。

（完）
