---
title: "Wiki-R1: Incentivizing Multimodal Reasoning for Knowledge-based VQA via Data and Sampling Curriculum"
title_zh: Wiki-R1：通过数据与采样课程激励基于知识的视觉问答多模态推理
authors: "Shan Ning, Longtian Qiu, Xuming He"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05256v1"
tags: ["query:main"]
score: 10.0
evidence: 激励基于知识的视觉问答的多模态推理
tldr: 针对基于知识的视觉问答（KB-VQA）中外部知识检索噪声大及分布差异显著的问题，本文提出Wiki-R1框架。该框架采用课程强化学习，通过可控数据生成和采样策略，系统性地激励多模态大模型的推理能力。Wiki-R1在Encyclopedic VQA和InfoSeek基准上均刷新了SOTA记录，显著提升了模型在复杂知识推理任务中的表现。
selection_source: fresh_fetch
motivation: 预训练多模态大模型在处理结构化百科知识和噪声检索时存在分布鸿沟，导致其在KB-VQA任务中推理困难。
method: 提出Wiki-R1框架，利用可控课程数据生成和采样策略，通过强化学习逐步引导模型从易到难掌握知识推理。
result: "在Encyclopedic VQA和InfoSeek基准上，Wiki-R1分别实现了37.1%和44.1%的准确率，均创下新的SOTA记录。"
conclusion: 通过数据与采样的双重课程设计，强化学习能有效弥合分布差距并显著增强多模态大模型的知识推理能力。
---

## 摘要
基于知识的视觉问答（KB-VQA）要求模型通过整合外部知识来回答有关图像的问题，由于检索噪声以及知识库的结构化、百科全书式特性，这带来了巨大的挑战。这些特性导致了其与预训练多模态大语言模型（MLLMs）之间的分布差异，使得在后训练阶段进行有效的推理和领域自适应变得困难。在这项工作中，我们提出了 Wiki-R1，这是一个基于数据生成的课程强化学习框架，旨在系统地激励 MLLMs 在 KB-VQA 中的推理能力。Wiki-R1 构建了一系列与模型不断进化的能力相匹配的训练分布，从而弥合了从预训练到 KB-VQA 目标分布之间的差距。我们引入了“可控课程数据生成”，通过操纵检索器来生成所需难度水平的样本，以及一种“课程采样策略”，用于选择在强化学习更新期间可能产生非零优势的信息样本。样本难度通过观察到的奖励进行估计，并传播到未观察到的样本以指导学习。在两个 KB-VQA 基准测试 Encyclopedic VQA 和 InfoSeek 上的实验表明，Wiki-R1 取得了新的最先进（SOTA）结果，在 Encyclopedic VQA 上的准确率从 35.5% 提高到 37.1%，在 InfoSeek 上从 40.1% 提高到 44.1%。项目页面见 https://artanic30.github.io/project_pages/WikiR1/。

## Abstract
Knowledge-Based Visual Question Answering (KB-VQA) requires models to answer questions about an image by integrating external knowledge, posing significant challenges due to noisy retrieval and the structured, encyclopedic nature of the knowledge base. These characteristics create a distributional gap from pretrained multimodal large language models (MLLMs), making effective reasoning and domain adaptation difficult in the post-training stage. In this work, we propose \textit{Wiki-R1}, a data-generation-based curriculum reinforcement learning framework that systematically incentivizes reasoning in MLLMs for KB-VQA. Wiki-R1 constructs a sequence of training distributions aligned with the model's evolving capability, bridging the gap from pretraining to the KB-VQA target distribution. We introduce \textit{controllable curriculum data generation}, which manipulates the retriever to produce samples at desired difficulty levels, and a \textit{curriculum sampling strategy} that selects informative samples likely to yield non-zero advantages during RL updates. Sample difficulty is estimated using observed rewards and propagated to unobserved samples to guide learning. Experiments on two KB-VQA benchmarks, Encyclopedic VQA and InfoSeek, demonstrate that Wiki-R1 achieves new state-of-the-art results, improving accuracy from 35.5\% to 37.1\% on Encyclopedic VQA and from 40.1\% to 44.1\% on InfoSeek. The project page is available at https://artanic30.github.io/project_pages/WikiR1/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Wiki-R1: Incentivizing Multimodal Reasoning for Knowledge-based VQA via Data and Sampling Curriculum》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：基于知识的视觉问答（KB-VQA）要求模型结合图像和外部百科知识（如维基百科）进行推理。然而，现有的多模态大模型（MLLM）在处理此类任务时面临两大障碍：
    1.  **分布鸿沟**：预训练数据与结构化、百科全书式的外部知识库之间存在显著的分布差异。
    2.  **检索噪声与奖励稀疏**：在强化学习（RL）阶段，由于检索系统不可避免地引入噪声，导致模型难以获得有效的正向反馈。作者观察到，直接在 KB-VQA 上应用 RL 时，超过 80% 的样本具有“零优势”（Zero Advantage），训练效率极低。
*   **研究动机**：通过构建一种“课程学习”机制，系统性地引导模型从简单的、低噪声的分布逐步过渡到复杂的、真实世界的 KB-VQA 目标分布，从而激励模型的多模态推理能力。

### 2. 方法论：Wiki-R1 框架
Wiki-R1 采用基于数据生成的课程强化学习框架，包含两个核心组件：
*   **可控课程数据生成（Controllable Data Generation）**：
    *   通过操纵检索器来控制训练难度。定义难度等级 $g$：
        *   **简单阶段 ($g=0$)**：强制检索结果仅包含 ground-truth（正确答案所在的）知识片段。
        *   **中间阶段**：逐渐增加干扰项（噪声片段）的数量。
        *   **困难阶段 ($g=G$)**：不再保证包含 ground-truth，完全模拟推理时的真实分布。
    *   **动态调整**：根据模型在训练过程中的实时准确率（滑动窗口平均值）自动升级难度等级。
*   **带观测传播的课程采样（Curriculum Sampling with Observation Propagation）**：
    *   **采样策略**：优先选择训练准确率接近 0.5 的样本，因为这些样本处于模型的“学习区”，能提供最强的梯度信号。
    *   **观测传播**：针对 RL 中奖励信号极其稀疏的问题，利用知识库文章之间的相似性（如 TF-IDF 或语义嵌入）构建图，将已观察到的样本奖励传播给未观察到的相似样本，从而实现对全量数据难度的可靠估计。
*   **训练目标**：在 DAPO（一种高效的 RL 算法）基础上，结合上述课程机制优化模型策略 $\pi_\theta$。

### 3. 实验设计
*   **数据集**：
    *   **Encyclopedic VQA (EVQA)**：包含 22.1 万个关于细粒度实体的问答对。
    *   **InfoSeek**：包含 130 万个信息寻求型视觉问题。
    *   **ViQuAE**：用于测试模型的零样本（Zero-shot）迁移和泛化能力。
*   **Benchmark 与对比方法**：
    *   **对比对象**：包括零样本 MLLMs（如 BLIP-2, LLaVA-1.5, Qwen2.5-VL, GPT-4V）和最先进的 RAG 方法（如 Wiki-LLaVA, EchoSight, ReflectiVA）。
    *   **评估指标**：EVQA 使用 BEM 分数，InfoSeek 使用官方准确率脚本，ViQuAE 使用 F1 和 Exact Match。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练时长**：
    *   3B 参数版本：约 **9 小时**。
    *   7B 参数版本：约 **12 小时**。
*   **数据规模**：仅使用了 4 万个平衡样本（20k 来自 EVQA，20k 来自 InfoSeek），远少于基线方法使用的百万级数据，体现了极高的数据效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流大场景基准上进行了详尽测试，并额外在第三个数据集上验证了泛化性。
*   **消融实验**：分别验证了“数据课程”、“采样课程”和“观测传播”的有效性。
*   **客观性与公平性**：
    *   进行了 **3 次独立运行**以验证结果的稳定性（见附录 B）。
    *   对比了不同规模的模型（3B 和 7B）。
    *   设置了 Oracle（真值检索）实验作为性能上限参考。
    *   实验设计严谨，通过消融实验清晰地展示了每个模块对解决奖励稀疏问题的贡献。

### 6. 主要结论与发现
*   **刷新 SOTA**：Wiki-R1 在 EVQA 上达到 37.1%（原纪录 35.5%），在 InfoSeek 上达到 44.1%（原纪录 40.1%）。
*   **强泛化性**：在未见过的实体（Unseen-Entity）和未见过的问题（Unseen-Question）设置下表现尤为突出，证明模型学到的是推理能力而非简单的记忆。
*   **解决稀疏性**：观测传播机制显著减少了训练中被忽略的“零奖励”轨迹，大幅提升了 RL 的收敛速度和最终性能。

### 7. 优点（亮点）
*   **系统性解决分布差异**：不同于以往只关注检索质量的研究，Wiki-R1 通过课程学习从根本上解决了预训练与下游任务的分布对齐问题。
*   **高效的数据利用**：仅用 40k 样本就超越了使用百万级数据微调的模型，证明了强化学习在推理任务中的潜力。
*   **创新的观测传播**：将非参数化的标签传播引入 RL 采样，巧妙解决了冷启动和奖励稀疏问题。

### 8. 不足与局限
*   **检索控制粒度**：目前的数据生成主要通过操纵检索数量和是否包含真值来控制，这种控制方式相对粗糙，未来可探索更细粒度的语义难度控制。
*   **依赖外部检索器**：虽然模型增强了对噪声的鲁棒性，但其性能上限仍受限于底层检索系统（如 EVA-CLIP）的召回能力。
*   **计算开销**：尽管训练时间不算长，但 RL 过程中的多次 Rollout（采样生成）相比传统 SFT 仍需要更多的计算资源。

（完）
