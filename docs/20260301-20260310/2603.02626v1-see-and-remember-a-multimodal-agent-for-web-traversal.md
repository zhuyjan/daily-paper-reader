---
title: "See and Remember: A Multimodal Agent for Web Traversal"
title_zh: 见与记：一种用于网页遍历的多模态智能体
authors: "Xinjun Wang, Shengyao Wang, Aimin Zhou, Hao Hao"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02626v1"
tags: ["query:main"]
score: 9.0
evidence: 具有显式记忆系统的多模态智能体
tldr: "针对自主网页导航中存在的空间迷失和循环导航问题，本文提出了V-GEMS架构。该架构结合了视觉定位以精准识别交互元素，并引入显式内存栈进行状态追踪，支持有效回溯。通过引入动态基准测试，实验证明V-GEMS在网页遍历任务中表现优异，较基准模型性能提升了28.7%，显著增强了多模态智能体的导航鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的网页导航智能体在复杂视觉环境下容易出现空间定向障碍和导航死循环。
method: 提出V-GEMS架构，集成视觉定位以解析模糊元素，并利用显式内存栈实现路径记录与有效回溯。
result: "实验显示V-GEMS在网页遍历任务中显著优于WebWalker基准，性能提升达28.7%。"
conclusion: 通过结合视觉感知与结构化记忆系统，V-GEMS显著提高了多模态智能体在深度网页导航任务中的精准度和鲁棒性。
---

## 摘要
自主网页导航要求智能体能够感知复杂的视觉环境并维持长期上下文，然而当前基于大语言模型（LLM）的智能体往往在空间迷失和导航循环方面面临挑战。在本文中，我们提出了具有普适性的 V-GEMS（视觉定位与显式记忆系统），这是一种专为精确且具韧性的网页遍历而设计的鲁棒多模态智能体架构。我们的智能体集成了视觉定位（visual grounding）以解析歧义交互元素，并引入了带有状态跟踪的显式记忆栈。这种双重机制使智能体能够维护其遍历路径的结构化地图，从而实现有效的回溯并防止深度导航任务中的循环失败。我们还引入了一个可更新的动态基准，以严格评估其适应性。实验表明，V-GEMS 显著优于 WebWalker 基线，实现了 28.7% 的大幅性能提升。代码可在 https://github.com/Vaultttttttttttt/V-GEMS 获取。

## Abstract
Autonomous web navigation requires agents to perceive complex visual environments and maintain long-term context, yet current Large Language Model (LLM) based agents often struggle with spatial disorientation and navigation loops. In this paper, we propose generally applicable V-GEMS(Visual Grounding and Explicit Memory System), a robust multimodal agent architecture designed for precise and resilient web traversal. Our agent integrates visual grounding to resolve ambiguous interactive elements and introduces an explicit memory stack with state tracking. This dual mechanism allows the agent to maintain a structured map of its traversal path, enabling valid backtracking and preventing cyclical failures in deep navigation tasks. We also introduce an updatable dynamic benchmark to rigorously evaluate adaptability. Experiments show V-GEMS significantly dominates the WebWalker baseline, achieving a substantial 28.7% performance gain. Code is available at https://github.com/Vaultttttttttttt/V-GEMS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **V-GEMS** 的多模态网页导航智能体架构，旨在解决现有智能体在复杂网页遍历中面临的“导航失忆”和“算术幻觉”等核心痛点。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：自主网页导航是 AI 实现深度研究和工作流自动化的关键。虽然视觉语言模型（VLM）提升了对 GUI 的感知，但现有的智能体在处理深层网页结构时，常因缺乏显式记忆而陷入循环导航，或在需要精确计数（如“找到5篇论文”）时产生算术幻觉。
*   **核心问题**：
    *   **模态效率低下**：仅依赖文本 DOM 无法捕捉视觉语义（如点击横幅）。
    *   **导航停滞**：缺乏状态追踪，导致智能体像没有“后退键”的浏览器，易迷失在深层链接中。
    *   **符号不准确**：LLM 在处理定量任务时存在固有的算术缺陷。

### 2. 方法论：核心思想与关键技术
V-GEMS 在传统的“探索者-评论者”（Explorer-Critic）双智能体架构基础上，引入了三个专门的模块化工具：
*   **符号计数器（Symbolic Counter）**：将定量追踪从语言推理中解耦。支持“配额搜索”（达到目标数量即停止）和“穷举统计”（遍历所有分页）两种模式，通过语义去重确保计数精准。
*   **URL 记忆栈（URL Stack）**：引入显式的拓扑记忆，强制执行深度优先搜索（DFS）策略。通过“入栈（Push）”记录路径，通过“出栈（Pop）”实现自主错误恢复（如遇到 404 或无关页面时回溯），防止循环。
*   **自适应理解分数计算器（US Calculator）**：动态评估页面的文本可解性。通过文本质量、语义相关性、结构清晰度和特殊情况惩罚四个维度计算分数 $\Phi(s_t)$。当分数低于阈值 $\tau$ 时，才触发高成本的 VLM 辅助，平衡了成本与精度。

### 3. 实验设计
*   **数据集/场景**：构建了 **EverWebQA** 动态基准测试，包含 680 个问答对，涵盖教育、会议、组织、游戏四个领域。
*   **任务分类**：分为单源（Single-source）和多源（Multi-source）任务，并按导航深度分为简单、中等、困难三个等级。
*   **基准模型（Benchmark）**：以 **WebWalker** 作为主要对比基线。
*   **底层模型**：使用 Qwen-3-Coder-Plus 作为 LLM，Qwen3-VL-Plus 作为 VLM。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **运行模式**：由于该系统主要基于大模型的 Zero-shot（零样本）推理能力和外部工具集成，不涉及大规模的基座模型训练，更多体现为推理侧的架构优化。

### 5. 实验数量与充分性
*   **实验规模**：进行了 680 组测试，样本量与基线模型 WebWalker 保持一致，确保了对比的公平性。
*   **消融实验**：设计了详尽的消融实验（共 8 种组合），利用 **Shapley Value（沙普利值）** 定量分析了计数器、URL 栈和 VLM 模块对不同任务类型的边际贡献。
*   **充分性评价**：实验覆盖了中英双语、跨领域场景及不同难度的导航路径，并进行了细致的错误分类分析（如幻觉、拒绝、不精确等），实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能飞跃**：V-GEMS 较 WebWalker 实现了 **28.7%** 的性能提升（平均成功率从 0.49 升至 0.65）。
*   **模块贡献**：
    *   **URL 栈**在多源跨页面任务中贡献最大，解决了“导航失忆”问题。
    *   **VLM 模块**在视觉密集型领域（如会议、游戏）表现卓越。
    *   **计数器**有效消除了定量任务中的幻觉。
*   **鲁棒性**：在复杂任务（Multi-Source Hard）中，V-GEMS 的表现远比基线稳健，证明了显式记忆的必要性。

### 7. 优点
*   **解耦设计**：将特定的认知功能（计数、记忆）从随机性的生成过程中解耦，极大地提高了系统的确定性。
*   **动态基准**：提出了可更新的评估流水线，解决了静态数据集因网页更新（链接失效、内容漂移）而迅速过时的问题。
*   **效率优化**：通过 US Calculator 实现模态按需切换，避免了全量调用 VLM 带来的高昂成本。

### 8. 不足与局限
*   **计算开销**：尽管有自适应切换，但引入 VLM 仍比纯文本模型增加了延迟和 Token 成本。
*   **入口依赖**：目前仍依赖预定义的根 URL 启动，尚未实现从通用搜索引擎开始的完全开放域探索。
*   **缺乏自我进化**：决策逻辑主要基于启发式规划，尚未集成强化学习（RL）来实现端到端的策略自我优化。

（完）
