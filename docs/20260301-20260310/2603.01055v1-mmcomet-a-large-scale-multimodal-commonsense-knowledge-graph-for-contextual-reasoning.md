---
title: "MMCOMET: A Large-Scale Multimodal Commonsense Knowledge Graph for Contextual Reasoning"
title_zh: MMCOMET：一个用于上下文推理的大规模多模态常识知识图谱
authors: "Eileen Wang, Hiba Arnaout, Dhita Pratama, Shuo Yang, Dangyang Liu, Jie Yang, Josiah Poon, Jeff Pan, Caren Han"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01055v1"
tags: ["query:main"]
score: 9.0
evidence: 用于上下文推理的多模态常识知识图谱
tldr: 本研究提出了MMCOMET，这是首个整合了物理、社会和事件知识的大规模多模态常识知识图谱。通过将ATOMIC2020扩展至视觉维度，该图谱包含超过90万个多模态三元组。MMCOMET旨在解决现有知识图谱在图像描述和故事讲述等复杂推理任务中的局限性。实验证明，利用该图谱生成的叙事内容比仅使用文本知识更丰富、连贯且具上下文关联性，为多模态常识推理奠定了新基础。
selection_source: fresh_fetch
motivation: 现有的多模态知识图谱在支持图像描述和故事讲述等复杂推理任务方面存在局限，缺乏整合物理、社会和事件的全面常识。
method: 通过高效的图像检索过程将ATOMIC2020知识图谱扩展到视觉维度，构建了包含逾90万个三元组的MMCOMET。
result: 在视觉故事讲述实验中，MMCOMET生成的叙事比仅依赖文本知识的方法更具丰富性、连贯性和上下文一致性。
conclusion: MMCOMET为多模态常识推理和叙事生成提供了重要的新资源和研究基础。
---

## 摘要
我们提出了 MMCOMET，这是首个整合了物理、社会和事件知识的多模态常识知识图谱 (MMKG)。MMCOMET 通过高效的图像检索过程扩展了 ATOMIC2020 知识图谱，使其包含视觉维度，从而产生了超过 90 万个多模态三元组。这一新资源解决了现有 MMKG 在支持图像描述和故事讲述等复杂推理任务方面的主要局限性。通过标准的视觉故事讲述实验，我们证明了与仅使用文本知识生成的故事相比，我们的整体方法能够生成更丰富、连贯且具有上下文依据的故事。该资源为多模态常识推理和叙事生成奠定了新的基础。

## Abstract
We present MMCOMET, the first multimodal commonsense knowledge graph (MMKG) that integrates physical, social, and eventive knowledge. MMCOMET extends the ATOMIC2020 knowledge graph to include a visual dimension, through an efficient image retrieval process, resulting in over 900K multimodal triples. This new resource addresses a major limitation of existing MMKGs in supporting complex reasoning tasks like image captioning and storytelling. Through a standard visual storytelling experiment, we show that our holistic approach enables the generation of richer, coherent, and contextually grounded stories than those produced using text-only knowledge. This resource establishes a new foundation for multimodal commonsense reasoning and narrative generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《MMCOMET: A Large-Scale Multimodal Commonsense Knowledge Graph for Contextual Reasoning》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的知识图谱（KG）存在两极分化：一类是如 ATOMIC2020 的**纯文本常识图谱**，虽富含社会和事件推理信号，但缺乏视觉对齐；另一类是**多模态百科图谱**（如 IMGpedia），虽有图像，但侧重于事实性实体（如地理、人物），缺乏对日常社会互动、事件动态和人类意图的覆盖。
*   **研究动机**：在视觉问答（VQA）、图像描述（IC）和视觉故事讲述（VST）等任务中，模型需要理解图像背后的隐性常识（如“爷爷开车，孙子坐在腿上”暗示的家庭关系）。作者旨在构建首个整合了物理、社会和事件维度的**大规模多模态常识知识图谱**，为多模态推理提供视觉化的常识支撑。

### 2. 方法论：核心思想与关键技术
MMCOMET 以 ATOMIC2020 为文本骨架，通过一个**混合视觉对齐流水线**为 98.9 万个三元组关联代表性图像：
*   **基础拓扑**：保留了 ATOMIC2020 中 19 种可视觉化的关系类型，包含 57.8 万个唯一文本节点。
*   **混合检索策略**：
    1.  **基于嵌入的相似度匹配**：针对视觉具体的短语（如“物体用途”），使用 CLIP 文本编码器在开源数据集（COCO, Flickr30K 等）中检索。为了解决计算压力，引入了**名词索引过滤机制**，先通过 POS 标签提取名词建立倒排索引，将计算量减少了约 60 倍。
    2.  **具体度感知网页检索**：针对抽象的社会/事件短语（如“意图”、“反应”），利用 4 万个词汇的具体度评分（Concreteness Ratings）。评分低于 4.0 的短语被路由至搜索引擎进行结构化搜索。
*   **后处理与筛选**：使用 CLIP 对候选图像进行重排序，设定 0.15 的相似度阈值，每个短语保留前 10-15 张图像。

### 3. 实验设计
论文通过**内在质量**和**外在效用**两个维度进行评估：
*   **内在评估（人类评价）**：从 19 种关系中随机抽取 1140 个短语（共 7980 个图像-文本对），由标注员评估“完全匹配”、“部分匹配”或“不匹配”。
*   **外在评估（下游任务）**：
    *   **视觉故事讲述 (VST)**：在 VIST 数据集上，将 MMCOMET 检索到的知识注入推理步骤。
    *   **视觉问答 (VQA)**：在 VQAv2 验证集上，通过 Few-shot 提示词注入相关常识。
    *   **图像描述 (IC)**：在 Flickr30k 上，将检索到的头/尾实体短语作为辅助上下文。
*   **Benchmark 与对比方法**：对比了 SRL-pmi、LLaVA、Qwen2.5-VL 等强基座模型在有无 MMCOMET 增强下的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型工具**：提到了使用 CLIP (ViT-B/32) 计算嵌入，使用 NLTK 进行自然语言处理。由于 MMCOMET 主要是作为外部检索资源使用，其核心贡献在于图谱构建而非模型训练，因此对算力的依赖主要体现在大规模嵌入计算和检索上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了近 100 万个三元组，人类评估样本量超过 7000 组，下游任务覆盖了三个主流视觉语言领域。
*   **充分性与客观性**：
    *   **多维度覆盖**：实验不仅验证了物理关系，还深入探讨了最具挑战性的社会和事件关系。
    *   **消融与对比**：对比了网页搜索与开源数据集检索的效果差异，并分析了具体度（Concreteness）对对齐质量的影响。
    *   **一致性**：人类评估中使用了评分者间一致性系数（ICC），证明了评估的客观性。整体实验设计较为严谨。

### 6. 主要结论与发现
*   **高质量对齐**：MMCOMET 实现了 90.7% 的语义对齐率（完全+部分匹配），其中网页检索在处理抽象概念上显著优于开源数据集。
*   **性能提升**：在不进行模型重训练的情况下，注入 MMCOMET 知识使 VQA 准确率提升了 1.1%，并在视觉故事讲述的连贯性和视觉依据（Grounding）指标上取得显著进步。
*   **超越字面理解**：定性分析显示，MMCOMET 能帮助模型推断出图像中未直接显示的意图（如“保护自己免受雨淋”）和社会角色（如“母亲提供的安慰”）。

### 7. 优点
*   **填补空白**：首次统一了物理、社会和事件三大维度的多模态常识，改变了以往 MMKG 偏重百科事实的局限。
*   **高效构建**：提出的名词索引过滤机制极大地优化了大规模多模态对齐的计算效率。
*   **即插即用**：作为一种检索增强资源，它不需要昂贵的模型微调即可提升现有大模型（如 LLaVA, Qwen）的推理能力。

### 8. 不足与局限
*   **模态局限**：目前主要集中在图像-文本对齐，尚未涵盖音频或视频等多模态信号。
*   **检索噪声**：尽管有过滤机制，但网页检索仍可能引入噪声图像，特别是对于极度抽象的社会情感概念。
*   **静态性**：知识图谱是静态构建的，对于快速演变的社会常识或特定文化背景的覆盖可能存在滞后或偏差。
*   **依赖基座模型**：其效果高度依赖于 CLIP 等预训练模型的表征能力，若基座模型存在视觉偏差，会影响检索质量。

（完）
