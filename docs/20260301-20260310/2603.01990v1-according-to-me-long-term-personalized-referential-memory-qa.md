---
title: "According to Me: Long-Term Personalized Referential Memory QA"
title_zh: According to Me：长期个性化指代记忆问答
authors: "Jingbiao Mei, Jinghong Chen, Guangyu Yang, Xinyu Hou, Margaret Li, Bill Byrne"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01990v1"
tags: ["query:main"]
score: 10.0
evidence: 多模态、多源个性化指代记忆问答
tldr: 本研究针对个性化AI助手在处理长期、多源、多模态个人记忆（如图像、视频、邮件）方面的不足，提出了首个多模态多源个性化指代记忆问答基准ATM-Bench。该基准包含四年的隐私保护数据及人工标注的QA对，涵盖了指代消解和多证据推理等挑战。同时，研究者提出了模式引导记忆（SGM）方法来结构化表示不同来源的记忆。实验表明，现有系统在复杂任务上表现不佳，而SGM能显著提升性能。
selection_source: fresh_fetch
motivation: 现有的长期记忆基准主要局限于对话历史，缺乏对现实生活中多模态、多来源个性化指代记忆的覆盖。
method: 引入了包含四年跨度数据的ATM-Bench基准，并提出模式引导记忆（SGM）框架以结构化整合不同来源的记忆信息。
result: "现有最先进的记忆系统在ATM-Bench-Hard测试集上的准确率低于20%，但SGM方法在性能上优于传统的描述性记忆方案。"
conclusion: 长期个性化指代记忆推理仍具挑战性，结构化的记忆表示方法是提升AI助手处理复杂个人查询的关键。
---

## 摘要
个性化 AI 助手必须能够召回并推理长期用户记忆，这些记忆自然地跨越了多种模态和来源，如图像、视频和电子邮件。然而，现有的长期记忆基准主要关注对话历史，未能捕捉到基于生活经验的真实个性化指代。我们推出了 ATM-Bench，这是首个针对多模态、多源个性化指代记忆问答的基准。ATM-Bench 包含约四年的隐私保护个人记忆数据，以及带有真实记忆证据的人工标注问答对，涵盖了需要解析个人指代、多源多证据推理以及处理冲突证据的查询。我们提出了模式引导记忆（Schema-Guided Memory, SGM），用于结构化地表示来自不同来源的记忆项。在实验中，我们实现了 5 种最先进的记忆系统以及一个标准的 RAG 基准，并评估了具有不同记忆摄取、检索和答案生成技术的变体。我们发现，在 ATM-Bench-Hard 集合上的表现较差（准确率低于 20%），并且 SGM 相比于先前工作中常用的描述性记忆（Descriptive Memory）提升了性能。代码可在以下网址获取：https://github.com/JingbiaoMei/ATM-Bench

## Abstract
Personalized AI assistants must recall and reason over long-term user memory, which naturally spans multiple modalities and sources such as images, videos, and emails. However, existing Long-term Memory benchmarks focus primarily on dialogue history, failing to capture realistic personalized references grounded in lived experience. We introduce ATM-Bench, the first benchmark for multimodal, multi-source personalized referential Memory QA. ATM-Bench contains approximately four years of privacy-preserving personal memory data and human-annotated question-answer pairs with ground-truth memory evidence, including queries that require resolving personal references, multi-evidence reasoning from multi-source and handling conflicting evidence. We propose Schema-Guided Memory (SGM) to structurally represent memory items originated from different sources. In experiments, we implement 5 state-of-the-art memory systems along with a standard RAG baseline and evaluate variants with different memory ingestion, retrieval, and answer generation techniques. We find poor performance (under 20\% accuracy) on the ATM-Bench-Hard set, and that SGM improves performance over Descriptive Memory commonly adopted in prior works. Code available at: https://github.com/JingbiaoMei/ATM-Bench

---

## 论文详细总结（自动生成）

这是一份关于论文《According to Me: Long-Term Personalized Referential Memory QA》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的长期记忆（Long-term Memory）基准测试主要局限于**对话历史**（纯文本），忽略了现实生活中个人记忆的多模态（图像、视频）和多来源（电子邮件、照片、收据等）属性。
*   **核心问题**：当前的 AI 助手难以处理“个性化指代”（Personalized Referential）查询。例如，用户询问“我去年在葡萄牙住的酒店花了多少钱？”，这需要 AI 能够跨越数年的多源数据（邮件预订、照片中的收据、GPS 轨迹）进行指代消解、冲突处理和多证据推理。
*   **整体含义**：论文旨在填补这一空白，提出了首个针对多模态、多源个人记忆的问答基准 **ATM-Bench**，并探讨了如何有效表示和检索这些异构记忆。

### 2. 论文提出的方法论
论文将记忆问答任务形式化为三个关键步骤，并提出了一种新的记忆表示方法：

*   **核心思想**：将异构的原始数据转化为统一的结构化表示，以增强检索的精确度和推理的可靠性。
*   **关键技术细节**：
    1.  **记忆摄取（Memory Ingestion）**：
        *   **Schema-Guided Memory (SGM)**：这是论文的核心贡献。不同于传统的“描述性记忆”（DM，将图像或邮件转为一段自然语言描述），SGM 将记忆项表示为固定的 Schema 字段（如：时间、地点、实体、OCR 内容、标签、来源等）。
        *   **记忆组织**：对比了“堆叠记忆”（Piled，无结构存储）和“链接记忆”（Linked，构建图结构，如 A-Mem 或 HippoRAG）。
    2.  **检索（Retrieval）**：使用嵌入模型（如 MiniLM 或 Qwen3-Embedding）对查询和记忆项进行向量化，通过最大内积搜索（MIPS）召回 Top-K 证据。
    3.  **答案生成（Answer Generation）**：对比了“单次生成”和“智能体迭代生成”（Agentic，如 Mem0，包含查询分解和多次检索）。
*   **算法流程**：$M = P(D)$（预处理原始数据 $D$ 得到记忆 $M$） $\rightarrow$ $E = R(q, M)$（根据查询 $q$ 检索证据 $E$） $\rightarrow$ $\hat{a} = A(q, E)$（根据证据生成答案 $\hat{a}$）。

### 3. 实验设计
*   **数据集（ATM-Bench）**：
    *   包含约 **4 年** 的真实个人数据（经隐私脱敏）。
    *   **1,038 个** 人工标注的 QA 对，涵盖 12,000 多个记忆项（邮件、图像、视频）。
    *   **ATM-Bench-Hard**：专门筛选出的高难度子集，平均每个问题需要 6.3 个证据项，且涉及时间跨度极长（平均 226 天）。
*   **对比方法**：
    *   **记忆智能体**：A-Mem, Mem0。
    *   **RAG 系统**：HippoRAG2, Self-RAG, 以及作者实现的 ATM-RAG。
    *   **上限/下限**：No-Evidence（无证据猜题）和 Oracle（给定金牌证据）。
*   **评估指标**：Question Type Score (QS，针对数字、列表、开放式问题分别采用 EM、Jaccard 和 LLM-Judge 评分)、Recall@k、Joint@k。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 5090** 的工作站上运行。
*   **模型使用**：
    *   **记忆摄取**：使用 Qwen3-VL-2B-Instruct（因其轻量化，适合端侧隐私保护）。
    *   **答案生成**：默认使用 Qwen3-VL-8B-Instruct。
    *   **评测模型**：使用 GPT-5-mini 作为 LLM-Judge。
*   **耗时**：记忆编码时间（Tenc）根据系统不同，从 0.5 小时（简单 RAG）到 16.7 小时（复杂 Agent 系统）不等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 5 种最先进的记忆系统及其变体。
    *   进行了**消融实验**，对比了 SGM vs DM（表示方法）、Piled vs Linked（组织方法）。
    *   测试了不同规模的检索器（从 MiniLM 到 Qwen3-4B）和重排序器。
    *   测试了多种主流大模型（Gemini 2.5, Claude 4.5, GPT-5）在 Oracle 模式下的表现。
*   **充分性与公平性**：实验设计较为严谨。所有系统使用相同的 Prompt 模板和温度参数；引入了双重 LLM 评测（GPT-5-mini 和 GLM-4.7）以证明评测的一致性；涵盖了从检索到生成的全链路分析。

### 6. 论文的主要结论与发现
1.  **性能鸿沟**：现有最先进系统在 ATM-Bench-Hard 上的准确率**低于 20%**，说明长期多源记忆推理仍是巨大挑战。
2.  **SGM 的优越性**：在所有测试系统中，**模式引导记忆（SGM）** 的表现均显著优于传统的描述性记忆（DM），在 Oracle 设置下提升了约 20%。
3.  **检索瓶颈**：随着检索深度 $K$ 的增加，性能并未持续提升，说明简单的增加上下文并不能解决复杂指代问题。
4.  **模型局限**：即使是 GPT-5 在给定完美证据的情况下，在 Hard 集合上的得分也仅为 74.7，说明模型在处理冲突证据和长跨度时间更新（Memory Update）时仍会犯错。

### 7. 优点
*   **基准真实性**：不同于以往的合成对话数据，ATM-Bench 基于真实的、跨度四年的多模态生活记录，具有极高的实用价值。
*   **隐私保护方案**：论文详细描述了多阶段脱敏流程（VLM 风险评估、人脸模糊、LLM 重写邮件、合成 PII 注入），为个人数据研究提供了范式。
*   **结构化思想**：提出的 SGM 方法证明了在处理异构数据时，结构化元数据比纯文本描述更有效。

### 8. 不足与局限
*   **数据规模限制**：虽然时间跨度长，但 QA 对数量（1000+）相对于大规模预训练基准仍较小。
*   **评测依赖性**：高度依赖闭源模型（如 GPT-5）作为裁判，可能存在潜在的评测偏差。
*   **应用限制**：目前的 SGM 依赖于预定义的 Schema，对于某些无法归类的非结构化个人记忆，其灵活性可能受限。
*   **计算成本**：复杂的 Linked Memory 组织方式（如 Mem0）编码时间极长，难以实现实时的记忆更新。

（完）
