---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 可解释的不确定性用于问答中的自适应检索与推理
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:search-agent"]
score: 8.0
evidence: 不确定性驱动的自适应检索用于智能体搜索
tldr: 大语言模型在问答中易产生幻觉且缺乏透明度。现有检索增强生成策略对何时检索的决策不透明或计算低效。本文提出基于模型内部表示的不确定性信号框架，区分知识不足与歧义，通过单次前向传播高效估计。该方法自适应触发检索或推理，提升了QA系统的透明性和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 现有RAG策略对何时检索的决策不透明或计算低效，且无法区分知识不足与歧义等不同不确定性类型。
method: 从LLM隐藏状态高效估计知识不足与歧义两类不确定性信号，通过单次前向传播实现，据此自适应触发检索或推理。
result: 在问答任务上实现自适应检索与推理，减少了不必要的检索，提升了事实性和整体效率。
conclusion: 提供了一种透明且实用的自适应问答方法，支持设计可解释的用户界面。
---

## 摘要
大型语言模型（LLM）在问答（QA）中表现出强大的性能，但仍然容易出现幻觉，并且透明度有限。检索增强生成（RAG）可以提高事实性，但关于何时以及如何从外部资源进行检索的决策通常基于不透明的策略或计算效率低下的多步提示过程。我们提出了一种基于从LLM内部表示中提取的显式信号的自适应QA不确定性感知框架。我们区分了知识不足和知识模糊或冲突，并在单次前向传递中从隐藏状态高效估计这些信号。这些信号指导系统行为：当知识不足时触发RAG，而当模糊性或冲突高时应用额外的推理。通过将自适应决策基于分解且可高效估计的不确定性信号，这种方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释的面向用户工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大型语言模型（LLM）在问答（QA）任务中表现优异，但存在幻觉、依赖不完整/过时内部知识、透明度低的问题。检索增强生成（RAG）通过引入外部知识可提升事实性，但现有策略在“何时检索”上存在缺陷：
  - 基于隐式学习策略的决策不透明，无法区分不确定性来源；
  - 基于多步提示推理的方法计算效率低。
- **核心问题**：现有方法不能区分两种根本不同的不确定性类型——**知识不足**（model lacks required information）和**知识模糊/冲突**（multiple plausible interpretations or conflicting evidence）。这两种情况需要不同的应对：知识不足应触发检索，模糊/冲突则应增加推理（如Chain-of-Thought）。
- **整体含义**：提出一个能够从LLM内部表示中显式、高效估计这两种不确定性并自适应驱动检索/推理决策的框架，同时保持透明性和可解释性，为可解释用户界面设计奠定基础。

## 2. 论文提出的方法论

- **核心思想**：利用LLM单次前向传播的隐藏状态，训练轻量级回归探针（regression probes）估计两种不确定性信号，据此决定是否触发RAG或额外推理。
- **关键技术细节**：
  - **知识不足估计**：训练回归探针，从隐藏状态预测事实在LLM预训练语料中出现的次数（occurrence count）。出现次数越少，知识不足程度越高。
  - **知识模糊/冲突估计**：训练回归探针，从隐藏状态预测不同版本事实出现次数的熵（entropy）。熵越高，歧义/冲突程度越高。
  - **决策机制**（阈值控制）：
    - 知识不足超过阈值 → 触发RAG；
    - 模糊/冲突超过阈值 → 分配额外测试时计算（如Chain-of-Thought、Self-Consistency）；
    - 两者均低 → 直接回答。
- **效率优势**：所有估计均在单次前向传播中完成，无需重复采样或辅助模型。

## 3. 实验设计

- **数据集**：Natural Questions (NQ) 数据集（事实型问答）。
- **模型**：Llama-2-7b-chat。
- **基准方法（Baselines）**：
  - LLM-only（无检索）；
  - Always-on RAG（始终检索）；
- **对比方法**（不确定性度量驱动RAG触发）：
  - Semantic Entropy (SE)；
  - Weighted Entropy Production Rate (WEPR)。
- **实验设置**：仅报告了一组初步结果，未提供训练/验证/测试集划分细节、回归探针训练方式、阈值设定等具体信息。

## 4. 资源与算力

- 论文中**未明确说明**所使用的GPU型号、数量、训练时长等算力资源。
- 仅指出使用单次前向传播估计不确定性，计算开销低，但未提供实际效率数据。

## 5. 实验数量与充分性

- **实验数量**：仅在一个数据集（NQ）上对一种模型（Llama-2-7b-chat）进行了一组对比实验（两种不确定性度量）。
- **充分性评价**：**极不充分**。缺乏：
  - 多数据集验证（如TriviaQA、WebQuestions等）；
  - 多模型验证（不同规模/家族）；
  - 消融实验（例如去掉不确定性估计直接使用阈值、不同探针设计）；
  - 与现有自适应RAG方法（如Self-RAG、Adaptive-RAG）的直接比较；
  - 阈值敏感性分析、计算开销测量。
- **公平性**：初步结果显著优于基线（McNemar检验，p<0.05），但实验设置简单，结论泛化能力存疑。

## 6. 论文的主要结论与发现

- 不确定性驱动的自适应QA在准确率上显著优于LLM-only和Always-on RAG基线（SE: +5.9% vs LLM-only, +3.3% vs Always-on RAG；WEPR: +4.7% vs LLM-only, +2.1% vs Always-on RAG）。
- 通过分解不确定性来源（不足 vs 模糊/冲突）并分别处理，可提升系统透明性和用户信任。
- 单次前向传播估计效率高，适用于多步或智能体场景。

## 7. 优点

- **方法新颖**：首次将知识不足与知识模糊/冲突明确区分，并分别映射到检索与推理两种补救策略。
- **高效**：仅需单次前向传播即可获得不确定性信号，避免额外采样或辅助模型。
- **可解释性**：决策基于显式的不确定性度量，用户和设计者可理解系统为何检索或推理，而非事后归因。
- **结构清晰**：将不确定性分解为两个可量化维度，为后续研究提供了通用框架。

## 8. 不足与局限

- **实验覆盖严重不足**：
  - 仅一个数据集（NQ）和一个模型（Llama-2-7b-chat），未展示跨域/跨语言表现。
  - 未与主流自适应RAG方法（如Self-RAG、Adaptive-RAG、FLARE）对比。
  - 无消融实验证明每个组件（如回归探针、阈值）的必要性。
- **方法细节缺失**：
  - 回归探针的训练数据如何获取？如何将事实出现次数和熵作为监督信号？
  - 阈值如何设定？是否根据验证集优化？未说明。
  - 隐藏状态从哪一层提取？如何聚合？未指定。
- **潜在偏差**：
  - 知识不足的估计依赖预训练语料中出现次数，但出现次数不一定对应模型内部知识存储（可能受上下文、位置等影响）。
  - 熵度量仅针对不同版本事实，未考虑答案集合的语义多样性。
- **应用限制**：
  - 框架假设LLM隐藏状态包含足够的信息来预测两种不确定性，但小模型或量化模型可能效果不佳。
  - 需要针对每个新LLM重新训练回归探针，迁移成本未评估。

（完）
