---
title: "ECHO: Prune to act, trace to learn with selective turn memory in agentic RL"
title_zh: ECHO：在智能体强化学习中通过选择性轮次记忆进行剪枝以行动、追踪以学习
authors: "Zijun Xie, Binbin Zheng, Enlei Gong, Jihua Liu, Yuyang You, Lingfeng Liu, Jiayao Tang, Guanqun Zhao, Aoqi Hu, Zeyu Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31650v1"
tags: ["query:search-agent"]
score: 9.0
evidence: 代理强化学习中的选择性回合记忆用于多步工具交互中的证据重用
tldr: "长期语言代理在有限上下文窗口下反复交互时，历史信息被压缩或丢弃，导致细粒度证据难以复用，且基于结果的强化学习无法将成功归因于具体证据。ECHO提出选择性回合记忆框架，将每个环境回合压缩为紧凑记录，通过选择这些记录重构策略上下文，并利用源索引将正向信用路由到支持答案的证据和选择动作。在BrowseComp-Plus上达到43.4%准确率，远超GRPO（28.9%）和SUPO（36.1%），同时使用更少回合和轨迹。该方法有效缓解历史坍塌，实现可追溯学习，并提升多目标问答、代码生成等任务的零样本泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31650v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1621, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1625, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1617, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1588, \"height\": 1019, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1621, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31650v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1614, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31650v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1578, \"height\": 447, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31650v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1636, \"height\": 530, \"label\": \"Table\"}]"
motivation: 现有方法在长回合中丢弃或压缩历史观测，导致细粒度证据丢失，且RL无法将成功结果直接追溯到支持它的证据和选择动作。
method: 压缩每个环境回合为紧凑记忆记录，通过选择这些记录重建有界策略上下文，并利用源索引将正向结果信用分配给对应证据和选择动作。
result: "在BrowseComp-Plus上达到43.4%保留下游准确率，超越GRPO（28.9%）和SUPO（36.1%），且使用更少回合和轨迹量。"
conclusion: ECHO通过选择性记忆与源索引重建，同时解决历史坍塌和可追溯学习，在多种任务上提升零样本泛化能力。
---

## 摘要
长程语言智能体必须反复与工具交互、积累证据，并在有限的上下文窗口内做出决策。现有的上下文管理方法通过截断遥远历史、将过去的轮次折叠成摘要或选择紧凑的记忆状态，使得这种展开变得可行。然而，这些突破引入了两个相互关联的局限。首先，随着轮次数量增加，历史观测被逐步移除或压缩成紧凑状态，使得策略更难重用细粒度证据。其次，一旦原始轮次不再可溯源，基于结果的强化学习就失去了将策略更新与支持成功最终答案的证据对齐的显式路径。为此，我们提出ECHO，一种选择性轮次记忆框架，通过源索引重建共同解决历史崩溃和可追踪学习。具体来说，ECHO将每个完成的环境轮次压缩成一个紧凑的记忆记录，通过从这些记录中选择来重建有界策略上下文，并重用所选源索引将积极结果归因于支持成功答案的证据和选择动作。在BrowseComp-Plus上，ECHO达到了43.4%的保留准确率，优于GRPO（28.9%）和滚动摘要基线SUPO（36.1%），同时使用的轮次和轨迹量少于SUPO（图1）。此外，训练后的策略在密集和MoE骨干上跨多目标问答、代码生成和深度信息寻求基准测试中提高了零样本泛化能力。

## Abstract
Long-horizon language agents must repeatedly interact with tools, accumulate evidence, and make decisions under bounded context windows. Existing context-management methods make such rollouts feasible by truncating distant history, folding past turns into summaries, or selecting compact memory states. However, these breakthroughs introduce two coupled limitations. First, as the number of turns grows, historical observations are progressively removed or collapsed into compressed states, making it harder for the policy to reuse fine-grained evidence. Second, once the original turns are no longer source-addressable, outcome-based RL loses an explicit path for aligning policy updates with the evidence that supported a successful final answer. To this end, we propose ECHO, a selective turn-memory framework that jointly addresses history collapse and traceable learning through source-indexed reconstruction. Specifically, ECHO compresses each completed environment turn into a compact memory record, reconstructs bounded policy contexts by selecting from these records, and reuses the selected source indices to route positive outcome credit to the evidence and selection actions that support successful answers. On BrowseComp-Plus, ECHO reaches 43.4% held-out accuracy, outperforming GRPO (28.9%) and the rolling-summary baseline SUPO (36.1%), while using fewer turns and lower trajectory volume than SUPO (Figure 1). Additionally, the trained policy improves zero-shot generalization across multi-objective QA, code generation, and deep information-seeking benchmarks on both dense and MoE backbones.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：长程语言智能体在有限上下文窗口下反复调用工具、积累证据、做出决策。现有方法通过截断、摘要或压缩来管理历史，但导致两个耦合局限：
  - **历史坍塌**：随着轮次增加，细粒度证据被移除或压缩，策略难以复用；
  - **不可追溯学习**：原始轮次不再可寻址，基于最终结果的强化学习（RL）无法将正向奖励明确分配到支持成功答案的关键证据轮次和选择动作上。
- **动机**：需要一种上下文管理方法，既能保持上下文效率（使长程展开可行），又能保留源级别的可追溯性，使学习信号能够准确路由到有用证据。

## 2. 论文提出的方法论：核心思想与关键技术细节
- **核心思想**：通过源索引重建（source-indexed reconstruction）同时解决行动（acting）和学习（learning）问题。每个完成的环境轮次被压缩为带源索引的记忆记录；策略通过这些记忆记录的选择来重建有界上下文；相同的源索引用于将正向结果信用路由到最终答案、被选中的历史证据轮次及其记忆摘要，以及记忆选择动作。

- **关键技术细节**：
  - **源索引记忆（Source-indexed turn memory）**：每完成一个工具调用轮次 \( u_i = (a_i, o_i) \)，策略生成一个紧凑的本地发现（finding）\( s_i \)（通过提示“sum last turn”），形成记录 \( e_i = (i, \alpha_i, m_i) \)，其中 \( i \) 是原始轮次索引，\( \alpha_i \) 是动作的紧凑表示，\( m_i \) 是发现文本。所有记忆保持为非折叠集合。
  - **自回归记忆选择（Autoregressive memory selection）**：当上下文预算达到上限时，策略基于当前上下文和渲染的记忆列表，生成一个选择动作 \( a^{sel}_j \)，指示需要哪些历史轮次。通过修复操作 \( \rho \) 确保有效、去重、预算合规，并自动保留最近 \( K \) 轮。
  - **有界上下文重建**：根据选中的源索引 \( I^{sel}_j \)，从记忆集合中取出对应记录并渲染到上下文中，加上最近局部历史 \( \Phi_{\text{local}} \)，使得 \( |c_{j,t}| \leq B \)（预算32k token）。
  - **来源引导的信用分配（Provenance-guided credit assignment）**：对每个轨迹，构建一个硬信用掩码 \( \mu_q \)，标记以下token为信用token：
    - 最终答案段token；
    - 被选入最终上下文的历史轮次中的动作token；
    - 这些轮次的发现（finding）token；
    - 所有记忆选择动作的token。
  - 仅对具有正优势 \( A^{(n)+} \) 的轨迹（即高于组平均的成功轨迹）应用这些信用token的梯度更新，避免从错误轨迹中引入噪声。
  - 最终目标函数为 \( L^{(n)}_{\text{ECHO}} = \sum_q \tilde{A}^{(n)}_q \log \pi_\theta(y_q | y_{<q}, c_q) \)，其中 \( \tilde{A}^{(n)}_q = A^{(n)+} \mu^{(n)}_q \)。

## 3. 实验设计
- **主要训练数据集**：**BrowseComp-Plus**（长程工具使用问答），训练集747例，保留验证集83例。
- **零样本泛化评测**：
  - 多目标QA（2/4/8/16目标，来自HotpotQA等聚合）；
  - 代码生成：CodeGym（中等难度子集）、LoCoBench-Agent（Python实例）；
  - 深度信息寻求：GAIA、HLE（人类最后考试）、Frames。
- **对比方法**：
  - GRPO（标准结果强化学习，无上下文管理）；
  - SUPO（滚动摘要基线，每次压缩时将远距离历史折叠为摘要）；
  - ECHO及其消融变体：
    - ECHO w/ Top-K retrieval（语义检索替代学习选择）；
    - ECHO w/ Top-K retrieval & w/o turn summary（用完整观测替代紧凑发现）；
    - ECHO w/o traceable CA（去掉信用掩码，使用GRPO式密集分配）；
    - ECHO w/o Traceable CA & Turn-level IS（再改用回合级重要性采样）。
- **骨干模型**：Qwen3-32B-Instruct（密集）及Qwen3-30B-A3B-Instruct（MoE稀疏）。

## 4. 资源与算力
- 论文说明：使用 **4节点，每节点8 GPU**（未明确GPU型号，推测为A100或H800），运行**完全异步的展开-训练流水线**。
- 训练步数：图1显示STEP达到约115步，但**未给出具体训练时长**（如小时/天）。
- 每个展开：采样8个rollout/提示，上下文预算32k token，最多5次压缩轮次（有效交互预算可达6×32k=192k token）。
- 总体：算力投入中等，但未详细报告。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：在图1中展示三个方法在BrowseComp-Plus上的训练曲线（准确率、轮次数、轨迹量）；
  - 消融实验（图4）：记忆组件（学习选择 vs 语义检索，紧凑发现 vs 完整观测）和信用分配（密集 vs 可追溯 vs 回合级IS）共6组；
  - 零样本泛化（表2）：在4类基准（多目标QA、代码生成、深度信息寻求）共7个数据集上，对比5种方法变体（GRPO、SUPO、ECHO及三个消融），并使用两个骨干（密集和MoE）；
  - 鲁棒性实验（图5）：在MoE骨干上重复BrowseComp-Plus训练比较（3方法）。
- **充分性与公平性**：
  - 充分：消融覆盖了核心组件（记忆选择、记忆格式、信用分配），零样本跨多个领域验证泛化，骨干多样性证明鲁棒性。
  - 客观公平：所有方法使用相同的工具环境、验证器、展开预算、采样配置；超参数固定（32k上下文，8 rollouts，最多5次压缩）。
  - 不足：主要训练仅在BrowseComp-Plus一个数据集上进行，缺乏在更多长程任务上的直接训练验证；零样本泛化尽管覆盖多个领域，但未展示在训练分布外更大规模数据集上的表现。

## 6. 论文的主要结论与发现
- ECHO在BrowseComp-Plus上达到 **43.4%保留准确率**，显著优于GRPO（28.9%）和SUPO（36.1%），同时使用的**轮次数更少**（45.3 vs 62.5）**轨迹量更低**（3.13 vs 4.18 trajectories/rollout）。
- 训练曲线显示：ECHO同时提升了准确率和效率，而SUPO因轮次膨胀导致效果受限。
- 消融证实：
  - **学习驱动的记忆选择**比静态语义检索贡献更大准确率提升；
  - **可追踪信用分配**是稳定训练和避免冗余搜索的关键；
  - 紧凑的轮次发现（finding）与完整观测性能相当，说明信息压缩有效。
- 零样本泛化：ECHO在密集和MoE骨干上平均得分**40.2%**（密集）和**30.5%**（MoE），超过GRPO（33.6%/25.9%）和SUPO（34.8%/26.9%），在16目标QA和Frames等证据密集型任务上增益最大。
- 对MoE骨干同样稳定：GRPO和SUPO在MoE上出现性能退化或坍塌，ECHO保持高准确率和合理轮次。

## 7. 优点
- **方法创新性**：首次提出通过源索引重建将上下文管理与信用分配统一，解决了长期智能体RL中历史坍塌与不可追溯的耦合问题。
- **设计简洁有效**：仅通过压缩轮次为紧凑发现+策略选择+信用掩码三个步骤实现，易于实现和集成。
- **实验全面**：包含主任务、消融、多种零样本领域、两种骨干，证据链完整。
- **结果显著**：不仅在准确率上超越强基线，还改善了训练效率（更少轮次和轨迹量），避免了SUPO的轮次膨胀。
- **跨骨干鲁棒**：在密集和MoE架构上均有效，表明方法不依赖于特定模型结构。

## 8. 不足与局限
- **最终追踪近似**：信用分配仅回溯到最终上下文所选中的源轮次，而不递归追踪这些轮次自身所依赖的更深层历史。对于深层嵌套依赖的长程任务，可能遗漏部分因果链（作者已承认）。
- **应用范围有限**：实验仅针对文本工具型智能体（搜索、打开页面、问答），未验证于GUI、具身智能或多智能体协作等场景。
- **额外计算开销**：记忆选择和压缩步骤引入了额外推理和训练计算（需要生成发现和选择动作），文中未量化此开销对整体训练时间的影响。
- **单任务训练**：仅在BrowseComp-Plus上训练，未在多个长程任务联合训练，泛化能力可能存在领域偏差风险。
- **算力报告不完整**：未明确GPU型号和训练时间，限制可复现性评估。

（完）
