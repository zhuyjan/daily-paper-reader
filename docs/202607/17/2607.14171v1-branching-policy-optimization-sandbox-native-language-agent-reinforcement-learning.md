---
title: "Branching Policy Optimization: Sandbox-Native Language Agent Reinforcement Learning"
title_zh: 分支策略优化：沙盒原生语言智能体的强化学习
authors: "Bowei He, Yankai Chen, Xiaokun Zhang, Xue Liu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14171v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 在策略强化学习与蒸馏用于长时域智能体轨迹
tldr: "针对LLM智能体强化学习训练中忽视沙箱可快照、可恢复特性的问题，提出分支策略优化（BPO）。该方法沿主干轨迹在高熵决策点分支，并行采样多条分支并计算基于同组收益的优势，方差更小。在WebShop等任务上，BPO相比GRPO和RLOO成功率提升3.6-6.1个百分点，梯度方差减半，用38%更少更新达到同等性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14171v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1264, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14171v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1196, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14171v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 874, \"height\": 431, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14171v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 981, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14171v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1073, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14171v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 584, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14171v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1142, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有RL算法使用独立轨迹采样，未利用沙箱确定性可快照恢复特性，导致策略梯度方差大。
method: BPO自适应快照高熵决策点，分支并行探索替代动作，基于同组收益计算优势估计，实现更低方差。
result: "在WebShop等任务上，BPO成功率提升3.6-6.1%，梯度方差减半，用38%更少策略更新匹配最优基线。"
conclusion: BPO利用沙箱特性设计的树状轨迹拓扑能有效降低方差、提升样本效率，为语言智能体RL训练提供新范式。
---

## 摘要
强化学习已成为训练与可执行沙盒交互的大型语言模型（LLM）智能体的主导范式。PPO、RLOO和GRPO等最先进的算法从RLHF继承了其展开拓扑结构：对于每个提示，从初始状态独立采样N条轨迹，并通过减去组基线计算优势。这种设计忽略了智能体沙盒的一个定义性特性：它们是确定性的、可快照的，并且可以从任何中间状态恢复。我们认为这一特性使得一种根本不同的展开拓扑结构成为可能：与其构建N棵深度为T的独立树，不如构建一棵具有N个叶节点的单棵树，其中兄弟节点共享前缀，因此也共享方差。我们将这一思想实例化为分支策略优化（BPO），这是一种沙盒原生的RL算法，它（i）在主干轨迹的高熵决策点自适应地快照沙盒，（ii）在每个分支点为每个替代动作分支出K个动作并展开至终止，以及（iii）从兄弟回报而非独立提示计算每步优势。我们证明该估计量无偏，并且其方差严格低于轨迹级基线，方差减少量等于回报方差中由前缀解释的部分。在WebShop、ALFWorld和SWE-bench Verified上，使用Qwen2.5-7B和Llama-3.1-8B作为主干网络，在相同计算量下，BPO相比GRPO和RLOO的成功率绝对提升了3.6-6.1个百分点，梯度范数方差减半，并使用少38%的策略更新达到了最佳基线的水平。

## Abstract
Reinforcement learning has emerged as the dominant paradigm for training large language model (LLM) agents that interact with executable sandboxes. State-of-the-art algorithms such as PPO, RLOO, and GRPO inherit their rollout topology from RLHF: for each prompt, N independent trajectories are sampled from the initial state, and an advantage is computed by subtracting a group baseline. This design ignores a defining property of agent sandboxes. They are deterministic, snapshottable, and resumable from any intermediate state. We argue that this property enables a fundamentally different rollout topology: rather than N independent trees of depth T, one can construct a single tree of N leaves whose siblings share prefixes, and therefore share variance. We instantiate this idea as Branching Policy Optimization (BPO), a sandbox-native RL algorithm that (i) adaptively snapshots the sandbox at high-entropy decision points along a backbone trajectory, (ii) forks K alternative actions per branch point and rolls out each to termination, and (iii) computes per-step advantages from sibling returns rather than from independent prompts. We prove this estimator is unbiased and has strictly lower variance than the trajectory-level baseline, with the reduction equal to the prefix-explained portion of return variance. On WebShop, ALFWorld, and SWE-bench Verified with Qwen2.5-7B and Llama-3.1-8B backbones, BPO improves success by 3.6--6.1 absolute points over GRPO and RLOO at matched compute, halves gradient-norm variance, and matches the best baseline using 38% fewer policy updates.

---

## 论文详细总结（自动生成）

### 论文结构化中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：当前训练LLM智能体的主流RL算法（如PPO、RLOO、GRPO）均采用“独立轨迹采样”的展开拓扑结构：对于每个提示，从初始状态独立采样N条轨迹，通过减去组基线（group baseline）计算优势。这种设计继承自RLHF的偏好学习范式，但忽略了智能体沙盒环境的本质特性。
- **核心问题**：智能体沙盒（如代码执行、网页交互、操作系统环境）具有确定性、可快照（snapshottable）和可从任意中间状态恢复（resumable）的特性。现有算法未利用这一特性，导致策略梯度方差高、样本效率低。
- **整体意义**：作者提出，利用沙盒的确定性可快照能力，可以构建“树状展开拓扑”替代“独立轨迹”拓扑，通过共享前缀来降低方差，从而提升训练效率与最终性能。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：对每个提示，先采样一条“主干轨迹”，然后在主干轨迹的高熵决策点（即策略不确定性高的位置）进行沙盒快照，分支出K个替代动作并分别展开至终止，形成一棵展开树。利用同辈轨迹（sibling rollouts）的回报计算局部优势（sibling-baseline advantage），并通过折扣传播至共享前缀，最终用于策略更新。
- **关键技术细节**：
  - **展开树构建**：主干轨迹由策略采样至终止；选择M个分支点（依据每步token级熵，限制最小间距）；在每个分支点快照沙盒，采样K-1个替代动作并独立执行至终止，与主干动作一起构成K个同辈轨迹。
  - **同辈基线优势**：对分支点t处的第k个同辈动作，优势估计为 \( \hat{A}_{\text{BPO}}(s_t, a_t^{(t,k)}) = G_t^{(t,k)} - \frac{1}{K-1}\sum_{j \neq k} G_t^{(t,j)} \)（留一法基线）。
  - **优势传播**：对于分支点之前的共享前缀步，优势通过折扣λ（λ=0.95）累加下游分支点的局部优势。
  - **损失函数**：采用PPO-clip目标，加上KL散度正则项。
  - **分支调度**：按策略熵从高到低选择分支点，避免在已确定的步骤上浪费计算。
- **公式与算法流程**（文字说明）：
  - 算法1描述了每步迭代：采样主干轨迹 → 计算各步熵 → 选Top-M分支点 → 每个分支点并行快照、采样K-1个替代动作并展开 → 计算分支点优势 → 传播至前缀 → 更新策略。

#### 3. 实验设计
- **数据集/场景**：三个沙盒基准测试：
  - **WebShop**：模拟电商网站，指令驱动的搜索与购买任务，奖励连续[0,1]，最大步数50。
  - **ALFWorld**：文本家庭成员环境，6种任务类型，最大步数40。
  - **SWE-bench Verified**：500个人工验证的GitHub问题，Docker沙盒，奖励为二值的解决成功。
- **基准方法对比**：
  - SFT（仅监督微调）、PPO（带值网络）、RLOO、GRPO、VinePPO（使用MC估计值）。
  - 所有方法在相同总回报样本数下比较（计算量匹配）。
- **主干模型**：Qwen2.5-7B-Instruct（主要实验结果）和Llama-3.1-8B-Instruct（扩缩消融）。
- **计算匹配策略**：BPO每提示使用1条主干 + M个分支× (K-1)个同辈，总数与GRPO等的N=8条独立轨迹匹配。

#### 4. 资源与算力
- 文中明确说明：训练在**8×A100-80GB**环境下进行；沙盒工作运行在独立的**32核CPU池**上。
- 训练步数：WebShop和ALFWorld为3,000步，SWE-bench为5,000步。
- 未提供总GPU小时数，但提供了各环境下达到GRPO最终性能的**墙钟时间**（WebShop: 8.2h vs 13.5h；ALFWorld: 11.4h vs 17.0h；SWE-bench: 47.6h vs 74.1h）。BPO因需额外快照操作，但总时长减少35–40%。

#### 5. 实验数量与充分性
- **实验组数**：主要结果在3个场景×2种模型上进行；消融实验包括：
  - 分支宽度K（1,2,4,8,16）在固定总样本数下的性能对比。
  - 分支调度策略（最高熵、最低熵、均匀随机、等间隔、用值网络做oracle）对比。
  - 传播折扣λ（0,0.5,0.9,0.95,0.99,1.0）的影响。
  - 梯度范数方差随训练步的演化。
  - 快照开销分析。
  - 另外测量了非退化优势比例的提升。
- **充分性与公平性**：对比方法均在同一代码库下复现，学习率、KL系数经过每（方法、环境、种子）配置调优；使用3个随机种子报告均值±标准差；计算量严格匹配。消融实验覆盖了关键超参数。总体实验设计客观、充分。

#### 6. 主要结论与发现
- **性能提升**：BPO在所有三个环境上超越所有基线，绝对成功率提升3.6–6.1个百分点；在长时域任务（SWE-bench）提升最大。
- **样本效率**：BPO用**38%更少的策略更新**即可达到GRPO的最终性能。
- **方差减半**：经验梯度范数方差约为GRPO的42%–58%（早期高变量时更大），证实了理论方差降低。
- **分支调度**：最高熵分支显著优于均匀或最低熵；K=4左右性能饱和；λ在0.9–0.99间最优。
- **快照开销可忽略**：相对长轨迹执行时间，快照开销仅2.4%–4.2%。

#### 7. 优点
- **方法创新**：首次将沙盒的“可快照/恢复”特性用作RL训练的一等公民（first-class training primitive），提出树状展开拓扑与同辈基线，理论上有严格的方差降低保证。
- **实验扎实**：在三个不同难度的沙盒（简单WebShop到复杂SWE-bench）上验证，使用两种主流LLM骨干，计算量严格匹配，消融实验覆盖关键因子。
- **理论支撑**：提供了无偏性和方差减少的严格证明（定理1、2），并能解释方差降低的来源（前缀解释的回报方差）。
- **实用价值**：训练时间减少35–40%，同时提升性能，且不需要值网络或搜索树，保持基线方法简单性。

#### 8. 不足与局限
- **快照依赖**：该方法要求沙盒原生支持快照/恢复，对不支持快速快照的环境（如某些真实API调用）不适用。
- **假设条件**：假设快照保真（Assumption 1）；若沙盒有随机性（如网络延迟、随机种子），快照恢复后仍需重新采样，可能削弱方差降低效果。
- **分支预算固定**：每提示分支数量M固定，未自适应分配；动态预算可能进一步优化。
- **实验未覆盖**：未探索与过程奖励模型的结合、递归分支、异构沙盒集群的分布式训练；K=16时因计算匹配限制分支点过少导致性能下降，未测试更大预算下的极限。
- **评估偏差**：实验仅在三个特定任务上进行，且均为单轮任务（一个指令、一次解决）；多轮交互或对话式场景未验证。
- **数值结果稳定性**：仅3个种子，但部分消融（如λ、K）标准差略大，需要更多种子确认趋势。

（完）
