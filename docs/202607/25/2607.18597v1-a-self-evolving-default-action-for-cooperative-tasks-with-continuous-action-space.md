---
title: A Self-Evolving Default Action for Cooperative Tasks with Continuous Action Space
title_zh: 一种用于连续动作空间协作任务的自演化默认动作
authors: Shuangyao Huang
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18597v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 提出面向多智能体强化学习的自进化默认动作
tldr: 针对连续动作空间多智能体合作任务中反事实信用分配偏差问题，本文提出SAFE框架。该方法从各智能体经验缓冲区采样自演化默认动作，构建无偏反事实基线，无需额外模拟或先验知识。在合作车辆任务中，SAFE显著优于现有模型，并保证收敛到局部最优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18597v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 1248, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 839, \"height\": 224, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1480, \"height\": 899, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1672, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1849, \"height\": 693, \"label\": \"Figure\"}]"
motivation: 现有方法通过蒙特卡洛采样近似反事实基线，在连续动作空间引入梯度偏差，且不保证收敛到局部最优。
method: SAFE基于每个智能体经验缓冲区中的自演化默认动作，构建条件反事实基线，实现无偏确定性策略梯度。
result: 在多项合作车辆任务中，SAFE的性能稳定超越当前最先进的多智能体强化学习模型。
conclusion: SAFE为连续动作空间提供无偏信用分配，确保策略收敛，同时避免额外模拟或领域知识依赖。
---

## 摘要
反事实信用分配在离散动作空间的多智能体强化学习中已被证明有效，但其扩展到连续动作的协作任务仍具挑战性。现有方法通过蒙特卡洛采样近似反事实基线，通常引入策略梯度偏差，且因采样动作可能未充分训练而无法保证收敛到局部最优。为解决这些局限，我们提出SAFE，一种新颖的多智能体强化学习框架，采用基于从每个智能体经验缓冲区中采样的自演化默认动作的条件反事实基线。该设计自然地扩展到连续动作空间，无需依赖额外模拟、奖励模型或环境特定先验知识。该基线准确量化每个智能体的贡献，且不引入确定性策略梯度偏差，确保收敛到局部最优。在协作车辆任务上的大量实验表明，SAFE始终优于最先进模型。

## Abstract
Counterfactual credit assignment has proven effective in multi-agent reinforcement learning (MARL) for discrete action spaces, yet its extension to continuous-action cooperative tasks remains challenging. Existing methods that approximate the counterfactual baseline via Monte Carlo sampling often introduce bias into policy gradients and fail to guarantee convergence to local optima, as the sampled actions may not have been sufficiently trained. To address these limitations, we propose SAFE, a novel MARL framework that employs a counterfactual baseline conditioned on a self-evolving default action sampled from each agent's experience buffer. This design naturally extends to continuous action spaces without relying on additional simulations, reward models, or environment-specific prior knowledge. The baseline accurately quantifies each agent's contribution, and introduces no bias into the deterministic policy gradient, ensuring convergence to local optima. Extensive experiments on cooperative vehicular tasks demonstrate that SAFE consistently outperforms state-of-the-art models.

---

## 论文详细总结（自动生成）

# 基于自演化默认动作的连续动作空间协作任务多智能体强化学习方法（SAFE）——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：多智能体强化学习（MARL）在离散动作空间（如星际争霸II）中取得了显著成功，但在连续动作控制的现实任务（如自动驾驶车辆编队）中部署仍面临挑战。现有信用分配方法（如COMA）依赖离散动作空间的反事实基线，直接扩展至连续空间时存在偏差和收敛性风险。
- **核心问题**：如何设计一个在连续动作空间下既能准确量化每个智能体贡献、又能保持无偏策略梯度并保证收敛到局部最优的反事实基线？
- **整体含义**：本文提出的SAFE框架通过一种“自演化默认动作”（从智能体经验缓冲区采样）构建反事实基线，无需额外模拟、奖励模型或领域先验知识，实现了对连续动作空间信用分配的无偏扩展，并理论证明了收敛性。

## 2. 论文提出的方法论

### 核心思想
- 针对连续动作空间，放弃对离散动作期望的显式计算，改为从每个智能体的经验回放缓冲区（experience replay buffer）中**均匀采样**一个“默认动作”\(\bar{a}_i(t)\)，用于替换该智能体的实际动作\(a_i\)，从而得到反事实基线\(b_i = Q(s, a_{-i}, \bar{a}_i(t))\)。
- 该默认动作随着训练过程“自演化”：随着训练进行，缓冲区中累积的动作分布逐渐反映智能体的平均行为，因此采样的默认动作越来越接近智能体最常执行的动作，从而基线准确估计其平均性能。

### 关键技术细节
- **网络结构**：集中式评论家（critic）输入全局状态\(s\)和所有智能体的联合动作-观测历史；每个智能体拥有自己的动作器（actor），基于其自身观测历史输出连续动作。动作器之间共享参数（适用于同构智能体场景）。
- **信用分配**：每个智能体的优势函数\(A_i = Q(s, a) - b_i\)，其中\(b_i\)由上述自演化默认动作产生。
- **策略梯度**：使用确定性策略梯度（DPG）更新动作器。文中证明该基线不引入偏差，不影响梯度的收敛性（引理0.1）：因为基线不依赖于智能体\(i\)的实际动作\(a_i\)，在期望中其贡献为零。

### 关键公式（文字说明）
- 传统COMA基线：\(b_i = \sum_{\bar{a}_i \in A} \pi(s, \bar{a}_i) Q(s, a_{-i}, \bar{a}_i)\)，依赖离散动作集。
- SAFE基线：\(b_i = Q(s, a_{-i}, \bar{a}_i(t))\)，\(\bar{a}_i(t) \sim D_t\)，其中\(D_t\)为训练步\(t\)时智能体\(i\)的经验缓冲区。
- 确定性策略梯度更新：评论家优化均方误差（式3）；动作器最大化评论家输出（式4）。

## 3. 实验设计

### 使用的场景与数据集
- 基于Highway-Env（Gym兼容环境）的**高速公路合作自动驾驶任务**，包含多种难度场景：
  - 2车1障碍（2V1O）
  - 3车2障碍（3V2O）
  - 5车2障碍（5V2O）
  - 7车2障碍（7V2O）
- 受控车辆（绿色）需在障碍车（蓝色）流中协作避碰，动作空间为连续转向角度\([-\pi/4, \pi/4]\)。

### 对比的基准方法（benchmark）
1. **VDN、QMIX、IQL**：均为离散动作空间方法，使用离散元动作（左移、不动、右移、加速、减速）。
2. **COMA**：原始反事实多智能体策略梯度，离散版本。
3. **MAPPO**：基于PPO的多智能体方法。
4. **额外消融基线**（见第5点）。

### 对比设置
- 所有方法在相同环境下运行，离散动作方法使用相同的5个元动作；SAFE直接输出连续动作。
- 每组实验运行3个独立随机种子。

## 4. 资源与算力

论文未明确说明训练总时长或具体算力消耗。仅提及所有实验在**NVIDIA RTX 5060Ti GPU（16 GB内存）**上完成。未报告单个实验运行时间、训练步数或硬件数量。

## 5. 实验数量与充分性

### 实验组数
- **主对比实验**：4个场景 × 6种方法（SAFE、VDN、QMIX、IQL、COMA、MAPPO）= 24组；每组3个随机种子，共计72条学习曲线。
- **参数研究**：在3V2O场景下比较不同采样数量K（1,30,50,80,100）对SAFE性能的影响。
- **消融实验**（均在3V2O场景）：
  - SAFE_a=0：默认动作固定为0
  - SAFE_batch_mean：默认动作取32个采样动作的均值
  - COMA_cont：连续COMA（蒙特卡洛近似）
  - Centralized_critic：仅集中式评论家，无信用分配
- 共计约10组额外对比。

### 充分性与公平性评估
- **充分性**：覆盖了从简单到复杂的多个场景，包括参数和消融研究，验证了设计选择的有效性。对比方法涵盖离散动作和连续动作的主流baseline。
- **客观公平性**：离散方法使用相同的元动作集，确保了与SAFE的比较基础；但未提及是否对baseline进行了超参数调优以匹配连续动作环境，可能对离散方法不利。此外，未提供方差或置信区间，仅展示平均曲线。
- **局限性**：所有实验仅在单一环境（高速公路）下进行，缺乏在其他连续控制协作任务（如机器人协同、无人机编队）上的验证。

## 6. 论文的主要结论与发现

1. **SAFE在连续动作空间合作任务中显著优于现有方法**：在所有4个场景中，SAFE均获得较高平均奖励，而离散动作方法在复杂场景（3车以上）中几乎无法避免碰撞。
2. **自演化默认动作基线有效且无偏**：理论证明和实验共同支持该基线不引入梯度偏差，保证收敛；训练过程中默认动作分布的变化反映了智能体行为的自适应演变。
3. **单采样比多采样更优**：参数研究表明，使用单个采样动作作为基线（SAFE_1）优于使用多个采样动作的平均（SAFE_30/50/80/100），因为后者的估计引入了额外偏差。
4. **性能增益源于信用分配机制**：消融实验表明，仅具有连续动作能力而无信用分配（如Centralized_critic）或使用固定/均值基线（SAFE_a=0、SAFE_batch_mean）的表现均远逊于SAFE，证明SAFE的优越性来自其反事实信用分配设计。

## 7. 优点

- **创新性**：提出一种新颖的“自演化默认动作”概念，将反事实信用分配自然扩展至连续动作空间，无需离散化或蒙特卡洛近似，避免了偏差和收敛性问题。
- **理论严谨性**：提供了基线无偏性和收敛性的数学证明（引理0.1），强于仅靠实验验证的工作。
- **实用性强**：不依赖额外模拟、奖励模型或任务特定先验，易于部署；集中训练、分散执行（CTDE）范式适用于现实场景。
- **实验设计合理**：包含主对比、参数研究、消融实验多层次验证，消融实验有效隔离了信用分配与连续动作适应的影响。
- **可视化有效**：通过默认动作概率密度分布图直观展示了动作的自演化过程，增强了论证的说服力。

## 8. 不足与局限

- **实验场景单一**：仅基于Highway-Env高速公路任务，缺少对其他连续控制合作任务（如机器人协作、飞行器编队等）的验证，泛化能力存疑。
- **对比方法的公平性存疑**：离散动作方法使用元动作（左/右/加速/减速），可能不适用于需要精细连续控制的场景，导致对比结果有偏。未尝试将这些方法扩展至连续动作（如使用连续值离散化）进行比较。
- **缺乏统计显著性分析**：未报告方差、置信区间或统计检验（如t检验），仅展示平均学习曲线，结果稳健性难以完全评估。
- **动作器参数共享假设**：默认动作采样来自经验缓冲区，假设所有智能体同构（共享参数），对于异构智能体场景不适用，未讨论扩展方案。
- **未考虑通信延迟与部分可观测性**：虽然采用CTDE，但未讨论执行阶段若存在通信限制（如丢包、延迟）时自演化默认动作是否仍然有效。
- **计算开销未分析**：未比较SAFE与baseline的训练时间或内存占用，仅说明集中式评论家输入维度随智能体数增长，但未量化Scalability问题。

（完）
